# FSM Studio — v1.5 Epic: UI/DX "Convenience Layer" — Wave-Plan-of-Record

**Document ID:** FSM-PLAN-UIDX-V15
**Version:** 1.0.0 (epic-kickoff plan-of-record; produces this doc only — no implementation, no other-doc edit, no build, no push).
**Status:** **Accepted plan-of-record.** The v1.5 architecture below is **already decided and accepted** (orchestrator decision under owner-delegated autonomy — the owner pre-framed this entire phase as the post-pipeline UI/DX convenience layer and explicitly delegated the architecture; `memory/project_user_messages_embeded_fsm_2026_05_16_v14_strategy` + the backlog SoT `deferred-v1.x` DX-1/DX-2 entries + Doc 00 §11.63 `[[feedback_embeded_fsm_pipeline_before_ui]]`). This document **formalizes** that decision; it is not a recommendation awaiting confirmation. The owner-decision callouts in §4 are the *residual* posture choices (publish/sign, hosting, certifiability GO), each with a non-blocking safe parked default — the same Doc 30 §3 escalation pattern.
**Author:** TL/PM (planning extraction under delegated autonomy; the implementer of this doc only).
**Date:** 2026-05-16.
**Base commit:** `868728e` (the worktree branch `phase5.0/v15-a0-doc31` base; `v1.4.0` tagged at commit `3bbcd0f` = `checkpoint/2026-05-16-v1_4`, tag-object `f0f931e` — annotated-tag object ≠ commit, never conflate).
**Depends on / reconciles:** Doc 30 (v1.4 Sim-Verification Wave-Plan — the structural template + the `[[feedback_embeded_fsm_pipeline_before_ui]]` "pipeline before UI" governing principle this epic is *unblocked by*), `docs/GATE_VERIFICATION_v1_4.md` (the §5.2-equivalent gate-row form + the carried owner-escalations v1.5 must re-state), `docs/META_SESSION_2026_05_16.md` (the v1.4 post-tag closeout — the process tweaks §7 adopts), `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` (PART A = the Certifiability track parked in §3; PART B = the doc-honesty staleness D1 must not propagate), Doc 22 (VSCode-Extension-Manifest spec), Doc 27 (VSCode-Extension-Architecture), Doc 26 (LSP-Architecture), Doc 14 (LSP-Capability-Spec), Doc 18 (CLI-Specification — the `fsm verify`/`fsm baseline` exit-code + `--json` contract A1 renders), Doc 00 §11 (decision ledger — v1.5 rows assigned at v1.5 *closeout*, see the numbering note below), `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 behavioural-acceptance, §11.3 phase-audit, §11.22 cold-quad, §11.30 tag-ordering).

> **Why a new sibling doc (numbering justification, per the Doc 26/28/29/30 precedent).** Doc 28 (v1.3-VSCode-Wave-Plan), Doc 29 (v1.3-W0-CST-Plan) and Doc 30 (v1.4-Sim-Verification-Wave-Plan) each established that an *epic-kickoff extraction + wave plan* is a first-class numbered sibling, distinct from the static spec corpus (Docs 01–27) and the per-release `GATE_VERIFICATION_*` / `AUDIT_PHASE_*` evidence docs. v1.5 is the next epic; its plan is **Doc 31** (the next free integer after Doc 30). It does **not** replace Doc 22 (the extension *manifest spec*, which stays normative) or Doc 27 (the extension *architecture*, which stays normative) — it is the *plan that sequences the v1.5 convenience-layer work on top of those specs*, exactly as Doc 28 sequenced Doc 27.

> **§11-ledger numbering refinement (the "waves report rows, no mid-flight churn" discipline — stated up front).** Per the v1.4 L6 lesson (`docs/META_SESSION_2026_05_16.md` Part D.8 L6) and the standing "waves report rows, orchestrator batch-appends" refinement that eliminated the v1.2-LSP §11 append-race friction: **v1.5 Doc 00 §11-ledger rows are assigned at the v1.5 closeout (W4-equivalent batched consolidation), NOT pre-allocated here.** The current ledger head is **§11.71** (v1.4's last row). Pre-allocating "v1.5 rows start §11.72" in this plan is exactly the mid-flight-churn anti-pattern: each wave *reports* its rows in its completion notes, the closeout batch *appends* them in one controlled commit (the Doc 00 append-only `comm -23` discipline). This document therefore deliberately does **not** name §11 row numbers for the waves; it names the closeout obligation instead (§5).

---

## 0. Executive summary (read this first)

**`v1.4.0` is shipped + tagged; the headless CLI pipeline is FACTORY-COMPLETE** (`GATE_VERIFICATION_v1_4.md` — the W4a factory-integration audit returned `FACTORY-COMPLETE`, 0 workflow gaps; the full `fsm verify → generate → check → baseline` loop is CI/factory-integratable, machine-readable, deterministic, zero-UI). The owner's governing **pipeline-before-UI** sequencing principle (`[[feedback_embeded_fsm_pipeline_before_ui]]` — *"complete the FULL functional pipeline at the CLI + language-server layer BEFORE the UI/DX convenience layer"*) is therefore **satisfied and the UI/DX convenience phase is unblocked**. The owner pre-framed this exact phase ("the convenience layer, after the pipeline … sorry for distracting you" — the backlog SoT `deferred-v1.x` DX-1/DX-2 entries) and explicitly delegated its architecture to the TL.

**v1.5 = the "Convenience Layer" epic — 5 sub-epics, priority-ordered, depth-first, each with a real §5.4 behavioural-acceptance gate (never symbol-presence):**

| Sub-epic | One-line scope | Risk shape | Droppable from the v1.5 tag? |
|---|---|---|---|
| **A — Surface v1.4 verification in VS Code** (split A1+A2) | The owner's explicit highest-value ask: `fsm.verify`/`fsm.baseline` in the editor | A1 = **zero Rust delta** (reuse the proven CLI seam); A2 = the **only keystone-violable wave** (the single Rust-layer change) | No — A is the epic's reason |
| **B1 — Marketplace-readiness manifest pass** | Make `editors/vscode/package.json` Marketplace-valid; publish-ready VSIX (NOT published) | Low (manifest + docs only) | No |
| **C1 → C2 — TS frontend-practices audit → scoped remediation** | Audit `editors/vscode/**` vs CellWar/EdgeForge yardstick; fix only the high-value set | C1 read-only; C2 = the Playwright verify-UX E2E (hardens A) + prettier + flat eslint | No (the high-value C2 set is the A1/A2 hardening) |
| **D1 — static EdgeForge-modeled docs/landing site** | Static landing + thin docs-site over the existing `docs/` corpus + CLI/factory page + OSS-files reconcile | Low (static HTML; no build pipeline) | **YES — the only droppable-from-tag item** (may slip to v1.6 without blocking the v1.5 tag) |

**The keystone-in-UI invariant carries forward from v1.4 unchanged, restated for the editor surface (§2, a BINDING tag-gate row):** VS Code verification is *either* (a) a spawn of the same `fsm` binary the CI calls (A1), *or* (b) `fsm-lsp` calling the same `fsm_verify::verify`/`reachability_diagnostics` the CLI calls (A2). There is **NO** reachability / deadlock / transition-selection / guard-eval re-implementation in `crates/fsm-lsp/**` or `editors/vscode/**`. A second verifier is the cardinal regression — the exact P0-1 / MV5-1 / v1.4-keystone class the project keeps re-learning. This is now an *editor-surface* application of the **epic-level architecture invariant** v1.4 codified (Doc 00 §11.63; `docs/META_SESSION_2026_05_16.md` L1/L7), not a fresh decision.

**Critical path:** `A0 (this) → A1 → A2 → (B1 ∥ C1) → C2 → D1 → phase-boundary audits → independent pre-tag four-lens audit → GATE_VERIFICATION_v1_5.md → §11.30-clean cold-quad at the gate-doc commit → v1.5.0 tag`. B1∥C1 are file-disjoint (manifest vs audit-doc) and may run in parallel (each in its own writer worktree, §6 H4); C2 depends on **both** C1 (its disposition set) **and** A2 (it tests the verify UX); D1 is last and may slip to v1.6.

**A note on this document's discipline (the Doc 30 precedent, applied here).** Per `[[feedback_ck_vpd_crm_source_of_truth_extraction]]` + `[[feedback_verify_status_claims_vs_code]]`: every load-bearing ground-truth claim in this plan was **independently verified against code at the worktree base, not transcribed from the accepted-architecture brief**. §"Ground-truth verification ledger" records each verification and **flags the one divergence found** (the `editors/vscode/package.json` `repository.url` is *not* a generic placeholder string — it is a concretely-wrong third-party slug `fsmstudio/sm-sdk` while the real owner remote is `BackendCat/embeded-fsm-sdk-project`; B1's framing is corrected accordingly, the *fix* is unchanged). Everything else in the accepted architecture **survived re-verification intact**.

---

## Ground-truth verification ledger (the Doc 30 verify-as-you-write discipline — divergences FLAGGED)

Every claim the accepted architecture rests on was re-derived from source at the worktree base. `file:line`/command cited for each.

| # | Accepted-architecture claim | Verification (command/file) | Verdict |
|---|---|---|---|
| GT-1 | `editors/vscode/package.json` contributes NO `fsm.verify`/`fsm.baseline` | `grep -n '"command"' editors/vscode/package.json` ⇒ the contributed set is `fsm.checkFile`, `fsm.generateC99`, `fsm.generateCpp17`, `fsm.copyIR`, `fsm.formatDocument`, `fsm.restartLanguageServer`, `fsm.showOutputChannel`, `fsm.openDiagram`, `fsm.refreshMachineExplorer`, `fsm.refreshEventExplorer` — **no `fsm.verify`/`fsm.baseline`** | ✅ **CONFIRMED** — A1 is genuinely net-new editor surface |
| GT-2 | `editors/vscode/package.json` `repository.url` is a placeholder → the real owner remote (B1 drift) | `package.json:10` `"url": "https://github.com/fsmstudio/sm-sdk"`; the real owner remote = `git remote -v` (main repo) = `https://github.com/BackendCat/embeded-fsm-sdk-project` | ⚠️ **CONFIRMED with a FLAGGED nuance** — it is **not** an obvious placeholder token (e.g. `<REPO>` / `TODO`); it is a *concretely-wrong* fictitious third-party slug `fsmstudio/sm-sdk`. This is **more** of a Marketplace-correctness defect, not less (a published extension would link users to a stranger's/non-existent repo). B1's *fix* (point at the owner remote) is unchanged; the *framing* is corrected here ("wrong-target drift", not "unfilled placeholder"). |
| GT-3 | `editors/vscode/src/commands/cliRunner.ts` + `cliBinary.ts` are the honest CLI seam A1 reuses | Both files exist. `cliRunner.ts` exports `runCli(...)`, `CliResult` (`exitCode`, `spawnError`), `RunCliOptions` — spawns the real `fsm`, captures exit+stdout+stderr, only a genuine spawn failure sets `spawnError`. `cliBinary.ts` exports `resolveCliBinary(...)`, `ResolvedCli`, `noCliBinaryMessage(...)` | ✅ **CONFIRMED** — the proven seam exists; A1 reuses it with zero new spawn machinery |
| GT-4 | `crates/fsm-lsp/Cargo.toml` has `fsm-verify` ABSENT; `fsm-parser`/`fsm-analyzer`/`fsm-ir` present | `crates/fsm-lsp/Cargo.toml` `[dependencies]` = `fsm-diagnostics`, `fsm-parser` (`../fsm-parser`), `fsm-analyzer` (`../fsm-analyzer`), `fsm-ir` (`../fsm-ir`), `rowan`, `tower-lsp`, `tokio`, `serde_json` — **no `fsm-verify` line anywhere** | ✅ **CONFIRMED** — adding `fsm-verify = { path = "../fsm-verify" }` is the *single* Rust-layer enabling change for A2 (the only keystone-violable point) |
| GT-5 | `crates/fsm-cli/src/cmd/verify.rs` defines the `fsm-verify/v1` `--json` schema A1 renders | `cmd/verify.rs:35-100` (module doc) **DEFINES** the schema: `"schema": "fsm-verify/v1"`, `"verdict": "verified"\|"property-violated"\|"inconclusive"`, `properties.deadlockFree.counterexample.witness` (event-sequence array), `properties.reachability.diagnostics[]` = `{code:"FSM-E0400", severity, message, line, col}`. Exit codes: **0** verified, **1** property-violated, **2** inconclusive (`Verdict::Inconclusive`, *explicitly NOT "verified"*) | ✅ **CONFIRMED** — A1's render contract is exactly this; the honest INCONCLUSIVE/`bound.hit` semantics are in-schema |
| GT-6 | `crates/fsm-verify/src/lib.rs` pub API A2 calls == what `cmd/verify.rs` calls | `lib.rs:59-65` `pub use engine::{verify, ExplorationStats, StopReason, Verdict, VerifyError, VerifyOptions, VerifyOutcome, DEFAULT_MAX_STATES, DEFAULT_MAX_STEPS}`, `pub use diagnostics::reachability_diagnostics`, `pub use reachability::ReachabilityReport`, `pub use deadlock::DeadlockReport`. `cmd/verify.rs:108-111` calls exactly `fsm_verify::{verify, reachability_diagnostics, ...}` | ✅ **CONFIRMED** — A2's `fsm-lsp` embed calls the *identical* public functions the CLI calls ⇒ editor ≡ CLI by construction (the differential oracle holds) |
| GT-7 | EdgeForge `/dev/18` `landing/`+`docs-site/` are static HTML+CSS+i18n.js (the React part is its dashboard *app*) | `/root/dev/edgeforge/landing/` = `index.html`, `style.css`, `script.js`, `i18n.js`, `privacy.html`, `terms.html` (static). `/root/dev/edgeforge/docs-site/` = `index.html` + per-topic `.html` (getting-started, api-reference, configuration, security, …) + `style.css` + `i18n.js` (static). The React app is `/root/dev/edgeforge/dashboard/` (separate) | ✅ **CONFIRMED** — D1's "no new build pipeline; static HTML+CSS+i18n.js" model is exactly EdgeForge's landing/docs-site shape |
| GT-8 | No `site/`/`landing/`/`docs-site/` in fsm-sdk; no top-level `LICENSE`/`CONTRIBUTING.md`/`CODE_OF_CONDUCT.md` | `ls site landing docs-site` ⇒ none. `ls LICENSE CONTRIBUTING.md CODE_OF_CONDUCT.md` (repo root) ⇒ none (only `README.md`) | ✅ **CONFIRMED** — D1 is net-new; the OSS-files reconcile is real (and the "No LICENSE" carried owner-escalation, `GATE_VERIFICATION_v1_4.md` §6.3, is live) |
| GT-9 | The carried v1.3.x `makeNonce` `Math.random`→CSPRNG nit + JC-3 `fsmLang.codegen.*` settings-discoverability nit are real (B1 resolves them) | `editors/vscode/src/diagram/diagramPanel.ts:362` `function makeNonce()` → `:367` `Math.random()`. `package.json:250` `contributes.configuration` has `fsmLang.compilerPath`/`fsmLang.enableInlayHints`/`fsmLang.inlayHints.*` — **no `fsmLang.codegen.*`** (JC-3 confirmed) | ✅ **CONFIRMED** — both carried v1.3.x tracked nits (`GATE_VERIFICATION_v1_4.md` §6.3) are real; B1 folds them |
| GT-10 | `editors/vscode/package.json` lacks Marketplace-required `icon`/`keywords`; `version` not release-ready | `package.json:1-32`: `version: 0.1.0`, `publisher: fsmstudio`, `license: MIT` (present), `repository` present (but GT-2 wrong target), `galleryBanner` present, `categories` present — **no `icon` field, no `keywords` field**; `version 0.1.0` is pre-release | ✅ **CONFIRMED** — the B1 manifest gap set is real |

**Net result of the verify-as-you-write pass:** the accepted v1.5 architecture **survives ground-truth re-verification**, with **one flagged framing correction** (GT-2: the `repository.url` is a wrong-target slug, not an unfilled placeholder — strengthens, not weakens, the B1 case; the fix is identical). No accepted-architecture claim failed verification. The keystone-enabling facts (GT-4 `fsm-verify` absent from `fsm-lsp`; GT-6 the CLI/`fsm-verify` API identity) are exactly as the architecture states — A1's zero-Rust-delta claim and A2's single-enabling-change claim are both verified true.

---

## 1. The 5 sub-epics as the numbered wave table

Each wave is a real §5.4 behavioural-acceptance gate. **For the verification UI, §5.4 means *the editor's verdict + witness byte-equals the CLI's `fsm verify --json` on a real FSM* — never "the command is registered", never "the function exists".** Depth-first = each wave delivers a thin-but-complete vertical slice with a real behavioural gate before the next builds on it (the v1.2 L1-spine / v1.3 V1-spine / v1.4 W1-spine precedent).

### W-A1 — Verification in VS Code via CLI-spawn (HIGHEST VALUE; ZERO Rust delta)

| Field | Content |
|---|---|
| **Scope** | Add `fsm.verify` + `fsm.baseline` VS Code commands consuming the **EXISTING** `fsm verify --json` / `fsm baseline --json` (the `fsm-verify/v1` schema GT-5) via the **proven** `editors/vscode/src/commands/cliRunner.ts` (`runCli`) + `cliBinary.ts` (`resolveCliBinary`) seam (GT-3). No new spawn/binary-resolution machinery. |
| **Deliverable** | (1) Two contributed commands in `package.json` + their TS handlers, wired through `runCli`; (2) a result-summary surface (verdict + stats); (3) **INCONCLUSIVE / `bound.hit` rendered HONESTLY** — exit-2 / `verdict:"inconclusive"` is shown as *inconclusive, not "verified"* (the cardinal verification-UI sin is a false "verified"); (4) the deadlock **counterexample witness** (`properties.deadlockFree.counterexample.witness`) as a **navigable list**, reusing the diagram's existing click→source `SourceLocation` mechanism (Doc 27 — the diagram already does click-to-source; do not fork it); (5) reachability `FSM-E0400` / `FSM-W0602` from `properties.reachability.diagnostics[]` surfaced as a dedicated VS Code **`DiagnosticCollection`** (line/col from the JSON). |
| **§5.4 behavioural acceptance** | An Extension-Host (`@vscode/test-electron`) test where: (a) the witness the extension decodes from a deadlocking fixture, **byte-equals** `fsm verify --json` `counterexample.witness` *recomputed in-test by spawning the real binary on the same fixture*; (b) a clean fixture → the UI shows "verified"; (c) a tiny `--max-states` → the UI shows **INCONCLUSIVE, asserted NOT "verified"** (the false-proven guard, mirrored from the CLI's `too_small_bound_is_inconclusive` discipline); (d) a reachability-defect fixture → the `DiagnosticCollection` entry's line/col **==** the CLI JSON `diagnostics[].line/col`. Symbol-presence (command-registered) is explicitly **not** acceptance. |
| **Parallel/seq** | **Sequential** (the epic spine; A2 builds the differential oracle on top of A1's CLI-JSON contract). |
| **Worktree** | `phase5.1/v15-a1-verify-cli-ui` (writer worktree, isolated per §6 H4). |
| **Keystone** | Holds **trivially** — A1 *is* a spawn of the same `fsm` binary the CI calls (GT-3/GT-5). There is no Rust to fork; the keystone-in-UI gate row (§2) is satisfied by construction for A1. |
| **Why A1 first / the split rationale (encode this)** | A1 delivers the owner's explicit ask with **zero Rust risk and zero new build surface day-one**, reusing the *factory-grade contract the pipeline gate just certified* (`GATE_VERIFICATION_v1_4.md` W4a `FACTORY-COMPLETE`). It **ships even if A2 slips**. And A1's CLI-JSON output becomes **A2's differential oracle** — A2 is gated by "the LSP-embedded verdict byte-equals what A1 spawns". |

### W-A2 — Verification in VS Code via LSP-embed (live, opt-in, GATED — the only keystone-violable wave)

| Field | Content |
|---|---|
| **Scope** | Wire `fsm-verify` into `fsm-lsp`: add `fsm-verify = { path = "../fsm-verify" }` to `crates/fsm-lsp/Cargo.toml` (currently **ABSENT**, GT-4 — this is the **single Rust-layer enabling change of the entire epic**). Expose a debounced, large-FSM-guarded LSP capability (code-lens *or* a custom request — **NOT** auto-on-every-keystroke) that calls the **SAME** `fsm_verify::verify` / `fsm_verify::reachability_diagnostics` that `crates/fsm-cli/src/cmd/verify.rs` calls (GT-6). |
| **Deliverable** | (1) the one `Cargo.toml` dep edge; (2) an opt-in LSP verification capability with a **debounce + large-FSM ceiling guard** (the state-space-explosion UX trap: an auto-verify on every keystroke of a large FSM would hang the editor — verification is bounded-by-construction in `fsm-verify` but the *trigger* must be explicit/debounced); (3) verdict+witness+reachability surfaced live in-editor through the SAME `fsm-verify` types A1 renders from JSON (editor ≡ CLI ≡ CI by construction). |
| **§5.4 behavioural acceptance** | A tower-lsp client test (the v1.2-LSP-L1..L7 / v1.3 precedent): the LSP capability's verdict + witness, on a **fixture battery** (clean / deadlocking / inconclusive-bound / reachability-defect), **byte-equals `fsm verify --json` on the same fixtures** (the **differential-oracle proof** — A1's CLI seam is the oracle). The inconclusive case asserts the LSP returns inconclusive, **NOT a false "verified"**. |
| **Parallel/seq** | **Sequential** (after A1 — A1's CLI-JSON is A2's oracle; and A2 is the single auditable keystone checkpoint, must not run concurrently with anything that could mask a fork). |
| **Worktree** | `phase5.2/v15-a2-verify-lsp-embed` (writer worktree). |
| **Keystone — MANDATORY post-A2 §11.3 source-derived phase-audit** | A2 is the **only place a forked verifier could enter** (it adds Rust). The wave is gated by a mandatory **post-A2 §11.3 keystone phase-audit** that **independently re-derives from source** (not from this doc, not from the wave's self-report — the §11.29 / v1.4-L1 "verify-the-record, re-derive don't echo" rigor): (i) `crates/fsm-lsp/Cargo.toml` depends on `fsm-verify`; (ii) **NO** reachability / deadlock / transition-selection / guard-eval / completion symbol is *defined* in `crates/fsm-lsp/**` or `editors/vscode/src/**` (negative grep on non-comment lines = ∅, the v1.4 W1/W2-audit method); (iii) the LSP verdict+witness **byte-equals** `fsm verify --json` on the fixture battery (the differential-oracle proof, re-run by the auditor). Frozen evidence doc, never overwritten (the v1.4 `AUDIT_PHASE_V1_4_W{1,2}` precedent). |
| **Why A2 is isolated as one gated wave (encode this)** | Isolating the *only* Rust-adding wave as a single gated checkpoint makes keystone-enforcement a **single auditable point** rather than a diffuse property smeared across waves. It is the structural application of the v1.4 lesson that the keystone is an epic-level invariant whose enforcement must be a *named, budgeted* audit (`docs/META_SESSION_2026_05_16.md` Part D.8 L1/L7 + Part D.7(c) recommendation 1 — "pre-budget the soundness-audit surface"). |

### W-B1 — VS Code Marketplace-readiness manifest pass

| Field | Content |
|---|---|
| **Scope** | `editors/vscode/package.json` Marketplace-validity: fix `repository.url` (GT-2 — change the **wrong-target slug** `fsmstudio/sm-sdk` to the real owner remote `https://github.com/BackendCat/embeded-fsm-sdk-project`); set/raise `version`, confirm `publisher`, add the missing `icon` + `keywords` (GT-10), confirm `categories`/`galleryBanner`; author a Marketplace-facing README + a user-facing CHANGELOG; resolve the carried v1.3 nits — JC-3 (`fsmLang.codegen.*` absent from `contributes.configuration`, GT-9) + `makeNonce` `Math.random`→CSPRNG (`diagramPanel.ts:367`, GT-9). |
| **Deliverable** | A **Marketplace-VALID VSIX** produced by `vsce package` (lint-clean). **Publish-ready, NOT published** (publishing is an owner credential decision — §4 OWNER-A). The shipped artifact is the **host-only VSIX**; the 5-platform binary tail is **G9-accepted-tracked-debt** (no cross toolchains on the box — infra-escalation, not grind; the `GATE_VERIFICATION_v1_4.md` §5.2 / §6.3 carried posture, unchanged). |
| **§5.4 behavioural acceptance** | `vsce package` runs **lint-clean** and emits an installable `.vsix`; the emitted VSIX installs into a clean Extension-Host and the existing extension Extension-Host tests still pass against it (no manifest change broke activation/contribution); `repository.url` resolves to the **owner's actual repo** (asserted, given GT-2 was a wrong target — a publish would otherwise mislink). Symbol-presence (field exists) is not enough; the VSIX must lint-validate. |
| **Parallel/seq** | **Parallel with C1** (file-disjoint: B1 touches `editors/vscode/package.json` + README/CHANGELOG + `diagramPanel.ts`; C1 produces a NEW audit doc and touches no shipped file). Each in its own writer worktree (§6 H4). |
| **Worktree** | `phase5.3/v15-b1-marketplace-manifest` (writer worktree). |

### W-C1 — TS frontend-practices audit (READ-ONLY)

| Field | Content |
|---|---|
| **Scope** | A **READ-ONLY** `editors/vscode/**` TypeScript frontend-practices audit against the **CellWar/agar.io (`/dev/16`) + EdgeForge (`/dev/18`) yardstick**: feature-slicing, a flat `eslint.config.ts` + custom rules, prettier, husky/lint-staged, Vitest unit + **Playwright E2E per user-facing feature** (the CellWar R6 standard), multi-tsconfig project-references. |
| **Deliverable** | A frozen audit doc `docs/AUDIT_VSCODE_FRONTEND_PRACTICES_2026-05-16.md` (or dated equivalent — the `AUDIT_PHASE_*` frozen-evidence convention). Every finding rated **{value, cost, risk}** with an explicit disposition: **fix-in-v1.5** / **defer-tracked** / **won't-do-with-rationale**. This is the **leave-and-explain discipline** (`[[feedback_embeded_fsm_*]]` §11.49 lineage; the C1 brief must state it explicitly): the `editors/vscode/**` surface is **working and well-tested** — do **NOT** churn it to chase a yardstick; only changes with a real value/cost case earn a fix-in-v1.5 disposition. |
| **§5.4 behavioural acceptance** | (Audit wave — acceptance is *audit-integrity*, the v1.4 W4a/pre-tag-audit precedent, not a code §5.4.) Every finding carries a concrete `file:line` + a {value,cost,risk} triple + a disposition with rationale; the disposition set is internally consistent (no "fix-in-v1.5" without a value case; no "won't-do" without a rationale). The auditor is briefed as a *talented senior UX/frontend 2026* (the audit-wave framing convention) but the deliverable is a disciplined disposition table, not a yardstick-cargo-cult. |
| **Parallel/seq** | **Parallel with B1** (file-disjoint). |
| **Worktree** | `phase5.4/v15-c1-fe-audit` (read-only — could be shared-tree per the reader-isolation rule; a worktree is used anyway for the new doc to keep the writer-isolation invariant uniform, §6 H4). |

### W-C2 — Scoped frontend remediation (executes ONLY C1's "fix-in-v1.5" set)

| Field | Content |
|---|---|
| **Scope** | Execute **ONLY** the C1 findings dispositioned **fix-in-v1.5**. The genuinely high-value item is the **Playwright Extension-Host E2E for the verify UX** (it *hardens A1/A2* — the verify-UX is the epic's headline surface and deserves a real browser/host E2E, the CellWar R6 "Playwright is the only truth" standard) + **prettier** + the **flat eslint migration**. Mechanical-move byte-identity where feasible (the v1.2 refactor `byte-identical` precedent). |
| **Deliverable** | The fix-in-v1.5 set landed; the Playwright Extension-Host verify-UX E2E green; prettier + flat eslint config in place; defer-tracked / won't-do items recorded (not silently dropped). |
| **§5.4 behavioural acceptance** | The Playwright (or `@vscode/test-electron`-driven) verify-UX E2E **drives the real extension** through the deadlock-fixture flow and asserts the witness list + the INCONCLUSIVE-not-verified honesty **end-to-end in the host** (not a unit mock) — this is the *hardening* of A1/A2's §5.4. prettier/eslint changes are byte-identity-verified where mechanical (no behavioural drift). |
| **Parallel/seq** | **Sequential — depends on BOTH C1 (the disposition set) AND A2 (it tests the live verify UX, which A2 completes).** Runs after A2 and after C1. |
| **Worktree** | `phase5.5/v15-c2-fe-remediation` (writer worktree). |

### W-D1 — Static EdgeForge-modeled docs/landing site (LAST; the only droppable-from-tag item)

| Field | Content |
|---|---|
| **Scope** | A **static** landing + a thin docs-site that **renders the EXISTING excellent `docs/` corpus** (do **NOT** re-author the 30+ spec docs — they are current per `ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` Part B for the *living* docs) + a **CLI / factory-integration page** (the real product seam — the headless `fsm verify→generate→check→baseline` loop is the product's factory value) + an **OSS-files reconcile** (LICENSE / CONTRIBUTING / CoC presence — all three are **absent** at repo root, GT-8). Modeled on EdgeForge's `landing/`+`docs-site/` (GT-7). |
| **Deliverable** | An in-repo `site/` (static HTML+CSS+i18n.js, **NO new build pipeline** — GT-7 confirms EdgeForge's landing/docs-site are static HTML; the React part is EdgeForge's *dashboard app*, irrelevant here — *this* product's "app" is the **already-shipped VS Code extension**), host-agnostic. The OSS-files reconcile surfaces the LICENSE/CONTRIBUTING/CoC gap (the "No LICENSE" item is a **carried owner/legal escalation**, `GATE_VERIFICATION_v1_4.md` §6.3 — D1 surfaces it, does not unilaterally pick a license; §4 OWNER-D). |
| **§5.4 behavioural acceptance** | The static site builds with **no build pipeline** (open `site/index.html` directly / a trivial static serve), all internal links resolve, the docs-site faithfully renders the *existing* `docs/` content (no re-authoring, no doc-honesty regression — the rendered content is the current corpus), the CLI/factory page accurately describes the *shipped* `fsm verify`/`fsm baseline`/`fsm generate`/`fsm check` contract (cross-checked vs Doc 18 + `GATE_VERIFICATION_v1_4.md`, not invented). |
| **Parallel/seq** | **Sequential — LAST.** Depends on nothing code-side; ordered last because it is the lowest-value-per-risk and the **only item droppable from the v1.5 tag** (may slip to v1.6 without blocking — §4 OWNER-D default). |
| **Worktree** | `phase5.6/v15-d1-docs-landing-site` (writer worktree). |

---

## 2. The "one core, many frontends" keystone-in-UI enforcement mechanism (a BINDING tag-gate row — Doc-30-§5.2 form)

> This is the load-bearing structural invariant of the epic, stated in the binding gate-row form (the `GATE_VERIFICATION_v1_4.md` §5.2 / Doc 30 §5.2 pattern). It is **NOT a fresh decision** — it is the *editor-surface* application of the v1.4 epic-level architecture invariant (Doc 00 §11.63; the verifier drives the shipped semantics, never forks them; `docs/META_SESSION_2026_05_16.md` L1/L7: *"if `fsm-verify` had its own transition-selection / guard-eval / clock, a 'verified' verdict would attest a second, unaudited semantics — the entire value proposition would be hollow"*). v1.5 extends that invariant from the CLI to the editor.

**KEYSTONE-IN-UI (binding v1.5 tag-gate criterion):**

> VS Code verification is **either** (a) a spawn of the *same* `fsm` binary the CI uses (W-A1, via `cliRunner.ts`/`cliBinary.ts` — GT-3) **or** (b) `fsm-lsp` calling the *same* `fsm_verify::verify` / `fsm_verify::reachability_diagnostics` that `crates/fsm-cli/src/cmd/verify.rs` calls (W-A2, via the single `crates/fsm-lsp/Cargo.toml` `fsm-verify` dep edge — GT-4/GT-6). There is **NO** reachability / deadlock / transition-selection / guard-eval / completion **re-implementation** in `crates/fsm-lsp/**` or `editors/vscode/**` (TS or Rust). The **editor verdict + witness byte-equals `fsm verify --json`** on the §5.4 acceptance fixture battery (the differential-oracle proof). The **post-A2 §11.3 phase-audit re-derives this FROM SOURCE** (negative-grep ∅ on non-comment lines in `crates/fsm-lsp/**` + `editors/vscode/src/**`; the byte-equality differential re-run by the auditor) — **not** from this doc's claim and **not** from the wave's self-report (the §11.29 / v1.4-L1 verify-the-record rigor). **A second verifier is the cardinal regression** — the exact P0-1 / MV5-1 / v1.4-keystone class the project keeps re-learning. This row PASSES iff the post-A2 source-derived audit returns the keystone intact AND the differential battery is byte-equal.

This is one auditable checkpoint by construction: only **W-A2** adds Rust to the verification path; **W-A1** holds the invariant *trivially* (it is literally the CLI binary). Concentrating the only fork-possible surface into a single gated wave with a mandatory source-derived audit is the structural realization of "keystone enforcement = a single named, budgeted checkpoint" (the v1.4 L1/L7 lesson).

---

## 3. Certifiability / MISRA–CERT-C track — PARKED owner-review checkpoint (non-blocking, NOT auto-scoped into v1.5)

> Surfaced exactly as Doc 30 §3 surfaced its owner-decisions: a crisp one-paragraph value/cost framing with a recommended slot and an explicit owner GO/defer, **NOT pre-decided**, **NOT auto-scoped into v1.5**.

**The item.** `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART A ranks **#1 the highest-value gap not in the ROADMAP**: a documented **MISRA-C / CERT-C compliance posture for the generated C** (the literal string "MISRA" appears in **0 files** across the entire doc corpus + codebase, while Doc 01 positions the product at ISO 26262 / DO-178C / IEC 62304 regulated firmware). It composes with verification (a deadlock-freedom proof is itself a safety-case input) into a coherent **"Certifiability & factory-integration" arc** (ASSESSMENT §A.4.1).

**Value/cost framing (one paragraph, for the owner).** *Value:* without a compliance posture the named factory/safety audience structurally cannot adopt the tool into a regulated line — it is the single highest *strategic* leverage gap, and the codegen is plausibly close. *Cost:* a MISRA/CERT-C rule-conformance matrix + an audit of the generated C against it is a substantial dedicated minor (likely a codegen-mode change + a conformance corpus), explicitly **NOT** a convenience-layer item — folding it into v1.5 would reproduce the mega-minor anti-pattern the §11.40 re-scope and Doc 30 §2.2 rejected. *Recommended slot:* the ASSESSMENT recommends **≈ v1.5-candidate (matrix+audit)** as a *named ROADMAP item*; this plan **recommends ROADMAP v1.6+ as its own minor**, sequenced *after* the v1.5 convenience layer and the v1.5 WS-server-or-not decision, because it is orthogonal to the UI/DX theme and deserves its own gated epic (the same "three minors, not one mega-minor" discipline as Doc 30 §2.2.3).

**Owner decision (PARKED — non-blocking default).** This is **OWNER-C** in §4. **Default if the owner is silent:** v1.5 proceeds as the convenience layer **without** any certifiability work; the Certifiability arc stays a **named, parked ROADMAP-v1.6+ candidate** (recorded in `GATE_VERIFICATION_v1_5.md` §"carried owner-escalations" so it is not silently dropped — the §11.62 discipline). The owner's explicit GO is required to *schedule* it; absent that GO it does **not** enter v1.5 and is **not** auto-started. (This mirrors how Doc 30 §3 kept OWNER-DECISION-1/-2 non-blocking with safe defaults — planning never stalls on it.)

---

## 4. Owner-action vs proceed-autonomously split (safe parked defaults — per `[[feedback_infra_constraint_escalation]]`)

> The architecture is **explicitly delegated** (owner pre-framed the phase + delegated the architecture — `memory/project_user_messages_embeded_fsm_2026_05_16_v14_strategy`; the backlog SoT `deferred-v1.x` DX-1/DX-2). So **everything is proceed-autonomously EXCEPT** the four residual posture choices below, each with a **non-blocking safe parked default** so no wave ever stalls on the owner (the Doc 30 §3 escalation pattern; the `[[feedback_dont_ask_technical_permission]]` line — engineering hygiene ships, only product-strategy/destructive choices escalate).

| ID | Owner decision | Safe parked default (proceed on this if the owner is silent) |
|---|---|---|
| **OWNER-A** | **VS Code Marketplace publish / signing** (a credential + product-identity decision; carried unchanged from `GATE_VERIFICATION_v1_4.md` §6.3 / v1.3 §5.3) | **Build a publish-ready, lint-clean VSIX (W-B1); do NOT publish; no PAT/credential used.** The installable VSIX is the shipped artifact; publishing is the owner's explicit credential action. |
| **OWNER-B** | **The G9 push + the 5-platform binary tail** (push of LOCAL `v1.0.0`–`v1.5.0` + checkpoints + the never-run CI matrix / SCA / JS lane; the cross-OS binary bundling — no cross toolchains on the box, an **infra-escalation, not a grind**; carried unchanged from `GATE_VERIFICATION_v1_4.md` §5/§6.3) | **Tag v1.5.0 LOCAL, §11.30-clean; carry G9 + the 5-platform tail in `GATE_VERIFICATION_v1_5.md` as accepted-tracked owner-escalation** (the §11.62 re-state-don't-drop discipline). The host-only VSIX (B1) is the shipped artifact. |
| **OWNER-C** | **The Certifiability / MISRA–CERT-C track GO** (§3 — a strategic ROADMAP-scheduling decision, NOT a convenience-layer item) | **PARKED.** v1.5 proceeds as the convenience layer with **no** certifiability work; the arc stays a named parked ROADMAP-v1.6+ candidate, recorded in the gate doc's carried-escalations. Requires explicit owner GO to schedule. |
| **OWNER-D** | **The docs-site hosting / domain, AND whether D1 ships in v1.5 or slips to v1.6** (a product/hosting decision) | **Build the static, host-agnostic `site/` (W-D1); pick NO domain/host; ship it in v1.5 ONLY if it is clean before the gate, else defer to v1.6 (D1 is the explicitly droppable-from-tag item).** No hosting/DNS action is taken autonomously. |

**Everything else = proceed autonomously.** Wave sequencing, the A1/A2 split, the keystone-in-UI enforcement, the C1 audit + C2 disposition execution, the prettier/eslint/Playwright engineering hygiene, the worktree isolation, the §11.3/§11.30 release mechanics — all are delegated and require no owner sign-off (engineering hygiene ships; only the four product-strategy/credential/destructive residuals above escalate).

---

## 5. §11.30-clean v1.5 tag-gate criteria (Doc-30-§5.2 form)

> The `GATE_VERIFICATION_v1_4.md` §1/§4/§5.2 / Doc 30 §5.2 gate-row form, applied to v1.5. The **5th consecutive prospective-clean release** (v1.1 retroactively reconciled §11.30; v1.2/v1.3/v1.4 prospectively; v1.5 prospectively).

- **KEYSTONE-IN-UI gate row (§2) — BINDING.** The post-A2 source-derived §11.3 phase-audit returns the keystone intact (negative-grep ∅ in `crates/fsm-lsp/**` + `editors/vscode/src/**` on non-comment lines) **AND** the editor verdict+witness byte-equals `fsm verify --json` on the §5.4 fixture battery (the differential oracle, re-run by the auditor). A second verifier is the cardinal regression and a hard tag-blocker.
- **§5.4 behavioural-acceptance on every wave** — for the verify UI: a *genuinely deadlocking FSM yields the correct witness list in the editor*, a *clean FSM shows "verified"*, a *too-small bound shows INCONCLUSIVE asserted-NOT-"verified"* (the false-proven guard), all **byte-cross-checked vs the CLI `--json`** (the differential oracle, not a UI mock). For B1: `vsce package` lint-clean + an installable VSIX. For C1: an audit-integrity disposition table. For C2: the Playwright/host verify-UX E2E green. For D1: the static site builds with no pipeline + links resolve + no doc-honesty regression. **Symbol-presence is never acceptance** (command-registered ≠ verified).
- **§11.30-clean cold-from-source quad green at the gate-doc commit** (§11.22 / §11.30 strictly): the release sequence is (1) author+commit `GATE_VERIFICATION_v1_5.md` + the batched Doc 00 §11 / CHANGELOG `[1.5.0]` / ROADMAP consolidation → commit `X`; (2) run the canonical cold quad **at `X`**; (3) `git tag -a v1.5.0` **at `X`** + `git tag checkpoint/<date>-v1_5` **at `X`** — quad-commit ≡ tag-commit ≡ `X`, zero off-by-one by construction.
- **The MANDATORY NOTE-1 genuine-cold STEP 0** (`[[feedback_embeded_fsm_tag_cold_quad_method]]`; `GATE_VERIFICATION_v1_4.md` §4.2 / Doc 00 §11.70): **before** any full-workspace gate, run **`cargo clean -p` of ALL first-party workspace crates** (`fsm-diagnostics fsm-lexer fsm-parser fsm-ir fsm-analyzer fsm-codegen-c fsm-simulator fsm-formatter fsm-cli fsm-lsp fsm-verify`; cargo 1.75 needs the repeated `-p` form) **then** the canonical quad. The box-infeasible "fresh non-shared `CARGO_TARGET_DIR`" ideal is **explicitly rejected** (a virgin target rebuilds the ~14 GiB dep graph; a blind full `cargo clean` evicts the registry cache → ~14 GiB from-zero — the box runs ~6–9 GiB free). The all-first-party `clean -p` evicts every first-party build+test artifact (incl. any stale `-wt-`-baked test binary — the v1.4 L3 *false-red* hazard) **while preserving the content-addressed registry dep cache** (Cargo fingerprint is path-independent). This is the recorded disk-protective conventional-CI-cache equivalent, **not a workaround to grind past**.
- **The JS lane at the gate-doc commit** (the v1.5 surface *is* `editors/vscode/**` — unlike v1.4 which touched no `editors/`, so this is a *re-activated* gate this epic): at commit `X`, `npm audit` (or the repo's equivalent) clean / triaged + `vsce package` **lint-clean** (the B1 §5.4 artifact, re-confirmed at the gate commit) + the Extension-Host / Playwright verify-UX E2E green. The Rust cold quad and the JS lane are both gated at `X`.
- **`#![forbid(unsafe_code)]` invariant preserved.** A2 adds a `Cargo.toml` dep edge to `fsm-lsp` (no new crate) — the forbid-unsafe root count is **expected unchanged at 12 roots / 11 crates** (the v1.4 figure; A2 adds no new crate root, only a dep edge). This is **re-derived from source at the gate-doc commit, not echoed** (`git grep -l '#![forbid(unsafe_code)]' -- 'crates/*/src/*.rs'` — the v1.4 §"Pinned gate numbers" method); if A2 *does* somehow add a crate this is flagged, not assumed.
- **Release-record symmetry check (the v1.4 L4 standing closeout sub-step).** The v1.5 gate doc's Rust-delta framing **must be re-derived from `git diff`, not echoed** — v1.5's Rust delta is *small by design* (A2's single `Cargo.toml` dep edge + the LSP capability wiring; A1/B1/C2 are TS; C1/D1 are docs/static). State it as *exactly that small additive delta* — neither under-stated (the v1.3 "zero Rust delta" cardinal error) nor over-stated. Grep the gate record for the v1.3-cardinal-error form (`zero|no|empty .* (rust )?delta`) returning 0 hits, AND independently re-derive `git diff <v1.4-tag-commit> X -- crates/ Cargo.toml Cargo.lock` (`[[feedback_embeded_fsm_verify_record_release_layer]]`).
- **Memory-SoT status-line-freshness gate at v1.5 closeout (the v1.4 D.7(c)#3 standing gate).** The single biggest drift the v1.4 meta-session found was the backlog-SoT head status line + missing release-record section lagging the actual tag. v1.5 closeout MUST, *in the same closeout pass*: advance the backlog SoT head status line to `v1.5.0 SHIPPED+TAGGED` + add a `v1.5.0 RELEASE RECORD` section with the wave→commit map (the D.3 deliverable, not deferred).
- **0 open P0, ≤5 well-scoped P1** in the independent pre-tag four-lens audit (the v1.1/v1.2/v1.3/v1.4 §3 standard).
- **Carry-overs honoured/recorded (re-stated, not necessarily resolved — the §11.62 / v1.4 §6.3 discipline):** G9 CI-never-run + the JS lane (owner push, OWNER-B); the 5-platform binary tail (OWNER-B); VSIX packaged-not-published (OWNER-A — *now* publish-*ready* via B1, still owner-published); **No top-level `LICENSE`/`CONTRIBUTING`/`CoC`** (GT-8 — D1 surfaces it; the license choice is OWNER-D / owner-legal); the R7 mechanised sim≡codegen deferral (still v1.5-stretch/later — *not* folded into the convenience layer; named-not-omitted); the Certifiability arc (OWNER-C, parked) — all re-stated in `GATE_VERIFICATION_v1_5.md` §"carried owner-escalations" so none is silently dropped.
- **CHANGELOG `[1.5.0]`** + the Doc 00 §11 rows (assigned at closeout — see the numbering note in the header, NOT pre-allocated) + a ROADMAP retrospective.

---

## 6. Risks + sequencing

| # | Risk | Likelihood | Mitigation |
|---|---|---|---|
| **H1** | **Disk pressure** on the shared box (the box runs ~6–9 GiB free; `cargo` + a JS toolchain + Playwright/`@vscode/test-electron` Electron downloads + `vsce` are all heavy) | High | **One build-heavy wave at a time** (the standing posture); out-of-tree JS / `node_modules` / Playwright-browser / VS-Code-download caches (do NOT let them land in `target/` or multiply per worktree); `df -h /` checked before/after every build-heavy wave; the §7-hygiene + `clean -p`-first-party reclaim. The orphan-worktree reclaim that recovered space is **DONE** (the box is in the known-tight-but-managed posture, not the unbounded one). A1/B1/C1/D1 are *not* Rust-build-heavy (TS/docs/static) — only A2 + C2 + the cold quad are; sequence the Rust-heavy ones serially. |
| **H2** | **Keystone-in-UI violated** — a forked verifier enters via A2 (the §2 cardinal regression) | Medium (a "fast in-editor" reimplementation is tempting) | **The §2 mechanism**: A2 isolated as the single Rust-adding wave; the mandatory post-A2 **source-derived** §11.3 phase-audit (negative-grep ∅ + the differential-oracle byte-equality re-run by the auditor, *not* echoed from the wave's self-report — the §11.29 / v1.4-L1 rigor); A1's CLI-JSON as A2's differential oracle. This is a **binding tag-gate row** (§5), not a nice-to-have. |
| **H3** | **Toolchain-probe-trap** — a verification brief asserts a rustc fact from a bare `rustc --version` (box default 1.95) and misreads it as pin drift | Medium (a recurring live trap; `[[feedback_embeded_fsm_toolchain_probe_trap]]`) | Any rustc assertion in any v1.5 brief MUST be `rustup show active-toolchain` **from inside the repo** (expects `1.75.0 … (overridden by '…/rust-toolchain.toml')`), **never** bare `rustc`. A bare-1.95 reading is a benign probe-context artifact, **NOT** a pin violation (the v1.4 §"Pinned gate numbers" toolchain row precedent, Doc 00 §11.51). |
| **H4** | **Concurrent-writer commit races** — B1 ∥ C1 (and any parallel fan-out) orphaning each other's commits | Medium (the recurring concurrent-agent class) | **Every writer wave gets its own isolated worktree** (`phase5.N/...` branches, the per-wave worktree column in §1); shared-tree only for pure readers; the orchestrator merges. B1∥C1 are file-disjoint by construction (manifest+code vs a new audit doc) — the disjointness is the *additional* safety, the worktree isolation is the *primary* one. No agent `git reset`/`git push` (`[[feedback_agent_no_reset]]` / the no-push rule). |
| **H5** | **Scope-creep boundary breach** — the WS simulator server / Web IDE / wasm pulled into v1.5 | Medium (the ROADMAP/backlog bundle adjacent minors) | **v1.5 = the VS Code/docs convenience layer ONLY.** Explicitly **NOT** the Doc 13 WS simulator server (its own later minor, the backlog SoT "simulator WS/JSON-RPC server → v1.5" line is a *menu placement*, NOT a v1.5-convenience-layer commitment — they are different epics; this plan's v1.5 is the **UI/DX convenience layer**, the deferred-WS-minor numbering is independent and may collide). **NOT** the Web IDE / wasm32 / Monaco / project-index (v1.6, ASSESSMENT §A.4 #7 confirms the placement). **If the epic numbering collides** (a deferred-WS-server-as-"v1.5" vs this UI/DX-as-"v1.5"), this epic may be **renumbered** — a *reversible ROADMAP edit*, decided by the orchestrator/owner, **out of this doc's scope** (this doc records the boundary; it does not pre-resolve a numbering collision). The §2.1-style "alternatives are the menu, not mid-epic drift" discipline (Doc 30 R5). |
| **H6** | **Doc-honesty staleness propagated publicly** — D1 surfaces the `docs/` corpus on a public-shaped landing site; the `ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B stale first-contact banners (README "v1.0 in active development / LSP deferred to v1.1+", Doc 14 "LSP deferred to v1.1", Doc 07 "all 22 docs written" with 30+ existing, the stale `docs/ROADMAP.md` "See also" pointer to `~/.claude/plans/toasty-prancing-goose.md` as "the live multi-phase execution plan") would be **broadcast** by D1 | Medium | The **ASSESSMENT PART B D-01/D-02 doc-honesty pass should precede or accompany D1** — it is a **separate already-identified backlog item** (`ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` §B.3; the v1.4 meta-session D.7(c)#5 carried the ROADMAP stale-pointer explicitly), **NOT** folded into a v1.5 *code* wave. **Specifically recorded here as a v1.5 doc-honesty consideration (NOT fixed in this doc — scope boundary):** `docs/ROADMAP.md` "See also" still calls `~/.claude/plans/toasty-prancing-goose.md` "the live multi-phase execution plan" — it is superseded by the backlog SoT + this Doc 31; the ROADMAP pointer must be reconciled to point at the backlog SoT + Doc 31. This is verified-true ground truth (`docs/ROADMAP.md:248`), carried as a finding for the doc-honesty batch that gates/accompanies D1; **not edited here** (this doc writes exactly one file). D1 must not be the thing that broadcasts a stale README. |
| **H7** | **Liveness / false-completion** — a `completed`-shaped notification wrapping an error advances a gate (esp. the post-A2 keystone audit or the pre-tag four-lens) | Medium (the recurring class; the v1.4 W4c transient-500 instance, `docs/META_SESSION_2026_05_16.md` L2) | **A `completed` envelope wrapping an error is neither a deliverable nor a verdict.** For any agent-completion that gates a release motion (the post-A2 §11.3 audit; the pre-tag audit; the cold quad), independently probe ground truth (`ps` + the output-file mtime + `git`/worktree state — `[[feedback_verify_agent_liveness]]`) **before** any gate advance; read-only audit work is idempotently re-dispatchable; **never advance a gate on a dead/erroring agent**. |

**Sequencing summary (the critical path, restated).** `W-A0 (this doc) → W-A1 (CLI-spawn UI, zero Rust) → W-A2 (LSP-embed + the mandatory source-derived post-A2 §11.3 keystone audit) → (W-B1 ∥ W-C1) → W-C2 (depends on C1's dispositions AND A2's live UI) → W-D1 (last; the only droppable-from-tag item) → phase-boundary audits → independent pre-tag four-lens audit → GATE_VERIFICATION_v1_5.md (commit X, the batched closeout incl. the §5 memory-SoT-freshness + release-record-symmetry sub-steps) → §11.30-clean cold-quad WITH the NOTE-1 STEP 0 at X (Rust quad + the re-activated JS lane) → v1.5.0 + checkpoint/<date>-v1_5 tag at X, LOCAL`. The §11.3 phase-boundary cadence is **at minimum the post-A2 keystone audit** (the single fork-possible boundary — the v1.4 keystone-first precedent) plus the standing pre-tag four-lens; the periodic-audit cadence (`[[feedback_periodic_audits]]`, every 2–3 implementer batches) is *pre-budgeted* per §7.

---

## 7. Process tweaks adopted from the v1.4 post-tag meta-session

> `docs/META_SESSION_2026_05_16.md` (the v1.4 post-tag closeout, on `main`) codified durable lessons + Part D.7(c) process-tweak recommendations. v1.5 adopts them explicitly *up front in the plan* (the D.7(c)#1 directive: "name it in the wave plan so it is planned, not absorbed").

1. **Pre-budget the soundness/keystone-audit surface (D.7(c)#1 + L1).** The post-A2 §11.3 keystone audit is the *expensive* part of this epic — it independently re-derives the keystone-in-UI invariant from source + re-runs the differential-oracle battery (the v1.4 triple-independent-derivation cost was real and "surprised the schedule"). It is **scheduled here as a first-class, named line item** (the W-A2 mandatory phase-audit row in §1 + the §2 binding gate row + the §5/§6-H2 gating), not absorbed silently. v1.5's soundness surface is *narrower* than v1.4's (the keystone is already an established invariant; v1.5 only needs to prove the *editor surface* honours it — a negative-grep + a differential battery, not a from-scratch bisimulation), but it is still budgeted, not assumed-free.
2. **A standing closeout release-record-symmetry check (D.7(c)#2 + L4).** The v1.4 L4 lesson: every "zero / unchanged / no-delta" claim in a release record is load-bearing and must be re-derived from `git diff`/source, not echoed (the v1.3 "zero Rust delta" cardinal near-miss vs the v1.4 clean re-derivation). v1.5's Rust delta is *small by design* (A2's dep edge + LSP wiring; the rest is TS/docs/static) — this is **exactly the framing-trap shape** (a small-but-nonzero delta easy to mis-state as "no Rust change"). The v1.5 gate-doc wave (closeout) MUST re-derive the Rust delta from `git diff <v1.4-tag-commit> X -- crates/ Cargo.toml Cargo.lock` and grep the record for the v1.3-cardinal-error form (0 hits) — codified in §5 as a gate criterion so it cannot be skipped under time pressure (`[[feedback_embeded_fsm_verify_record_release_layer]]`).
3. **A memory-SoT status-line-freshness gate at v1.5 closeout (D.7(c)#3 — the single biggest drift v1.4 found).** The cardinal v1.4 backlog drift was the SoT head status line + missing `v<x>.0 RELEASE RECORD` section lagging the actual tag. The v1.5 closeout MUST, *in the same closeout pass* (not deferred): advance the backlog SoT head status line to `v1.5.0 SHIPPED+TAGGED` + add a `v1.5.0 RELEASE RECORD` section with the wave→commit map (codified in §5 as a gate criterion).
4. **Carry the in-repo `docs/ROADMAP.md` stale-pointer (D.7(c)#5 + the L-series doc-honesty lineage).** `docs/ROADMAP.md` "See also" (verified at `docs/ROADMAP.md:248`) still calls `~/.claude/plans/toasty-prancing-goose.md` "the live multi-phase execution plan" — it is a v1.1-era plan superseded by the backlog SoT (and now this Doc 31). **Doc 31 records that the ROADMAP must be reconciled to point at the backlog SoT + Doc 31** — folded into the **v1.5 doc-honesty consideration (H6)**, **NOT fixed here** (the scope boundary forbids editing any other doc; this doc writes exactly one file). The `[[feedback_verify_status_claims_vs_code]]` "a first-contact-surface pointer drifted two minors stale is a lie" concern applies — it is tracked so it is not lost, to be fixed by the doc-honesty batch that precedes/accompanies D1.

*(D.7(c)#4 — the cosmetic slug-alias memory-hygiene normalisation — is a low-priority memory-store pass, not a v1.5 code/doc concern; noted as out-of-epic, not adopted as a v1.5 wave.)*

---

## 8. See also

- `docs/30-v1_4-Simulation-Verification-Wave-Plan.md` (the structural template + the `[[feedback_embeded_fsm_pipeline_before_ui]]` governing principle this epic is unblocked by) + its §5.2 gate-row form + §3 owner-decision pattern
- `docs/GATE_VERIFICATION_v1_4.md` (the §1/§4/§5.2-equivalent gate-row form; §5/§6.3 the carried owner-escalations v1.5 re-states; §4.2 the NOTE-1 cold-quad STEP 0)
- `docs/META_SESSION_2026_05_16.md` (Part D.8 L1–L7 + Part D.7(c) — the process tweaks §7 adopts; L1/L7 the keystone-as-epic-invariant; L4 release-record symmetry; L3 the cold-quad method + the false-red hazard)
- `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART A (the §3 Certifiability/MISRA-CERT-C parked track) + PART B §B.3 (the doc-honesty staleness H6/D1 must not propagate)
- `crates/fsm-cli/src/cmd/verify.rs` (the `fsm-verify/v1` `--json` schema A1 renders + A2's differential oracle) · `crates/fsm-verify/src/lib.rs` (the `pub` API A2 calls) · `crates/fsm-lsp/Cargo.toml` (the single `fsm-verify` dep edge A2 adds)
- `editors/vscode/src/commands/{cliRunner,cliBinary}.ts` (the proven CLI seam A1 reuses) · `editors/vscode/package.json` (the B1 manifest target) · Doc 22 (manifest spec) · Doc 27 (extension architecture — the click→source `SourceLocation` A1 reuses) · Doc 14 / Doc 26 (LSP capability + architecture — the A2 capability)
- `/root/dev/edgeforge/{landing,docs-site}/` (the D1 static-site model; the React part is `/root/dev/edgeforge/dashboard/`, irrelevant) · `/dev/16` CellWar/agar.io + `/dev/18` EdgeForge (the C1 TS frontend-practices yardstick)
- `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 acceptance, §11.3 phase-audit, §11.22 cold-quad, §11.30 tag-ordering) · `docs/00-Decisions-And-Reconciliation.md` §11 (the ledger; v1.5 rows assigned at v1.5 closeout — see the header numbering note)

*End of FSM-PLAN-UIDX-V15 v1.0.0 — accepted plan-of-record; the orchestrator gates/merges; v1.5 §11-ledger rows are assigned at v1.5 closeout, not pre-allocated here.*
