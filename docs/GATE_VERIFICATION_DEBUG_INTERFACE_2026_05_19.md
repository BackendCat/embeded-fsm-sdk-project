# GATE VERIFICATION — Interactive Debug-Interface Epic (Phase-8.0)

- **Document ID:** GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19
- **Date:** 2026-05-19
- **Milestone tag (locked TL decision — a minor that does NOT consume the owner-reserved v1.6):** `checkpoint/2026-05-19-debug-interface` (the proven `checkpoint/<name>` immutable-anchor pattern — LOCAL, no push; G9 is the owner's switch — OWNER-2). **This milestone is deliberately NOT a `vX.Y.0` tag.** The owner's roadmap reserves **v1.6.0 for the landing-page + doc-honesty pass (post-factory)**; the debug-interface minor lands as its **own post-v1.6 minor**, `checkpoint/<name>`-tagged at the §11.30-clean gate-doc commit until the release-numbering call is made (D-3 / OWNER-1 — the surface-don't-auto-number discipline the factory epic set, Doc 00 §11.80). Naming this `v1.6.0` would unilaterally consume the owner-reserved minor — so the `checkpoint/<name>` anchor (the same immutable-tag rigor, no version-number claim) is the correct vehicle. The §11.30/factory-GT-12 cold-quad rigor applies to this checkpoint tag **identically** to a release tag.
- **Scope:** the interactive debug-interface epic delta over the factory-reliability/CI milestone baseline `fbae38b` (`checkpoint/factory-reliability-ci-2026-05-18^{commit}` = `fbae38b1ccf7eb00fb344b0a034d16973254add9`, independently re-derived — the epic-boundary anchor) through the Phase-8.0 arc: **W0** (Doc 33 the wave-plan-of-record, `phase8.0/debug-iface-plan-of-record`) → **W1** (the `fsm/simulate*` LSP request layer + the single sanctioned non-stepping `Interpreter::set_context_field` helper — the only keystone-violable Rust/LSP wave, `1de83b0`) → **W2** (the VS Code debug WebviewPanel — overlay/inject/inspector/timeline over `fsm/simulate`, NO semantics, `032438c`) → **W3** (breakpoints + time-travel — the StepRecord-predicate + snapshot/restore client logic, NO semantics, `b79e052`/`ec903e5`) → **W4a** (capture→`.trace.json` round-trip — recorded-from-the-oracle via the shipped `write_trace_yaml`, byte-identical by construction, `ae774dd`→`8092d3a`) → the **W4b source-derived keystone phase-audit** (`phase8.0/w4b-keystone-audit`, audited `8092d3a`, verdict **`KEYSTONE-INTACT`**) → the **W4c independent pre-tag four-lens audit** (`phase8.0/w4c-four-lens-audit`, audited `8092d3a`, verdict **`TAG-CLEAR`**, 0 P0 / 0 P1), and the release commit **`X`** (this document + the batched Doc 00 §11 / CHANGELOG / ROADMAP / metrics consolidation; see §4 + §7 + §8). The epic-arc delta also folds the **gating fixes since the factory checkpoint** (#128 F-1 const-resolver, F-2 queue-config silent-misconfig, the `defer` doc-drift fold) that landed on the same arc — included in the re-derived Rust delta below, framed precisely (§8).
- **Companion evidence (frozen, never edited — the `AUDIT_*` never-overwritten convention):** the W4b binding keystone phase-audit `docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md` (branch `phase8.0/w4b-keystone-audit`, audited `8092d3a`, verdict `KEYSTONE-INTACT`); the W4c independent pre-tag four-lens audit `docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md` (branch `phase8.0/w4c-four-lens-audit`, audited `8092d3a`, verdict `TAG-CLEAR` — 0 P0 / 0 P1 / 3 P2 / 2 P3); Doc 33 `docs/33-Debug-Interface-Wave-Plan.md` (the accepted wave-plan-of-record — frozen); the v1.0–v1.5 baselines `docs/GATE_VERIFICATION_v1_{0,1,2,3,4,5}.md` + the factory-reliability milestone `docs/GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md`; the structural template `docs/GATE_VERIFICATION_v1_5.md` + `docs/GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` (mirrored 1:1).
- **Verdict:** **PASS — the interactive debug-interface epic (Phase-8.0) is milestone-tag-ready.** 0 open P0; the **KEYSTONE-IN-DEBUG binding tag-gate row (Doc 33 §2/§7) PASSES** via the independent W4b source-derived phase-audit (verdict `KEYSTONE-INTACT` — no forked semantics anywhere in the W1 `fsm/simulate*` LSP transport or the `editors/vscode/src/debug/**` webview; the shipped `fsm_simulator::execute_trace` oracle + the `cmd/test.rs` `execute_trace` seam + `write_trace_yaml` **byte-untouched across the entire epic arc** `fbae38b..8092d3a` save the single sanctioned additive non-stepping `Interpreter::set_context_field` helper, blob-SHA-identical per file; the host differential **3×**-deterministic + the W1 `fsm/simulate` LSP acceptance + the 47-test ExtHost suite re-run **by the auditor**, the catalogue-integrity contract `11+3+0=14` holding non-vacuously, the §W4 `.trace.json` replay independently confirmed non-vacuous — re-derived from source, not echoed). The W4c independent four-lens pre-tag audit returned `TAG-CLEAR` (0 P0, 0 P1, 3 P2, 2 P3 — the v1.1–factory §3 standard of 0 P0 AND ≤5 well-scoped P1 met with **zero P1**); the binding Rust SCA lens was **empirically run** = 0 vulnerabilities / 0 warnings over 207 deps; **no new network attack surface** (the epic rides `fsm-lsp`'s existing `LspService::build().custom_method` stdio trust boundary — NO standalone daemon, NO new port, NO new socket; the owner D-2 "attack surface removed by construction" posture confirmed). Every NOTE is documentation-of-record / closeout-batch / accepted-tracked-debt, folded by this closeout or carried (§6). The independent pre-tag four-lens audit is satisfied (W4c, frozen). The canonical confirming **cold-from-source** quad **with the §11.30/NOTE-1 mandatory `cargo clean -p` STEP 0** (§4.2) **and the BINDING JS lane** (§4.3 — the debug surface *does* touch `editors/vscode/**` via W2/W3/W4, *exactly like v1.5/factory*) at the release commit `X` is the gating step the milestone tag is contingent on (§4) — recorded here as the contingent gate, **not a completed result; no quad/tag result is fabricated**.

This document is the canonical release record for the interactive debug-interface epic (Phase-8.0). It captures the MVP-gate status (§1), the Phase-8.0 surface (§2 — the `fsm/simulate*` LSP transport + the debug WebviewPanel + breakpoints/time-travel + the capture round-trip, plus the F-1/F-2 gating fixes), the verification evidence (§3 — the W4b keystone phase-audit + the W4c four-lens pre-tag audit, both frozen + the contingent cold-quad), the §11.22/§11.30-mandated **cold-from-source** quad posture **with the NOTE-1 explicit `cargo clean -p` STEP 0** and **the binding JS lane** (§4), the post-tag owner actions (§5), the accepted-tracked-debt + carried owner-escalations a consumer must be aware of (§6), the sign-off (§7), the §11.30-clean tag topology + the **Pinned gate numbers re-derived at `X` not echoed** (§8), and the **DRAFT Doc 00 §11 rows reported** for the orchestrator to independently re-derive & append at W4d (§9 — NOT in the append-only ledger).

---

## 1. MVP gate (Doc 23 §9) — Phase-8.0 status

| Gate | Phase-8.0 status | Evidence |
|---|---|---|
| **G1** `fsm check` ok / clean reject, no panic, no UB | ✅ | The Phase-8.0 Rust delta vs the factory checkpoint (`fbae38b`) is **moderate-and-additive-by-design**: `git diff --shortstat fbae38b..8092d3a -- crates/` = **28 files / +3260 / −214** (independently re-derived at the gate-doc commit — §8 "Pinned gate numbers"). It is the W1 `fsm/simulate*` LSP transport (`crates/fsm-lsp/src/capabilities/simulate.rs` +693, a pure `Interpreter`-marshalling layer + the W4a `capture` op) + its acceptance suite (`crates/fsm-lsp/tests/simulate_lsp_acceptance.rs` +945) + the **single sanctioned non-stepping `Interpreter::set_context_field` helper** (`crates/fsm-simulator/src/interpreter.rs` +25, a 3-line guarded `HashMap::insert`, **0 `StepRecord` by construction**) + the gating #128 F-1/F-2 codegen-correctness fixes (`crates/fsm-analyzer/**`, `crates/fsm-codegen-c/**`, `crates/fsm-ir/src/model.rs`, `crates/fsm-parser/**` — genuine corrections of a #110-class silent-misconfig the W4c Lens-2 confirmed) + the test-corpus expansion (`crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` +64/−4 — factory-wave additive, NOT an oracle change). `fsm check`'s parse→analyze→diagnostics path's only delta is the **intended** F-1/F-2 fixes (each adding a hard rejecting diagnostic — `FSM-E0411`/`FSM-E0412` — never a silent drop; the live `DiagnosticCode` count is re-derived = **75**, the F-1/F-2 +2, §8); the differential **oracle** `crates/fsm-simulator/src/` (save the one sanctioned helper) + the `cmd/test.rs` `execute_trace` seam + `write_trace_yaml` are **byte-untouched** across the whole epic arc (W4b §1 near-EMPTY-diff + per-file blob-SHA-identical; the keystone holds — §3). |
| **G2** `fsm check broken.fsm` → exit 1 + Rust-style error | ✅ | Preserved + **strengthened**: the F-1/F-2 fixes add **two hard rejecting diagnostics** (`FSM-E0411` "timer duration must be a compile-time constant", `FSM-E0412` non-power-of-2 in-source queue capacity) replacing two previously-**silent** miscompiles (the divergent-second-resolver class — W4c Lens-2 DD-14/DD-15/DD-16). The live `DiagnosticCode` count re-derived = **75** (the CI-lock `const EXPECTED: usize = 75` at `crates/fsm-diagnostics/src/lib.rs:851`, §8); the G7 count-discipline lock is GREEN + non-vacuous (W4c DD-12). |
| **G3** `fsm generate` emits the C unit set + HAL | ✅ | Preserved. The debug epic adds **no codegen surface**; the F-1/F-2 fixes **deliberately changed production-C for the better** (the divergent const/queue resolvers unified into one shared source of truth ⇒ `after CONST ms`-timers + in-source `queue {}` capacity now reach generated C *and behave correctly at runtime* — W4c DD-11 gcc-compile-and-RUN 4/4, both Switch & Table strategies). A *correctness* fix, not a regression; the keystone is *no forked oracle*, not frozen output, and that holds (W4b/W4c). |
| **G4** `gcc -std=c99 -Wall -Wextra -Wpedantic -Werror` zero-warning | ✅ | Unchanged; subsumed in the cold quad's `cargo test --workspace` §5.4 gcc-compile-and-RUN battery (incl. the F-2 `queue_config_runs.rs` gcc-RUN harness — W4c DD-11). No new codegen warning surface. |
| **G5** `fsm fmt` idempotent | ✅ | Unchanged — `crates/fsm-formatter/` is **not** in the Phase-8.0 delta (`git diff --stat fbae38b..8092d3a -- crates/fsm-formatter/` ⇒ ∅; the §8 file list contains no formatter crate). |
| **G6** all examples full-chain + sim-trace-match | ✅ | Preserved + **strengthened**. The W1 host differential + the W1 `fsm/simulate` LSP acceptance prove the debug transport's `StepRecord` stream **byte-equals** the shipped `fsm_simulator::execute_trace`/`StepRecord` oracle; the §W4 `capture`→`.trace.json` round-trips through the SAME `write_trace_yaml`/`execute_trace` `fsm test` uses ⇒ byte-identical by construction (W4b §3/§4, frozen; the auditor independently re-ran the host differential 3× + the W1 LSP acceptance + the 47-test ExtHost suite incl. the W2/W3/§W4 byte-match-vs-oracle trio). |
| **G7** ≥1 test per diagnostic code | ⚠️ tracked (improved this epic) | The W3 conformance lock (factory-W3) makes the live-enum→fixture coverage a build-failing invariant. Re-derived: the conformance MANIFEST grew **53 → 55** fixtures (parser 7 / validator 13 / semantic 28 / codegen-c 4 / formatter 3 — re-derived §8; the +2 are VAL-NEG-009/010 for the new F-1/F-2 `FSM-E0411`/`FSM-E0412`); the G7 lock is **4/4** incl. `every_live_code_has_a_conformance_fixture_or_a_justified_allowlist_entry` (real fixtures, no allowlist dodge — W4c DD-12). The `.contains` codegen-c conformance oracle remains a tracked deferred-tail (the #123-class item, owned by the G7 wave for behavioural conversion — W4c DF-04, accepted-tracked §6); the W1/W2 differential + the W3 lock are the binding behavioural gate in the interim. **Improved, not regressed.** |
| **G8** `cargo build/test/clippy/fmt` clean | ✅ | **cold-from-source** quad posture below (§4), with the **NOTE-1 explicit `cargo clean -p` STEP 0** written into §4.2 as a mandatory step **and** the **binding JS lane** (§4.3 — Phase-8.0 touches `editors/vscode/**` via W2/W3/W4). De-risking predicate (NOT a substitute for the canonical run): the W4b auditor re-ran the host differential **3×** deterministic `7 passed / 0 failed; 2 ignored` + the W1 LSP acceptance `7 passed / 0 failed` (×2) + the 47-test ExtHost suite (exit 0) at `8092d3a`; W4c independently re-ran the full workspace **911 passed; 0 failed** + the host differential `7/0` + the W1 LSP acceptance `7/0` + the F-2 gcc-RUN `analyzer 7/7 + codegen-c 4/4` + the G7 lock `4/4` at `8092d3a`; **this closeout re-ran** the host differential (`7 passed; 0 failed; 2 ignored`) + the W1 LSP acceptance (`7 passed; 0 failed`) at the working tree — a red canonical run is improbable, **not** a substitute for the contingent run at `X`. |
| **G9** CI matrix linux/macos/windows | ⚠️ tracked (lanes ready from the factory epic, never run remote) | `.github/workflows/ci.yml` carries the factory epic's 2-job `build`(1.75 matrix)+`sca`(`cargo audit`) + the 4 additive-isolated lanes (`conformance`/`extension-host`/`coverage-rust`/`on-target`) — **byte-untouched by the debug epic** (the debug epic adds **no** `.github/` change; §8). **None of these has ever executed on a remote CI runner** (local-only repo — owner controls the remote, a hard project rule). The debug-epic ExtHost lane (`debugPanel.test.ts` + the v1.3-path-identity `diagram.test.ts`) is CI-runner-only on this disk-tight box (W4c "Sequence note": `node_modules` absent + an Electron download required — the factory on-target-lane honest-skip-by-design precedent; NOT a finding) — it must run green in the `extension-host` CI lane at `X` (the binding JS lane, §4.3, run by the orchestrator at `X`). The auditors source-verified its structure (the W4 `fsm test` byte-identity assertion + the `#svg && #banner` v1.3-path-identity guard). Post-tag owner action §5. |

G7 + G9 are the **same explicitly-tracked partials accepted at the v1.0–v1.5 + factory gates** — neither is a Phase-8.0 regression (G7 *improved*: the F-1/F-2 fixes add 2 codes + 2 fixtures, the lock GREEN; G9's lanes are factory-ready + locally `make ci-local`-provable, still remote-unrun); both are documented in CHANGELOG `[debug-interface]` *Known limitations* and the ROADMAP.

---

## 2. Phase-8.0 surface delivered (since the factory checkpoint `fbae38b`)

**Theme: the interactive debug interface — an in-editor live simulation/debug surface that is JUST ANOTHER FRONTEND of the one shipped `fsm_simulator` oracle, never a second semantics.** The owner's governing **pipeline-before-UI** sequencing principle (`[[feedback_embeded_fsm_pipeline_before_ui]]`) was satisfied at v1.4 (W4a `FACTORY-COMPLETE`); the v1.5 convenience layer + the factory-reliability/CI proof layer shipped on top. This epic is the owner-decided (D-1/D-2/D-3) interactive debug interface — surfacing the shipped simulator in VS Code via the proven v1.5 W-A2 `custom_method` LSP seam, with **the single keystone-violable surface concentrated into one gated wave (W1)** and W2–W4 holding the no-fork invariant *trivially* (they render the oracle's output and compute nothing).

### 2.1 W0 — the wave-plan-of-record (Doc 33)

Doc 33 `docs/33-Debug-Interface-Wave-Plan.md` — the accepted plan-of-record (the Doc 28/29/30/31/32 epic-kickoff-doc precedent; the next free integer after Doc 32, verified). It formalised the **KEYSTONE-IN-DEBUG binding gate row** (§2), the 4 owner-decision residuals with non-blocking safe parked defaults (§4 OWNER-1..OWNER-4), the §11.30 Phase-8.0 tag-gate criteria (§7), the carried Zero-Legacy findings CF-1/CF-2 (§5), and the §11-ledger numbering refinement (Phase-8.0 rows assigned at the *closeout*, NOT pre-allocated — the v1.4-L6 / v1.5-Doc-31 / factory-Doc-32 precedent; ledger head independently verified §11.86). Its ground-truth verification ledger (GT-1..GT-15) re-derived the entire keystone surface from source and **flagged a two-line DBGUX citation drift** (DBGUX §3.2 `trace.rs:64`/`:172` → actual `:65`/`:174`; corrected in Doc 33's GT-7; the frozen DBGUX doc NOT edited — design evidence). Frozen — not annotated by this closeout (Doc 33 is the wave-plan of record).

### 2.2 W1 — the `fsm/simulate*` LSP request layer + the `set_context_field` helper (the SINGLE keystone-violable wave) (`1de83b0`)

The **single Rust/LSP-layer enabling change of the entire epic**: `crates/fsm-lsp/Cargo.toml` gained exactly one `fsm-simulator = { path = "../fsm-simulator" }` **internal path-dep edge** (+ a WHY comment) + a 1-line `Cargo.lock` dependency-list entry — **no new crate** (re-derived: §8 — `#![forbid(unsafe_code)]` stays 12 roots / 11 crates; the only Cargo.lock change is `+ "fsm-simulator"` in the `fsm-lsp` dep list, 0 new external `[[package]]`). A new `fsm/simulate` LSP custom request (`crates/fsm-lsp/src/capabilities/simulate.rs`, +693) is a **pure transport frontend** of `fsm_simulator::Interpreter`: every `op` (`init`/`dispatch`(+payload)/`advanceClock`/`setContext`/`snapshot`/`restore`/`capture`) is **exactly one shipped `Interpreter` call** (or one `write_trace_yaml` for `capture`) + serde marshalling — computing **zero** simulator facts locally. The **single sanctioned `crates/` behaviour delta of the entire epic** is the one additive non-stepping `Interpreter::set_context_field` helper (`crates/fsm-simulator/src/interpreter.rs` +25 — a 3-line guarded `rt.context.insert` with a `NotInitialized` guard, the *same nature* as `InitOptions::initial_context`, returning `Result<(), StepError>` so it carries **no `StepRecord` by construction**; it runs **no** transition/guard/step/clock/queue/timer logic — it does **not** implement semantics). The 945-line `crates/fsm-lsp/tests/simulate_lsp_acceptance.rs` is the in-process tower-lsp differential-oracle suite (the `StepRecord` stream byte-equals `fsm test`'s `execute_trace` + the `set_context_field`-emits-zero-StepRecord contract + the no-fake-clean-end bar). **The KEYSTONE-IN-DEBUG binding gate row (Doc 33 §2/§7) is recorded PASSED via the independent W4b source-derived audit (verdict `KEYSTONE-INTACT`)** — see §3.

### 2.3 W2 — the VS Code debug WebviewPanel (overlay / inject / inspector / timeline — NO semantics) (`032438c`)

`editors/vscode/src/debug/{debugPanel,index,simulateClient}.ts` + `editors/vscode/src/debug/webview/debugWebview.ts`: the debug WebviewPanel — the v1.3 statechart **reused VERBATIM** (`renderModel` imported, NOT reimplemented — no second renderer) + active-config overlay + transport rail + Inject panel (typed-payload form revealed adjacent to the picker — the proximity principle) + advance-clock + context inspector with the Δ column + the StepRecord timeline; all driven through the W1 `fsm/simulate` requests over the **existing** v1.5 W-A2 `LanguageClient` boundary (NO new daemon/port/socket — D-2 confirmed); the v1.3 empty/honest states inherited (stale-banner reused VERBATIM, transport disabled-with-inline-reason, never blanks/fakes). The one v1.3-source change (`diagramWebview.ts`) is a disclosed **behaviour-preserving** refactor (additive named exports + a `getVsCodeApi()` singleton + an `#svg && #banner` standalone-guard) — the v1.3 *bundled output* `dist/webview/diagramWebview.js` is byte-untouched (esbuild per-entrypoint); the v1.3-path-identity `diagram.test.ts` is the pre/post-identity bar (W4c DF-01 — non-gating, the precise framing folded here). NO breakpoints/time-travel/capture yet (W3/W4).

### 2.4 W3 — breakpoints + time-travel (the StepRecord-predicate + snapshot/restore client logic — NO semantics) (`b79e052`/`ec903e5`)

Breakpoint glyphs on diagram nodes/edges (click-to-set); the client-side StepRecord-**predicate** evaluator `evaluateBreakpoint` (`debugPanel.ts:188–209` — a pure filter: `step.enteredStates.includes(targetId)` / `exitedStates.includes(targetId)` / `transitionTaken?.stableId === targetId`, reading ONLY the three oracle-emitted `StepRecord` fields — **NEVER** a guard/transition re-evaluation, W4b §2(b)); auto-pause-at-breakpoint via restore-the-pre-step-snapshot; the timeline as the time-travel surface (`◀ rewind to #N` as a row action — proximity); the UI-paced single-step reveal over W1's `Vec<StepRecord>`. Time-travel is `Interpreter::snapshot`/`restore` only (no client-side state reconstruction — W4b §2(c) `highlightFromOracle` selects *which* oracle field to paint, never derives a configuration). NO capture yet (W4).

### 2.5 W4a — capture→`.trace.json` round-trip (recorded-from-the-oracle, byte-identical by construction) (`ae774dd`→`8092d3a`)

The capture→`.trace.json` round-trip: the `TraceCommand`s the author actually injected + the W1 oracle's actual `Vec<StepRecord>` → the shipped `write_trace_yaml` (whose content is JSON despite the historical name) → a `.trace.json` fixture; `fsm test` later re-runs it via the SAME `execute_trace` ⇒ byte-identical by construction (this *inverts* F6: the test is recorded-from-the-oracle, not guessed). The W4a `capture` op (`simulate.rs:578–660`) is **one `write_trace_yaml` call + serde marshalling, zero `Interpreter`/semantics**, surfaces the serializer's own error verbatim (no fake "captured ✓"), posts "captured ✓" **only after** the file exists on disk (the no-premature-success / no-"Сохранено"-before-confirm bar, DBGUX §6), and **rejects empty `expected`** at the source ("`fsm test` would report nothing-to-verify — not a pass"). **The `.trace.json`-not-`.trace.yaml` self-correction (`ae774dd`→`8092d3a`) is real and load-bearing** — `fsm test`'s `collect_traces` (`cmd/test.rs:159`, byte-identical to the anchor) discovers ONLY `*.trace`/`*.trace.json`; a `.trace.yaml` would be **silently skipped → a vacuous pass**. W4b §4 independently confirmed the §W4 replay is **non-vacuous** (`fsm test` reports a real `pass`, not `skip`, on the `.trace.json` form; `.trace.yaml` would be silently undiscovered).

### 2.6 The gating #128 F-1/F-2 codegen-correctness fixes + the `defer` doc-drift fold (on the same arc)

The epic-arc delta over `fbae38b` also folds the gating fixes that landed on the arc (W4c Lens-2, DD-14..DD-17): **F-1** (const-resolver) unifies a divergent SECOND resolver (the lowerer's `eval_i64` omitted `EXPR_NAME_REF` while the check resolved it ⇒ `after CONST ms` passed `fsm check` but the lowerer silently dropped the timer — a #110-class silent miscompile) into ONE shared `util::eval_const_expr_value` + `util::file_const_table` consumed by BOTH lowerer and check (lower≡check by construction — the keystone-pattern applied to const-folding), with a hard `FSM-E0411` for the genuinely-deferred runtime-variable form (never a silent drop); **F-2** (queue-config) fixes `lower_queue` matching the KEY not the RHS + codegen reading the CLI default not the IR queue (codegen↔sim silently diverged) via a typed `ConfigEntry::value()` (single SoT shared with the analyzer check — the F-1 principle) + `CodegenConfig::resolve_queue` (explicit per-field precedence + a `note:` on shadow, never silent), with a hard `FSM-E0412` for a non-power-of-2 in-source capacity; the **`defer` doc-drift fold** replaces the stale "v1.0 limitation / rejected by `FSM-E0903`" §9.4 paragraph in `docs/04-DSL-Specification.md` with honest v1.1-shipped status (a genuine doc-honesty correction — W4c DD-17). Each is a genuine correction of a #110-class silent-misconfig (the divergent-second-resolver de-duplication), behaviourally proven end-to-end (W4c DD-11 gcc-RUN), the G7 count-discipline honestly followed (EXPECTED 73→75, +2 fixtures, the lock GREEN + non-vacuous — W4c DD-12/DD-14).

**Planning doc of record:** Doc 33 `docs/33-Debug-Interface-Wave-Plan.md` (frozen — not annotated by this closeout; it is the wave-plan of record).

---

## 3. Independent phase-boundary audits (read-only; frozen evidence)

The §11.3 cadence for Phase-8.0 was, per Doc 33 §1 W4 / §2 / §7, **at minimum the W4b source-derived keystone phase-audit** (W1 being the only Rust/LSP-adding, single fork-possible boundary — the v1.4/v1.5/factory keystone-first precedent) plus the standing independent pre-tag four-lens (W4c). The W4b keystone audit is the **expensive, binding** one (it independently re-derives the KEYSTONE-IN-DEBUG invariant from source across the whole debug surface **and re-runs the host differential + the W1 LSP acceptance + the 47-test ExtHost suite + the §W4 non-vacuity hand-run** — pre-budgeted as a first-class line item per Doc 33 §1/§2, *not* absorbed silently).

| Audit (frozen doc / branch) | HEAD audited | Verdict | Key result |
|---|---|---|---|
| **W4b KEYSTONE** `AUDIT_PHASE_DEBUG_W4_2026_05_19.md` / `phase8.0/w4b-keystone-audit` | `8092d3a` | **`KEYSTONE-INTACT`** (0 P0; the cardinal regression — a forked oracle / a re-implemented step-transition-guard / a synthesized completion / a client-side time-travel reconstruction / a catalogue-integrity violation / a vacuous (`skip`) §W4 replay — explicitly **absent**) | _Verdict independently re-read in full at this closeout (the §11.29 verify-the-record discipline — not echoed; the doc's VERDICT/§1/§2/§3/§4/Explicit-confirmations each state `KEYSTONE-INTACT`)._ The auditor **re-derived the no-fork from source** (not from a wave's self-report): over the entire epic arc `fbae38b..8092d3a`, the ONLY delta under `crates/fsm-simulator/src/` + `crates/fsm-cli/src/cmd/test.rs` is the **single sanctioned additive non-stepping `Interpreter::set_context_field` helper** (W1 `1de83b0`, +25, the full hunk read = a 3-line guarded `rt.context.insert`, no `StepRecord` by construction); `cmd/test.rs` is **NOT in the diff at all** (blob-SHA `6ffb91aa…` identical at both endpoints); `src/trace.rs` (`write_trace_yaml`+`execute_trace`) + every transition/guard/step/clock/queue/timer/LCA/completion/history module are **blob-SHA byte-identical** (auditor-recomputed per file); negative-grep ∅ for transition-selection/guard-eval/completion-synthesis/RTC-step/active-config-computation *defined* across the WHOLE debug surface (`simulate.rs` 693 ln + `editors/vscode/src/debug/**` 2946 ln incl. `webview/` — every grep hit read + adjudicated a disclaimer / comment / error-label projection / StepRecord-field read / the documented breakpoint predicate / the pure IR-JSON edge-id↔stableId bridge; **every `fsm/simulate` op handler = exactly one shipped `Interpreter` call + marshalling**). The auditor **re-ran the host differential 3× deterministic `7 passed; 0 failed; 2 ignored`**, re-derived the catalogue-integrity contract from source AND the differential's own `--nocapture` output (`CORPUS`=14, `BYTE_EQUAL`=11, `BEHAVIOURALLY_EQUIVALENT_JUSTIFIED`=3, `KNOWN_DIVERGENT`=`&[]`; partition `11+3+0=14` dynamically asserted in-test; the never-game-guard **proof bodies read** + non-vacuous), re-ran the W1 `fsm/simulate` LSP acceptance `7 passed, 0 failed` (×2) + the ExtHost suite under `xvfb-run` `47 passing, exit 0` incl. the three keystone-critical W2/W3/§W4 byte-match-vs-oracle tests, and **independently hand-ran `fsm test` on the `.trace.json` form** confirming a real `pass` (not `skip`) — the §W4 replay non-vacuity, the `.trace.yaml` would be silently undiscovered. |
| **W4c pre-tag four-lens** `AUDIT_PRE_TAG_DEBUG_2026_05_19.md` / `phase8.0/w4c-four-lens-audit` | `8092d3a` | **`TAG-CLEAR`** — **0 P0 / 0 P1 / 3 P2 / 2 P3** | _Verdict independently re-read in full at this closeout (not echoed; the doc's Verdict/§Findings/end-note each state `TAG-CLEAR`, "No P0. No P1.")._ The standing independent pre-tag four-lens (the v1.1–factory §3 pattern), scoped to the **debug-epic delta** `fbae38b..8092d3a` = **57 files / +10045 / −243** (NOT a keystone re-litigation — W4b did that, frozen; NOT a whole-codebase re-audit; the delta also covers the F-1/F-2 gating fixes + the design docs + Doc 33). Each lens independently re-derived (not echoed): **Lens 1 Arch** PASS (the single net-new Rust dep edge is the *internal* `fsm-lsp`→`fsm-simulator` path dep — **0 new external `[[package]]`**, the v1.5 W-A2 single-internal-edge shape; no layering inversion — `fsm-simulator`'s `Cargo.toml` does NOT depend back; `forbid(unsafe_code)` re-derived 12/12 files unchanged; the v1.3 renderer reused VERBATIM, the one v1.3-source change a disclosed behaviour-preserving refactor — DF-01); **Lens 2 Correctness** PASS (the F-1/F-2 fixes are genuine #110-class corrections — the auditor built+ran the F-2 gcc-RUN harness `analyzer 7/7 + codegen-c 4/4`, the byte-identity property empirically REPRODUCED `simulate_lsp_acceptance` 7/7 incl. 3 byte-equal-to-`execute_trace` differentials; full workspace **911 passed; 0 failed**; G7 EXPECTED 73→75 the lock GREEN+non-vacuous); **Lens 3 Reliability** PASS (`StepError` surfaced verbatim, malformed→honest invalid-params, the v1.3 stale-banner reused VERBATIM, the **pending-timer honestly degraded** — the #141 deferred surface NOT faked, no fake ✓; the disclosed ~3.4mb `debugWebview.js` bundle is the pre-existing v1.3-ELK-per-entrypoint pattern, NOT an epic regression — `package-lock.json` unchanged); **Lens 4 Security/SCA** PASS (binding) — `cargo +stable audit` **empirically run** over **207 deps** = **0 vulnerabilities / 0 warnings** (NOT honest-skipped — the tool is present), the epic added **ZERO new npm deps** (`package.json` manifest-contributions-only; `editors/vscode/package-lock.json` NOT in the 57-file delta — independently derived TWO ways), the 3 pre-existing devDep-only transitive npm vulns byte-identical to the factory-tag accepted-with-rationale set (epic added 0 of them) → v1.6-FE-hygiene-tracked, not gating; **no new network attack surface** — the debug surface rides the EXACT existing W-A2 stdio `custom_method` boundary (no daemon/port/socket; the only net/process in the delta is test-only `gcc`/`fsm test`). 5 findings total, **none gating** (DF-01/DF-03/DF-04 P2, DF-02/DF-05 P3). |
| **W4b + W4c applicability at `X`** *(re-derived at this closeout, not echoed)* | `X` | **applicable by construction** | Both W4b (`KEYSTONE-INTACT`) and W4c (`TAG-CLEAR`) audited `8092d3a` (the W4a HEAD == base main of this closeout's worktree `phase8.0/w4-closeout`, base main `8092d3a` — re-confirmed `git rev-parse HEAD` = `8092d3a02a7bd6a6ee9d3dad306580d9b3019a0b`). `X` adds **only** this gate doc + the batched Doc 00 §11 / CHANGELOG / ROADMAP / metrics consolidation — **no `crates/`/`.github/` code-tree touch whatsoever** (this closeout's scope boundary explicitly forbids it; there is NO F-05-equivalent code tidy in this epic's closeout — the W4c findings DF-01..DF-05 are framing/doc-precision/accepted-tracked, **none requiring a code change in scope**). No keystone/oracle/seam/comparator-fn logic is touched at `X` — so W4b's `KEYSTONE-INTACT` and W4c's `TAG-CLEAR` hold byte-for-byte at `X` by construction. The contingent cold quad + the binding JS lane at `X` (§4) is the empirical re-confirmation. |

### 3.1 Why these audits are not rubber-stamps (recorded so the record is honest)

A `KEYSTONE-INTACT` verdict on the only fork-possible boundary of an epic that adds a new simulator-touching dep edge + a stateful interactive surface + two production-codegen-correctness fixes is worse than a found problem if it merely echoes a wave's claim. It did not: W4b did **not** trust any wave's negative-grep or doc-comments — it ran the near-EMPTY `git diff fbae38b..8092d3a` itself **+ per-file blob SHAs** (proving `cmd/test.rs`/`trace.rs`/the whole `fsm-simulator/src/` semantic core byte-identical and the ONLY delta the sanctioned 3-line `set_context_field`), read **every** negative-grep hit across the whole debug surface and adjudicated each (the breakpoint predicate confirmed a pure StepRecord-field filter), **re-ran the host differential 3×** + re-derived the catalogue-integrity contract from both source and the differential's own `--nocapture` output + **read the never-game-guard proof bodies**, re-ran the W1 LSP acceptance + the 47-test ExtHost suite, and **independently hand-ran `fsm test`** to prove the §W4 `.trace.json` replay non-vacuous (a `.trace.yaml` would be silently undiscovered — the exact trap the `ae774dd`→`8092d3a` self-correction avoids). W4c independently **reproduced the byte-identity property** (its own `simulate_lsp_acceptance` 7/7), **re-derived the npm-zero-delta TWO ways** (package.json manifest-only + package-lock.json not in the delta), **actually ran** `cargo +stable audit` (not honest-skipped), source-verified the no-new-network-surface posture, and built+ran the F-2 gcc-RUN harness to prove the silent-misconfig class closed end-to-end. The epic is clean *because* every wave reported-then-§11.1-verified with per-commit `git show` — corroborated, not assumed.

---

## 4. Cold-from-source release quad (§11.22 / §11.30 / NOTE-1) + the binding JS lane — the tag gate

### 4.1 The binding commit-ordering rule (Doc 00 §11.30, quoted)

> **Refinement (binding for v1.2+):** the release sequence is (1) author+commit `GATE_VERIFICATION_<tag>.md`, (2) run the cold-from-source quad at THAT commit, (3) `git tag -a` THAT commit + the `checkpoint/<date>` anchor — quad-commit ≡ tag-commit exactly.

This document makes code-summarising assertions (the §1 gate evidence; the §8 "Pinned gate numbers" — live `DiagnosticCode`=75, `forbid(unsafe_code)`=12 roots/11 crates, conformance=55, the Phase-8.0 Rust delta = 28 files / +3260 / −214) that a code delta could in principle falsify. The §11.30 sequence is followed strictly: this doc + the batched Doc 00 §11 / CHANGELOG / ROADMAP / metrics consolidation commit first (commit `X`) — **with NO `crates/`/`.github/` code-tree touch** (this epic's closeout has no F-05-equivalent; the W4c DF-01..DF-05 findings are framing/doc-precision/accepted-tracked, none requiring an in-scope code change) — the canonical cold quad + the binding JS lane run **at that exact commit `X`**, and the milestone tag placed on **that same commit** — zero off-by-one by construction (the v1.2/v1.3/v1.4/v1.5/factory prospective-§11.30 pattern, the **7th consecutive** prospective-clean release: v1.1 retroactively reconciled §11.30; v1.2–v1.5 + the factory checkpoint prospectively; Phase-8.0 prospectively). Because this closeout adds **no code-tree change at all**, the v1.1-§7 one-pure-docs-commit grace would in principle apply; the §11.30 sequence is followed strictly regardless (the conservative, established pattern).

### 4.2 Cold-quad method (§11.22) — **with the MANDATORY NOTE-1 `cargo clean -p` STEP 0 (binding v1.4+)**

§11.22 mandates a **cold from-source** green quad for a release tag (a warm shared-`CARGO_TARGET_DIR` can serve stale cross-worktree `-wt-`-baked-path test binaries — the v1.4 L3 *false-red* hazard; a warm pass is necessary-but-not-sufficient). **NOTE-1 hardening (Doc 00 §11.70 — binding v1.4+):** the cold-quad's "cold" precondition is made **explicit and mandatory** as a written step, *not* merely the word "cold":

> **MANDATORY STEP 0 (NOTE-1 / §11.22 / §11.30):** **before** any full-workspace gate, run **`cargo clean -p` of ALL first-party workspace crates** — `cargo clean -p fsm-diagnostics -p fsm-lexer -p fsm-parser -p fsm-ir -p fsm-analyzer -p fsm-codegen-c -p fsm-simulator -p fsm-formatter -p fsm-cli -p fsm-lsp -p fsm-verify` (cargo 1.75 needs the repeated `-p` form) — **then** the canonical quad. This is non-negotiable. The box-infeasible "fresh non-shared `CARGO_TARGET_DIR`" ideal is **explicitly rejected** (a virgin target rebuilds the ~14 GiB dep graph; the box runs disk-tight). The all-first-party `clean -p` evicts every first-party build+test artifact (incl. any stale `-wt-`-baked test binary) **while preserving the content-addressed registry dep cache** (Cargo fingerprint is path-independent), and Phase-8.0 added **0 new external deps** (re-derived §8 — the single Cargo.lock edge is the *internal* `fsm-lsp`→`fsm-simulator` path dep, already a workspace member) so the registry cache is unchanged from the factory checkpoint. The recorded disk-protective conventional-CI-cache equivalent, **not a workaround to grind past** (`[[feedback_embeded_fsm_tag_cold_quad_method]]`).

After Step 0: `cargo build/test/clippy/fmt --workspace` + the conformance runner (`fsm test tests/conformance`, 55/55) + the §5.4 `gcc` compile-and-**RUN** acceptance (subsumed inside `cargo test --workspace`, incl. the F-2 `queue_config_runs.rs` gcc-RUN harness) + the W1 `fsm/simulate` LSP acceptance + the host differential + the G7 lock. `df -h /` checked before/after (the box is at ~19 G free / 76% — above the ≥5% margin; the cold trough is managed by the validated *clean-first-party-from-source, keep the content-addressed registry dep cache* pattern). **De-risking predicate (NOT a substitute for the canonical run):** the W4b auditor re-ran the host differential **3×** `7 passed / 0 failed; 2 ignored` + the W1 LSP acceptance `7/0` (×2) + the 47-test ExtHost suite (exit 0) at `8092d3a`; W4c re-ran the full workspace `911 passed; 0 failed` + the host differential `7/0` + the W1 LSP acceptance `7/0` + the F-2 gcc-RUN `7/7+4/4` + the G7 lock `4/4` at `8092d3a`; this closeout re-ran the host differential (`7 passed; 0 failed; 2 ignored`) + the W1 LSP acceptance (`7 passed; 0 failed`) at the working tree — a red canonical run is improbable, **not** a substitute for the contingent run at `X`.

### 4.3 The BINDING JS lane at the gate-doc commit (binding for Phase-8.0 — the surface *does* touch `editors/vscode/**`)

Phase-8.0's surface **does touch `editors/vscode/**`** (W2 the debug WebviewPanel, W3 breakpoints+time-travel, W4 capture — `editors/vscode/src/debug/**` + 1 new ExtHost test `debugPanel.test.ts` + the one disclosed behaviour-preserving `diagramWebview.ts` refactor) — *exactly like v1.5/factory, unlike v1.4*. The JS lane is therefore a **binding gate at the gate-doc commit `X`**, the `GATE_VERIFICATION_v1_5.md` §4.3 / `GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` §4.3 re-activated/binding-JS-lane precedent:

> **The JS lane at `X`:** (a) `npm audit` (the repo's equivalent) clean / **triaged** — at `X`, expected to re-derive the W4c finding: **3 PRE-EXISTING devDependency-only transitive vulns** (`{esbuild, serialize-javascript, mocha}` — 1 moderate / 2 high), byte-identical to the factory-tag baseline (the epic's `package.json` is manifest-contributions-only; `editors/vscode/package-lock.json` is **NOT in the 57-file delta** — W4c DD-19/DD-20, frozen), each `accept-with-written-rationale` (build-time/test-only, NOT in the shipped extension runtime, no non-breaking fix, the epic introduced **ZERO** of them); they do **not** gate the milestone tag under the Doc 00 §11.48/§11.42 standard applied **honestly** to pre-existing non-shipped devDep transitives (§6.3); (b) the **`xvfb-run @vscode/test-electron` Extension-Host suite** green — incl. the new `debugPanel.test.ts` (the W2 panel-renders-the-oracle / W3 breakpoint-pauses-pre-step+rewind / §W4 capture-round-trips-byte-identical + "captured ✓"-only-after-the-file-exists tests, all byte-match-vs-oracle) **and** the v1.3-path-identity `diagram.test.ts` (the DF-01 behaviour-preserving-refactor pre/post-identity bar); (c) `npm`, NOT `pnpm` (`[[feedback_cellwar_uses_npm_not_pnpm]]` / the project rule — pnpm quarantines packages). The Rust cold quad (§4.2) and the binding JS lane are **both** gated at `X`.

This run **has not yet happened** at the time this document is written; it is described here as the **gating step the milestone tag is contingent on**, not a completed result — **no run-at-`X` transcript is fabricated**. The de-risking predicate is the W4b auditor's own `xvfb-run -a npm test` run at `8092d3a` = **47 passing, exit 0** (with the real shipped `target/debug/{fsm,fsm-lang-server}` binaries resolved fresh under the 1.75 pin) incl. the three keystone-critical W2/W3/§W4 byte-match-vs-oracle tests + the honest-degradation guards — re-confirmed at `X` by the contingent JS lane. (W4c source-verified the ExtHost lane's *structure* but did NOT run it locally — CI-runner-only by the disk-tight infra constraint, `node_modules` absent + an Electron download required, the factory on-target-lane honest-skip-by-design precedent; the load-bearing byte-identity proof was empirically re-run at the LSP/CLI layer by both auditors, the ExtHost layer is pure marshalling over it — `[[feedback_embeded_fsm_pipeline_before_ui]]`.)

### 4.4 The canonical confirming quad + JS lane (the step the milestone tag is contingent on)

The **canonical confirming cold-from-source quad — with MANDATORY STEP 0 (§4.2) — and the binding JS lane (§4.3) are executed at the release commit `X` itself** (orchestrator-run after this document + the batched consolidation commit), and the transcript is recorded in the **annotated `checkpoint/2026-05-19-debug-interface` tag message**. So quad-commit ≡ JS-lane-commit ≡ tag-commit = `X`, satisfying §11.22 (cold from-source) **and** §11.30 (zero off-by-one) **and** NOTE-1 (the explicit `cargo clean -p` STEP 0) simultaneously. The milestone tag is placed **only if** the canonical cold quad (incl. Step 0 + conformance 55/55 + the §5.4 gcc-RUN battery + the W1 LSP acceptance + the host differential + the G7 lock) **and** the binding JS lane (npm audit triaged + the Extension-Host suite green incl. `debugPanel.test.ts` + the v1.3-path-identity `diagram.test.ts`) are green **and** the independent pre-tag four-lens audit returned `TAG-CLEAR` (W4c — frozen, satisfied) **and** the W4b keystone phase-audit is `KEYSTONE-INTACT` (frozen, satisfied; applicable at `X` per §3). **Recorded here as the contingent gate, not a completed result — no quad/tag result is fabricated** (the `GATE_VERIFICATION_v1_5.md` §4 / `GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` §4 "recorded here as the contingent gate, not a completed result — no verdict fabricated" precedent verbatim). **The orchestrator places the tag + runs the cold-quad + the JS lane at `X`; this closeout does NOT tag/push/run-the-quad** (the no-push/no-tag boundary).

---

## 5. Post-tag actions required of the repository owner

The milestone tag and the unpushed commits are **local only** (the owner controls the remote — a hard project rule). The carried owner/infra dependencies (the v1.4/v1.5/factory §5 documented-accepted posture; the Phase-8.0-specific changes noted inline):

1. **G9 CI matrix + the factory-epic lanes' first remote run + the new debug-epic ExtHost surface (the v1.5/factory §5 carry-forward; Phase-8.0-specific update — OWNER-2).** The GitHub Actions matrix (linux/macos/windows × the 1.75 `build` matrix + `sca`), the 4 factory lanes (`conformance`/`extension-host`/`coverage-rust`/`on-target`), and now the **debug-epic ExtHost tests** (`debugPanel.test.ts` + the v1.3-path-identity `diagram.test.ts` riding the existing `extension-host` lane — the debug epic adds **no** new ci.yml lane, §8) have **never executed against any v1.2–v1.5 / factory / Phase-8.0 commit** (local-only repo). For Phase-8.0 the JS lane is a **binding local gate** (§4.3 — run at `X`) and the load-bearing byte-identity proof was empirically re-run at the LSP/CLI layer by both auditors, but the ExtHost lane has **never run on a remote CI runner** — G9 stays **load-bearing** for the automatic remote pipeline ever to exist. **The owner should push the milestone tag + the LOCAL `v1.0.0`–`v1.5.0` tags + ALL `checkpoint/*` anchors (incl. `checkpoint/factory-reliability-ci-2026-05-18` AND this epic's `checkpoint/2026-05-19-debug-interface`) + commits to exercise the full CI matrix + the SCA job + the 4 lanes, and keep a patch lane ready.** G9 gates neither progress nor quality (both are local — the per-wave quad + `make ci-local`); it is the switch for *when the owner chooses to publish*.
2. **The G9-gated 5-platform binary-bundling tail.** Carried unchanged (the OWNER-1/v1.4 lineage) — the host-only artifact ships; the full 5-platform tail is **blocked-on-G9 existing** (no cross toolchains on the box; an infra-escalation, not a grind). Accepted-tracked-debt until the owner provisions the cross-OS matrix. Explicitly **NOT** a ci.yml lane in this epic.
3. **VS Code Marketplace publish / signing (the v1.5 OWNER-A lineage).** The extension is **Marketplace-VALID and publish-READY** (v1.5 B1 — a lint-clean VSIX, `repository.url` corrected, `icon`/`keywords`/`version 1.0.0`) — but it is **NOT published** (no PAT/credential used by any wave; Phase-8.0 added only the `fsm.openDebug` command/keybinding/menu manifest contributions, no extension runtime dependency). An owner credential/product decision.
4. **The v1.6 D1 landing page + the doc-honesty pass that must precede/accompany it (recorded — NOT scoped into this epic; the §5a-style publish-readiness framing).** This epic delivers the **interactive debug interface only** (the owner D-1/D-2/D-3 scope). "Publish-ready" = the factory-reliability proof + this debug interface **+** the v1.6 D1 landing all land. D1 (the static EdgeForge-modeled docs/landing site — the v1.5 OWNER-D droppable-from-tag item, slipped to v1.6, NOT cut) + a **small doc-honesty pass** (README / Doc-07 / Doc-14 staleness — `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B; **plus the CF-2 Doc 16 §2–§6 HAL-symbol staleness — Doc 33 §5 CF-2 / cross-ref task #103**; **plus the DF-02 DBGUX `.trace.yaml`→`.trace.json` prose precision** + the Doc 33 GT-7 DBGUX line-drift) **must precede or accompany D1** so the public site is accurate. **This epic does NOT scope D1 or the doc-honesty pass** — it records the relationship. The in-repo `docs/ROADMAP.md` "See also" stale pointer was **already reconciled at the v1.5 closeout** (it now points at the backlog SoT + the per-epic wave-plan docs — `GATE_VERIFICATION_v1_5.md` §6.4); Phase-8.0's ROADMAP edit is the debug-epic retrospective + the milestone-tag record only (it does **not** re-touch the already-fixed pointer). No hosting/DNS action is taken autonomously.
5. **The memory-SoT status-line-freshness gate (Doc 33 §7 / the v1.4 D.7(c)#3 / v1.5/factory standing gate — recorded; the SoT advance is the orchestrator's, decoupled from this commit by the no-push/no-tag boundary).** Per the D.7(c)#3 / v1.4-cardinal-drift discipline, *in the same closeout pass* the project backlog SoT head status line must advance to the Phase-8.0 SHIPPED+milestone-TAGGED state + a Phase-8.0 RELEASE RECORD section with the wave→commit map must be added. The wave→commit map is recorded in this gate doc's §"Scope" (W0 `phase8.0/debug-iface-plan-of-record` → W1 `1de83b0` → W2 `032438c` → W3 `b79e052`/`ec903e5` → W4a `ae774dd`→`8092d3a` → W4b `phase8.0/w4b-keystone-audit` → W4c `phase8.0/w4c-four-lens-audit` → W4d=`X`) so the orchestrator can advance the SoT head + RELEASE RECORD in the same closeout motion. The backlog SoT is the orchestrator's memory file (outside the repo + outside this wave's hard-guardrail scope, exactly as the v1.5/factory closeouts recorded it and the orchestrator advanced it) — recorded here, honest-prospective ("gate-doc landed; the milestone tag + the SoT-freshness advance pending the contingent steps", no fabricated tag). See §"Completion report → memory-reconciliation note" for the exact orchestrator fold.

---

## 6. Accepted-tracked-debt + owner-escalations (consumer-relevant)

> Re-stated so none is silently dropped (the §11.62 discipline) and surfaced in CHANGELOG `[debug-interface]` *Known limitations* + the Doc 00 §11 Phase-8.0 rows (§9 — the orchestrator appends at W4d). The W4c carry-list + the Doc 33 §5 carried-findings + the carried v1.5/factory owner-escalations are folded in ONE batch here (the §11.62 batched-doc discipline — never piecemeal).

### 6.1 The W4c pre-tag four-lens P2/P3 carry-list (recorded; none gating — W4c is `TAG-CLEAR`)

From `docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md` §Findings (FROZEN — all non-gating; W4c verdict `TAG-CLEAR` 0 P0 / 0 P1):

- **[DF-01 P2 — the "v1.3 core untouched" framing vs the one v1.3-source modification]** The brief / commit bodies say "v1.3 diagram core untouched". Precisely: the v1.3 *bundled output* (`dist/webview/diagramWebview.js`) IS byte-untouched (esbuild per-entrypoint), but the v1.3 *source* (`editors/vscode/src/diagram/webview/diagramWebview.ts`) was modified — additive named exports + a `getVsCodeApi()` singleton (wrapping the once-only `acquireVsCodeApi()`) + an `#svg && #banner` standalone-guard. This is a **disclosed, behaviour-preserving** refactor enabling verbatim `renderModel` reuse with NO fork (architecturally sound), but "v1.3 core untouched" is imprecise for the *source* layer. **Disposition: accepted with the precise framing FOLDED HERE** (this gate doc §2.3 + §3 states it precisely: "the v1.3 *bundled output* is byte-untouched; the v1.3 *source* was behaviour-preservingly refactored to enable verbatim `renderModel` reuse — `diagram.test.ts` is the pre/post-identity bar"). Non-gating (a sound behaviour-preserving refactor, not a fork or regression). The `diagram.test.ts` v1.3-path-identity is CI-runner-only (the ExtHost lane) — re-confirmed in the W4d cold-quad's binding JS lane (§4.3, correctly contingent, not asserted here). **The v1.3-framing-precision is accepted-with-rationale, recorded — no code change.**
- **[DF-02 P3 — the DBGUX `.trace.yaml` prose vs the shipped `.trace.json`]** `docs/design/DEBUG_INTERFACE_UX_DESIGN_2026_05_19.md` (DBGUX prose) says capture writes `.trace.yaml`, but the SHIPPED `collect_traces` (`cmd/test.rs:159`) globs `.trace`/`.trace.json` and `write_trace_yaml` actually emits JSON — so the panel correctly writes `*.trace.json` (a `.trace.yaml` would be SILENTLY SKIPPED → a vacuous pass). The implementation is **correct** (re-derived-from-source, the trap explicitly avoided + disclosed in `debugPanel.ts:1313-1318`); only the design-doc prose is loosely phrased (the factory-F-02 "loose doc phrasing vs verified source" class). **Disposition: defer the DBGUX-prose `.trace.yaml`→`.trace.json` precision to the owner-reserved v1.6 doc-honesty pass** (cross-ref task #103 — the frozen design doc is design-evidence, do **NOT** edit it here; same class as the GT-7 DBGUX line-drift Doc 33 already records). Carried, not resolved.
- **[DF-03 P2 — the 3 extension transitive devDep npm vulns]** `{esbuild, serialize-javascript, mocha}` (1 moderate / 2 high). Pre-existing — byte-identical to the factory-tag baseline (the epic's `package.json` is manifest-contributions-only; `editors/vscode/package-lock.json` is **NOT in the 57-file delta** — W4c DD-19/DD-20), **devDependency/build-time/test-only**, **NOT in the shipped extension runtime** (runtime deps = `{elkjs, vscode-languageclient}`; the VSIX excludes `node_modules/**`), **not exploitable in our build/CI usage** (one-shot bundler; trusted-input-only Mocha over the project's own ExtHost fixtures), **no non-breaking fix** (the offered fixes are a major esbuild bump / a major mocha downgrade), and the **epic introduced ZERO of them**. Each `accept-with-written-rationale`; **does NOT gate the milestone tag** (the §11.48/§11.42 standard applied honestly to pre-existing non-shipped devDep transitives — the v1.5/factory-F-03 precedent). Tracked for the **v1.6 FE-hygiene batch** (await an upstream mocha pulling serialize-javascript ≥7.0.5; the esbuild major-bump its own scoped chore).
- **[DF-04 P2 — the `.contains` codegen-c conformance oracle (the #123-class deferred tail)]** The normative codegen-c conformance oracle is substring-presence (`.contains`), flagged P1 in `docs/AUDIT_RELIABILITY_STAGE_GATE_2026_05_18.md:306` (frozen, factory-epic), **owned by the G7 wave** for behavioural conversion (the 4 codegen-c MANIFEST fixtures are the tail). `accept-tracked-with-rationale` — a known/owned/deferred test-debt item, **NOT regressed** by the debug epic, **NOT a tag-blocker**: the W1/W2 differential + the W3 lock are the binding behavioural gate for the corpus in the interim. Carried from the factory tag (factory-F-04), re-stated.
- **[DF-05 P3 — `crates/fsm-analyzer/src/type_check.rs::resolve_simple_literal` F-1 adjacent-site, informational]** The F-1 commit discloses `resolve_simple_literal` is a DISTINCT non-defect const-fold (returns i128 to defer overflow to the `FSM-E0206` caller; literal-only by design; check-only, no lowerer counterpart ⇒ provably NOT the F-1 divergent-second-resolver defect) — left untouched + characterized in-place. W4c confirmed it is a distinct, intentional helper (no lowerer counterpart to drift from), so the leave-and-explain is correct. **Disposition: non-gating, informational — no action required** (the adjacent-site characterization is correct; recorded for audit completeness, the W1-FU adjacent-site-audit discipline correctly applied). An **informational non-defect note**, recorded.

### 6.2 The Doc 33 §5 carried Zero-Legacy findings (recorded, NOT fixed here — the scope boundary; OWNER-4)

From `docs/33-Debug-Interface-Wave-Plan.md` §5 (the FSM-DESIGN-CHIPMATRIX-threaded carried findings — NOT in this epic's scope; the debug interface touches neither AVR codegen nor the HAL doc):

- **CF-1 — the AVR Harvard `.rodata` + TABLE-dispatch UNTESTED CORRECTNESS HAZARD** (TARGET_CHIP_MATRIX §1.3/§2.3). AVR is the only Harvard-architecture target in scope; a plain `.rodata` pointer deref reads RAM not flash ⇒ wrong data; the family-agnostic codegen does not emit `__flash`/`PROGMEM`+`pgm_read_*`; large machines auto-selecting TABLE (>64 states) are the exposed case; only ARM Cortex-M3 is on-target-tested (the factory W2 hard gate). The proposed verification path (an `avr-gcc -mmcu=atmega328p` link+run of a >64-state TABLE fixture on an AVR emulator, differential-checked against the shipped oracle — the factory W1→W2 method extended to a second family) is **CI-runner-only** (the box is disk-tight — an infra-escalation, NOT a grind). Disposition: a **named candidate for the factory-reliability/CI epic's on-target-matrix extension** — a *codegen-correctness* item, **NOT a debug-interface item** (must not be auto-scoped here). Recorded so the AVR-TABLE hazard is auditable and not lost (OWNER-4 default; W4c "Tracked-deferrals" #134 confirmed tracked-not-dropped).
- **CF-2 — Doc 16 §2–§6 staleness vs the emitted HAL symbols** (TARGET_CHIP_MATRIX threading finding (b)). Doc 16 (HAL-Specification) §2–§6 documents the older `_ms`/`_fail(file,line,msg)` shapes; the codegen emits `fsm_hal_clock_now_ms`/`fsm_hal_assert(bool,msg)` — a docs-only first-contact-surface drift. **NOT a code change** (the emitted symbols are the correct shipped contract — the doc drifted, the v1.4 §3.G Doc-18 finding shape) and **NOT a debug-interface item**. Disposition: fold into the **owner-reserved v1.6 doc-honesty pass** (the same v1.5 Doc 31 §6 H6 / ASSESSMENT PART B doc-honesty batch — the README/Doc-07/Doc-14 staleness pass that must precede/accompany D1), **cross-reference task #103**. Recorded so the Doc 16 staleness is carried into the v1.6 doc-honesty scope, not dropped (OWNER-4 default; W4c "Tracked-deferrals" #103 confirmed tracked-not-dropped).

### 6.3 Carried owner-escalations (re-stated, NOT resolved — the §11.62 / v1.5 §6.3 / factory §6.3 / Doc 33 §7 discipline)

- **OWNER-1 — the release-numbering call.** Which `vX.Y.0` for this minor, and its position relative to the owner-reserved v1.6 — a product-numbering decision. **Safe parked default applied:** a `checkpoint/2026-05-19-debug-interface` annotated anchor at the §11.30-clean gate-doc commit, LOCAL only; does NOT consume v1.6 (owner-reserved for landing+doc-honesty — D-3); does NOT unilaterally claim a `vX.Y.0`. The orchestrator/owner assigns the final number at closeout (the factory-epic `checkpoint/`-not-`v1.6.0` precedent, Doc 00 §11.80). Re-stated, not resolved.
- **OWNER-2 / G9 — the push of the LOCAL tags + checkpoints + commits + the never-run remote CI matrix.** The push of the LOCAL `v1.0.0`–`v1.5.0` tags + ALL `checkpoint/*` anchors (incl. `checkpoint/factory-reliability-ci-2026-05-18` + this epic's `checkpoint/2026-05-19-debug-interface`) + commits; the CI matrix / SCA / the 4 factory lanes / the debug-epic ExtHost tests have **never run on a remote runner** (the JS lane is now a binding *local* gate, §4.3, but remote-unrun). G9 stays the owner's publication switch, decoupled from BOTH progress AND quality (quality is local every wave — the per-wave quad + `make ci-local`). Owner push action; re-stated, **not resolved**.
- **OWNER-3 — the deferred full `signal`-primitive epic GO.** D-1 decided the SHAPE (the full first-class `signal` primitive — poll/interrupt/edge/debounce, `feature signals`-gated, append-only keywords, cohesively retiring the #127 AUDIT-DETPRIM `edge`/`level`/`debounce` GAPs); D-3 sequenced it **AFTER this debug-interface epic AND after the owner-reserved v1.6**. It is its own gated epic (append-only keyword/grammar = a minor-version event, Doc 00 §B-03) with its own future Doc-34-class wave-plan-of-record; its Part-2(d) per-family interrupt-model dependency is already delivered (FSM-DESIGN-CHIPMATRIX §1.2). Recorded in Doc 33 §8 (scope captured, tracked-not-lost); opened only on explicit orchestrator/owner schedule (W4c "Tracked-deferrals" #139 `raise` codegen gap confirmed folded into this parked epic, not dropped). Re-stated, not resolved.
- **The carried v1.5/factory escalations unchanged (re-stated, not resolved):**
  - **VSIX packaged-not-published (the v1.5 OWNER-A lineage).** The extension is **Marketplace-VALID + publish-READY** (v1.5 B1), **NOT published**. Still an owner credential/product action (no PAT used; Phase-8.0 added only the `fsm.openDebug` manifest contributions, no extension runtime dep).
  - **No top-level `LICENSE`/`CONTRIBUTING.md`/`CODE_OF_CONDUCT.md`** at the repo root (carried from v1.4/v1.5/factory §6.3 — the license choice is owner/legal, coupled to publishing; the v1.5 OWNER-D lineage now carried to v1.6 with the D1 slip).
  - **The Certifiability / MISRA–CERT-C codegen-mode arc — PARKED.** A strategic ROADMAP-scheduling decision (a MISRA-C/CERT-C compliance posture for the *generated C* — the **codegen-mode** half). The factory epic un-parked **only the testing half** of Certifiability/OWNER-C; the MISRA-codegen half stays a **named, parked ROADMAP-v1.6+ candidate** requiring an explicit owner GO to schedule. Re-stated, not resolved.
  - **The v1.6 D1 landing + the doc-honesty pass — recorded, NOT scoped (§5#4).** D1 (the slipped v1.5 OWNER-D item, slipped not cut) + its preceding/accompanying doc-honesty pass (README/Doc-07/Doc-14 + CF-2 Doc-16 + DF-02 DBGUX-prose + the Doc 33 GT-7 DBGUX line-drift + the OSS-files LICENSE/CONTRIBUTING/CoC reconcile) is the **carried publish-readiness work, recorded-not-scoped into this epic**. "Publish-ready" = the factory-reliability proof + this debug interface + the v1.6 D1 landing all land. Re-stated, not resolved.
  - **The 5-platform binary tail** (host-only artifact ships; the cross-OS tail blocked-on-G9 — no cross toolchains on the box, infra-escalation not grind). Carried unchanged. Explicitly **NOT** a ci.yml lane in this epic.
  - **task #17 — CI-matrix still owner-blocked** (local-only repo, owner controls the remote). Re-stated, not resolved.
  - **R7 — the mechanised sim≡codegen-equivalence formal proof.** The factory epic delivered the *empirical* host+target differential; the *fully mechanised* sim≡codegen-equivalence **formal proof** remains the carried v1.4-stretch/later named DEFERRAL (unchanged from v1.4/v1.5/factory §6 — *not* folded into this debug epic; named-not-omitted). Re-stated, not resolved.
- **W4c "Tracked-deferrals" confirmed tracked-not-dropped (none re-opened as a new finding):** #138 Item-3 queue co-design (Doc 33 §0 D-3/§4 OWNER-1/§7 — the D-3 sequencing); #141 pending-timer surface (Doc 33 §4 OWNER-2; honestly degraded in `debugWebview.ts:598-609`, NOT faked); #139 `raise` codegen gap (folded into the parked OWNER-3 signal epic); #134 AVR CF-1 (Doc 33 §5 CF-1 / OWNER-4 — the factory on-target-matrix candidate); #123 W3-§6 catalog tail (the `.contains` codegen-c oracle — the factory-F-04, owned by G7); #103 v1.6 doc-honesty CF-2 (Doc 33 §5 CF-2 / OWNER-4 — folded into v1.6). **All carried with the explicit §11.49 leave-and-explain / §11.62 re-state-don't-drop discipline — none silently dropped.**

### 6.4 FLAGGED (not fixed here — the separate v1.6 doc-honesty batch; Doc 33 §5 / the scope boundary)

The broader **doc-honesty staleness pass** (the repo-root `README.md` / Doc 14 / Doc 07 staleness — `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B; **+ CF-2 Doc 16 §2–§6 + DF-02 DBGUX `.trace.yaml`-prose + the Doc 33 GT-7 DBGUX line-drift**) is a **SEPARATE already-identified backlog item** (Doc 31 §6 H6 / Doc 32 §7-tweak-5 / Doc 33 §5), to be fixed by the doc-honesty batch that precedes/accompanies the deferred v1.6 D1. **It is FLAGGED here, NOT fixed in this epic.** The in-repo `docs/ROADMAP.md` "See also" stale pointer was **already reconciled at the v1.5 closeout** (`GATE_VERIFICATION_v1_5.md` §6.4 — it now points at the backlog SoT + the per-epic wave-plan docs; independently re-confirmed: `docs/ROADMAP.md:310` carries the "Reconciled 2026-05-17" note); Phase-8.0's ROADMAP edit is the debug-epic retrospective + the milestone-tag record only (it does **not** re-touch the already-fixed pointer). D1 (when it lands in v1.6) must not be the thing that broadcasts a stale README/Doc-16/DBGUX-prose; this gate doc records the dependency so it is not lost (`[[feedback_verify_status_claims_vs_code]]` "a first-contact-surface pointer drifted stale is a lie").

---

## 7. Sign-off

The interactive debug-interface epic (Phase-8.0) meets the release gate: 0 open P0; the **KEYSTONE-IN-DEBUG binding tag-gate row (Doc 33 §2/§7) PASSES** via the independent W4b source-derived phase-audit (verdict `KEYSTONE-INTACT` — the no-fork re-derived from source by the auditor: the shipped oracle + the `cmd/test.rs` `execute_trace` seam + `write_trace_yaml` byte-untouched across the whole epic arc `fbae38b..8092d3a` save the single sanctioned additive non-stepping `set_context_field` helper, per-file blob-SHA-identical; negative-grep ∅ across the whole debug surface with every hit read + adjudicated; the host differential 3×-deterministic + the W1 LSP acceptance + the 47-test ExtHost suite re-run by the auditor; the catalogue-integrity contract `11+3+0=14` holding non-vacuously; the §W4 `.trace.json` replay independently hand-confirmed non-vacuous); the W4c independent four-lens pre-tag audit returned `TAG-CLEAR` (0 P0, 0 P1, 3 P2, 2 P3 — every lens independently re-derived, the byte-identity property empirically REPRODUCED, the Rust SCA empirically run = 0 vulns/0 warnings over 207 deps, no new network attack surface source-verified). Every NOTE is documentation-of-record / closeout-batch / accepted-tracked-debt, folded by this closeout or carried (§6). Scope statements are code-verified and conservative — **every gate numeric independently re-derived at the gate-doc commit, not echoed** (live `DiagnosticCode`=**75** re-derived from `crates/fsm-diagnostics/src/lib.rs:851` `const EXPECTED: usize = 75` — the F-1/F-2 +2 vs the frozen factory 73, *correctly attributed* to the two new rejecting diagnostics; `forbid(unsafe_code)`=**12 roots / 11 crates** unchanged because W1 added an *internal path-dep edge* + a `set_context_field` helper into the already-forbid-unsafe `fsm-simulator`, **no crate root** — re-derived from `git grep -l` at both `fbae38b` & `8092d3a`, identical; conformance=**55** re-derived from the MANIFEST [grew 53→55, the F-1/F-2 +2 VAL-NEG-009/010]; the differential CORPUS partition re-derived from source AND the test's `--nocapture` output [`11 BYTE_EQUAL + 3 BEHAVIOURALLY_EQUIVALENT_JUSTIFIED + 0 KNOWN_DIVERGENT = 14 == CORPUS.len()`, dynamically asserted in-test — the never-game discipline]; the ExtHost suite = **47 passing** [`debugPanel.test.ts` = 6 debug E2Es]; **the release-record Rust delta stated PRECISELY**: `git diff fbae38b..8092d3a -- crates/` = **28 files / +3260 / −214** [the W1 `simulate.rs` +693 + `simulate_lsp_acceptance.rs` +945 + the W1 `set_context_field` +25 + the gating F-1/F-2 codegen-correctness fixes + the additive test-corpus expansion, with **0 new external deps** — the single Cargo.lock change is the *internal* `fsm-lsp`→`fsm-simulator` path-dep edge], **NOT echoed from any wave report** but independently re-derived at the gate-doc commit, **neither under-stated** [the v1.3 cardinal "zero Rust delta" error] **nor over-stated**). **Release-record-symmetry self-check (the v1.3-cardinal-error-form grep of THIS record):** the broad form `grep -nioE '(no|zero|empty) (new |external |rust )*(rust )?delta'` over this document returns lexical hits **only** as explicit citations of the v1.3 cardinal-error *as the anti-pattern being avoided* (`[the v1.3 cardinal "zero Rust delta" error]`, `neither under-stated … nor over-stated`) — filtering those mandated repudiation clauses ⇒ **0 hits asserting Phase-8.0 has no/zero/empty Rust delta**; the record states the moderate additive `28 files / +3260 / −214` delta explicitly throughout (the v1.3 "zero Rust delta" cardinal release-record sin is **absent** — `[[feedback_embeded_fsm_verify_record_release_layer]]` applied symmetrically at the closeout layer). The recurring prose-vs-code reconciliation rigor was applied **at the closeout layer** (the W4b/W4c verdicts re-read in full not echoed; every pinned numeric independently re-derived at the gate-doc commit; the frozen W4b/W4c audits + Doc 33 + the frozen design docs NOT overwritten — the §11.29 symmetric verify-the-record discipline). **This closeout makes NO code-tree change** (no F-05-equivalent — the W4c DF-01..DF-05 findings are framing/doc-precision/accepted-tracked, none requiring an in-scope code change; DF-01's precise framing folded into this gate doc's prose, DF-02 deferred to v1.6 doc-honesty, DF-03/DF-04 accepted-tracked, DF-05 informational). All residuals + owner-escalations are re-stated in CHANGELOG `[debug-interface]`, the ROADMAP, and the §9 DRAFT Doc 00 §11 rows (the orchestrator independently re-derives & appends §11.87+ at W4d). The release is contingent on the canonical cold-from-source quad — **with MANDATORY STEP 0 (the NOTE-1 `cargo clean -p` precondition, §4.2)** — **and the binding JS lane (§4.3)** — at the release commit `X` being green (§4.4); the independent pre-tag four-lens (W4c) returned `TAG-CLEAR` (frozen, satisfied) and the W4b keystone audit is `KEYSTONE-INTACT` (frozen, satisfied; applicable at `X` per §3). **Recorded as the contingent gate, not a completed result — no quad/tag result is fabricated.**

Tagging the interactive debug-interface minor as `checkpoint/2026-05-19-debug-interface` (annotated, LOCAL) — the proven `checkpoint/<name>` immutable-anchor pattern, **deliberately NOT a `vX.Y.0` tag** (v1.6.0 is owner-reserved for the landing+doc-honesty post-factory; this minor lands as its own post-v1.6 minor — D-3/OWNER-1). (The `v1.0.0`–`v1.5.0` release tags + the `checkpoint/<date>`/`checkpoint/<name>` anchors incl. `checkpoint/factory-reliability-ci-2026-05-18` are immutable and **not moved**.) The orchestrator places the tag + runs the cold-quad + the binding JS lane at `X` (the no-push/no-tag boundary; this closeout is the sole writer of its scoped files, the commit is NOT merged/pushed/tagged).

— TL/PM, FSM Studio

---

## 8. Tag topology (pre-emptively correct per §11.30) + the Pinned gate numbers

Constructed §11.30-clean from the start (no off-by-one to reconcile — the 7th consecutive prospective-clean release):

- **Sequence:** (1) author + commit this document + the batched Doc 00 §11 (the Phase-8.0 rows appended by the orchestrator at the next free integer §11.87 — §9 carries the DRAFT, NOT pre-allocated) / CHANGELOG `[debug-interface]` / ROADMAP (the debug-epic retrospective + the milestone-tag record) / `docs/metrics/2026-05-19-debug-interface.json` — **NO `crates/`/`.github/` code-tree touch** (no F-05-equivalent in this epic's closeout) → commit `X`; (2) run the canonical cold-from-source quad **at `X`** — **with MANDATORY STEP 0** (`cargo clean -p` of all 11 first-party crates, the NOTE-1 §4.2 precondition) — **and the binding JS lane at `X`** (§4.3) — transcript recorded in the annotated tag message; (3) `git tag -a checkpoint/2026-05-19-debug-interface` **at `X`**. Result: **cold-quad-commit ≡ JS-lane-commit ≡ tag-commit ≡ `X`**, exactly — zero off-by-one by construction (§11.30 satisfied at its source, the v1.2–v1.5/factory prospective pattern). **The orchestrator places the tag + runs the cold-quad + the JS lane at `X`; this closeout does NOT tag/push/run-the-quad.**
- **Pin lineage (immutable, not moved):** v1.0.0 → `b4800036…`; v1.1.0 → `abc70044…` (== `checkpoint/2026-05-15`); v1.2.0 → `71043935…` (== `checkpoint/2026-05-16`); v1.3.0 → `a036c381…` (== `checkpoint/2026-05-16-v1_3`); v1.4.0 → `3bbcd0f5…` (== `checkpoint/2026-05-16-v1_4`); v1.5.0 → `2ff8ac31…` (== `checkpoint/2026-05-17-v1_5`); the Factory-Reliability/CI milestone → `fbae38b…` (== `checkpoint/factory-reliability-ci-2026-05-18`); the interactive debug-interface minor → `X` (== `checkpoint/2026-05-19-debug-interface`). Release tags are **immutable and never moved** (re-cutting a release tag is a destructive op never performed). v1.6.0 is **owner-reserved** (landing+doc-honesty, post-factory) — this minor is a `checkpoint/<name>` anchor, **not** a version-number claim.
- **The `git rev-parse` vs `git rev-list -1` annotated-tag-object distinction.** The v1.x release tags + the annotated `checkpoint/<name>` anchors are **annotated**: `git rev-parse <tag>` yields the tag-**OBJECT** sha; `git rev-list -1 <tag>` (or `<tag>^{commit}`) yields the **commit**. They differ and must not be conflated. Independently re-confirmed at this commit: `git rev-parse checkpoint/factory-reliability-ci-2026-05-18` = `a598fb63…` (the tag-**OBJECT**) **≠** `checkpoint/factory-reliability-ci-2026-05-18^{commit}` = `fbae38b1ccf7eb00fb344b0a034d16973254add9` (the **commit** — the epic-boundary anchor used throughout this doc); `git rev-list -1 v1.5.0` = `2ff8ac31…` (the commit). `git merge-base --is-ancestor fbae38b 8092d3a` → **TRUE** (the EMPTY-core-diff spans the entire debug-epic arc). Recorded pre-emptively so no future metrics/audit wave misreads the `checkpoint/2026-05-19-debug-interface` tag-object sha as a mismatched commit (the `GATE_VERIFICATION_v1_{1,2,3,4,5}` / factory §7/§8 lineage).
- **Frozen-attestation discipline:** `GATE_VERIFICATION_v1_{0,1,2,3,4,5}.md` + `GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` correctly cite their own pinned values *at their pinned commits*; a frozen attestation is never rewritten. This document cites the Phase-8.0 live values *at `X`* — each independently re-derived at `X` (not echoed). The frozen W4b/W4c audits + Doc 33 + the frozen design docs are NOT overwritten.

### Pinned gate numbers (independently re-derived at the gate-doc commit `X` — DERIVED from `git`/source at the working tree == `X` [no code-tree delta vs `8092d3a`], NOT echoed)

| Metric | Value | Method (independently re-derived, not echoed — command + result) |
|---|---|---|
| Factory-checkpoint baseline anchor (the epic boundary) | `fbae38b…` | `git rev-parse checkpoint/factory-reliability-ci-2026-05-18` ⇒ `a598fb63a3701c3b0e314f2c37a1c457fab78680` (the tag-**OBJECT**); `checkpoint/factory-reliability-ci-2026-05-18^{commit}` ⇒ `fbae38b1ccf7eb00fb344b0a034d16973254add9` (the **commit**). The `git rev-parse` (tag-OBJECT) vs `^{commit}` distinction observed — the **commit** `fbae38b` is used for every delta below. `git merge-base --is-ancestor fbae38b 8092d3a` ⇒ exit 0 (TRUE — the arc is well-formed). |
| Phase-8.0 Rust delta vs the factory checkpoint `fbae38b` | **28 files / +3260 / −214; 0 new external deps** | `git diff --shortstat fbae38b..8092d3a -- crates/` **at the working tree == `X`** ⇒ **28 files changed, 3260 insertions(+), 214 deletions(-)**. With `Cargo.toml Cargo.lock`: `git diff --shortstat fbae38b..8092d3a -- crates/ Cargo.toml Cargo.lock` ⇒ **29 files / +3261 / −214** (the +1 file / +1 line is the `Cargo.lock` `fsm-lsp` dep-list edge). This is the W1 `fsm/simulate*` LSP transport (`crates/fsm-lsp/src/capabilities/simulate.rs` +693 [pure `Interpreter`-marshalling], `capabilities/mod.rs` +24, `lib.rs` +16/−?, `server.rs` +46, `Cargo.toml` +19 [the single `fsm-simulator` internal path-dep edge + a WHY comment]) + `crates/fsm-lsp/tests/simulate_lsp_acceptance.rs` +945 (the in-process tower-lsp differential-oracle suite) + the **single sanctioned non-stepping `set_context_field` helper** `crates/fsm-simulator/src/interpreter.rs` +25 + the gating #128 F-1/F-2 codegen-correctness fixes (`crates/fsm-analyzer/**` ~+670, `crates/fsm-codegen-c/**` ~+790, `crates/fsm-diagnostics/src/lib.rs` +35 [the E0411/E0412 +2], `crates/fsm-ir/src/model.rs` +29, `crates/fsm-parser/src/ast/top_level.rs` +34, `crates/fsm-cli/{cli,cmd/generate}.rs` ~+69) + the additive test-corpus expansion `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` +64/−4 (factory-wave additive, NOT an oracle change — W4b §1 frozen). `Cargo.lock`: `git diff fbae38b..8092d3a -- Cargo.lock \| grep -E '^\+(name\|source) ='` ⇒ **none** (no new `[[package]]`); the only change is exactly `+ "fsm-simulator"` inside the `fsm-lsp` dependencies array (the v1.5 W-A2 single-internal-edge shape; `fsm-simulator` is already a workspace member) ⇒ **0 new external `[[package]]`**. **A MODERATE ADDITIVE delta, stated correctly here as exactly that — NEITHER under-stated (the v1.3 cardinal "zero Rust delta" error) NOR over-stated (the v1.4 verification-core was a +7398 NEW-crate epic; the factory epic was +8192; this debug-interface epic is +3260, the W1 transport+acceptance + the gating F-1/F-2 fixes + the additive corpus, NO new crate, NO new external dep).** Re-derived at `X`, **NOT echoed** from W4c's `fbae38b..8092d3a = 57 files / +10045 / −243` (that is the WHOLE-delta incl. `docs/`/`editors/`/`tests/`; the `-- crates/`-scoped Rust delta is the 28/+3260/−214 stated here — both correct at their respective scopes, exactly the `git rev-parse`-vs-`git diff --shortstat -- crates/` scope distinction). |
| v1.3-cardinal-error-form grep of this record | **0 hits (no "no/zero/empty Rust delta" *assertion*)** | The broad form `grep -nioE '(no\|zero\|empty) (new \|external \|rust )*(rust )?delta' docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md` returns lexical hits; filtering out the explicit anti-pattern citations + the additive framing (`grep -viE 'NOT.*\|neither\|nor over\|0 new external\|cardinal\|anti-pattern\|under-stated'`) ⇒ **0 hits** — every "zero/empty Rust delta" occurrence in this doc is an explicit citation of the v1.3 cardinal-error *as the anti-pattern being avoided* (`[the v1.3 cardinal "zero Rust delta" error]`, `the v1.3 "zero Rust delta" cardinal release-record sin is **absent**`), and the only "0 new" claim is the **0 new external `[[package]]`** Cargo.lock fact (re-derived true). **There is NO claim asserting Phase-8.0 has no/zero/empty Rust delta** — the record states the moderate additive `28 files / +3260 / −214` delta explicitly throughout (`[[feedback_embeded_fsm_verify_record_release_layer]]` applied symmetrically at the closeout layer). |
| `#![forbid(unsafe_code)]` | **12 roots / 11 crates** | `git grep -l '#![forbid(unsafe_code)]' 8092d3a -- 'crates/*/src/*.rs'` ⇒ **12** files (fsm-analyzer/lib, fsm-cli/main, fsm-codegen-c/lib, fsm-diagnostics/lib, fsm-formatter/lib, fsm-ir/lib, fsm-lexer/lib, **fsm-lsp/lib + fsm-lsp/main = 2 roots / 1 crate**, fsm-parser/lib, fsm-simulator/lib, fsm-verify/lib); the distinct crate set (parse `crates/<crate>/src/...`) ⇒ **11** crates. Same at `fbae38b` ⇒ **12** files / **11** crates (re-derived, not echoed). **UNCHANGED 12/11** — W1 added an *internal `fsm-lsp`→`fsm-simulator` path-dep edge* (`fsm-simulator` already `#![forbid(unsafe_code)]`) + the one `set_context_field` helper into the existing `fsm-simulator`, **no new crate root**; flagged-if-changed, not assumed (the precondition was verified — W4c DD-04 corroborates 12 files at both, the same fact counted as files). **Disclosed judgment call:** the W4c audit phrases this as "12 at both" (counting *files*); the v1.5/factory canonical convention is "roots/crates" (fsm-lsp = lib.rs+main.rs = 2 roots / 1 crate ⇒ 12 roots / 11 crates) — *the same source fact*, stated in the canonical roots/crates form here. |
| Live `DiagnosticCode` count (the `EXPECTED` lock) | **75** | `git grep -n 'const EXPECTED' 8092d3a -- crates/fsm-diagnostics/src/lib.rs` ⇒ `crates/fsm-diagnostics/src/lib.rs:851:        const EXPECTED: usize = 75;` (the CI-lock `all_codes_matches_expected_count`). **+2 vs the frozen factory figure 73** — *correctly attributed*: the #128 F-1/F-2 gating fixes added exactly **two** hard rejecting diagnostics (`FSM-E0411` "timer duration must be a compile-time constant"; `FSM-E0412` non-power-of-2 in-source queue capacity) replacing two previously-silent miscompiles — NOT a silent code-count drift; the G7 count-discipline lock is GREEN + non-vacuous (W4c DD-12/DD-14 corroborates 73→75 +E0411/+E0412). **Flagged-and-attributed (it moved, by exactly the two intended diagnostics).** |
| Conformance fixtures | **55** (parser 7 / validator 13 / semantic 28 / codegen-c 4 / formatter 3) | `git show 8092d3a:tests/conformance/MANIFEST.json` parsed (`sum(len(c['fixtures']) for c in categories)`) ⇒ **55**; by-category `{parser:7, validator:13, semantic:28, codegen-c:4, formatter:3}`. **Grew 53 → 55** (`git show fbae38b:tests/conformance/MANIFEST.json` ⇒ **53** `{parser:7, validator:11, semantic:28, codegen-c:4, formatter:3}`): the +2 are **VAL-NEG-009/010** for the new F-1/F-2 `FSM-E0411`/`FSM-E0412` (the G7 discipline — one fixture per new code, no allowlist dodge; W4c DD-25 corroborates 53→55 independently). A positive quality delta, correctly attributed. |
| Differential CORPUS partition (re-derived from source AND the test's own `--nocapture` output — the never-game discipline) | **CORPUS=14 = 11 BYTE_EQUAL + 3 BEHAVIOURALLY_EQUIVALENT_JUSTIFIED + 0 KNOWN_DIVERGENT** | `git show 8092d3a:crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` parsed: `CORPUS` (`:193`) = 14 members; `BYTE_EQUAL` (`:1214`) = 11; `BEHAVIOURALLY_EQUIVALENT_JUSTIFIED` (`:1327`) = 3; `KNOWN_DIVERGENT` (`:1444`) = **`&[]`** (the array body is comments only — "The audit's KNOWN_DIVERGENT list is now empty"). Partition **`11 + 3 + 0 = 14 == CORPUS.len()`** is **dynamically asserted in-test** (`codegen_equivalence_smoke.rs:1531–1559` — no FSM in >1 bucket, the buckets' union == the corpus, every BYTE_EQUAL FSM `panic!`s on regression). **Re-derived from the test's OWN runtime output** (this closeout's re-run): `cargo test -p fsm-simulator --test codegen_equivalence_smoke` ⇒ `test result: ok. 7 passed; 0 failed; 2 ignored` (the 2 ignored are `#[ignore]` diagnostic dumps, NOT gated tests — the count reconciles 9 = 7+2); the `--nocapture` output confirms the 3 BEHAVIOURALLY_EQUIVALENT_JUSTIFIED (vending-machine #5, submachine #0, stress-completion-chain #3) correctly RED-as-record-model-difference-but-behaviourally-proven-identical. W4b independently re-ran it **3× deterministic** + read the never-game-guard proof bodies — the catalogue cannot be gamed by silently dropping/mis-bucketing a member. |
| W1 `fsm/simulate` LSP acceptance (the byte-equality-to-`execute_trace` battery, re-run — the never-game discipline) | **7 passed / 0 failed** | This closeout re-ran `cargo test -p fsm-lsp --test simulate_lsp_acceptance` ⇒ `test result: ok. 7 passed; 0 failed; 0 ignored`. Decisive cases (W4b/W4c, frozen): `init_dispatch_payload_stream_byte_equals_execute_trace_oracle`, `advance_clock_timer_stream_byte_equals_execute_trace_oracle`, `snapshot_restore_rewind_is_byte_identical_to_oracle_replay`, `set_context_field_emits_zero_step_record_and_is_not_a_step`, `step_error_is_surfaced_verbatim_not_a_fake_clean_end`. W4b re-ran ×2 + W4c re-ran independently — all `7/0`, not echoed. |
| ExtHost suite (Extension-Host `@vscode/test-electron`/`xvfb-run`) | **47 passing** (`debugPanel.test.ts` = 6 debug E2Es) | `git show 8092d3a:editors/vscode/src/test/suite/` parsed + per-file `it(`/`test(` count: `commands.test.ts` 9 + `debugPanel.test.ts` **6** + `diagram.test.ts` 4 + `extension.test.ts` 4 + `grammar.test.ts` 7 + `tree.test.ts` 6 + `verify.test.ts` 5 + `verifyLive.test.ts` 6 = **47** (corroborated by the W4b auditor's own `xvfb-run -a npm test` run = **47 passing, exit 0** at `8092d3a`, incl. the three keystone-critical W2/W3/§W4 byte-match-vs-oracle tests). The debug epic's net-new ExtHost surface is `debugPanel.test.ts` (the 6 W2/W3/§W4 E2Es); `diagram.test.ts` (4) is the v1.3-path-identity DF-01 pre/post-identity bar. |
| Toolchain (asserted from inside the worktree) | `1.75.0` (pinned) | `rustup show active-toolchain` from `/root/dev/embeded-fsm-sdk-wt-w4-closeout` ⇒ `1.75.0-x86_64-unknown-linux-gnu (overridden by '/root/dev/embeded-fsm-sdk-wt-w4-closeout/rust-toolchain.toml')`. The `rust-toolchain.toml` pin is honored for all in-repo `cargo` work (a bare `rustc --version` reporting the box default would be a probe-context artifact, NOT a pin violation — the toolchain-probe-trap, Doc 00 §11.51; W4b/W4c assert the same from their worktrees). |
| Doc 00 §11 ledger head (READ-ONLY — the orchestrator appends §11.87+ at W4d, NOT this closeout) | **§11.86** (86 rows; the Phase-8.0 rows are §9 DRAFT, NOT pre-allocated) | `grep -cE '^\| §11\.[0-9]+ ' docs/00-Decisions-And-Reconciliation.md` ⇒ **86**; the highest actual `§11.NN` row ⇒ **§11.86** (the factory-W6c batched-closeout row — independently confirmed from the file; the Phase-8.0 rows are **NOT pre-allocated** per Doc 33's header numbering refinement + the v1.4 L6 lesson). The orchestrator independently re-derives & appends the Phase-8.0 rows starting at the next free integer **§11.87** at W4d (the §9 DRAFT rows below are PLACEHOLDERS `§11.DRAFT-A..D`, NOT assignments — the `comm -23` append-only discipline; **this closeout does NOT edit Doc 00 §11**). |
| df-gate | **19 G free / 76%** | `df -h /` at the worktree ⇒ `/dev/sda1 78G 56G 19G 76% /` — above the ≥5%-margin disk discipline (deps warm from the auditors' builds; no `cargo clean -p` needed for this read+doc-only closeout; the canonical cold-quad-with-Step-0 is the orchestrator's at `X`). |

---

## 9. §11 rows (DRAFT) — **DRAFT ONLY; the orchestrator independently re-derives, renumbers (§11.87+) & appends at W4d; NOT in the append-only ledger**

> **⚠️ THESE ARE DRAFT ROWS. They are NOT appended to `docs/00-Decisions-And-Reconciliation.md` by this closeout** (the BOUNDARY: this closeout does NOT edit Doc 00 §11). Per the release-layer verify-the-record discipline (`[[feedback_embeded_fsm_verify_record_release_layer]]` — Doc 00 §11 is the orchestrator's append-only ledger; the orchestrator independently re-derives every load-bearing claim from source/git before appending, never echoes a wave's self-report) **and** the Doc 31/32/33 "§11-ledger rows are assigned at epic closeout, NOT pre-allocated" discipline: the current ledger head is **§11.86** (independently verified — §8). These DRAFT rows are reported here for **the orchestrator to independently re-derive, renumber (§11.87+ at W4d, batched), and append in one controlled commit** (the `comm -23` append-only discipline). The row numbers below are **PLACEHOLDERS** (`§11.DRAFT-A`..`§11.DRAFT-E`), **NOT** assignments. Doc 33 §9's `§11.DRAFT-A..D` (the owner decisions D-1/D-2/D-3 + Doc 33-authored) were reported by Doc 33's own kickoff wave for the orchestrator to append; whether those were already appended (they would be §11.87–§11.90-class) is the orchestrator's to re-derive at W4d — the rows below are the **closeout/implementation** rows (the epic SHIPPED + the W4b/W4c verdicts + the §11.30 gate-doc-commit), distinct from Doc 33 §9's *design-decision* DRAFT rows. The orchestrator reconciles both sets against the live ledger head at W4d.

**`§11.DRAFT-A` — DRAFT — the orchestrator independently re-derives & appends §11.87+ at W4d; NOT in the append-only ledger.**
> **Debug-interface epic W1 — the `fsm/simulate*` LSP transport + the single sanctioned non-stepping `Interpreter::set_context_field` helper SHIPPED; the KEYSTONE-IN-DEBUG binding tag-gate row PASSES via the W4b source-derived audit (`KEYSTONE-INTACT`).** W1 (`1de83b0`) added the `fsm/simulate` LSP custom request (`crates/fsm-lsp/src/capabilities/simulate.rs` +693 — every `op` exactly one shipped `Interpreter` call or one `write_trace_yaml` + serde, **zero** simulator facts computed locally) over the proven v1.5 W-A2 `LspService::build().custom_method` stdio seam, plus the **single sanctioned `crates/` behaviour delta of the entire epic**: one additive non-stepping `Interpreter::set_context_field` helper (`crates/fsm-simulator/src/interpreter.rs` +25 — a 3-line guarded `rt.context.insert` with a `NotInitialized` guard, the same nature as `InitOptions::initial_context`, `Result<(), StepError>` so **no `StepRecord` by construction**; runs no transition/guard/step). The single Rust-layer enabling change is **one `fsm-simulator = { path = "../fsm-simulator" }` internal path-dep edge** on `crates/fsm-lsp/Cargo.toml` (+ a 1-line `Cargo.lock` dep-list entry) — **0 new external `[[package]]`**, no new crate (`#![forbid(unsafe_code)]` 12 roots/11 crates unchanged, re-derived from source not echoed). The **KEYSTONE-IN-DEBUG binding tag-gate row** (Doc 33 §2) is recorded PASSED via the independent W4b source-derived phase-audit (`docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md`, audited `8092d3a`, verdict `KEYSTONE-INTACT` — the no-fork re-derived FROM SOURCE: the oracle + the `cmd/test.rs` `execute_trace` seam + `write_trace_yaml` byte-untouched across the whole epic arc `fbae38b..8092d3a` save the one sanctioned helper, per-file blob-SHA-identical; negative-grep ∅ across the whole debug surface every hit read; the host differential 3×-deterministic + the W1 LSP acceptance + the 47-test ExtHost suite re-run by the auditor; FROZEN — not echoed). | <why: W1's single keystone-violable Rust/LSP surface + the one sanctioned non-stepping helper + the binding KEYSTONE-IN-DEBUG gate recorded commit-anchored so the no-second-semantics obligation is on record and auditable; honest — the proven `custom_method` seam + the one helper + the no-fork independently re-derived from source by W4b, not echoed> | Phase-8.0-W1 (`1de83b0`; `crates/fsm-lsp/{Cargo.toml,src/capabilities/simulate.rs,src/capabilities/mod.rs,src/lib.rs,src/server.rs}` + `crates/fsm-lsp/tests/simulate_lsp_acceptance.rs` + `crates/fsm-simulator/src/interpreter.rs` `set_context_field`; the FROZEN `docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md` `KEYSTONE-INTACT`)

**`§11.DRAFT-B` — DRAFT — the orchestrator independently re-derives & appends §11.87+ at W4d; NOT in the append-only ledger.**
> **Debug-interface epic W2/W3/W4a — the debug WebviewPanel + breakpoints/time-travel + the capture→`.trace.json` round-trip SHIPPED; NO semantics, the v1.3 renderer reused VERBATIM, the §W4 replay non-vacuous.** W2 (`032438c`) the VS Code debug WebviewPanel (`editors/vscode/src/debug/{debugPanel,index,simulateClient}.ts` + `webview/debugWebview.ts`) — the v1.3 statechart **reused VERBATIM** (`renderModel` imported, NOT reimplemented), active-config overlay + Inject panel (proximity-adjacent typed-payload form) + context inspector + the StepRecord timeline, all driven through W1's `fsm/simulate` over the EXISTING v1.5 W-A2 `LanguageClient` (NO new daemon/port/socket — D-2 confirmed); the one v1.3-source change (`diagramWebview.ts`) a disclosed **behaviour-preserving** refactor (the v1.3 *bundled output* byte-untouched — esbuild per-entrypoint; `diagram.test.ts` the pre/post-identity bar — W4c DF-01). W3 (`b79e052`/`ec903e5`) breakpoints (the client-side StepRecord-**predicate** `evaluateBreakpoint` — a pure filter over the three oracle-emitted `StepRecord` fields, NEVER a guard/transition re-eval) + time-travel (`Interpreter::snapshot`/`restore` only, no client-side reconstruction). W4a (`ae774dd`→`8092d3a`) the capture→`.trace.json` round-trip (the `capture` op = one `write_trace_yaml` + serde, zero `Interpreter`/semantics; "captured ✓" only after the file exists; rejects empty `expected`); the `.trace.json`-not-`.trace.yaml` self-correction is **real and load-bearing** (`fsm test`'s `collect_traces` globs `.trace`/`.trace.json` — a `.trace.yaml` would be silently undiscovered → vacuous; W4b §4 independently hand-confirmed the replay non-vacuous: a real `pass`, not `skip`). W2–W4 hold the keystone *trivially* (they render the oracle's output and compute nothing — W4b §2 ∅). | <why: the W2–W4 TS surface + the v1.3-verbatim-reuse + the StepRecord-predicate-not-re-eval + the §W4-non-vacuity recorded commit-anchored so the no-second-semantics-in-the-webview obligation + the load-bearing `.trace.json` correctness are on record; honest — W4b independently negative-grepped the whole debug surface ∅ + hand-confirmed the §W4 replay non-vacuous, not echoed> | Phase-8.0-W2/W3/W4a (`032438c` + `b79e052`/`ec903e5` + `ae774dd`→`8092d3a`; `editors/vscode/src/debug/**` + `editors/vscode/src/diagram/webview/diagramWebview.ts` the disclosed behaviour-preserving refactor + `editors/vscode/src/test/suite/debugPanel.test.ts`; the FROZEN `docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md` §2/§4)

**`§11.DRAFT-C` — DRAFT — the orchestrator independently re-derives & appends §11.87+ at W4d; NOT in the append-only ledger.**
> **The gating #128 F-1/F-2 codegen-correctness fixes + the `defer` doc-drift fold landed on the debug-epic arc — genuine #110-class silent-misconfig corrections (the keystone-pattern de-duplication of divergent resolvers), each with a hard rejecting diagnostic; EXPECTED 73→75.** F-1 (const-resolver) unified a divergent SECOND resolver (the lowerer's `eval_i64` omitted `EXPR_NAME_REF` while the check resolved it ⇒ `after CONST ms` passed `fsm check` but the lowerer silently dropped the timer — a #110-class silent miscompile) into ONE shared `util::eval_const_expr_value`+`util::file_const_table` consumed by BOTH lowerer and check (lower≡check by construction — the keystone-pattern applied to const-folding), with a hard `FSM-E0411` for the deferred runtime-variable form (never silent). F-2 (queue-config) fixed `lower_queue` matching the KEY not the RHS + codegen reading the CLI default not the IR queue (codegen↔sim silently diverged) via a typed `ConfigEntry::value()` (single SoT shared with the analyzer check) + `CodegenConfig::resolve_queue` (explicit per-field precedence + a note-on-shadow, never silent), with a hard `FSM-E0412` for a non-power-of-2 in-source capacity; the silent-misconfig class is closed END-TO-END (W4c DD-11 gcc-compile-and-RUN analyzer 7/7 + codegen-c 4/4, both Switch & Table). The `defer` doc-drift fold replaced the stale "v1.0 limitation / FSM-E0903" §9.4 paragraph in `docs/04-DSL-Specification.md` with honest v1.1-shipped status (a genuine doc-honesty correction). The live `DiagnosticCode` count moved **73→75** — *correctly attributed* to exactly the two new rejecting diagnostics (NOT a silent code-count drift); the G7 count-discipline lock is GREEN + non-vacuous (conformance 53→55, +VAL-NEG-009/010 — re-derived from source, not echoed). | <why: the F-1/F-2 fixes recorded commit-anchored so the divergent-second-resolver de-duplication (the keystone-pattern applied to const-folding/queue-config) + the never-a-silent-drop hard diagnostics + the EXPECTED 73→75 correctly-attributed-not-drift + the silent-misconfig-class-closed-end-to-end are auditable; honest — W4c independently built+ran the F-2 gcc-RUN harness + re-derived 73→75 + the G7 lock non-vacuous, not echoed> | Phase-8.0 gating fixes (#128 F-1 const-resolver + F-2 queue-config + the `defer` doc-drift fold; `crates/fsm-analyzer/**` + `crates/fsm-codegen-c/**` + `crates/fsm-diagnostics/src/lib.rs` + `crates/fsm-ir/src/model.rs` + `crates/fsm-parser/src/ast/top_level.rs` + `docs/04-DSL-Specification.md`; W4c Lens-2 DD-11/DD-14/DD-15/DD-16/DD-17, frozen)

**`§11.DRAFT-D` — DRAFT — the orchestrator independently re-derives & appends §11.87+ at W4d; NOT in the append-only ledger.**
> **W4c — the independent pre-tag four-lens audit (`TAG-CLEAR`, 0 P0 / 0 P1 / 3 P2 / 2 P3) — FROZEN evidence.** W4c (`docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md`, branch `phase8.0/w4c-four-lens-audit`, audited `8092d3a`, verdict **`TAG-CLEAR`**) is the standing independent pre-tag four-lens (the v1.1–v1.5–factory §3 pattern), delta-scoped (`fbae38b..8092d3a` = 57 files / +10045 / −243, NOT a keystone re-litigation — W4b's binding job, frozen; NOT a whole-codebase re-audit): **Lens 1 Arch** PASS (the single net-new Rust dep edge is the *internal* `fsm-lsp`→`fsm-simulator` path dep — 0 new external `[[package]]`, no layering inversion, `forbid(unsafe_code)` 12/12 files unchanged, the v1.3 renderer reused VERBATIM); **Lens 2 Correctness** PASS (the F-1/F-2 fixes genuine #110-class corrections, the byte-identity property empirically REPRODUCED `simulate_lsp_acceptance` 7/7, full workspace 911 passed/0 failed); **Lens 3 Reliability** PASS (`StepError` verbatim, the v1.3 stale-banner reused VERBATIM, the #141 pending-timer surface honestly degraded NOT faked, the disclosed ~3.4mb bundle the pre-existing v1.3-ELK-per-entrypoint pattern not an epic regression); **Lens 4 Security/SCA** PASS (binding) — `cargo +stable audit` EMPIRICALLY RUN over 207 deps = 0 vulns/0 warnings (NOT honest-skipped), the epic added ZERO new npm deps (`package.json` manifest-only, `package-lock.json` NOT in the delta — derived TWO ways), the 3 pre-existing devDep-only transitive npm vulns byte-identical to the factory-tag accepted-with-rationale set (epic added 0 of them), **no new network attack surface** (rides the existing stdio `custom_method` boundary — no daemon/port/socket; the only net/process in the delta is test-only). 5 findings, **NONE gating** (DF-01 v1.3-framing-precision P2 / DF-02 DBGUX-prose→v1.6 P3 / DF-03 3 pre-existing devDep npm vulns P2 / DF-04 #123 G7-owned codegen-c-`.contains` P2 / DF-05 F-1-adjacent-site informational P3). **Both W4b + W4c FROZEN — never overwritten by the closeout** (the `AUDIT_*` convention; the §11.29 verify-the-record discipline — re-read in full, not echoed). | <why: the independent four-lens-TAG-CLEAR-0-P0-0-P1 + the Rust-SCA-empirically-run + the byte-identity-empirically-reproduced + the no-new-network-surface-source-verified + the 5-findings-none-gating recorded commit-anchored so the pre-tag audit's adversarial integrity is auditable; honest — both docs FROZEN, the verdicts re-read in full not echoed at the closeout> | Phase-8.0-W4c (`phase8.0/w4c-four-lens-audit`; `docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md` `TAG-CLEAR` 0 P0/0 P1/3 P2/2 P3, FROZEN; the W4b keystone audit `docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md` `KEYSTONE-INTACT` is §11.DRAFT-A's frozen evidence — cited there, not duplicated)

**`§11.DRAFT-E` — DRAFT — the orchestrator independently re-derives & appends §11.87+ at W4d; NOT in the append-only ledger.**
> **Phase-8.0-W4d batched closeout = the §11.30 gate-doc commit `X`; every pinned numeric RE-DERIVED at `X` not echoed + the W4c carry-list + the Doc 33 §5 carried-findings + the carried owner-escalations folded in ONE batch; NO code-tree touch (no F-05-equivalent).** This row's commit is the Phase-8.0 batched doc/ledger closeout (the §11.62 batched-doc discipline — fold ALL carry items in one controlled batch, never piecemeal; the v1.2–v1.5/factory precedent) **and** is the §11.30 gate-doc commit the `checkpoint/2026-05-19-debug-interface` milestone tag lands on. It authors **`docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md`** (mirroring the `GATE_VERIFICATION_v1_5.md` / `GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` §1–§8 skeleton 1:1, every pinned numeric **independently RE-DERIVED at `X`, NOT echoed**: live `DiagnosticCode`=**75** [the F-1/F-2 +2 vs the frozen factory 73 — correctly attributed to the two new rejecting diagnostics, NOT a drift], `forbid(unsafe_code)`=**12 roots/11 crates** unchanged [W1 added an internal path-dep edge + a `set_context_field` helper into the already-forbid-unsafe `fsm-simulator`, no crate root], conformance=**55** [the MANIFEST grew 53→55, +VAL-NEG-009/010], the differential CORPUS partition re-derived from source AND the test's `--nocapture` output [`11+3+0=14==CORPUS.len()` dynamically asserted — the never-game discipline], the ExtHost suite=**47 passing**, **the release-record Rust delta stated PRECISELY**: `git diff fbae38b..8092d3a -- crates/` = **28 files / +3260 / −214** with **0 new external deps** [the single Cargo.lock edge the *internal* `fsm-lsp`→`fsm-simulator` path dep] — stated as exactly that moderate additive delta, **neither under-stated** [the v1.3 cardinal "zero Rust delta" error] **nor over-stated**, **NOT echoed** from W4c's whole-delta `57 files / +10045 / −243` but independently re-derived at the `-- crates/` scope at `X`; the v1.3-cardinal-error-form grep of this record = **0 hits asserting "no/zero/empty Rust delta"** — `[[feedback_embeded_fsm_verify_record_release_layer]]`) + CHANGELOG `[debug-interface]` + the **ROADMAP debug-epic retrospective + the milestone-tag record** (the in-repo `docs/ROADMAP.md` "See also" stale-pointer was ALREADY reconciled at the v1.5 closeout — Phase-8.0's ROADMAP edit does NOT re-touch it) + `docs/metrics/2026-05-19-debug-interface.json`. It folds in ONE batch: **(a)** the W4c **DF-01** (the v1.3-framing-precision — accepted-with-rationale, the precise framing FOLDED into the gate doc prose) + **DF-02** (the DBGUX `.trace.yaml`-prose → the owner-reserved v1.6 doc-honesty pass, cross-ref #103 — the frozen design doc NOT edited) + **DF-03** (3 pre-existing devDep-only npm vulns — accept-with-written-rationale ×3, NOT gating, → the v1.6 FE-hygiene batch) + **DF-04** (the `.contains` codegen-c #123-class deferred tail — accept-tracked, owned by the G7 wave, carried from the factory tag) + **DF-05** (the F-1-adjacent-site `resolve_simple_literal` — informational non-defect, no action); **(b)** the Doc 33 §5 carried Zero-Legacy findings re-stated NOT fixed (CF-1 the AVR Harvard `.rodata`+TABLE-dispatch hazard — a CI-runner-only factory on-target-matrix candidate, NOT a debug-interface item; CF-2 the Doc 16 §2–§6 HAL-symbol staleness — folds into the owner-reserved v1.6 doc-honesty pass, cross-ref #103); **(c)** carried owner items **re-stated, NOT resolved** (the §11.62 discipline): OWNER-1 the release-numbering call (the `checkpoint/`-not-`vX.Y.0` parked default); OWNER-2/G9 push of LOCAL `v1.0.0`–`v1.5.0`+ALL checkpoints+this milestone tag + the CI-matrix/SCA/the 4 factory lanes/the debug ExtHost tests never run on a remote runner (G9 stays load-bearing); OWNER-3 the deferred full `signal`-primitive epic (D-1 shape / D-3 sequenced after this epic + v1.6); VSIX packaged-not-published (v1.5 OWNER-A); no top-level `LICENSE`/`CONTRIBUTING`/`CoC` (owner/legal, the v1.5 OWNER-D lineage carried to v1.6 with D1); the parked Certifiability/MISRA codegen-mode arc; the v1.6 D1 landing + the doc-honesty pass (recorded-NOT-scoped); the 5-platform binary tail (blocked-on-G9, NOT a ci.yml lane); task #17 CI-matrix owner-blocked; R7 the mechanised sim≡codegen-equivalence *formal proof* (the factory epic delivered the empirical differential; the formal proof remains the named v1.4-stretch deferral); and the W4c "Tracked-deferrals" #138/#141/#139/#134/#123/#103 all confirmed tracked-not-dropped — **none silently dropped**. The broader README/Doc-07/Doc-14 doc-honesty staleness pass is **FLAGGED not fixed** (Doc 31 §6 H6 / Doc 32 §7-tweak-5 / Doc 33 §5 — the SEPARATE already-identified backlog item; the ROADMAP "See also" pointer was already fixed at v1.5). The **memory-SoT-freshness gate** (Doc 33 §7 / Doc 32 §7-tweak-3): the wave→commit map is recorded in the gate doc §"Scope"/§5#5 so the orchestrator can advance the backlog SoT head status line + add the Phase-8.0 RELEASE RECORD section *in the same closeout motion* (the SoT is the orchestrator's memory file — outside the repo + outside this wave's hard-guardrail scope, exactly as the v1.5/factory closeouts recorded it; honest-prospective, no fabricated tag). The cold-quad WITH the NOTE-1 STEP 0 + the **binding JS lane** (the surface DOES touch `editors/vscode/**` via W2/W3/W4 — exactly like v1.5/factory) at `X` is described as the **contingent gating step** (no fabricated quad/tag transcript — the `GATE_VERIFICATION_v1_5.md` §4 honest-prospective precedent verbatim). **NO code-tree touch at `X`** (no F-05-equivalent — the W4c findings are framing/doc-precision/accepted-tracked, none requiring an in-scope code change). **Exclusive-writer discipline:** this closeout is the sole writer of its owned files (the gate doc + CHANGELOG + ROADMAP + the metrics JSON; the §9 DRAFT §11 rows are REPORTED here for the orchestrator, NOT appended); the commit is NOT merged/pushed/tagged — the orchestrator gates (the pre-tag four-lens W4c TAG-CLEAR [frozen] + W4b KEYSTONE-INTACT [frozen] → independently re-derives + appends §11.87+ → the cold-quad WITH NOTE-1 Step 0 + the binding JS lane at this commit → tag at this commit, §11.30 strictly; the 7th consecutive prospective-clean release). | <why: the single batched closeout + the §11.30 gate-doc-commit role recorded so the every-pinned-numeric-re-derived-at-X-not-echoed (incl. NOT echoing W4c's whole-delta but re-deriving the `-- crates/`-scoped +3260 at X) + the carry-list→fold mapping + the DF-01..DF-05 + CF-1/CF-2 dispositions + the carried-owner-escalations-none-dropped + the memory-SoT-freshness recorded-for-the-orchestrator + the tag-topology §11.30-clean-by-construction are auditable; honest, conservative — the frozen W4b/W4c + Doc 33 + the frozen design docs NOT overwritten, the cold-quad+JS-lane the contingent gating step (no fabricated transcript), NO code-tree touch, the milestone tag a checkpoint/<name> NOT a vX.Y.0 (v1.6 owner-reserved)> | Phase-8.0-W4d batched closeout = the §11.30 gate-doc commit (`phase8.0/w4-closeout`; folds the frozen `docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md` + `docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md` carry-lists + the Doc 33 §5 CF-1/CF-2; `docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md` §6/§7/§8/§9; NO code-tree touch)

---

## 10. See also

- `docs/33-Debug-Interface-Wave-Plan.md` (FSM-PLAN-DBGIFACE-33 — the accepted wave-plan-of-record: §1 the W1–W4 decomposition, §2 the KEYSTONE-IN-DEBUG binding gate, §4 OWNER-1..OWNER-4, §5 CF-1/CF-2, §7 the §11.30 tag-gate criteria, §8 the deferred `signal` epic, §9 the design-decision DRAFT §11 rows; **frozen — not annotated by this closeout**)
- `docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md` (FSM-AUDIT-PHASE-DEBUG-W4 — the binding source-derived keystone phase-audit, audited `8092d3a`, verdict `KEYSTONE-INTACT`; **FROZEN, never overwritten**) · `docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md` (FSM-AUDIT-PRE-TAG-DEBUG — the independent pre-tag four-lens, audited `8092d3a`, verdict `TAG-CLEAR` 0 P0/0 P1; **FROZEN, never overwritten**)
- `docs/GATE_VERIFICATION_v1_5.md` + `docs/GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` (the §1–§8 structural template mirrored 1:1; the honest-prospective contingent-gate precedent verbatim) · `docs/GATE_VERIFICATION_v1_{0,1,2,3,4}.md` (the v1.0–v1.4 baselines)
- `docs/metrics/2026-05-19-debug-interface.json` (the Phase-8.0 metrics snapshot — the v1.4/v1.5/factory D.8 metrics-contract precedent) · `docs/metrics/2026-05-17-v1_5.json` (the mirrored metrics shape)
- `CHANGELOG.md` `[debug-interface]` (the milestone-not-`vX.Y.0` section + *Known limitations* + the carried owner-escalations) · `docs/ROADMAP.md` (the debug-epic `## … SHIPPED` + retrospective + the `### v1.6 candidates` carry; the "See also" pointer already reconciled at v1.5 — NOT re-touched here)
- `docs/00-Decisions-And-Reconciliation.md` §11 (the append-only ledger — head **§11.86**, independently verified; the Phase-8.0 rows are §9 DRAFT, **NOT pre-allocated**; the orchestrator independently re-derives & appends §11.87+ at W4d — **this closeout does NOT edit §11**)
- `crates/fsm-lsp/src/capabilities/simulate.rs` (the W1 `fsm/simulate` transport — every `op` exactly one shipped `Interpreter` call/`write_trace_yaml`) · `crates/fsm-simulator/src/interpreter.rs` (the `set_context_field` helper W1 adds — the one sanctioned non-stepping `crates/` delta) · `crates/fsm-cli/src/cmd/test.rs` (the `execute_trace` seam + the `collect_traces` `.trace`/`.trace.json` discovery rule — byte-untouched across the epic arc) · `editors/vscode/src/debug/**` (the W2/W3/W4 webview — NO semantics) · `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 behavioural-acceptance; §11.3 phase-boundary audits; §11.22/§11.30/NOTE-1 cold-quad + tag-ordering)

*End of GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19 — the canonical release record for the interactive debug-interface epic (Phase-8.0). The orchestrator gates/merges {W4b-frozen, W4c-frozen, this batch}, independently re-derives & appends Doc 00 §11.87+ at W4d, runs the §11.30-clean cold-quad WITH the NOTE-1 `cargo clean -p` STEP 0 + the binding JS lane at `X`, and tags `checkpoint/2026-05-19-debug-interface` at `X` — quad-commit ≡ JS-lane-commit ≡ tag-commit ≡ `X`, LOCAL only (G9 = the owner's push). This closeout does NOT merge/tag/push.*
