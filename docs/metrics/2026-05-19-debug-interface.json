{
  "$comment": "FSM Studio metrics snapshot at the interactive debug-interface epic (Phase-8.0) milestone (pending the contingent cold-quad WITH the NOTE-1 cargo-clean-p Step 0 + the BINDING JS lane + the pre-tag four-lens audit step). COMPANION to the v1.5.0 snapshot docs/metrics/2026-05-17-v1_5.json + the factory snapshot — the prior frozen attestations are NOT overwritten. 'deltas_vs_prev' carries factory-checkpoint->debug-interface movement; 'attention:true' marks a metric the next status report must examine. Reproducible: every metric has a 'method'. KEY FACT (release-record-symmetry, Doc 33 §7 — the v1.3 cardinal-error guard): the Phase-8.0 Rust delta vs the factory checkpoint (fbae38b == checkpoint/factory-reliability-ci-2026-05-18^{commit}) is MODERATE AND ADDITIVE BY DESIGN — `git diff --shortstat fbae38b..8092d3a -- crates/` = 28 files / +3260 / -214 (29 / +3261 / -214 incl. Cargo.toml Cargo.lock; the +1 file/line is the Cargo.lock fsm-lsp dep-list edge). It is the W1 fsm/simulate* LSP transport (crates/fsm-lsp/src/capabilities/simulate.rs +693 pure-Interpreter-marshalling) + its acceptance suite (crates/fsm-lsp/tests/simulate_lsp_acceptance.rs +945) + the SINGLE sanctioned non-stepping Interpreter::set_context_field helper (crates/fsm-simulator/src/interpreter.rs +25, a 3-line guarded HashMap::insert, 0 StepRecord by construction) + the gating #128 F-1/F-2 codegen-correctness fixes (crates/fsm-analyzer/** + crates/fsm-codegen-c/** + crates/fsm-diagnostics/src/lib.rs +35 [the E0411/E0412 +2] + crates/fsm-ir/src/model.rs + crates/fsm-parser/src/ast/top_level.rs + crates/fsm-cli/{cli,cmd/generate}.rs) + the additive test-corpus expansion (crates/fsm-simulator/tests/codegen_equivalence_smoke.rs +64/-4, factory-wave additive NOT an oracle change). This is stated as EXACTLY that moderate additive delta — NEITHER under-stated (the v1.3 cardinal error wrongly summarised a real delta as zero) NOR over-stated (the v1.4 verification-core was a +7398 NEW-crate epic; the factory epic was +8192; this debug-interface epic is +3260, the W1 transport+acceptance + the gating F-1/F-2 fixes + the additive corpus, NO new crate, NO new external dep). RELEASE-RECORD-SYMMETRY SELF-CHECK (the form, not the lexeme — exactly as the frozen v1.4/v1.5/factory metrics precedent applies it): the v1.3-cardinal-error FORM is a FALSE ASSERTION that the actual (whole) Rust delta is zero/no/empty. The whole-Phase-8.0-Rust-delta is asserted EVERYWHERE in this record + GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md as `28 files / +3260 / -214, a moderate additive delta` — NOT empty, NOT zero. There are ZERO false-zero whole-delta assertions (verified by a Python context-check of all 12 lexical hits of the broad regex `(no|zero|empty) ... (rust )?delta` against GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md: all 12 are explicit anti-pattern citations / mandated repudiation clauses [`[the v1.3 cardinal \"zero Rust delta\" error]`, `neither under-stated ... nor over-stated`, `the v1.3 \"zero Rust delta\" cardinal release-record sin is absent`] or the TRUE `0 new external [[package]]` Cargo.lock fact — 0 false whole-delta-is-zero ASSERTIONS). The naive broad regex returns >0 on BOTH this record AND the frozen v1.4/v1.5/factory metrics precedents BY CONSTRUCTION, because the brief itself MANDATES the repudiation clause 'neither under-stated (the v1.3 ... cardinal error) nor over-stated' — quoting the v1.3 error to repudiate it is REQUIRED, not the error; the binding discipline is 0 false whole-delta-is-zero ASSERTIONS, which holds. 0 NEW EXTERNAL DEPS: `git diff fbae38b..8092d3a -- Cargo.lock | grep -E '^\\+(name|source) ='` => none (no new [[package]]); the only Cargo.lock change is `+ \"fsm-simulator\"` inside the fsm-lsp dependencies array (an INTERNAL path dep — fsm-simulator is already a workspace member; the v1.5 W-A2 single-internal-edge shape). THE DIFFERENTIAL ORACLE IS BYTE-UNTOUCHED: over the entire epic arc fbae38b..8092d3a the ONLY delta under crates/fsm-simulator/src/ + crates/fsm-cli/src/cmd/test.rs is the single sanctioned additive non-stepping set_context_field helper (W4b §1: cmd/test.rs NOT in the diff at all, blob-SHA-identical; trace.rs/write_trace_yaml/execute_trace + every transition/guard/step/clock/queue/timer/LCA/completion/history module blob-SHA byte-identical — the keystone holds, the W4b source-derived audit independently re-confirmed KEYSTONE-INTACT). The Rust metrics that DIFFER from the frozen factory figures do so for an attributed reason: live DiagnosticCode 73->75 because the #128 F-1/F-2 gating fixes added exactly TWO hard rejecting diagnostics (FSM-E0411/FSM-E0412) replacing two previously-silent miscompiles (NOT a silent code-count drift); conformance 53->55 (+VAL-NEG-009/010, the G7 one-fixture-per-new-code discipline); forbid-unsafe 12 roots/11 crates UNCHANGED because W1 added an internal path-dep edge + a helper into the already-forbid-unsafe fsm-simulator (no new crate root). Each is independently re-derived at the gate-doc commit (the working tree == X has NO code-tree delta vs 8092d3a — no F-05-equivalent in this epic's closeout), not echoed.",
  "date": "2026-05-19",
  "milestone": "Interactive debug-interface epic (Phase-8.0) — the in-editor live simulation/debug surface (W1 the fsm/simulate* LSP transport + the single sanctioned non-stepping set_context_field helper; W2 the debug WebviewPanel; W3 breakpoints+time-travel; W4a capture->.trace.json round-trip) as JUST ANOTHER FRONTEND of the one shipped fsm_simulator oracle, never a second semantics. A quality milestone, NOT a vX.Y.0 (v1.6.0 owner-reserved for landing+doc-honesty). Tag is the contingent cold-quad (WITH NOTE-1 Step 0) + the BINDING JS lane + pre-tag-four-lens step (GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §4/§7)",
  "tag_topology": {
    "$comment": "A quality milestone tagged checkpoint/2026-05-19-debug-interface (the proven checkpoint/<name> immutable-anchor pattern — LOCAL, no push; G9 the owner's switch). Deliberately NOT a vX.Y.0: v1.6.0 is owner-reserved for the landing-page + doc-honesty pass (post-factory); this minor lands as its own post-v1.6 minor (D-3/OWNER-1 — the factory-epic checkpoint/<name>-not-v1.6.0 precedent, Doc 00 §11.80). The §11.30/factory-GT-12 cold-quad rigor applies identically to a release tag.",
    "debug_interface_gate_doc_commit": "X (pending — this metrics file + GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md + CHANGELOG [debug-interface] + ROADMAP [debug-epic retro + the milestone-tag record]; the orchestrator independently re-derives & appends Doc 00 §11.87+ at W4d — NOT in this closeout's scope; NO crates/.github/ code-tree touch [no F-05-equivalent]; the release commit)",
    "debug_interface_head_pre_gate": "8092d3a",
    "debug_interface_head_pre_gate_full": "8092d3a02a7bd6a6ee9d3dad306580d9b3019a0b (== the W4a HEAD: 'feat(debug-w4a): capture->.trace.json — recorded-from-the-oracle via the shipped write_trace_yaml, byte-identical by construction'; == base main of the closeout worktree phase8.0/w4-closeout)",
    "debug_interface_head_subject": "feat(debug-w4a): capture->.trace.json — recorded-from-the-oracle via the shipped write_trace_yaml, byte-identical by construction",
    "checkpoint_2026_05_19_debug_interface_commit": "X (== the milestone tag commit, per §11.30: gate-doc commit -> cold quad WITH NOTE-1 Step 0 + the BINDING JS lane -> tag, all at the same commit; prospective §11.30-clean — the 7th consecutive)",
    "note": "Phase-8.0 follows the §11.30 prospective ordering (gate-doc commit X -> cold-from-source quad AT X with the MANDATORY NOTE-1 `cargo clean -p` of all 11 first-party crates Step 0 + the BINDING JS lane AT X -> tag AT X), the v1.2/v1.3/v1.4/v1.5/factory pattern repeated (7th consecutive prospective-clean release: v1.1 retroactively reconciled §11.30; v1.2-v1.5 + the factory checkpoint prospectively; Phase-8.0 prospectively). The JS lane is a BINDING gate for Phase-8.0 (like v1.5/factory, unlike v1.4 which touched no editors/) because the Phase-8.0 surface DOES touch editors/vscode/** (W2/W3/W4 the debug webview + 1 new ExtHost test debugPanel.test.ts + the one disclosed behaviour-preserving diagramWebview.ts refactor). De-risking predicate (NOT a substitute for the canonical run at X): the W4b auditor's own xvfb-run -a npm test run at 8092d3a = 47 passing, exit 0 (real shipped target/debug/{fsm,fsm-lang-server} resolved fresh under the 1.75 pin) incl. the three keystone-critical W2/W3/§W4 byte-match-vs-oracle tests + the host differential 3x-deterministic 7/0;2-ignored + the W1 LSP acceptance 7/0 x2; W4c re-ran the full workspace 911/0 + the host differential 7/0 + the W1 LSP acceptance 7/0 + the F-2 gcc-RUN 7/7+4/4 + the G7 lock 4/4 at 8092d3a; this closeout re-ran the host differential 7/0;2-ignored + the W1 LSP acceptance 7/0 at the working tree. Canonical record: docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md.",
    "factory_checkpoint_baseline": "the epic-boundary anchor is checkpoint/factory-reliability-ci-2026-05-18 — `git rev-parse` (tag-OBJECT) = a598fb63a3701c3b0e314f2c37a1c457fab78680; `^{commit}` = fbae38b1ccf7eb00fb344b0a034d16973254add9 (the COMMIT, used for every delta). `git merge-base --is-ancestor fbae38b 8092d3a` => exit 0 (TRUE — the EMPTY-core-diff spans the entire debug-epic arc W0->W4a). v1.5.0 commit = `git rev-list -1 v1.5.0` = 2ff8ac31f91eff922e48fb29ab1026b91a9166df.",
    "pin_lineage_immutable_not_moved": {
      "v1_0_0_commit": "b4800036", "v1_1_0_commit": "abc70044", "v1_2_0_commit": "71043935",
      "v1_3_0_commit": "a036c381", "v1_4_0_commit": "3bbcd0f5", "v1_5_0_commit": "2ff8ac31",
      "factory_reliability_ci_commit": "fbae38b (== checkpoint/factory-reliability-ci-2026-05-18; tag-object a598fb63)",
      "debug_interface_commit": "X (== checkpoint/2026-05-19-debug-interface)",
      "note": "Release tags are ANNOTATED + immutable + never moved (re-cutting a release tag is a destructive op never performed). The checkpoint/<name> anchors are likewise annotated + immutable. v1.6.0 is OWNER-RESERVED (landing+doc-honesty, post-factory) — the debug-interface minor is a checkpoint/<name> anchor, NOT a version-number claim."
    },
    "tag_object_vs_commit_note": "Release tags + the annotated checkpoint/<name> anchors are ANNOTATED: `git rev-parse <tag>` yields the tag-OBJECT sha; `git rev-list -1 <tag>` (or `<tag>^{commit}`) yields the COMMIT. They differ and must not be conflated. Independently re-confirmed at this commit: `git rev-parse checkpoint/factory-reliability-ci-2026-05-18` = a598fb63 (tag-OBJECT) != `checkpoint/factory-reliability-ci-2026-05-18^{commit}` = fbae38b (commit — the epic-boundary anchor used throughout). Recorded per Doc 00 §11.30 / GATE_VERIFICATION_v1_{1,2,3,4,5}/factory §7/§8 so no future wave misreads the checkpoint/2026-05-19-debug-interface tag-object sha as a mismatched commit."
  },

  "phase8_0_rust_delta_is_moderate_additive_by_design": {
    "rust_delta_vs_factory_checkpoint": "28 files / +3260 / -214 (-- crates/); 29 files / +3261 / -214 (incl. Cargo.toml Cargo.lock)",
    "method": "`git diff --shortstat fbae38b..8092d3a -- crates/` => 28 files changed, 3260 insertions(+), 214 deletions(-). With Cargo.toml Cargo.lock: `git diff --shortstat fbae38b..8092d3a -- crates/ Cargo.toml Cargo.lock` => 29 files / +3261 / -214 (the +1 file/+1 line is the Cargo.lock fsm-lsp dep-list edge). Independently re-derived at the gate-doc commit (the working tree == X has NO code-tree delta vs 8092d3a); W4c independently re-derived the WHOLE delta `fbae38b..8092d3a` = 57 files / +10045 / -243 (incl. docs/editors/tests — a different scope; the `-- crates/`-scoped Rust delta is the 28/+3260/-214 stated here, both correct at their respective scopes — the git-diff-scope distinction, NOT a discrepancy).",
    "breakdown": {
      "w1_fsm_simulate_lsp_transport": "crates/fsm-lsp/src/capabilities/simulate.rs +693 (a PURE TRANSPORT FRONTEND of fsm_simulator::Interpreter — every op [init/dispatch(+payload)/advanceClock/setContext/snapshot/restore/capture] is exactly ONE shipped Interpreter call or one write_trace_yaml + serde; ZERO simulator facts computed locally; the W4b source-derived audit read every op handler) + capabilities/mod.rs +24 + lib.rs +16 + server.rs +46 + Cargo.toml +19 (the SINGLE fsm-simulator = { path = \"../fsm-simulator\" } INTERNAL path-dep edge + a WHY comment — the single Rust-layer enabling change of the entire epic; the v1.5 W-A2 single-internal-edge shape)",
      "w1_acceptance_suite": "crates/fsm-lsp/tests/simulate_lsp_acceptance.rs +945 (the in-process tower-lsp differential-oracle suite — the StepRecord stream byte-equals fsm test's execute_trace + the set_context_field-emits-zero-StepRecord contract + the no-fake-clean-end bar; 7 tests, re-run 7/0 by W4b x2 + W4c + this closeout)",
      "w1_set_context_field_the_one_sanctioned_crates_delta": "crates/fsm-simulator/src/interpreter.rs +25 — a single additive non-stepping Interpreter::set_context_field(&mut self, name, value) -> Result<(), StepError> helper: a 3-line guarded rt.context.insert with a NotInitialized guard, the SAME NATURE as InitOptions::initial_context (which already pokes rt.context at init); runs NO transition/guard/step/clock/queue/timer; the return type carries NO StepRecord by construction. The ONLY permitted crates/ behaviour delta of the entire epic (W4b §1 full hunk read + the passing set_context_field_emits_zero_step_record_and_is_not_a_step acceptance test). NOT a semantics.",
      "gating_128_f1_f2_codegen_correctness_fixes": "crates/fsm-analyzer/** ~+670 (checks/queue.rs +74, checks/timer.rs +166/-?, util.rs +89, lower/{ids,machine,state}.rs, tests/queue_config.rs +160) + crates/fsm-codegen-c/** ~+790 (config.rs +196, emit/mod.rs +82, tests/queue_config_runs.rs +476 the gcc-RUN harness) + crates/fsm-diagnostics/src/lib.rs +35 (the FSM-E0411/FSM-E0412 +2) + crates/fsm-ir/src/model.rs +29 + crates/fsm-parser/src/ast/top_level.rs +34 + crates/fsm-cli/{cli,cmd/generate}.rs ~+69 — genuine #110-class silent-misconfig corrections that landed on the epic arc (the divergent-second-resolver de-duplication: F-1 unifies eval_const_expr_value+file_const_table consumed by BOTH lowerer & check; F-2 a typed ConfigEntry::value() + CodegenConfig::resolve_queue with explicit per-field precedence + a note-on-shadow), each with a hard rejecting diagnostic for the deferred form (never a silent drop). W4c Lens-2 DD-11/DD-14/DD-15/DD-16 frozen.",
      "additive_test_corpus_expansion": "crates/fsm-simulator/tests/codegen_equivalence_smoke.rs +64/-4 — two corpus members added (timer-const-ref, queue-overflow) + the dynamic catalogue-arithmetic assertion correspondingly updated (11+3+0=14==CORPUS.len()). Factory-wave additive (provenance: factory FW-F1/F-2 commits, NOT debug-epic W1-W4a changes — W4b §1); modifies NO run_differential logic, NO oracle invocation, NO FSM semantics — it only EXPANDS the differential test surface, strengthening the no-fork guarantee.",
      "cargo_lock": "+1 line — a single `+ \"fsm-simulator\"` inside fsm-lsp's dependencies array; NO new [[package]] (fsm-simulator is already a workspace member; its transitive deps were already in the workspace lock). An INTERNAL path-dep edge.",
      "differential_oracle_byte_untouched": "Over the ENTIRE epic arc fbae38b..8092d3a the ONLY delta under crates/fsm-simulator/src/ + crates/fsm-cli/src/cmd/test.rs is the one sanctioned set_context_field helper. cmd/test.rs is NOT in the diff at all (blob-SHA 6ffb91aa... identical at both endpoints — W4b §1). trace.rs (write_trace_yaml+execute_trace) + every transition/guard/step/clock/queue/timer/LCA/completion/history module are blob-SHA byte-identical (auditor-recomputed per file). THE KEYSTONE HOLDS (independently re-derived from source by the W4b audit, verdict KEYSTONE-INTACT).",
      "rust_toolchain_toml": "byte-unchanged — pin held at 1.75 (`rustup show active-toolchain` from /root/dev/embeded-fsm-sdk-wt-w4-closeout => 1.75.0-x86_64-unknown-linux-gnu (overridden by '<worktree>/rust-toolchain.toml'))"
    },
    "cargo_lock_new_external_deps": "0",
    "cargo_lock_method": "`git diff fbae38b..8092d3a -- Cargo.lock | grep -E '^\\+(name|source) ='` => none (no new [[package]] block — the only Cargo.lock change is the fsm-lsp->fsm-simulator dependency-list edge; fsm-simulator is already a workspace member, its transitive deps already in the lock).",
    "interpretation": "The Phase-8.0 Rust delta vs the factory checkpoint (fbae38b) is MODERATE AND ADDITIVE BY DESIGN — a +3260/-214 across 28 files, NOT empty and NOT zero, confined to the W1 fsm/simulate* LSP transport + its acceptance suite + the one sanctioned non-stepping set_context_field helper + the gating #128 F-1/F-2 codegen-correctness fixes + the additive test-corpus expansion + a 1-line Cargo.lock internal-edge entry. This is the Phase-8.0-shaped instance of the release-record-symmetry discipline (Doc 33 §7): the delta is easy to mis-attribute (W2-W4 are TS — only W1 + the gating fixes are Rust) — it is stated as EXACTLY what it is, NEITHER under-stated (the v1.3 cardinal 'zero Rust delta' error) NOR over-stated (the v1.4 +7398 NEW-crate verification-core epic; the factory +8192 epic). The Rust metrics that DIFFER from the frozen factory figures do so for an attributed reason (DiagnosticCode 73->75 the F-1/F-2 +2; conformance 53->55 +VAL-NEG-009/010); forbid-unsafe 12/11 UNCHANGED because W1 added an internal path-dep edge + a helper into the already-forbid-unsafe fsm-simulator, no crate root. Each independently re-derived at the gate-doc commit, not echoed.",
    "release_record_symmetry_self_check": {
      "$comment": "The form, not the lexeme — the frozen v1.4/v1.5/factory metrics precedent applied to Phase-8.0. The v1.3-cardinal-error FORM is a FALSE ASSERTION that the actual (whole) Rust delta is zero/no/empty.",
      "broad_regex_lexical_hits_on_gate_doc": 12,
      "false_whole_delta_is_zero_assertions": 0,
      "method": "Python context-check of all 12 lexical hits of `(no|zero|empty) (new |external |rust )*(rust )?delta` (case-insensitive) against docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md: each hit's ±90/70-char context was classified. ALL 12 are explicit anti-pattern citations / mandated repudiation clauses (`[the v1.3 cardinal \"zero Rust delta\" error]`, `neither under-stated ... nor over-stated`, `the v1.3 \"zero Rust delta\" cardinal release-record sin is absent`) or the TRUE `0 new external [[package]]` Cargo.lock fact. 0 hits assert Phase-8.0 has a no/zero/empty Rust delta. The naive broad regex returns >0 BY CONSTRUCTION because the brief MANDATES the repudiation clause; the binding discipline is 0 false whole-delta-is-zero ASSERTIONS — which HOLDS (the frozen v1.4/v1.5/factory metrics $comment documents the identical reasoning)."
    }
  },

  "keystone_in_debug": {
    "$comment": "The BINDING Phase-8.0 tag-gate row (Doc 33 §2/§7): every debug UI verb is EXACTLY ONE shipped fsm_simulator::Interpreter call marshalled over the W1 fsm/simulate* LSP custom request (the proven v1.5 W-A2 custom_method seam); breakpoints are a PREDICATE over the StepRecord fields the oracle emits, NEVER a guard/transition re-eval; time-travel is Interpreter::snapshot/restore; capture round-trips the SAME write_trace_yaml/execute_trace fsm test uses (byte-identical by construction). NO transition-selection/guard-eval/completion-synthesis/RTC-step/active-config-computation in crates/fsm-lsp/** OR editors/vscode/** (TS or Rust). The single sanctioned crates/ behaviour delta is the one additive non-stepping set_context_field helper. RECORDED PASSED.",
    "verdict": "KEYSTONE-INTACT",
    "evidence_doc": "docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md (branch phase8.0/w4b-keystone-audit, audited 8092d3a — FROZEN, never overwritten)",
    "no_fork_source_derived": "Over the entire epic arc fbae38b..8092d3a the ONLY delta under crates/fsm-simulator/src/ + crates/fsm-cli/src/cmd/test.rs is the single sanctioned additive non-stepping set_context_field helper (W4b §1: cmd/test.rs NOT in the diff at all, blob-SHA 6ffb91aa identical at both endpoints; trace.rs/write_trace_yaml/execute_trace + every transition/guard/step/clock/queue/timer/LCA/completion/history module blob-SHA byte-identical — auditor-recomputed per file). Negative-grep ∅ across the WHOLE debug surface (simulate.rs 693 ln + editors/vscode/src/debug/** 2946 ln incl. webview/) for any fn DEFINING transition-selection/guard-eval/completion-synthesis/RTC-step/active-config-computation = ∅ (non-comment, EVERY hit read by the auditor — disclaimer/comment/error-label projection/StepRecord-field read/the documented breakpoint predicate/the pure IR-JSON edge-id<->stableId bridge). EVERY fsm/simulate op handler = exactly one shipped Interpreter call + marshalling (read by the auditor). ZERO simulator facts computed locally.",
    "differential_battery_rerun_by_auditor": "The W4b auditor re-ran the host differential 3x deterministic (7 passed; 0 failed; 2 ignored), re-derived the catalogue-integrity contract from source AND the differential's own --nocapture output (CORPUS=14, BYTE_EQUAL=11, BEHAVIOURALLY_EQUIVALENT_JUSTIFIED=3, KNOWN_DIVERGENT=&[]; partition 11+3+0=14 dynamically asserted in-test; the never-game-guard proof bodies READ + non-vacuous), re-ran the W1 fsm/simulate LSP acceptance 7 passed/0 failed x2, ran the ExtHost suite under xvfb-run 47 passing/exit 0 incl. the three keystone-critical W2/W3/§W4 byte-match-vs-oracle tests, and INDEPENDENTLY HAND-RAN `fsm test` on the .trace.json form confirming a real `pass` (not `skip`) — the §W4 replay non-vacuity (a .trace.yaml would be silently undiscovered → vacuous; the ae774dd->8092d3a self-correction is real and load-bearing). This closeout re-ran the host differential (7 passed; 0 failed; 2 ignored) + the W1 LSP acceptance (7 passed; 0 failed) at the working tree.",
    "p2_p3_carry_list": "DF-01 (the 'v1.3 core untouched' framing vs the one disclosed behaviour-preserving diagramWebview.ts refactor — P2 accept-with-rationale, the precise framing FOLDED into the gate doc prose; the v1.3 bundled output byte-untouched, diagram.test.ts the pre/post-identity bar), DF-02 (the DBGUX .trace.yaml prose vs the shipped .trace.json — P3, the implementation correct + disclosed in-code, the design-doc prose deferred to the owner-reserved v1.6 doc-honesty pass cross-ref #103; the frozen design doc NOT edited here), DF-03 (3 pre-existing devDep-only transitive npm vulns esbuild/serialize-javascript/mocha — P2 accept-with-written-rationale x3, byte-identical to the factory-tag baseline, epic added 0, → the v1.6 FE-hygiene batch), DF-04 (the .contains codegen-c conformance oracle — P2 accept-tracked, the #123-class deferred tail owned by the G7 wave, carried from the factory tag, NOT regressed), DF-05 (the F-1-adjacent-site resolve_simple_literal — P3 informational non-defect, no action). All non-gating; recorded in GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §6.1 + the §9 DRAFT Doc 00 §11 rows (the orchestrator appends §11.87+ at W4d).",
    "attention": false
  },

  "debug_interface_surface": {
    "$comment": "NEW Phase-8.0 editor+LSP surface — the interactive debug interface as just another frontend of the one shipped fsm_simulator oracle. W1 = the single keystone-violable Rust/LSP wave (the fsm/simulate* transport + the one sanctioned non-stepping helper); W2-W4a = TS, holding the no-fork invariant trivially.",
    "w0_wave_plan": "Doc 33 docs/33-Debug-Interface-Wave-Plan.md — the accepted plan-of-record (the Doc 28/29/30/31/32 epic-kickoff precedent; next free integer = 33, verified). Formalised the KEYSTONE-IN-DEBUG binding gate row (§2), OWNER-1..OWNER-4 with non-blocking safe parked defaults (§4), the carried CF-1/CF-2 (§5), the §11.30 tag-gate criteria (§7), the deferred signal epic (§8), the design-decision DRAFT §11 rows (§9 — separate from this closeout's implementation DRAFT rows). GT-1..GT-15 re-derived the keystone surface from source + flagged a 2-line DBGUX citation drift (corrected in GT-7; the frozen DBGUX doc NOT edited). FROZEN — not annotated by this closeout.",
    "w1_fsm_simulate_lsp_layer": "1de83b0 — the fsm/simulate LSP custom request (crates/fsm-lsp/src/capabilities/simulate.rs +693, a pure Interpreter-marshalling transport over the proven v1.5 W-A2 LspService::build().custom_method stdio seam — NO new daemon/port/socket, D-2 confirmed) + the single sanctioned non-stepping Interpreter::set_context_field helper (crates/fsm-simulator/src/interpreter.rs +25, 0 StepRecord by construction) + the +945 in-process tower-lsp differential-oracle acceptance suite. The single Rust-layer enabling change = ONE internal fsm-lsp->fsm-simulator path-dep edge (no new crate, no new external [[package]]). THE single keystone-violable wave; W4b verdict KEYSTONE-INTACT.",
    "w2_debug_webview_panel": "032438c — editors/vscode/src/debug/{debugPanel,index,simulateClient}.ts + webview/debugWebview.ts: the debug WebviewPanel — the v1.3 statechart REUSED VERBATIM (renderModel imported, NOT reimplemented — no second renderer), active-config overlay + transport rail + Inject panel (proximity-adjacent typed-payload form) + advance-clock + context inspector with the Δ column + the StepRecord timeline, all driven through W1's fsm/simulate over the EXISTING v1.5 W-A2 LanguageClient; the v1.3 empty/honest states inherited (stale-banner reused VERBATIM, transport disabled-with-inline-reason). The one v1.3-source change (diagramWebview.ts) a disclosed BEHAVIOUR-PRESERVING refactor (additive named exports + a getVsCodeApi() singleton + an #svg&&#banner standalone-guard) — the v1.3 BUNDLED OUTPUT byte-untouched (esbuild per-entrypoint); diagram.test.ts the pre/post-identity bar (W4c DF-01). NO breakpoints/time-travel/capture yet.",
    "w3_breakpoints_time_travel": "b79e052/ec903e5 — breakpoint glyphs on diagram nodes/edges (click-to-set) + the client-side StepRecord-PREDICATE evaluator evaluateBreakpoint (debugPanel.ts:188-209, a pure filter over the three oracle-emitted StepRecord fields — step.enteredStates/exitedStates/transitionTaken?.stableId — NEVER a guard/transition re-eval, W4b §2(b)) + auto-pause-at-breakpoint via restore-the-pre-step-snapshot + the timeline as the time-travel surface (◀ rewind to #N proximity row action) + the UI-paced single-step reveal over W1's Vec<StepRecord>. Time-travel = Interpreter::snapshot/restore ONLY (no client-side state reconstruction — highlightFromOracle SELECTS which oracle field to paint, never derives a configuration, W4b §2(c)). NO capture yet.",
    "w4a_capture_round_trip": "ae774dd->8092d3a — the capture->.trace.json round-trip: the TraceCommands the author injected + the W1 oracle's actual Vec<StepRecord> -> the shipped write_trace_yaml (content is JSON despite the historical name) -> a .trace.json fixture; fsm test re-runs it via the SAME execute_trace ⇒ byte-identical by construction (inverts F6 — recorded-from-the-oracle, not guessed). The capture op (simulate.rs:578-660) = one write_trace_yaml + serde, ZERO Interpreter/semantics; 'captured ✓' only after the file exists on disk (the no-premature-success bar); REJECTS empty `expected` at the source ('fsm test would report nothing-to-verify — not a pass'). The .trace.json-not-.trace.yaml self-correction is REAL AND LOAD-BEARING — fsm test's collect_traces (cmd/test.rs:159, byte-identical to the anchor) globs .trace/.trace.json; a .trace.yaml would be SILENTLY SKIPPED → a vacuous pass. W4b §4 independently HAND-CONFIRMED the §W4 replay non-vacuous (a real `pass`, not `skip`, on the .trace.json form; .trace.yaml silently undiscovered).",
    "gating_128_f1_f2": "F-1 (const-resolver) unified a divergent SECOND resolver (the lowerer's eval_i64 omitted EXPR_NAME_REF while the check resolved it ⇒ `after CONST ms` passed fsm check but the lowerer silently dropped the timer — a #110-class silent miscompile) into ONE shared util::eval_const_expr_value+util::file_const_table consumed by BOTH lowerer & check (the keystone-pattern applied to const-folding), with a hard FSM-E0411 for the deferred form (never silent). F-2 (queue-config) fixed lower_queue matching the KEY not the RHS + codegen reading the CLI default not the IR queue (codegen<->sim silently diverged) via a typed ConfigEntry::value() (single SoT shared with the analyzer check) + CodegenConfig::resolve_queue (explicit per-field precedence + a note-on-shadow, never silent), with a hard FSM-E0412 for a non-power-of-2 in-source capacity; the silent-misconfig class is CLOSED END-TO-END (W4c DD-11 gcc-compile-and-RUN analyzer 7/7 + codegen-c 4/4, both Switch & Table). The `defer` doc-drift fold replaced the stale 'v1.0 limitation / FSM-E0903' §9.4 paragraph in docs/04-DSL-Specification.md with honest v1.1-shipped status (a genuine doc-honesty correction). Genuine #110-class corrections, NOT band-aids — W4c Lens-2.",
    "attention": false
  },

  "diagnostic_codes": {
    "live_count": 75,
    "factory_baseline": 73,
    "delta_vs_factory": 2,
    "phase8_0_change": "+2 — the #128 F-1/F-2 gating fixes added EXACTLY TWO hard rejecting diagnostics: FSM-E0411 ('timer duration must be a compile-time constant; runtime-variable is post-v1.0' — the F-1 deferred-form rejection, replacing a silent timer-drop miscompile) + FSM-E0412 (non-power-of-2 in-source queue capacity — the F-2 deferred-form rejection, replacing a silent queue-misconfig). NOT a silent code-count drift — each is a never-a-silent-drop hard diagnostic for a genuinely-deferred construct (the divergent-second-resolver de-duplication, the keystone-pattern applied to const-folding/queue-config). The G7 count-discipline lock is GREEN + non-vacuous (W4c DD-12/DD-14).",
    "ci_lock": "all_codes_matches_expected_count test asserts `const EXPECTED: usize = 75` (crates/fsm-diagnostics/src/lib.rs:851).",
    "method": "`git grep -n 'const EXPECTED' 8092d3a -- crates/fsm-diagnostics/src/lib.rs` => crates/fsm-diagnostics/src/lib.rs:851: const EXPECTED: usize = 75; (independently re-derived at the gate-doc commit, not echoed). +2 vs the frozen factory figure 73 — correctly attributed to FSM-E0411/FSM-E0412 (W4c DD-14 corroborates 73->75 +E0411/+E0412 independently).",
    "flag_resolution": "RESOLVED-AND-ATTRIBUTED — the count MOVED 73->75, by exactly the two intended F-1/F-2 rejecting diagnostics (NOT a silent drift; the precondition — the +2 are the two new codes, no other code added/retired — was verified, not assumed). G7 lock GREEN.",
    "attention": false
  },

  "conformance": {
    "manifest_fixtures_total": 55,
    "manifest_by_category": { "parser": 7, "validator": 13, "semantic": 28, "codegen-c": 4, "formatter": 3 },
    "factory_baseline_fixtures": 53,
    "delta_vs_factory": 2,
    "added_phase8_0": "+2 — VAL-NEG-009/010 for the new F-1/F-2 FSM-E0411/FSM-E0412 (the G7 one-fixture-per-new-code discipline — real fixtures, no allowlist dodge; the lock GREEN + non-vacuous, W4c DD-12). A positive quality delta, correctly attributed.",
    "method": "`git show 8092d3a:tests/conformance/MANIFEST.json` parsed (sum(len(c['fixtures']) for c in categories)) => 55 {parser:7, validator:13, semantic:28, codegen-c:4, formatter:3}; `git show fbae38b:tests/conformance/MANIFEST.json` => 53 {parser:7, validator:11, semantic:28, codegen-c:4, formatter:3} (independently re-derived at the gate-doc commit; W4c DD-25 corroborates 53->55 independently). The +2 are the two validator fixtures for E0411/E0412.",
    "deferred_tail": "The .contains codegen-c conformance oracle (the 4 codegen-c fixtures) remains a tracked deferred-tail — the #123-class item flagged P1 in docs/AUDIT_RELIABILITY_STAGE_GATE_2026_05_18.md:306 (frozen, factory-epic), owned by the G7 wave for behavioural conversion, NOT regressed by the debug epic, NOT a tag-blocker (the W1/W2 differential + the W3 lock are the binding behavioural gate in the interim — W4c DF-04, accept-tracked).",
    "attention": false
  },

  "safety": {
    "crate_roots_with_forbid_unsafe": 12,
    "crates_forbid_unsafe": 11,
    "delta_vs_factory": "0 roots / 0 crates — W1 added an INTERNAL fsm-lsp->fsm-simulator path-dep edge (fsm-simulator already #![forbid(unsafe_code)] since v1.4) + the one set_context_field helper into the existing fsm-simulator; NO new crate root.",
    "root_files": [
      "crates/fsm-analyzer/src/lib.rs",
      "crates/fsm-cli/src/main.rs",
      "crates/fsm-codegen-c/src/lib.rs",
      "crates/fsm-diagnostics/src/lib.rs",
      "crates/fsm-formatter/src/lib.rs",
      "crates/fsm-ir/src/lib.rs",
      "crates/fsm-lexer/src/lib.rs",
      "crates/fsm-lsp/src/lib.rs",
      "crates/fsm-lsp/src/main.rs",
      "crates/fsm-parser/src/lib.rs",
      "crates/fsm-simulator/src/lib.rs",
      "crates/fsm-verify/src/lib.rs"
    ],
    "method": "`git grep -l '#![forbid(unsafe_code)]' 8092d3a -- 'crates/*/src/*.rs'` => 12 root files; the distinct crate set (parse crates/<crate>/src/...) => 11 crates (fsm-lsp contributes 2 roots lib.rs+main.rs across 1 crate => 12 roots / 11 crates). Same at fbae38b => 12 files / 11 crates (independently re-derived at the gate-doc commit, not echoed). UNCHANGED 12/11.",
    "disclosed_judgment_call": "The W4c audit phrases this as '12 at both' (counting root FILES; DD-04); the v1.5/factory canonical convention is 'roots/crates' (fsm-lsp = lib.rs+main.rs = 2 roots / 1 crate => 12 roots / 11 crates). The SAME source fact, stated in the canonical roots/crates form here (the v1.5/factory metrics shape). NOT a discrepancy — a counting-granularity convention disclosed for the orchestrator's W4d re-derivation.",
    "cargo_lock_new_external_check": "`git diff fbae38b..8092d3a -- Cargo.lock | grep -E '^\\+(name|source) ='` => none (no new [[package]]) — the precondition (W1 added no new crate, only an internal path-dep edge) was VERIFIED, not assumed.",
    "unsafe_blocks": 0,
    "flag_resolution": "RESOLVED — the Doc 33 §7 expectation 'unchanged at 12/11 because W1 adds a dep edge + a helper into the already-forbid-unsafe fsm-simulator, not a crate' is CONFIRMED from source at the gate-doc commit (W4c DD-04 independently re-derived 12 files at both; the precondition — no new [[package]] — verified).",
    "interpretation": "The forbid-unsafe invariant holds unchanged. Phase-8.0's only Rust-layer enabling change is the internal fsm-lsp->fsm-simulator path-dep edge + the new fsm/simulate capability + the one set_context_field helper; fsm-simulator was already #![forbid(unsafe_code)] (its lib.rs is the 12th root, present since v1.4). No new unsafe-capable root entered.",
    "attention": false
  },

  "loc": {
    "rust_src_excl_tests_base_factory_frozen": "see the prior frozen attestations (a frozen attestation is not rewritten)",
    "phase8_0_rust_delta": "28 files / +3260 / -214 in crates/ (29 / +3261 / -214 incl. Cargo.toml Cargo.lock). The semantics-bearing src delta is the W1 simulate.rs +693 (a PURE TRANSPORT FRONTEND — zero simulator facts) + the one set_context_field helper +25 + the gating F-1/F-2 fixes (genuine #110-class corrections); the rest is wiring + the +945 acceptance test + the +476 F-2 gcc-RUN harness + the additive +64 corpus expansion. NO new crate (contrast v1.4's +1695 NEW fsm-verify src crate / the factory +8192 epic).",
    "phase8_0_ts_delta_scope": "The Phase-8.0 surface DOES touch editors/vscode/** (W2/W3/W4 the debug webview src/debug/** + 1 new ExtHost test debugPanel.test.ts + the one disclosed behaviour-preserving diagramWebview.ts refactor) — this is why the JS lane is a BINDING tag gate (§4.3), like v1.5/factory, unlike v1.4. The whole-delta `git diff --shortstat fbae38b..8092d3a` = 57 files / +10045 / -243 (incl. docs/editors/tests — W4c DD-01); the `-- crates/`-scoped Rust delta is the 28/+3260/-214 stated above (both correct at their respective scopes — the git-diff-scope distinction, NOT a discrepancy).",
    "method": "Rust delta = `git diff --shortstat fbae38b..8092d3a -- crates/` (28 files / +3260 / -214) + Cargo.lock +1; whole delta = `git diff --shortstat fbae38b..8092d3a` (57 files / +10045 / -243, W4c DD-01). The prior frozen base attestations are not rewritten.",
    "interpretation": "Phase-8.0's headline LOC story is the W1 fsm/simulate* LSP transport + its acceptance suite (a pure-marshalling Rust frontend, the single keystone-violable surface) + the gating #128 F-1/F-2 codegen-correctness fixes that landed on the arc, with the W2-W4 TS webview surface on top. A focused interactive-debug epic that surfaces the shipped fsm_simulator oracle in the editor WITHOUT forking it (the keystone — confirmed source-derived by the W4b audit, KEYSTONE-INTACT)."
  },

  "tests": {
    "$comment": "The full cold-quad test total is the contingent gating step (GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §4.4) — NOT asserted as a completed number here (no run-at-X transcript is fabricated). Phase-8.0 ADDS test surface: crates/fsm-lsp/tests/simulate_lsp_acceptance.rs (+945, the in-process tower-lsp differential-oracle suite, 7 tests) + editors/vscode/src/test/suite/debugPanel.test.ts (6 ExtHost E2Es for W2/W3/§W4) + the F-2 crates/fsm-codegen-c/tests/queue_config_runs.rs (+476 the gcc-compile-and-RUN harness, 4 tests) + crates/fsm-analyzer/tests/queue_config.rs (+160, 7 tests) + the +2 VAL-NEG-009/010 conformance fixtures + the +2 corpus members in codegen_equivalence_smoke.rs.",
    "w4b_auditor_runs": "W4b (audited 8092d3a): host differential 3x deterministic 7 passed/0 failed/2 ignored; W1 fsm/simulate LSP acceptance 7 passed/0 failed x2; ExtHost suite under xvfb-run 47 passing/exit 0 incl. the three keystone-critical W2/W3/§W4 byte-match-vs-oracle tests + the honest-degradation guards; the §W4 .trace.json replay non-vacuity independently hand-confirmed (a real `pass`, not `skip`).",
    "w4c_auditor_runs": "W4c (audited 8092d3a): full workspace `cargo test --workspace` 911 passed/0 failed/exit 0; host differential 7 passed/0 failed/2 ignored; W1 LSP acceptance 7 passed/0 failed; the F-2 silent-misconfig class closed end-to-end `cargo test -p fsm-analyzer --test queue_config -p fsm-codegen-c --test queue_config_runs` => analyzer 7/7 + gcc-RUN 4/4; the G7 lock `cargo test -p fsm-cli --test conformance_code_coverage_lock` 4/4; `cargo +stable audit` 207 deps exit 0 0-vuln/0-warn; `npm audit --package-lock-only` exactly the 3 factory-baseline devDep findings.",
    "this_closeout_reruns": "host differential `cargo test -p fsm-simulator --test codegen_equivalence_smoke` => test result: ok. 7 passed; 0 failed; 2 ignored; W1 LSP acceptance `cargo test -p fsm-lsp --test simulate_lsp_acceptance` => test result: ok. 7 passed; 0 failed; 0 ignored. At the working tree (toolchain re-asserted 1.75 from the worktree). De-risking predicate, NOT a substitute for the canonical cold-quad at X.",
    "vscode_extension_host_suite": "47 it()/test() across the suite (commands 9 + debugPanel 6 + diagram 4 + extension 4 + grammar 7 + tree 6 + verify 5 + verifyLive 6 = 47; corroborated by the W4b auditor's own xvfb-run -a npm test = 47 passing, exit 0 at 8092d3a). The debug epic's net-new ExtHost surface is debugPanel.test.ts (the 6 W2/W3/§W4 byte-match-vs-oracle E2Es); diagram.test.ts (4) is the v1.3-path-identity DF-01 pre/post-identity bar. CI-runner-only on this disk-tight box (node_modules absent + an Electron download required — the factory on-target-lane honest-skip-by-design precedent; W4c source-verified its structure, NOT a finding); it must run green in the extension-host CI lane at X (the binding JS lane, §4.3).",
    "new_phase8_0_test_surface": "Rust: crates/fsm-lsp/tests/simulate_lsp_acceptance.rs (in-process tower-lsp differential oracle — the fsm/simulate StepRecord stream byte-equals fsm test's execute_trace + the set_context_field-emits-zero-StepRecord contract + the no-fake-clean-end bar; 7 tests) + crates/fsm-codegen-c/tests/queue_config_runs.rs (the F-2 gcc-compile-and-RUN harness; 4 tests) + crates/fsm-analyzer/tests/queue_config.rs (7 tests). TS: editors/vscode/src/test/suite/debugPanel.test.ts (the W2/W3/§W4 ExtHost E2Es driven end-to-end through a real headless host vs the real fsm-binary oracle — the panel byte-matches the W1 fsm/simulate response, a transition breakpoint pauses PRE-step + rewind both byte-identical to an independent W1 snapshot/restore, capture->.trace.json replays GREEN via fsm test + no premature ✓; 6 tests).",
    "method": "Cold-quad total is the contingent step (NOT fabricated); the new-surface inventory = `git diff --name-only fbae38b..8092d3a -- 'crates/*/tests/' 'editors/vscode/src/test/'` filtered to the Phase-8.0 acceptance files. The W4b/W4c run figures are from the frozen docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md + docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md (re-read, not echoed); this closeout's re-runs are this closeout's own commands at the working tree.",
    "attention": false
  },

  "decisions_ledger": {
    "section_11_total_rows": 86,
    "ledger_head_before_this_batch": "§11.86 (the factory-W6c batched-closeout row — independently confirmed from the file; the Phase-8.0 rows are NOT pre-allocated per Doc 33's header numbering refinement + the v1.4 L6 lesson)",
    "phase8_0_impl_rows": "REPORTED as §9 DRAFT rows (§11.DRAFT-A..E) in GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §9 — the orchestrator independently RE-DERIVES, renumbers (§11.87+ at W4d, batched), and appends in ONE controlled commit (the comm -23 append-only discipline). THIS CLOSEOUT DOES NOT EDIT Doc 00 §11 (the hard BOUNDARY).",
    "method": "`grep -cE '^\\| §11\\.[0-9]+ ' docs/00-Decisions-And-Reconciliation.md` => 86; the highest actual §11.NN row => §11.86 (read from the file, not echoed). The Doc 33 §9 design-decision DRAFT rows (§11.DRAFT-A..D — the owner D-1/D-2/D-3 + Doc 33-authored) were reported by Doc 33's own kickoff wave for the orchestrator to append (they would be §11.87-§11.90-class); whether already appended is the orchestrator's to re-derive at W4d. This closeout's §9 DRAFT rows are the CLOSEOUT/IMPLEMENTATION rows (the epic SHIPPED + W4b/W4c verdicts + the §11.30 gate-doc-commit), distinct from Doc 33 §9's design-decision rows; the orchestrator reconciles both sets against the live ledger head at W4d.",
    "phase8_0_draft_rows_summary": "§11.DRAFT-A W1 the fsm/simulate* LSP transport + the single sanctioned non-stepping set_context_field helper SHIPPED + the KEYSTONE-IN-DEBUG binding tag-gate row PASSES via W4b (KEYSTONE-INTACT, the no-fork re-derived from source not echoed); §11.DRAFT-B W2/W3/W4a the debug WebviewPanel + breakpoints/time-travel + the capture->.trace.json round-trip SHIPPED (NO semantics, the v1.3 renderer reused VERBATIM, the §W4 replay non-vacuous, the .trace.json-not-.trace.yaml correction load-bearing); §11.DRAFT-C the gating #128 F-1/F-2 codegen-correctness fixes + the `defer` doc-drift fold (genuine #110-class corrections, EXPECTED 73->75 correctly attributed); §11.DRAFT-D W4c the independent pre-tag four-lens (TAG-CLEAR 0 P0/0 P1/3 P2/2 P3, FROZEN; the Rust SCA empirically run, the byte-identity property empirically reproduced, no new network attack surface; DF-01..DF-05 none gating); §11.DRAFT-E the W4d batched closeout = the §11.30 gate-doc commit (every pinned numeric re-derived at X not echoed + the W4c carry-list + the Doc 33 §5 CF-1/CF-2 + the carried owner-escalations folded in ONE batch; NO code-tree touch [no F-05-equivalent]; the cold-quad+binding-JS-lane the contingent gating step, no fabricated transcript; the milestone tag a checkpoint/<name> NOT a vX.Y.0)."
  },

  "gate_status": {
    "cleanly_met": 7,
    "acceptable_partial": 2,
    "blocked": 0,
    "detail": "GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §1: G1-G6, G8 clean (G2/G3/G6 STRENGTHENED by the F-1/F-2 fixes + the W1 differential). The cold quad is the contingent gating step at the gate-doc commit X (WITH the MANDATORY NOTE-1 `cargo clean -p` of all 11 first-party crates Step 0 — binding v1.4+, GATE §4.2) PLUS the BINDING JS lane at X (npm audit triaged + the Extension-Host suite green incl. debugPanel.test.ts + the v1.3-path-identity diagram.test.ts — binding for Phase-8.0 because the surface DOES touch editors/vscode/**, GATE §4.3). G7 (formal conformance 55 fixtures incl. the F-1/F-2 +2, not all 75 live codes; the .contains codegen-c #123-class tail owned by G7) + G9 (remote CI never run + the factory lanes + the debug ExtHost tests remote-unrun + the G9-gated 5-platform tail) are the same acceptable-partial posture as v1.0-v1.5/factory (G7 IMPROVED — the F-1/F-2 +2 codes + 2 fixtures, the lock GREEN). 0 open P0. The W4b source-derived keystone phase-audit returned KEYSTONE-INTACT (the binding tag-gate row PASSES); the W4c independent pre-tag four-lens returned TAG-CLEAR (0 P0 / 0 P1 / 3 P2 / 2 P3)."
  },

  "phase_boundary_audits": [
    "docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md (W4b KEYSTONE, branch phase8.0/w4b-keystone-audit, audited 8092d3a, KEYSTONE-INTACT — the no-fork RE-DERIVED FROM SOURCE by the auditor [over the entire epic arc fbae38b..8092d3a the ONLY crates/fsm-simulator/src + cmd/test.rs delta is the one sanctioned non-stepping set_context_field helper; cmd/test.rs NOT in the diff at all blob-SHA-identical; trace.rs/write_trace_yaml/execute_trace + every transition/guard/step/clock/queue/timer/LCA/completion/history module blob-SHA byte-identical; negative-grep ∅ across the whole debug surface non-comment every hit read; every fsm/simulate op handler = exactly one shipped Interpreter call + marshalling] + the differential battery RE-RUN by the auditor [host differential 3x deterministic 7/0;2-ignored; the catalogue-integrity contract 11+3+0=14 re-derived from source AND --nocapture + the never-game-guard proof bodies READ; W1 LSP acceptance 7/0 x2; ExtHost 47 passing/exit 0 incl. the W2/W3/§W4 byte-match-vs-oracle trio] + the §W4 .trace.json replay non-vacuity INDEPENDENTLY HAND-CONFIRMED [a real `pass`, not `skip`; a .trace.yaml silently undiscovered]; 0 P0, the cardinal regression explicitly ABSENT = FROZEN evidence)",
    "docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md (W4c pre-tag four-lens, branch phase8.0/w4c-four-lens-audit, audited 8092d3a, TAG-CLEAR — 0 P0 / 0 P1 / 3 P2 / 2 P3; the v1.1-v1.5-factory §3 standard of 0 P0 AND ≤5 well-scoped P1 met with ZERO P1; delta-scoped fbae38b..8092d3a 57 files/+10045/-243, NOT a keystone re-litigation [W4b's binding job], NOT a whole-codebase re-audit. Lens 1 Arch PASS [the internal fsm-lsp->fsm-simulator path dep, 0 new external [[package]], no inversion, forbid-unsafe 12/12 files unchanged, the v1.3 renderer reused VERBATIM]; Lens 2 Correctness PASS [the F-1/F-2 genuine #110-class corrections, the byte-identity property empirically REPRODUCED simulate_lsp_acceptance 7/7, full workspace 911/0]; Lens 3 Reliability PASS [StepError verbatim, the v1.3 stale-banner reused VERBATIM, the #141 pending-timer surface honestly degraded NOT faked, the ~3.4mb bundle the pre-existing v1.3-ELK-per-entrypoint pattern not an epic regression]; Lens 4 Security/SCA PASS (binding) — cargo +stable audit EMPIRICALLY RUN over 207 deps 0-vuln/0-warn NOT honest-skipped, the epic added ZERO new npm deps [package.json manifest-only, package-lock.json NOT in the delta — derived TWO ways], the 3 pre-existing devDep-only transitive npm vulns byte-identical to the factory-tag accepted-with-rationale set epic-added-0, no new network attack surface — the existing stdio custom_method boundary, no daemon/port/socket; 5 findings DF-01..DF-05 NONE gating = FROZEN evidence)",
    "W4b + W4c applicability at X (re-derived at this closeout, not echoed): both audited 8092d3a (== the W4a HEAD == base main of the closeout worktree phase8.0/w4-closeout, re-confirmed `git rev-parse HEAD` = 8092d3a02a7bd6a6ee9d3dad306580d9b3019a0b). X adds ONLY this gate doc + CHANGELOG + ROADMAP + this metrics JSON — NO crates/.github/ code-tree touch whatsoever (no F-05-equivalent — the W4c DF-01..DF-05 findings are framing/doc-precision/accepted-tracked, none requiring an in-scope code change). So W4b's KEYSTONE-INTACT and W4c's TAG-CLEAR hold byte-for-byte at X by construction. The contingent cold-quad + the binding JS lane at X is the empirical re-confirmation (no quad/tag result fabricated)."
  ],

  "accepted_tracked_debt": {
    "w4c_pre_tag_four_lens_carry_list": {
      "DF_01_v1_3_framing_precision": {
        "item": "DF-01 P2 — the 'v1.3 core untouched' framing vs the one v1.3-source modification (editors/vscode/src/diagram/webview/diagramWebview.ts)",
        "status": "ACCEPTED-WITH-RATIONALE — the precise framing FOLDED into the gate doc prose (§2.3/§3): the v1.3 BUNDLED output (dist/webview/diagramWebview.js) IS byte-untouched (esbuild per-entrypoint); the v1.3 SOURCE was behaviour-preservingly refactored (additive named exports + a getVsCodeApi() singleton + an #svg&&#banner standalone-guard) to enable VERBATIM renderModel reuse with NO fork — diagram.test.ts the pre/post-identity bar (CI-runner-only, re-confirmed in the W4d binding JS lane). Non-gating (a sound behaviour-preserving refactor, not a fork or regression).",
        "source": "docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md DF-01 (FROZEN)"
      },
      "DF_02_dbgux_trace_yaml_prose": {
        "item": "DF-02 P3 — the DBGUX prose says capture writes .trace.yaml but the shipped collect_traces globs .trace/.trace.json + write_trace_yaml emits JSON (the panel correctly writes *.trace.json; a .trace.yaml would be SILENTLY SKIPPED → vacuous)",
        "status": "DEFER-TRACKED -> the owner-reserved v1.6 doc-honesty pass (cross-ref task #103). The implementation is CORRECT (re-derived-from-source, the trap explicitly avoided + disclosed in debugPanel.ts:1313-1318); only the design-doc prose is loosely phrased (the factory-F-02 'loose doc phrasing vs verified source' class). The frozen design doc is design-evidence — NOT edited here. Same class as the GT-7 DBGUX line-drift Doc 33 already records.",
        "source": "docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md DF-02 (FROZEN)"
      },
      "DF_03_3_devdep_npm_vulns": {
        "item": "DF-03 P2 — 3 pre-existing devDep-only transitive npm vulns {esbuild mod, serialize-javascript high, mocha high}",
        "status": "ACCEPT-WITH-WRITTEN-RATIONALE x3 — byte-identical to the factory-tag baseline (the epic's package.json is manifest-contributions-only; editors/vscode/package-lock.json NOT in the 57-file delta — W4c DD-19/DD-20), devDependency/build-time/test-only, NOT in the shipped extension runtime, not exploitable in our build/CI usage, no non-breaking fix, the epic introduced ZERO of them. Does NOT gate the milestone tag (the §11.48/§11.42 standard applied honestly to pre-existing non-shipped devDep transitives — the v1.5/factory-F-03 precedent). -> the v1.6 FE-hygiene batch.",
        "source": "docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md DF-03 / §4.2 (FROZEN)"
      },
      "DF_04_contains_codegen_c_oracle": {
        "item": "DF-04 P2 — the .contains codegen-c conformance oracle (the #123-class deferred tail)",
        "status": "ACCEPT-TRACKED-WITH-RATIONALE — flagged P1 in docs/AUDIT_RELIABILITY_STAGE_GATE_2026_05_18.md:306 (frozen, factory-epic), owned by the G7 wave for behavioural conversion (the 4 codegen-c MANIFEST fixtures the tail); NOT regressed by the debug epic, NOT a tag-blocker (the W1/W2 differential + the W3 lock are the binding behavioural gate in the interim). Carried from the factory tag (factory-F-04), re-stated.",
        "source": "docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md DF-04 (FROZEN)"
      },
      "DF_05_f1_adjacent_site_informational": {
        "item": "DF-05 P3 — crates/fsm-analyzer/src/type_check.rs::resolve_simple_literal (the F-1 adjacent-site)",
        "status": "NON-GATING, INFORMATIONAL — a DISTINCT non-defect const-fold (returns i128 to defer overflow to the FSM-E0206 caller; literal-only by design; check-only, no lowerer counterpart ⇒ provably NOT the F-1 divergent-second-resolver defect); left untouched + characterized in-place; W4c confirmed it is a distinct intentional helper (no lowerer counterpart to drift from), so the leave-and-explain is correct. NO action required (the W1-FU adjacent-site-audit discipline correctly applied; recorded for audit completeness).",
        "source": "docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md DF-05 (FROZEN)"
      }
    },
    "doc33_carried_zero_legacy_findings_owner4": {
      "CF_1_avr_harvard_rodata_table_hazard": {
        "item": "CF-1 — the AVR Harvard .rodata + TABLE-dispatch UNTESTED CORRECTNESS HAZARD (TARGET_CHIP_MATRIX §1.3/§2.3)",
        "status": "RECORDED-NOT-FIXED — AVR is the only Harvard-architecture target in scope; a plain .rodata pointer deref reads RAM not flash ⇒ wrong data; the family-agnostic codegen does not emit __flash/PROGMEM+pgm_read_*; large machines auto-selecting TABLE (>64 states) are the exposed case; only ARM Cortex-M3 is on-target-tested (the factory W2 hard gate). A CI-runner-only avr-gcc link+run candidate for the factory-reliability/CI epic's on-target-matrix extension — a CODEGEN-CORRECTNESS item, NOT a debug-interface item (must not be auto-scoped here). W4c 'Tracked-deferrals' #134 confirmed tracked-not-dropped.",
        "source": "docs/33-Debug-Interface-Wave-Plan.md §5 CF-1 / OWNER-4"
      },
      "CF_2_doc16_hal_symbol_staleness": {
        "item": "CF-2 — Doc 16 §2-§6 staleness vs the emitted HAL symbols (TARGET_CHIP_MATRIX threading finding (b))",
        "status": "DEFER-TRACKED -> the owner-reserved v1.6 doc-honesty pass (cross-ref task #103). Doc 16 documents the older _ms/_fail(file,line,msg) shapes; the codegen emits fsm_hal_clock_now_ms/fsm_hal_assert(bool,msg) — a docs-only first-contact-surface drift. NOT a code change (the emitted symbols are the correct shipped contract — the doc drifted, the v1.4 §3.G Doc-18 finding shape) and NOT a debug-interface item. Folds into the same v1.5 Doc 31 §6 H6 / ASSESSMENT PART B doc-honesty batch (README/Doc-07/Doc-14) that must precede/accompany D1. W4c 'Tracked-deferrals' #103 confirmed tracked-not-dropped.",
        "source": "docs/33-Debug-Interface-Wave-Plan.md §5 CF-2 / OWNER-4"
      }
    },
    "carried_owner_escalations": [
      "OWNER-1 the release-numbering call for this minor (the checkpoint/2026-05-19-debug-interface-not-vX.Y.0 parked default; does NOT consume v1.6 owner-reserved — D-3; the orchestrator/owner assigns the final number; the factory-epic checkpoint/<name>-not-v1.6.0 precedent Doc 00 §11.80) — re-stated, not resolved",
      "OWNER-2/G9 push of LOCAL v1.0.0-v1.5.0 tags + ALL checkpoint/* anchors (incl. checkpoint/factory-reliability-ci-2026-05-18 + this epic's checkpoint/2026-05-19-debug-interface) + commits + the CI matrix/SCA/the 4 factory lanes/the debug ExtHost tests never run on a remote runner (the JS lane is now a binding LOCAL gate §4.3 but remote-unrun; G9 stays the owner's publication switch decoupled from progress AND quality — quality is local every wave) — re-stated, not resolved",
      "OWNER-3 the deferred full signal-primitive epic GO (D-1 decided the SHAPE — the full first-class signal primitive poll/interrupt/edge/debounce feature signals-gated append-only keywords cohesively retiring the #127 AUDIT-DETPRIM edge/level/debounce GAPs; D-3 sequenced it AFTER this debug-interface epic AND after the owner-reserved v1.6; its own gated Doc-34-class epic; its Part-2(d) per-family interrupt-model dependency already delivered FSM-DESIGN-CHIPMATRIX §1.2; recorded Doc 33 §8 tracked-not-lost; W4c #139 raise-codegen-gap confirmed folded into this parked epic) — re-stated, not resolved",
      "VSIX packaged-not-published (the v1.5 OWNER-A lineage — Marketplace-VALID + publish-READY, NOT published; no PAT used; Phase-8.0 added only the fsm.openDebug manifest contributions, no extension runtime dep) — re-stated, not resolved",
      "No top-level LICENSE/CONTRIBUTING.md/CODE_OF_CONDUCT.md at the repo root (owner/legal, coupled to publishing; carried from v1.4/v1.5/factory §6.3, the v1.5 OWNER-D lineage now carried to v1.6 with the D1 slip) — re-stated, not resolved",
      "The Certifiability/MISRA-CERT-C codegen-mode arc — PARKED (the factory epic un-parked only the TESTING half of Certifiability/OWNER-C; the MISRA-codegen half stays a named parked ROADMAP-v1.6+ candidate requiring an explicit owner GO) — re-stated, not resolved",
      "The v1.6 D1 landing + the doc-honesty pass that must precede/accompany it (recorded-NOT-scoped into this epic — 'publish-ready' = the factory-reliability proof + this debug interface + the v1.6 D1 landing all land; D1 the slipped v1.5 OWNER-D item slipped-not-cut + the README/Doc-07/Doc-14 + CF-2 Doc-16 + DF-02 DBGUX-prose + the Doc 33 GT-7 DBGUX line-drift + the OSS-files reconcile) — re-stated, not resolved",
      "The 5-platform binary tail (host-only artifact ships; the cross-OS tail blocked-on-G9, no cross toolchains on the box — infra-escalation not grind; explicitly NOT a ci.yml lane in this epic) — re-stated, not resolved",
      "task #17 CI-matrix still owner-blocked (local-only repo, owner controls the remote) — re-stated, not resolved",
      "R7 the mechanised sim≡codegen-equivalence FORMAL proof — the factory epic delivered the EMPIRICAL host+target differential; the fully-mechanised proof remains the named v1.4-stretch/later DEFERRAL, carried unchanged from v1.4/v1.5/factory §6 (NOT folded into this debug epic; named-not-omitted) — re-stated, not resolved",
      "W4c 'Tracked-deferrals' #138 Item-3 queue co-design / D-3 sequencing (Doc 33 §0 D-3/§4 OWNER-1/§7), #141 pending-timer surface (Doc 33 §4 OWNER-2; honestly degraded in debugWebview.ts:598-609, NOT faked), #139 raise codegen gap (folded into the parked OWNER-3 signal epic), #134 AVR CF-1 (Doc 33 §5/OWNER-4 — the factory on-target-matrix candidate), #123 W3-§6 catalog tail (the .contains codegen-c oracle — factory-F-04, owned by G7), #103 v1.6 doc-honesty CF-2 (Doc 33 §5/OWNER-4 — folded into v1.6) — ALL confirmed tracked-not-dropped, none re-opened as a new finding (the §11.49 leave-and-explain / §11.62 re-state-don't-drop discipline)"
    ],
    "flagged_not_fixed_here": "The broader doc-honesty staleness pass (the repo-root README.md / Doc 14 / Doc 07 staleness — ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md PART B; + CF-2 Doc 16 §2-§6 + DF-02 DBGUX .trace.yaml-prose + the Doc 33 GT-7 DBGUX line-drift) is a SEPARATE already-identified backlog item (Doc 31 §6 H6 / Doc 32 §7-tweak-5 / Doc 33 §5), to be fixed by the doc-honesty batch that precedes/accompanies the deferred v1.6 D1. FLAGGED in GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §6.4, NOT fixed in this epic. The in-repo docs/ROADMAP.md 'See also' stale pointer was ALREADY reconciled at the v1.5 closeout (GATE_VERIFICATION_v1_5.md §6.4 — independently re-confirmed: docs/ROADMAP.md:310 carries the 'Reconciled 2026-05-17' note); Phase-8.0's ROADMAP edit is the debug-epic retrospective + the milestone-tag record only (it does NOT re-touch the already-fixed pointer)."
  },

  "security": {
    "rust_known_vulns": 0,
    "rust_note": "W4c Lens-4 (binding, Doc 00 §11.48/§11.42): `cargo +stable audit` (v0.22.1, present — NOT honest-skipped) over 207 crate dependencies = 0 vulnerabilities / 0 warnings (exit 0, 1093 advisories loaded). The new fsm-lsp->fsm-simulator Cargo edge adds 0 new external crates (an internal path dep already in the lock graph — `git diff fbae38b..8092d3a -- Cargo.lock | grep -E '^\\+(name|source) ='` => none). The dependency surface is unchanged; the pinned-1.75 CVSS-4.0-DB-parse caveat sidestepped via +stable. The SCA cargo audit ci.yml job is byte-unchanged + still unexercised on a remote runner (G9, carried). Note for the W4d cold-quad: `cargo +stable audit` works on this box (it ran clean for W4c) — re-run it at X per the canonical cold-quad sca step; no honest-skip-pending-note needed for the Rust SCA (the tool is present).",
    "node_audit": "BINDING for Phase-8.0 (like v1.5/factory, unlike v1.4 which touched no editors/) — the Phase-8.0 surface DOES touch editors/vscode/** (W2/W3/W4 the debug webview + 1 new ExtHost test debugPanel.test.ts + the one disclosed behaviour-preserving diagramWebview.ts refactor). The debug epic added ZERO new npm deps (editors/vscode/package.json change is manifest-contributions-only — the fsm.openDebug command/keybinding/menus; editors/vscode/package-lock.json is NOT in the 57-file delta — W4c DD-19/DD-20 independently derived TWO ways), so the npm vuln set is byte-identical to the factory-tag baseline by construction: the 3 pre-existing devDep-only transitive findings {esbuild mod, serialize-javascript high, mocha high} — accept-with-written-rationale x3 (build-time/test-only, NOT in the shipped extension runtime, not exploitable in our usage, no non-breaking fix, epic-added-0). The JS lane (npm audit triaged + the Extension-Host suite green incl. debugPanel.test.ts + the v1.3-path-identity diagram.test.ts) is a BINDING gate at the gate-doc commit X (GATE §4.3) — described there as the contingent gating step (NOT a fabricated transcript; it has not yet run at X). De-risking predicate: the W4b auditor's own xvfb-run -a npm test = 47 passing, exit 0 at 8092d3a (W4c source-verified the ExtHost lane's structure but did NOT run it locally — CI-runner-only by the disk-tight infra constraint, the factory on-target-lane honest-skip-by-design precedent).",
    "no_new_network_attack_surface": "OWNER D-2 'attack surface removed by construction' CONFIRMED (W4c §4.3, source-verified not echoed): the debug surface rides the EXACT existing v1.5 W-A2 stdio custom_method seam (crates/fsm-lsp/src/lib.rs LspService::build(Backend::new).custom_method('fsm/verify',…).custom_method('fsm/simulate',…).finish() over tokio::io::stdin()/stdout() — NO TcpListener/bind/serve-on-a-port, NO standalone daemon, NO new socket) + the TS side's client.sendRequest('fsm/simulate', params) over the existing LanguageClient (NO createServer/net./WebSocket/child_process-spawn in the shipped path). The only net/socket/process in the 57-file delta is TEST-ONLY (Command::new('gcc') in tests/queue_config_runs.rs + tests/codegen_equivalence_smoke.rs + execFileSync in debugPanel.test.ts invoking the shipped fsm test). The Doc 13 standalone WS-daemon (DBGUX §8 fork-1 option B) is the documented not-built alternative (a second long-lived process speaking a second protocol = pure surface+security cost for an editor-bound use case; D-2 removes that attack surface by construction).",
    "verification_surface_soundness": "The debug surface honours the KEYSTONE-IN-DEBUG invariant (Doc 33 §2/§7) — every debug UI verb is EXACTLY ONE shipped fsm_simulator::Interpreter call marshalled over the W1 fsm/simulate* LSP custom request; breakpoints are a PREDICATE over the StepRecord fields the oracle emits, NEVER a guard/transition re-eval; time-travel is Interpreter::snapshot/restore; capture round-trips the SAME write_trace_yaml/execute_trace fsm test uses ⇒ byte-identical by construction. The W4b source-derived audit independently re-confirmed this from source (the oracle + the cmd/test.rs execute_trace seam + write_trace_yaml byte-untouched across the whole epic arc save the one sanctioned non-stepping helper; negative-grep ∅ across the whole debug surface every hit read; the host differential + the W1 LSP acceptance + the 47-test ExtHost suite re-run by the auditor; the §W4 .trace.json replay independently hand-confirmed non-vacuous). KEYSTONE-INTACT."
  },

  "deltas_vs_prev": {
    "_prev_milestone": "the Factory-Reliability/CI milestone (2026-05-18, fbae38b == checkpoint/factory-reliability-ci-2026-05-18^{commit}; the prior frozen attestations are NOT overwritten)",
    "rust_workspace": {
      "delta": "MODERATE ADDITIVE BY DESIGN: 28 files / +3260 / -214 vs the factory checkpoint fbae38b (-- crates/; 29 / +3261 / -214 incl. Cargo.toml Cargo.lock), the W1 fsm/simulate* LSP transport (simulate.rs +693 pure-marshalling + the +945 acceptance suite) + the one sanctioned non-stepping set_context_field helper (+25, 0 StepRecord) + the gating #128 F-1/F-2 codegen-correctness fixes + the additive test-corpus expansion (+64/-4); 0 new external deps; rust-toolchain.toml byte-unchanged; the differential oracle (crates/fsm-simulator/src + cmd/test.rs) byte-untouched save the one sanctioned helper (W4b §1: cmd/test.rs blob-SHA-identical, NOT in the diff at all).",
      "attention": false,
      "note": "`git diff --shortstat fbae38b..8092d3a -- crates/` = 28 files / +3260 / -214 — a MODERATE ADDITIVE delta, NOT empty and NOT zero. The Phase-8.0-shaped instance of the release-record-symmetry discipline (Doc 33 §7): the delta is easy to mis-attribute (W2-W4 are TS — only W1 + the gating fixes are Rust) — it is stated as EXACTLY what it is, NEITHER under-stated (the v1.3 cardinal 'zero Rust delta' error) NOR over-stated (the v1.4 +7398 NEW-crate verification-core epic; the factory +8192 epic). 0 new external deps (`... | grep -E '^\\+(name|source) ='` => none; no new [[package]] — the only Cargo.lock change is the INTERNAL fsm-lsp->fsm-simulator path-dep edge). The differential oracle is byte-untouched (the keystone holds — re-derived from source by the W4b audit, KEYSTONE-INTACT). NOT echoed from W4c's whole-delta `57 files / +10045 / -243` (a different scope incl. docs/editors/tests — the `-- crates/`-scoped Rust delta is the 28/+3260/-214 here; both correct at their respective scopes). Independently re-derived this snapshot at the gate-doc commit (the working tree == X has NO code-tree delta vs 8092d3a)."
    },
    "vscode_surface_new": {
      "factory": "the Phase-6.0 surface touched editors/vscode/** via W4 c8 + W5 extension-host (binding JS lane)", "phase8_0": "the interactive debug interface in VS Code: W2 the debug WebviewPanel (v1.3 renderModel reused VERBATIM) + W3 breakpoints/time-travel (StepRecord-predicate, snapshot/restore) + W4a the capture->.trace.json round-trip + 1 new ExtHost test debugPanel.test.ts + the one disclosed behaviour-preserving diagramWebview.ts refactor + the fsm.openDebug manifest contributions",
      "delta": "the debug epic's editors/vscode/** delta is part of the whole `git diff --shortstat fbae38b..8092d3a` = 57 files / +10045 / -243 (W4c DD-01); editors/vscode/package.json is manifest-contributions-only + package-lock.json is NOT in the delta (0 new npm deps — W4c DD-19/DD-20)",
      "attention": false,
      "note": "The Phase-8.0 interactive-debug-interface epic W0-W4a. The headline is the W1 fsm/simulate* LSP transport (the single keystone-violable Rust surface) + its acceptance suite; the W2-W4 TS webview surface holds the no-fork invariant trivially (renders the oracle's output, computes nothing — W4b §2 ∅). This is why the JS lane is a BINDING tag gate (§4.3), like v1.5/factory, unlike v1.4."
    },
    "live_diagnostic_codes": { "factory": 73, "phase8_0": 75, "delta": 2, "attention": false, "note": "MOVED 73->75 — the #128 F-1/F-2 gating fixes added EXACTLY TWO hard rejecting diagnostics (FSM-E0411/FSM-E0412) replacing two previously-silent miscompiles. NOT a silent code-count drift — correctly attributed; the G7 count-discipline lock is GREEN + non-vacuous. Re-derived from source (lib.rs:851 const EXPECTED:75) at the gate-doc commit, not echoed." },
    "conformance_fixtures": { "factory": 53, "phase8_0": 55, "delta": 2, "attention": false, "note": "GREW 53->55 — +VAL-NEG-009/010 for the new F-1/F-2 FSM-E0411/FSM-E0412 (the G7 one-fixture-per-new-code discipline, no allowlist dodge; the lock GREEN + non-vacuous). A positive quality delta, correctly attributed. Re-derived from the MANIFEST at the gate-doc commit (W4c DD-25 corroborates 53->55)." },
    "forbid_unsafe_roots": { "factory": 12, "phase8_0": 12, "delta": 0, "attention": false, "note": "Unchanged — W1 added an INTERNAL fsm-lsp->fsm-simulator path-dep edge (fsm-simulator already #![forbid(unsafe_code)] since v1.4) + the one set_context_field helper into the existing fsm-simulator; no new [[package]] (`git diff fbae38b..8092d3a -- Cargo.lock | grep -E '^\\+(name|source) ='` => none); no new #![forbid(unsafe_code)] root. Re-derived from `git grep -l` at both fbae38b & 8092d3a, identical (W4c DD-04 corroborates 12 files at both)." },
    "forbid_unsafe_crates": { "factory": 11, "phase8_0": 11, "delta": 0, "attention": false, "note": "Unchanged — no new crate (W1 = an internal path-dep edge into the existing fsm-lsp + the existing fsm-simulator). fsm-lsp = lib.rs+main.rs = 2 roots / 1 crate => 12 roots / 11 crates (the canonical v1.5/factory roots/crates form; W4c's '12 at both' counts root FILES — the same source fact)." },
    "decisions_section_11_rows": {
      "factory": 86, "phase8_0": "REPORTED as §9 DRAFT (the orchestrator appends §11.87+ at W4d, NOT this closeout)", "delta": "0 (this closeout does NOT edit Doc 00 §11 — the hard BOUNDARY)",
      "attention": false,
      "note": "The ledger head is §11.86 (the factory-W6c batched-closeout row — independently confirmed from the file; `grep -cE '^\\| §11\\.[0-9]+ ' docs/00-Decisions-And-Reconciliation.md` => 86). The Phase-8.0 rows are REPORTED as §9 DRAFT (§11.DRAFT-A..E) in GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md §9 for the orchestrator to independently re-derive, renumber (§11.87+), and append in ONE controlled commit at W4d (the comm -23 append-only discipline; NOT pre-allocated — Doc 33's header numbering refinement + the v1.4 L6 lesson). Doc 33 §9's design-decision DRAFT rows (the owner D-1/D-2/D-3 + Doc 33-authored) are a SEPARATE set the orchestrator also reconciles against the live head at W4d."
    },
    "regression_flags": "NONE — Phase-8.0's Rust delta is MODERATE + ADDITIVE (the W1 fsm/simulate* transport + its acceptance suite + the one sanctioned non-stepping set_context_field helper + the gating #128 F-1/F-2 codegen-correctness fixes + the additive corpus); the differential oracle (crates/fsm-simulator/src save the one sanctioned helper + cmd/test.rs) is BYTE-UNTOUCHED (the W4b source-derived audit independently re-confirmed the no-fork from source — KEYSTONE-INTACT). The Rust metrics that MOVED did so for an attributed reason (DiagnosticCode 73->75 the F-1/F-2 +2 rejecting diagnostics; conformance 53->55 +VAL-NEG-009/010 — a positive quality delta); forbid 12/11 unchanged (no new crate). G2/G3/G6 STRENGTHENED (the F-1/F-2 fixes close a #110-class silent-misconfig end-to-end + the W1 differential proves the debug transport byte-equals the oracle). The TS surface is well-tested (the W2/W3/§W4 byte-match-vs-oracle ExtHost E2Es debugPanel.test.ts + the v1.3-path-identity diagram.test.ts; the W4b auditor's own xvfb-run run 47 passing/exit 0). No metric crossed into attention-territory this release. The debug surface is keystone-honouring by construction (every UI verb = one shipped Interpreter call; W2-W4 compute nothing — W4b §2 ∅) + W4b/W4c-verified."
  },

  "process": {
    "phase8_0_waves": "W0 (Doc 33 the wave-plan-of-record, phase8.0/debug-iface-plan-of-record) + W1 (the fsm/simulate* LSP transport + the single sanctioned non-stepping set_context_field helper — the single keystone-violable Rust/LSP wave, 1de83b0) + W2 (the debug WebviewPanel, 032438c) + W3 (breakpoints+time-travel, b79e052/ec903e5) + W4a (the capture->.trace.json round-trip, ae774dd->8092d3a) + the W4b source-derived keystone phase-audit (phase8.0/w4b-keystone-audit, audited 8092d3a, KEYSTONE-INTACT, FROZEN) + the W4c independent pre-tag four-lens audit (phase8.0/w4c-four-lens-audit, audited 8092d3a, TAG-CLEAR 0 P0/0 P1/3 P2/2 P3, FROZEN) + the gate-doc batched closeout = the §11.30 gate-doc commit X (this metrics file + GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19 + CHANGELOG [debug-interface] + ROADMAP [debug-epic retro + the milestone-tag record]; the orchestrator independently re-derives & appends Doc 00 §11.87+ at W4d — NOT in this closeout's scope; NO crates/.github/ code-tree touch). The gating #128 F-1/F-2 codegen-correctness fixes + the `defer` doc-drift fold landed on the same arc.",
    "sprint_theme": "The interactive debug interface (Doc 33 / the owner D-1/D-2/D-3 decisions): an in-editor live simulation/debug surface as JUST ANOTHER FRONTEND of the one shipped fsm_simulator oracle, never a second semantics — surfacing the shipped simulator in VS Code via the proven v1.5 W-A2 custom_method LSP seam (NO standalone WS daemon, NO new network surface — the attack surface removed by construction, D-2). The single keystone-violable surface is concentrated into ONE gated wave (W1); W2-W4 hold the no-fork invariant trivially (they render the oracle's output and compute nothing). The pipeline-before-UI bar ([[feedback_embeded_fsm_pipeline_before_ui]]) was satisfied at v1.4 (W4a FACTORY-COMPLETE); the v1.5 convenience + the factory-reliability/CI proof layers shipped on top; this is the owner-decided interactive debug interface. The full signal-primitive epic -> OWNER-3 parked (after this epic + the owner-reserved v1.6, D-3).",
    "canonical_release_record": "docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md (authored at the Phase-8.0 gate-doc commit X; §11.30 prospective ordering followed — gate-doc commit -> cold quad WITH the MANDATORY NOTE-1 cargo-clean-p Step 0 + the BINDING JS lane -> tag, all at X; the 7th consecutive prospective-clean release)",
    "frozen_phase_audits": "docs/AUDIT_PHASE_DEBUG_W4_2026_05_19.md (W4b KEYSTONE, KEYSTONE-INTACT) + docs/AUDIT_PRE_TAG_DEBUG_2026_05_19.md (W4c pre-tag four-lens, TAG-CLEAR 0 P0/0 P1) + Doc 33 docs/33-Debug-Interface-Wave-Plan.md (the accepted wave-plan-of-record) + the frozen design docs (DBGUX/EXTSIG/CHIPMATRIX) — all FROZEN, NOT edited by this closeout (audit/plan/design evidence is never overwritten — the §11.29 frozen-attestation discipline)",
    "roadmap_doc": "docs/ROADMAP.md (the interactive debug-interface epic = SHIPPED pending the contingent tag; the debug-epic retrospective added; the milestone-tag record checkpoint/2026-05-19-debug-interface; the v1.6 candidates carry [D1 + the doc-honesty pass incl. CF-2 Doc-16 + DF-02 DBGUX-prose, the FE-hygiene batch incl. DF-03, the parked Certifiability owner-review]; the broader README/Doc-07/Doc-14 doc-honesty pass FLAGGED not fixed — Doc 33 §5 / the separate backlog item; the 'See also' pointer was ALREADY reconciled at v1.5 — NOT re-touched here)",
    "key_process_refinements": [
      "KEYSTONE-IN-DEBUG held end-to-end + was the binding tag-gate row: every debug UI verb is EXACTLY ONE shipped fsm_simulator::Interpreter call marshalled over the W1 fsm/simulate* LSP custom request (the proven v1.5 W-A2 custom_method seam); breakpoints a PREDICATE over the oracle-emitted StepRecord fields NEVER a guard/transition re-eval; time-travel Interpreter::snapshot/restore; capture round-trips the SAME write_trace_yaml/execute_trace fsm test uses; NO forked semantics in crates/fsm-lsp/** or editors/vscode/**; the single sanctioned crates/ delta the one additive non-stepping set_context_field helper. The W4b source-derived §11.3 phase-audit RE-DERIVED the no-fork from source (the oracle + cmd/test.rs + write_trace_yaml byte-untouched across the whole epic arc save the one helper, per-file blob-SHA-identical; negative-grep ∅ across the whole debug surface every hit read; every op handler read) + RE-RAN the differential battery itself (the host differential 3x-deterministic + the W1 LSP acceptance + the 47-test ExtHost suite + the §W4 .trace.json replay non-vacuity hand-confirmed) — NOT echoed from the wave (the v1.4-W1/v1.5-A2/factory-W6a keystone-first precedent applied a FOURTH time, to the single Rust/LSP-adding wave; one auditable checkpoint by construction)",
      "verify-the-record load-bearing AT THE CLOSEOUT LAYER (the §11.29 symmetric lineage): the W4b/W4c verdicts RE-READ IN FULL not echoed; EVERY pinned numeric INDEPENDENTLY RE-DERIVED at the gate-doc commit (live codes 75 = the F-1/F-2 +2 correctly attributed not a drift; conformance 55 = +VAL-NEG-009/010; forbid 12/11 unchanged because W1 added an internal path-dep edge + a helper into the already-forbid-unsafe fsm-simulator, no crate root; the differential CORPUS partition re-derived from source AND the test's --nocapture output 11+3+0=14 dynamically asserted — the never-game discipline); the Phase-8.0 Rust delta stated as a MODERATE ADDITIVE delta (28 files/+3260/-214) — the Phase-8.0-shaped instance of the release-record-symmetry discipline, NEITHER under-stated (the v1.3 cardinal 'zero Rust delta' error) NOR over-stated (the v1.4 +7398 / factory +8192), NOT echoed from W4c's whole-delta 57/+10045/-243 but re-derived at the `-- crates/` scope at X; the v1.3-cardinal-error-form grep of the gate doc = 0 false whole-delta-is-zero assertions (Python context-check of all 12 lexical hits — [[feedback_embeded_fsm_verify_record_release_layer]] applied symmetrically at the closeout layer)",
      "the BINDING JS lane (Doc 33 §7 / the v1.5/factory re-activated-lane precedent): the Phase-8.0 surface IS editors/vscode/** (W2/W3/W4 the debug webview + 1 new ExtHost test debugPanel.test.ts + the one disclosed behaviour-preserving diagramWebview.ts refactor) — the JS lane (npm audit triaged + the Extension-Host suite green incl. debugPanel.test.ts + the v1.3-path-identity diagram.test.ts) is a BINDING gate at the gate-doc commit X, described as the contingent gating step (no fabricated transcript — the v1.4-W4b/v1.5/factory honest-prospective posture; the W4b auditor's own 47-passing/exit-0 xvfb-run run at 8092d3a the de-risking predicate; W4c source-verified the lane structure, CI-runner-only by the disk-tight infra constraint — [[feedback_embeded_fsm_pipeline_before_ui]] the load-bearing byte-identity proof empirically re-run at the LSP/CLI layer, the ExtHost layer pure marshalling over it)",
      "the memory-SoT status-line-freshness gate (Doc 33 §7 / Doc 32 §7-tweak-3 — the single biggest drift the v1.4 meta-session found): the wave->commit map is recorded in the gate doc §Scope/§5#5 so the orchestrator can advance the backlog SoT head status line + add the Phase-8.0 RELEASE RECORD section IN THE SAME closeout motion (the SoT is the orchestrator's memory file — outside the repo + outside this wave's hard-guardrail scope, exactly as the v1.5/factory closeouts recorded it; phrased honest-prospective 'gate-doc landed; the milestone tag + the SoT-freshness advance pending the contingent steps', NOT a fabricated tag)",
      "the in-repo ROADMAP stale-pointer was ALREADY reconciled at the v1.5 closeout (independently re-confirmed: docs/ROADMAP.md:310 carries the 'Reconciled 2026-05-17' note pointing at the backlog SoT + the per-epic wave-plan docs) — Phase-8.0's ROADMAP edit is the debug-epic retrospective + the milestone-tag record only (it does NOT re-touch the already-fixed pointer; the factory-epic precedent — Doc 32 §7-tweak-5)",
      "Batched doc/ledger writes (the v1.2/v1.3/v1.4/v1.5/factory generalisation): the Phase-8.0 closeout is a single controlled batch (GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19 + CHANGELOG [debug-interface] + ROADMAP [retro + the milestone-tag record] + this metrics companion + the §9 DRAFT §11 rows REPORTED for the orchestrator) folding the FROZEN W4c carry-list (DF-01..DF-05) + the Doc 33 §5 carried CF-1/CF-2 + the carried owner-escalations in ONE batch (the §11.62 batched-doc discipline — never piecemeal). NO code-tree touch (no F-05-equivalent — the W4c findings are framing/doc-precision/accepted-tracked, none requiring an in-scope code change)",
      "Doc 33 + the W4b/W4c audits + the frozen design docs FROZEN-not-rewritten (the v1.3/v1.4/v1.5/factory pattern): the wave-plan-of-record + the audit evidence + the design docs are NEVER overwritten by the closeout; the broader README/Doc-07/Doc-14 + CF-2 Doc-16 + DF-02 DBGUX-prose doc-honesty pass is FLAGGED not fixed (Doc 33 §5, the separate already-identified v1.6 backlog item — only the ROADMAP 'See also' pointer was in v1.5's scope, already fixed)",
      "no-push/no-tag boundary + exclusive-writer discipline (the factory-W6c precedent): this closeout is the SOLE writer of its scoped files (the gate doc + CHANGELOG + ROADMAP + this metrics JSON; the §9 DRAFT §11 rows are REPORTED for the orchestrator, NOT appended — this closeout does NOT edit Doc 00 §11); the commit is NOT merged/pushed/tagged — the orchestrator gates (W4c TAG-CLEAR [frozen] + W4b KEYSTONE-INTACT [frozen] → independently re-derives + appends §11.87+ → the cold-quad WITH NOTE-1 Step 0 + the binding JS lane at this commit → tag checkpoint/2026-05-19-debug-interface at this commit, §11.30 strictly; the 7th consecutive prospective-clean release; LOCAL only — G9 = the owner's push)"
    ]
  }
}
