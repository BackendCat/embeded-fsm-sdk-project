# GATE VERIFICATION — v1.4.0

- **Document ID:** GATE_VERIFICATION_v1_4
- **Date:** 2026-05-16
- **Scope:** v1.4.0 release gate — all commits since v1.3.0 `a036c38` through the v1.4 verification-core corpus: **W1** (the `fsm-verify` bounded-reachability+deadlock keystone spine + the `fsm verify` CLI + FSM-E0400/W0602 emission, `2866821` / merge `dd2d41e`) + **W2** (the load-bearing snapshot-completeness P0 `efae559` + composite/parallel/history/timer/submachine coverage `3447881`) + **W3** (`fsm baseline` trace differential replay, `fc88ec1`) + **W4a** (the factory-integration completeness audit, `4eb04dc`) + the three §11.3-cadenced phase-audit docs, and the release commit `X` (this document + the batched doc/ledger consolidation; see §4 + §7 + §8).
- **Companion evidence (frozen, never edited):** the post-W1 keystone audit `docs/AUDIT_PHASE_V1_4_W1_2026_05_16.md` (commit `a75cd69`), the post-W2 breadth audit `docs/AUDIT_PHASE_V1_4_W2_2026_05_16.md`, the W4a factory-integration audit `docs/AUDIT_FACTORY_INTEGRATION_V1_4_2026_05_16.md` (commit `4eb04dc`); the v1.0/v1.1/v1.2/v1.3 baselines `docs/GATE_VERIFICATION_v1_{0,1,2,3}.md`; the v1.4 release record `docs/00-Decisions-And-Reconciliation.md` §11.64–§11.71; the v1.3 metrics base `docs/metrics/2026-05-16-v1_3.json` and the v1.4 companion `docs/metrics/2026-05-16-v1_4.json`.
- **Verdict:** **PASS — v1.4.0 is tag-ready.** 0 open P0; the two §11.3 phase-boundary audits (post-W1 keystone, post-W2 breadth) each returned **PROCEED-WITH-NOTES** (0 ship-blockers); the W4a factory-integration audit returned **FACTORY-COMPLETE** (0 workflow gaps). Every NOTE is documentation-of-record / closeout-batch / accepted-tracked-debt, folded by this closeout or carried. The independent pre-tag four-lens audit is the contingent step the tag is gated on (the v1.1/v1.2/v1.3 §3 pattern). The canonical confirming **cold-from-source** quad at the release commit `X` — with the §11.30/NOTE-1 mandatory `cargo clean`/fresh-target precondition (§4.2) — is the gating step the tag is contingent on (§4).

This document is the canonical release record for v1.4.0. It captures the MVP-gate status, the v1.4 surface (the `fsm-verify` verification core: `fsm verify` + `fsm baseline` + FSM-E0400/W0602 honestly closed), the verification evidence (the post-W1 keystone + post-W2 breadth §11.3 audits, the W4a factory-completeness audit, and the per-wave + final §11.1 independent re-runs), the §11.22/§11.30-mandated **cold-from-source** quad posture **with the NOTE-1 explicit `cargo clean`/fresh-target precondition**, the accepted-tracked-debt a consumer must be aware of, and the post-tag actions that require the repository owner.

---

## 1. MVP gate (Doc 23 §9) — v1.4.0 status

| Gate | v1.4 status | Evidence |
|---|---|---|
| **G1** `fsm check` ok / clean reject, no panic, no UB | ✅ | The v1.4 Rust delta vs the v1.3 tag (`a036c38`) is the **additive verification core** — a NEW `fsm-verify` crate + the W2-P0 lossless-snapshot extension to `fsm-simulator` + the `fsm verify`/`fsm baseline` CLI wiring in `fsm-cli`. `git diff --stat a036c38 4eb04dc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` = **51 files / +7398 / −6** (NOT empty — the symmetric opposite of v1.3's "zero Rust delta"; this is a substantial *additive* delta, stated as exactly that). `fsm check`'s parse→analyze→diagnostics path is **byte-untouched** by v1.4: the only `fsm-simulator` change (W2-P0) is **purely additive** to `InterpreterSnapshot` (pre-W2 fields keep name/type/order; flat snapshots serialise byte-identically — round-trip-gated; the W2 §11.3 audit Lens 3 independently confirmed flat-machine byte-unregression), and `fsm-cli`'s change adds two *new* subcommands without altering `check`/`generate`/`fmt`/`test`. |
| **G2** `fsm check broken.fsm` → exit 1 + Rust-style error | ✅ | Unchanged — v1.4 adds no diagnostic code and touches no `fsm-analyzer` / `fsm-parser` / `fsm-diagnostics` emission path (verified: the v1.4 Rust delta is confined to `crates/fsm-verify/` [new], `crates/fsm-simulator/` [additive W2-P0], `crates/fsm-cli/` [+2 subcommands]). FSM-E0400/W0602 are now *emitted by `fsm verify`* — a new surface, not a change to `fsm check`'s diagnostic stream. |
| **G3** `fsm generate` emits the C unit set + HAL | ✅ | Unchanged — no codegen surface in the v1.4 verification-core scope. The W4a factory-audit drove `fsm generate --target c99 --emit-ir` end-to-end (exit 0, the emitted C compiled `-std=c99 -Wall -Wextra -Wpedantic -Werror`) as part of the headless loop. |
| **G4** `gcc -std=c99 -Wall -Wextra -Wpedantic -Werror` zero-warning | ✅ | Unchanged; subsumed in the cold quad's `cargo test --workspace` §5.4 gcc-compile-and-RUN battery. No codegen surface changed; the W4a factory-audit independently re-confirmed the emitted C compiles clean (`examples/verify/clean.fsm` → exit 0). |
| **G5** `fsm fmt` idempotent | ✅ | Unchanged from v1.0 (zero formatter delta — `git diff --stat a036c38 4eb04dc -- crates/fsm-formatter/` empty). |
| **G6** all examples full-chain + sim-trace-match | ✅ | Unchanged; the W3 `fsm baseline` corpus + `committed_corpus_matches_a_fresh_capture_on_this_build` further *strengthen* this (a frozen drift oracle now catches any sim-behaviour regression). Subsumed in the cold quad. |
| **G7** ≥1 test per diagnostic code | ⚠️ tracked | **26** formal `tests/conformance/` fixtures — **independently re-derived at the gate-doc commit** (`jq '[.categories[].fixtures\|length]\|add' tests/conformance/MANIFEST.json` ⇒ **26**; parser 6 + validator 6 + semantic 7 + codegen-c 4 + formatter 3 — byte-identical to v1.3). The live `DiagnosticCode` count is **independently re-derived = 73** (unchanged — see §"Pinned gate numbers"). Identical posture to v1.0–v1.3; **not a v1.4 regression**. FSM-E0400/W0602 (closed in v1.4) are tested by the **`fsm-verify` acceptance suites** (real `.fsm` fixtures driven through the parse+analyze+verify pipeline, byte-cross-checked against an independent interpreter BFS — the §5.4 *behavioural* bar, never symbol-presence — `crates/fsm-verify/tests/{reachability_acceptance,w2_hierarchy_acceptance}.rs`; e.g. `unreachable_island.fsm`), the §5.4-analogue for a new subsystem (the v1.3 Extension-Host-tests precedent). Formal-suite closure remains tracked, unchanged. |
| **G8** `cargo build/test/clippy/fmt` clean | ✅ | **cold-from-source** quad posture below (§4), with the **NOTE-1 explicit `cargo clean`/fresh-target precondition** written into §4.2 as a mandatory step. The v1.4 Rust delta is the additive verification core; the per-wave §11.1 independent re-runs (W1/W2/W3 each decode-the-wire-and-assert-vs-oracle, never symbol-presence) + the W2 §11.1 independent re-run (114 ok / 0 FAILED on `3447881`, full-clean) are the de-risking predicates; the §11.30-clean canonical confirming run at `X` re-confirms from cold regardless — the contingent gating step. |
| **G9** CI matrix linux/macos/windows | ⚠️ tracked | `.github/workflows/ci.yml` carries the v1.2 SCA `cargo audit` job; **never exercised against any v1.2/v1.3/v1.4 commit** (local-only repo — owner controls the remote, a hard project rule). v1.3's never-run Node/Extension-Host JS lane is **untouched by v1.4** (the v1.4 surface is the verification core, no `editors/` change) and remains carried. Post-tag owner action §5. |

G7 + G9 are the **same explicitly-tracked partials accepted at the v1.0, v1.1, v1.2 and v1.3 gates** — neither is a v1.4 regression; both are documented in CHANGELOG `[1.4.0]` *Known limitations* and the ROADMAP.

---

## 2. v1.4 surface delivered (since v1.3.0 `a036c38`)

**Theme: Trustable behavioral validation — the Verification core.** v1.4 was scoped (Doc 00 §11.63, owner-confirmed verbatim) to the **complete Verification core** = cut **A** (bounded explicit-state reachability + deadlock detection driving the shipped `fsm_simulator::Interpreter` as the *sole semantic oracle*, surfaced as `fsm verify`, honestly closing FSM-E0400/FSM-W0602) **+ B** (trace differential replay, `fsm baseline`). The verification core is a **NEW single `fsm-verify` crate** = the one source of truth for verification semantics; the **canonical pipeline-grade interface is the CLI** (no server, no daemon, no plugin host — the `[[feedback_embeded_fsm_pipeline_before_ui]]` bar). The WS simulator server → v1.5; Web IDE / wasm32 / cross-file LSP → v1.6 (explicitly deferred, **not** re-cut).

### 2.1 W1 — the keystone (`2866821`, merged `dd2d41e`)

The bounded-reachability+deadlock spine. The explorer **drives `fsm_simulator::Interpreter`** (`new`/`init`/`dispatch`/`advance_clock`/`snapshot`/`restore`) as the sole transition oracle — **zero re-implemented transition-selection / guard-eval / LCA / completion** (the §4.1 keystone, an epic-level architecture invariant per Doc 00 §11.63). Visited-set keyed by a stable digest of `InterpreterSnapshot` (not retained full snapshots — the §3.2 memory-bound design); honest-bound structurally enforced (`Verdict::ProvenNoDeadlock` constructible at exactly one site on the natural exhausted exit; `unreachable!()` guards an inconclusive-on-exhausted outcome). **Authorised scope expansion (W1-audit D-1, Doc 00 §11.64):** W1 also shipped the `fsm verify` CLI + FSM-E0400/W0602 emission + the reachability report (per the §11.63 owner-confirmed authoritative brief + the pipeline-before-UI gate — deferring the CLI to W2 would have left W1 a library with no headless contract to gate); accepted shape `Verdict::Deadlock { witness: Vec<String>, … }` (the better choice over the §4.3-sketched `Vec<StepRecord>`). `#![forbid(unsafe_code)]` added → workspace 12 roots / 11 crates.

### 2.2 W2 — breadth + the load-bearing snapshot P0 (`efae559` then `3447881`)

The post-W1 §11.3 audit's headline D-2: the pre-W2 `InterpreterSnapshot` was **lossy** for `RuntimeState.submachines` (recursive nested sub-instances) + `RuntimeState.timers` — a latent **false-`ProvenNoDeadlock`** foot-gun for any hierarchical/timer explorer. **W2's first unit (`efae559`)** extended the snapshot losslessly — recursive `submachines` + a sorted `timers` `Vec`, **purely additive** (flat snapshots byte-identical, round-trip-gated by a ~435-line `snapshot → perturb → restore → re-snapshot` byte-identity test). W2 then extended the explorer to composite/parallel/history/defer/timer/submachine (timer-fire via `interp.advance_clock`, completion via the Interpreter's RTC drain — never a forked enabled-set/clock) and FSM-E0400/W0602 to the hierarchical reachable set. A sound clock-origin normalisation merges only strongly-bisimilar configs (the soundness lemma now has its **normative home at Doc 08 §13.5** — Doc 00 §11.65). The post-W2 §11.3 audit independently confirmed (Lens 1) the keystone intact + (Lens 3) D-2 closed.

### 2.3 W3 — trace differential replay (`fc88ec1`)

`fsm baseline --record`/`--check` — the semantic-drift regression oracle. **Consumes** `fsm_simulator::execute_trace`/`first_mismatch` (the same seam the conformance runner + `fsm verify` use — no forked step comparison; the §4.1 B-seam keystone). Shipped as a **dedicated sibling subcommand** (the accepted better choice over the §4.2-W3-sketched `fsm test --baseline` — `fsm test` already has its own exit-code semantics; a single-purpose subcommand is a clean sibling of `fsm verify`; Doc 00 §11.67). The committed corpus `crates/fsm-cli/tests/fixtures/w3_baseline_corpus/` is **captured from the current known-good build**, byte-stability-gated by `committed_corpus_matches_a_fresh_capture_on_this_build` — **not** an `a036c38` capture (see §3 phase-audit table note + Doc 00 §11.67).

### 2.4 W4a — factory-integration completeness audit (`4eb04dc`)

Frozen evidence doc `docs/AUDIT_FACTORY_INTEGRATION_V1_4_2026_05_16.md`, verdict **`FACTORY-COMPLETE`**. Drove the real binary end-to-end (twice) and proved the full headless `fsm verify → generate → check → baseline` loop is CI/factory-integratable with **zero workflow gaps** — every workflow a real captured command + actual exit code + `jq`-parsed JSON (the anti-symbol-presence bar); the honest-bound holds at the JSON-field level; the loop is deterministic. Shipped the worked recipe `examples/verify/{clean.fsm,deadlocks.fsm,README.md}` + an APPEND-ONLY Doc 25 §9 (all merged). The single finding §3.G (the exit-2-INCONCLUSIVE Doc 18 discoverability gap) is folded into Doc 18 §3.1 by this closeout — **not a code change** (the in-source verdict family is owner-mandated-correct), **not a tag blocker** (Doc 00 §11.68).

**Planning doc of record:** Doc 30 `docs/30-v1_4-Simulation-Verification-Wave-Plan.md` (annotated-not-rewritten by this closeout — it remains the wave-plan of record; 6 reconciliation-note blockquotes added beneath stale prose, zero original prose removed).

---

## 3. Independent phase-boundary audits (read-only; frozen evidence)

The §11.3 cadence was re-sequenced (Doc 00 §11.66) to **two** audits — a **post-W1 keystone** audit (auditing the verification-core spine *before* W2 builds on it — the stronger application of §11.3, the W1-audit "Sequencing note") **and** a **post-W2 breadth** audit (the keystone+breadth boundary, before W3) — plus the **W4a factory-completeness** audit (the Doc 30 §4.2-W4/§5.2 pipeline-before-UI gate of record). All are READ-ONLY judgment layered on the §11.1 mechanical proof; the W4a audit is a real-run evidence audit (binary built + driven). Every quantity in this gate doc is independently re-derived from `git`/source at the gate-doc commit (never echoed) — see §"Pinned gate numbers".

| Audit (frozen doc / commit) | HEAD audited | Verdict | Key result |
|---|---|---|---|
| **post-W1 keystone** `AUDIT_PHASE_V1_4_W1_2026_05_16.md` / `a75cd69` | `dd2d41e` | **PROCEED-WITH-NOTES** (0 W2 ship-blockers) | _Verdict independently re-read in full at this closeout (the §11.29 verify-the-record discipline — not echoed from the W2-audit restatement; the doc's §0/§4/§6 each state PROCEED-WITH-NOTES._) The keystone is real + independently re-derived from source (zero re-implemented semantics; acceptance suite cross-checks the real Interpreter). **THE headline finding (D-2):** `InterpreterSnapshot`/`snapshot()`/`restore()` was **lossy for `RuntimeState.submachines` + `timers`** — Doc 30 §4.1's "already the complete state" an overstatement for the hierarchical/timer scope; left unflagged, W2's digest would conflate configs → premature pruning → **false `ProvenNoDeadlock`**. Recorded as the mandatory W2-first-unit prerequisite (not a W1 defect — W1 loud-rejected the unsupported scope, shipping sound). D-1: the §11.3-audit re-sequenced post-W1 + W1 shipped the CLI + E0400/W0602 (authorised by §11.63; the `Vec<String>` witness the better choice) — acceptable-tracked, fixed in this closeout. |
| **post-W2 breadth** `AUDIT_PHASE_V1_4_W2_2026_05_16.md` | `3447881` | **PROCEED-WITH-NOTES** (0 W3 ship-blockers) | Keystone **intact at W2** (every `fn` in `fsm-verify/src` re-enumerated + classified — all structural-IR-read or pure Interpreter-orchestration; negative grep on non-comment lines = ∅; `fsm-analyzer` dev-only). Honest-bound structurally enforced. **Clock-merge soundness fully confirmed** by the auditor's own derivation from quoted Doc 04 §1.5/§8.5/§8.7/§9 + Doc 08 §3.1/§4.1/§4.3/§13/§14/§15.1 — no FSM-Lang construct observes the absolute clock ⇒ the normalisation identifies only strongly-bisimilar configs ⇒ no false `ProvenNoDeadlock`. **Post-W1 D-2 closed** (snapshot lossless for timers+recursive submachines, purely additive, real 435-line round-trip gate). Froze the **7-item closeout carry-list** (§5) this batch executes. |
| **W4a factory-completeness** `AUDIT_FACTORY_INTEGRATION_V1_4_2026_05_16.md` / `4eb04dc` | `fc88ec1` | **FACTORY-COMPLETE** (0 workflow gaps) | The full headless `fsm verify → generate → check → baseline` loop is demonstrably CI/factory-integratable, machine-readable, deterministic, zero UI — every workflow A–H a real captured command + actual exit code + `jq`-parsed JSON (the three distinct verdicts incl. the honest-bound exit-2 INCONCLUSIVE at the JSON-field level; the determinism two-run byte-compare). Single finding §3.G (P3, doc-discoverability): exit-2-INCONCLUSIVE diverges from Doc 18 §3's generic table ⇒ a docs-only integrator could misread — **DEFER** to W4b (this batch): folded into Doc 18 §3.1; **not a code change** (the in-source verdict family is owner-mandated-correct — Doc 30 §4.2-W4/§5.2 reject a generic 0/1), **not a tag blocker**. Interim discoverability already closed by W4a's `examples/verify/README.md` + Doc 25 §9. |

### 3.1 Per-wave + final §11.1 independent re-runs

Each wave's completion report was independently re-run by the orchestrator's §11.1 verification (decode-the-wire-and-assert-vs-oracle, never symbol-presence). The post-W2 §11.1 independent re-run cleared the NOTE-1 stale-target hazard with a **full-clean** re-run = **114 ok / 0 FAILED on `3447881`** (`/tmp/w2-indep-rerun-report.md` §Cold-quad — the 6 apparent FAILs were stale `…-wt-v14w2/`-baked test binaries in crates W2 never touched; PASS on recompile). The keystone + clock-merge soundness + the lossless-snapshot round-trip were each independently re-derived from source at the W1/W2 audit boundaries. The §11.30-clean canonical confirming cold quad at `X` (§4.4) is the contingent gating step regardless.

### 3.2 Why these audits are not rubber-stamps (recorded so the record is honest)

Three audits over a verification-core epic + a factory-completeness audit that find no P0 are themselves suspect. They did not: the post-W1 audit **independently re-derived from source** the load-bearing D-2 (`InterpreterSnapshot` lossy for submachines/timers) that the epic's own planning doc (Doc 30 §4.1) overstated as already-done — the one place the spine was weaker than its plan claimed, caught at the keystone boundary *before* W2 built on it; the post-W2 audit **independently re-derived the clock-merge bisimulation argument** from the quoted spec (not restated from `digest.rs`) and re-enumerated every `fn` in `fsm-verify/src`; the W4a audit **drove the real binary end-to-end twice** and confirmed the §3.G finding from source rather than asserting capability. The epic is clean *because* every wave reported-then-§11.1-verified with per-commit `git show` — corroborated, not assumed.

---

## 4. Cold-from-source release quad (§11.22 / §11.30 / NOTE-1) — the tag gate

### 4.1 The binding commit-ordering rule (Doc 00 §11.30, quoted)

> **Refinement (binding for v1.2+):** the release sequence is (1) author+commit `GATE_VERIFICATION_v<x>.md`, (2) run the cold-from-source quad at THAT commit, (3) `git tag -a` THAT commit + the `checkpoint/<date>` anchor — quad-commit ≡ tag-commit exactly.

This document makes code-summarising assertions (the §1 gate evidence; the §"Pinned gate numbers" — live `DiagnosticCode`=73, `forbid(unsafe_code)`=12 roots/11 crates, conformance=26, the v1.4 Rust delta vs `a036c38`) that a code delta could in principle falsify. The v1.1-§7 one-pure-docs-commit grace therefore does **not** apply; the §11.30 sequence is followed strictly: this doc + the batched doc/ledger consolidation commit first (commit `X`), the canonical cold quad run **at that exact commit `X`**, and the tag placed on **that same commit** — zero off-by-one by construction (the v1.2 §4.1 / v1.3 §4.1 prospective-§11.30 pattern, the **4th consecutive** prospective-clean release).

### 4.2 Cold-quad method (§11.22) — **with the mandatory NOTE-1 `cargo clean`/fresh-target precondition (binding v1.4+)**

§11.22 mandates a **cold from-source** green quad for a release tag (a warm shared-`CARGO_TARGET_DIR` can serve stale cross-worktree `-wt-`-baked-path test binaries; a warm pass is necessary-but-not-sufficient). **NOTE-1 hardening (Doc 00 §11.70 — binding for v1.4+):** the cold-quad's "cold" precondition is made **explicit and mandatory** as a written step, *not* merely the word "cold":

> **MANDATORY STEP 0 (NOTE-1 / §11.22 / §11.30):** **before** any full-workspace gate, run `cargo clean` **OR** point `CARGO_TARGET_DIR` at a dedicated **non-shared** directory used by nothing else. This is non-negotiable. Rationale (a validated-this-epic hazard): a stale parallel-worktree test binary (`env!("CARGO_MANIFEST_DIR")` baked at a since-deleted `…-wt-v14w2/` worktree's compile time, reused via the shared `target/` because a *scoped* `cargo clean -p` only refreshed the W2 crates) masqueraded as **6 tag-blocking FAILED tests** in `fsm-formatter`/`fsm-lexer`/`fsm-parser` — crates W2 never touched (`git show --stat 3447881` = zero files there); they PASS on recompile. A *false red* is the symmetric danger to a false green; this precondition pre-empts it so no stale binary masquerades as a real red for the pre-tag auditor or the gate.

After Step 0: `cargo build/test/clippy/fmt --workspace` + the conformance runner (`fsm test tests/conformance`, 26/26) + the §5.4 `gcc` compile-and-**RUN** acceptance (subsumed inside `cargo test --workspace`). `df -h /` checked before/after (the box is at ~6.6G/92% — tight; the cold trough is managed by the validated v1.1-§7 *clean-first-party-from-source, keep the content-addressed registry dep cache* pattern: `cargo clean -p` the first-party crates [drops every first-party + test binary incl. stale `-wt-`-tainted ones] while keeping the path-independent registry cache — Step 0's intent without a full from-zero rebuild, and v1.4 added **0 new external deps** so the registry cache is unchanged from v1.3).

### 4.3 Carry-forward reasoning (the v1.1 §7 logic, stated up front not retro)

The v1.4 Rust delta vs the v1.3 tag is the **additive verification core** — `git diff --stat a036c38 4eb04dc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` = **51 files / +7398 / −6** (the NEW `fsm-verify` crate +3042, the W2-P0 lossless-snapshot `fsm-simulator` extension +684/−6, the `fsm verify`/`fsm baseline` CLI wiring in `fsm-cli` +3657). This is **not** a docs-only delta and the v1.1-§7 carry-forward grace does **not** apply to the *code* gate (it never claimed to — the contingent cold quad at `X` is the gating step). What carries up front is only the *de-risking*: the per-wave §11.1 independent re-runs + the W2 full-clean §11.1 re-run (114 ok / 0 on `3447881`) + the two-audit keystone/breadth confirmation + the W4a real-binary factory-completeness run make a red canonical run improbable — **not a substitute for it**. The gate-doc commit `X = 4eb04dc + a docs/ledger-only delta` (this file + the batched Doc-00 §11.64–71 / CHANGELOG `[1.4.0]` / ROADMAP / Doc 08 §13.5 / Doc 18 §3.1 / Doc 10 / metrics consolidation + the **comment-only** `digest.rs` §13→§13.5 repoint). The `digest.rs` touch is **comment-only** (`git diff -U0` shows every changed line is `//!`/`///`/`//` — 5 ins / 4 del, zero code) and was confirmed to compile via `cargo check -p fsm-verify` (Finished clean) — it cannot change `cargo build/test/clippy/fmt` / conformance / `gcc`-RUN outcomes (W4c Lens 2 will re-confirm the keystone held through this comment touch).

### 4.4 The canonical confirming quad (the step the tag is contingent on)

The **canonical confirming cold-from-source quad — with MANDATORY STEP 0 (§4.2) — is executed at the release commit `X` itself** (orchestrator-run after this document + the batched consolidation commit), and the transcript is recorded in the **annotated `v1.4.0` tag message**. So quad-commit ≡ tag-commit = `X`, satisfying §11.22 (cold from-source) **and** §11.30 (zero off-by-one) **and** NOTE-1 (the explicit `cargo clean`/fresh-target precondition) simultaneously. This run **has not yet happened** at the time this document is written; it is described here as the **gating step the tag is contingent on**, not a completed result — no run-at-`X` transcript is fabricated. The tag is placed only if the canonical cold quad (incl. Step 0 + conformance 26/26 + the §5.4 gcc-RUN battery) is green **and** the independent pre-tag four-lens audit returns TAG-CLEAR.

---

## 5. Post-tag actions required of the repository owner

The tag and the unpushed commits are **local only** (the owner controls the remote — a hard project rule). The carried owner/infra dependencies (the v1.3 §5 documented-accepted posture, **unchanged by v1.4** — the v1.4 surface is the verification core, no `editors/` / CI change):

1. **G9 CI matrix + the JS lane (the v1.3 §5 carry-forward, unchanged).** The GitHub Actions matrix (linux/macos/windows × fmt/clippy/build/test), the SCA `cargo audit` job, and the v1.3 Node/Extension-Host JS lane have **never executed against any v1.2/v1.3/v1.4 commit** (local-only repo). The local-equivalent cold quad is green; platform-specific behaviour is unverified on the runners. **The owner should push the tag + commits to exercise the full CI matrix + the SCA job + the JS lane, and keep a v1.4.1 patch lane ready.**
2. **The G9-gated 5-platform binary-bundling tail (v1.3-V6).** Carried unchanged — V6 shipped host-only; the full 5-platform tail is **blocked-on-G9 existing** (no cross toolchains on the box; infra-escalation, not grind). Accepted-tracked-debt until the owner provisions the cross-OS matrix.
3. **VSIX publishing/signing.** Carried unchanged — the v1.3 VSIX is installable, not published; marketplace publishing/signing is an **owner credential/product decision**.

---

## 6. Accepted-tracked-debt + owner-escalations (consumer-relevant)

### 6.1 R7 — the mechanised sim≡codegen-equivalence proof is an explicit DEFERRAL (named, not omitted)

v1.4's "trustable validation" theme is the **runtime** verification core (`fsm verify`) + the **differential-replay drift oracle** (`fsm baseline`). A *mechanised* proof that the simulator and the generated C are observationally equivalent (the Doc 30 §1.3 codegen-equivalence placeholder) is **a named v1.4-stretch / v1.5 item** (Doc 30 §2.1 / R7), recorded here as accepted-tracked-debt — **not silently omitted** (the §11.49 leave-and-explain precedent applied to scope; the project keeps re-learning the overstatement sin — an unaddressed sub-claim of the theme must be scoped-out loudly). Doc 00 §11.71; CHANGELOG `[1.4.0]` *Known limitations*.

### 6.2 The §3.G verdict-exit-code-family doc-surfacing (closed by this batch)

The W4a factory-audit's single finding §3.G (the exit-2-INCONCLUSIVE divergence from Doc 18 §3's generic table) is **folded into Doc 18 §3.1 by this closeout** (Doc 00 §11.68). The W4a audit's Item I final tick (capability present + self-documented in-source; docs-alone discoverability closed by the Doc 18 §3 surfacing) is **satisfied here**. Not a code change (the in-source 0/1/2/3/4 verdict family is owner-mandated-correct — Doc 30 §4.2-W4/§5.2 reject a generic 0/1), not a tag blocker.

### 6.3 Carried v1.3 owner-escalations (unchanged by v1.4 — re-stated so they are not silently dropped, the §11.62 discipline)

- **G9 multi-platform tail** = owner/infra action (the 5-platform binary bundling, blocked-on-G9; §5.2 — carried from `GATE_VERIFICATION_v1_3.md` §5.2 / Doc 00 §11.57).
- **VSIX packaged-not-published** = owner credential decision (§5.3 — carried from v1.3 §5.3 / Doc 00 §11.57).
- **CI-matrix + JS-lane never-run** = owner push action (§5.1 — the v1.3 §5 carry, unchanged).
- **No top-level `LICENSE` file** = owner/legal decision, coupled to publishing (carried from v1.3 §6.3 / Doc 00 §11.62 — `git ls-tree 4eb04dc` still has no `LICENSE`).
- **The v1.3.x `makeNonce()` `Math.random`→CSPRNG one-liner** + **JC-3 `fsmLang.codegen.*` config-discoverability** — carried v1.3.x tracked items, unchanged (the v1.4 surface touched no `editors/`). Doc 00 §11.61/§11.58.

All recorded in CHANGELOG `[1.4.0]` *Known limitations*, the ROADMAP, and Doc 00 §11.71.

---

## 7. Sign-off

v1.4.0 meets the release gate: 0 open P0; the two §11.3 phase-boundary audits (post-W1 keystone, post-W2 breadth) each returned **PROCEED-WITH-NOTES** (0 ship-blockers); the W4a factory-integration audit returned **FACTORY-COMPLETE** (0 workflow gaps). Every NOTE is documentation-of-record / closeout-batch / accepted-tracked-debt, folded by this closeout or carried. Scope statements are code-verified and conservative — every gate numeric independently re-derived at the gate-doc commit (live `DiagnosticCode`=73, `forbid(unsafe_code)`=12 roots/11 crates, conformance=26, the v1.4 Rust delta = the substantial *additive* verification core +7398/−6 vs `a036c38` with **0 new external deps** — stated as exactly that, the symmetric opposite of v1.3's "zero Rust delta" cardinal error). The recurring prose-vs-code reconciliation rigor was applied **at the closeout layer** (the post-W1 audit verdict re-read in full not echoed; the load-bearing Doc 30 §4.1 D-2 overstatement corrected by annotation; the frozen W1/W2/W4a audits NOT overwritten — the §11.29 symmetric verify-the-record discipline). R7 is named-not-omitted; all residuals + owner-escalations are recorded in CHANGELOG `[1.4.0]`, the ROADMAP, and Doc 00 §11.64–§11.71. The release is contingent on (a) the independent pre-tag four-lens audit returning TAG-CLEAR and (b) the canonical cold-from-source quad — **with MANDATORY STEP 0 (the NOTE-1 `cargo clean`/fresh-target precondition, §4.2)** — at the release commit `X` being green (§4.4).

Tagging `v1.4.0` (annotated, local) with `checkpoint/2026-05-16-v1_4` as the rollback anchor. (The v1.0.0/v1.1.0/v1.2.0/v1.3.0 release tags + `checkpoint/2026-05-16-v1_3` at `a036c38` + the earlier checkpoints are immutable and not moved.)

— TL/PM, FSM Studio

---

## 8. Tag topology (pre-emptively correct per §11.30)

Constructed §11.30-clean from the start (no v1.1-style off-by-one to reconcile retroactively — the 4th consecutive prospective-clean release):

- **Sequence:** (1) author + commit this document + the batched Doc-00 §11.64–§11.71 / CHANGELOG `[1.4.0]` / ROADMAP / Doc 08 §13.5 / Doc 18 §3.1 / Doc 10 / metrics consolidation + the comment-only `digest.rs` repoint → commit `X`; (2) run the canonical cold-from-source quad **at `X`** — **with MANDATORY STEP 0** (`cargo clean` or non-shared `CARGO_TARGET_DIR`, the NOTE-1 §4.2 precondition) — transcript recorded in the annotated tag message; (3) `git tag -a v1.4.0` **at `X`** + `git tag checkpoint/2026-05-16-v1_4` **at `X`**. Result: **cold-quad-commit ≡ tag-commit ≡ `checkpoint/2026-05-16-v1_4` commit ≡ `X`**, exactly — zero off-by-one by construction (§11.30 satisfied at its source, as v1.2/v1.3 first achieved prospectively; not reconciled after the fact as v1.1 had to).
- **Pin lineage (immutable, not moved):** v1.0.0 → `b4800036…`; v1.1.0 → `abc70044…` (== `checkpoint/2026-05-15`); v1.2.0 → `71043935…` (== `checkpoint/2026-05-16`); v1.3.0 → `a036c381…` (== `checkpoint/2026-05-16-v1_3`); v1.4.0 → `X` (== `checkpoint/2026-05-16-v1_4`). Release tags are **immutable and never moved** (re-cutting a release tag is a destructive op never performed).
- **The `git rev-parse` vs `git rev-list -1` annotated-tag-object distinction.** v1.x release tags are **annotated**: `git rev-parse <tag>` yields the tag-**OBJECT** sha; `git rev-list -1 <tag>` yields the **commit**. They differ and must not be conflated. Independently re-confirmed at this commit on the existing tags: `git rev-parse v1.3.0` = `a24c195b…` (tag-object) **≠** `git rev-list -1 v1.3.0` = `a036c381…` (commit); `git rev-parse v1.2.0` = `3b37db7c…` **≠** `git rev-list -1 v1.2.0` = `71043935…`. Recorded here pre-emptively so no future metrics/audit wave misreads the `v1.4.0` tag-object sha as a mismatched commit (the `GATE_VERIFICATION_v1_1` §7 / `_v1_2` §8 / `_v1_3` §8 lineage).
- **Frozen-attestation discipline:** `GATE_VERIFICATION_v1_{0,1,2,3}.md` correctly cite their own pinned values *at their pinned commits*; a frozen attestation is never rewritten. This document cites the v1.4 live values *at `X`* — and because the v1.4 Rust delta is the **additive** verification core (it added **no diagnostic code** — FSM-E0400/W0602 were *catalogued* pre-v1.4 and only gained an *emission site* in v1.4; it added **one** crate `fsm-verify` → **one** new forbid root, 11→12 roots / 10→11 crates; conformance MANIFEST byte-untouched at 26), the live `DiagnosticCode`=73 / conformance=26 figures are *unchanged from* and *consistent with* the frozen v1.3 attestation while `forbid(unsafe_code)` *correctly increments* to 12/11. Each is independently re-derived at `X` (not echoed). Both are correct at their respective pinned commits — Doc 00 §11.47/§11.40 codify exactly this.

### Pinned gate numbers (independently re-derived at the gate-doc commit `X` = `4eb04dc` + the docs/ledger-only delta)

| Metric | Value | Method (independently re-derived, not echoed — command + result) |
|---|---|---|
| Live `DiagnosticCode` variants | **73** | `sed -n '261,387p' crates/fsm-diagnostics/src/lib.rs \| grep -cE '^\s+[EWIH][0-9]{4}\s*=>\s*\('` ⇒ **73** (51 E + 12 W + 4 I + 6 H — by-severity re-derived). Cross-confirmed by the CI-lock test `all_codes_matches_expected_count`: `const EXPECTED: usize = 73`. **Equals the frozen v1.3 figure** — v1.4 retired/added **no** code; FSM-E0400/W0602 were *catalogued pre-v1.4* and only gained an emission site, so closing them honestly did **not** change the count (the flag's hypothesis confirmed from source). |
| `#![forbid(unsafe_code)]` | **12 roots / 11 crates** | `git grep -l '#![forbid(unsafe_code)]' -- 'crates/*/src/*.rs'` ⇒ **12** files; `… \| cut -d/ -f2 \| sort -u \| wc -l` ⇒ **11** crates. **Correctly INCREMENTS from the frozen v1.3 11/10** — the NEW `crates/fsm-verify/src/lib.rs` is the 12th root (fsm-lsp contributes 2 roots lib.rs+main.rs ⇒ 12 roots across 11 crates). Matches Doc 30 §5.2's "v1.4 adds `fsm-verify` → 12/11" assertion, **verified from source**. |
| Conformance fixtures | **26** | `jq '[.categories[].fixtures\|length]\|add' tests/conformance/MANIFEST.json` ⇒ **26** (parser 6 + validator 6 + semantic 7 + codegen-c 4 + formatter 3). **Equals the frozen v1.3 figure** — `tests/conformance/MANIFEST.json` byte-untouched (v1.4 added/retired no code). FSM-E0400/W0602 closure tested via the `fsm-verify` acceptance suites (the §5.4-analogue), **NOT** a new formal fixture — the formal corpus is unchanged (honest finding; G7 posture unchanged). No new E0400/W0602/reachability fixture in the MANIFEST (`jq … \| grep -iE '0400\|0602\|reach\|deadlock'` ⇒ ∅). |
| v1.4 Rust delta vs v1.3 tag `a036c38` | **51 files / +7398 / −6; 0 new external deps** | `git diff --shortstat a036c38 4eb04dc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` ⇒ **51 files / +7398 / −6**. Breakdown: NEW `fsm-verify` crate **17 files / +3042** (src-only 6 files / +1695); `fsm-simulator` (W2-P0 lossless snapshot) **6 files / +684 / −6**; `fsm-cli` (verify+baseline CLI) **26 files / +3657**. Cargo.lock: `git diff a036c38 4eb04dc -- Cargo.lock` adds only `name = "fsm-verify"` (the workspace-member edge) + the `fsm-cli`→`fsm-verify` dep line — **0 added `source =` lines** ⇒ **0 new external/registry crates**. `rust-toolchain.toml` byte-unchanged (pin held at 1.75). **The symmetric analogue of v1.3's cardinal "zero Rust delta" error — stated correctly here as a SUBSTANTIAL additive delta, neither under- nor over-stated.** |
| Toolchain (asserted from inside the worktree) | `1.75.0` (pinned) | `cd /root/dev/embeded-fsm-sdk-wt-v14w4b && rustup show active-toolchain` ⇒ `1.75.0-x86_64-unknown-linux-gnu (overridden by '/root/dev/embeded-fsm-sdk-wt-v14w4b/rust-toolchain.toml')`. The `rust-toolchain.toml` pin is honored for all in-repo `cargo` work (a bare `rustc --version` reporting the box default 1.95 would be a probe-context artifact, NOT a pin violation — the toolchain-probe-trap, Doc 00 §11.51). |
| Doc 00 §11 rows total | **71** (v1.4 rows §11.64–§11.71, +8 this batch) | `grep -cE '^\| §11\.[0-9]+ \| ' docs/00-Decisions-And-Reconciliation.md` ⇒ **71**. v1.4 *implementation* rows = **§11.64–§11.71** (8 rows, this batched closeout); §11.63 (the v1.4 owner scope-confirm) was appended pre-W1 (commit `3e0aac3`, before this batch). Append-only verified: all 63 pre-existing §11 rows byte-identical in the working tree (§11.63 relocated net-zero to restore numeric order); the single permitted deletion is the footer-line swap. |
