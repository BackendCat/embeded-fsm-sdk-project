# FSM Studio — Phase 6.0 Epic: Factory-Reliability & "Push-and-it-Runs" CI — Wave-Plan-of-Record

**Document ID:** FSM-PLAN-FACTORY-CI-V60
**Version:** 1.0.0 (epic-kickoff plan-of-record; produces this doc only — no implementation, no other-doc edit, no build, no push).
**Status:** **Accepted plan-of-record.** The architecture below is **already decided and owner-endorsed** (the owner consulted a side analysis, *endorsed its drafted Directive verbatim as the instruction*, and reaffirmed full TL/PM autonomy — `memory/project_user_messages_embeded_fsm_2026_05_17_testing_gap_factory_epic.md`; the owner this session said "keep going / no push needed now / on-target-vision confirmed" ⇒ the safe defaults are owner-affirmed). This document **formalizes** that decision; it is not a recommendation awaiting confirmation. The owner-decision callouts in §4 are the *residual* posture choices (the G9 publication switch, the CI-first box default, the parked MISRA track), each with a non-blocking safe parked default — the same Doc 30 §3 / Doc 31 §4 escalation pattern.
**Author:** TL/PM (planning extraction under delegated autonomy; the implementer of this doc only).
**Date:** 2026-05-17.
**Base commit:** `ef303a0` (the worktree branch `phase6.0/factory-w0-doc32` base — the v1.5 pre-tag four-lens audit commit; `v1.5.0` is tagged, the epic-boundary is cleared. Annotated-tag object ≠ commit — never conflate, the v1.4-L / Doc 31 base-commit precedent).
**Depends on / reconciles:** Doc 31 (v1.5-UIDX-Wave-Plan — the structural template mirrored 1:1; the §5.2 gate-row form + §4 owner-decision pattern + the ground-truth-verification-ledger discipline this doc inherits), Doc 30 (v1.4-Sim-Verification-Wave-Plan — the §3 owner-escalation pattern + the `[[feedback_embeded_fsm_pipeline_before_ui]]` "pipeline before UI" governing principle this epic *completes the proof-half of*), `docs/GATE_VERIFICATION_v1_5.md` (the §5.2-equivalent gate-row form + the §4.2/§4.3 NOTE-1 cold-quad + re-activated-JS-lane mechanics this epic re-uses verbatim + the carried owner-escalations re-stated; **G9 CI-never-run** the load-bearing carry), `docs/GATE_VERIFICATION_v1_4.md` (the §4.2 NOTE-1 cold-quad STEP 0 + §6.3 carried escalations), `docs/META_SESSION_2026_05_16.md` (Part D.8 L1–L7 + D.7(c) — the verify-the-record / release-record-symmetry / memory-SoT-freshness disciplines this epic inherits), `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` (PART A = the MISRA/CERT-C Certifiability track that stays parked OWNER-C; PART B = the doc-honesty staleness the v1.6 D1 landing must not propagate), `tests/conformance/MANIFEST.json` + `tests/conformance/COVERAGE_MAP.md` (the G7 conformance corpus + its drift, a finding folded), `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` (the **R7 v1.0 placeholder** — the keystone prerequisite), `crates/fsm-simulator/src/trace.rs` (the `StepRecord` oracle), `crates/fsm-cli/src/cmd/{verify,baseline,test}.rs` (the same-oracle seam the keystone drives, never forks), `.github/workflows/ci.yml` (the 2-job baseline this epic extends), Doc 18 (CLI-Specification — the `fsm verify/baseline/generate/check` contract), `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 behavioural-acceptance, §11.3 phase-audit, §11.22/§11.30 cold-quad + tag-ordering), Doc 00 §11 (decision ledger — Phase-6.0 rows assigned at the epic *closeout*, see the numbering note below).

> **Why a new sibling doc (numbering justification, per the Doc 28/29/30/31 precedent).** Doc 28 (v1.3-VSCode-Wave-Plan), Doc 29 (v1.3-W0-CST-Plan), Doc 30 (v1.4-Sim-Verification-Wave-Plan) and Doc 31 (v1.5-UIDX-Wave-Plan) each established that an *epic-kickoff extraction + wave plan* is a first-class numbered sibling, distinct from the static spec corpus (Docs 01–27) and the per-release `GATE_VERIFICATION_*` / `AUDIT_PHASE_*` evidence docs. This Factory-Reliability/CI epic is the next epic; its plan is **Doc 32** (the next free integer after Doc 31). It does **not** replace any normative spec doc — it is the *plan that sequences the factory-reliability/CI proof work on top of the already-shipped pipeline*, exactly as Doc 31 sequenced the convenience layer on top of the pipeline.

> **§11-ledger numbering refinement (the "waves report rows, no mid-flight churn" discipline — stated up front, the v1.4-L6 / v1.5-Doc-31 precedent).** Per `docs/META_SESSION_2026_05_16.md` Part D.8 L6 + the standing "waves report rows, orchestrator batch-appends" refinement: **Phase-6.0 Doc 00 §11-ledger rows are assigned at the epic closeout (W6 batched consolidation), NOT pre-allocated here.** Pre-allocating "Phase-6.0 rows start §11.X" in this plan is exactly the mid-flight-churn anti-pattern the v1.2-LSP §11 append-race eliminated. Each wave *reports* its rows in its completion notes; the W6 closeout batch *appends* them in one controlled commit (the Doc 00 append-only `comm -23` discipline). This document therefore deliberately does **not** name §11 row numbers for the waves; it names the closeout obligation instead (§5).

---

## 0. Executive summary (read this first)

**`v1.5.0` is shipped + tagged; the headless CLI pipeline is FACTORY-COMPLETE and now surfaced in VS Code** (`GATE_VERIFICATION_v1_5.md` — PASS, the keystone-in-UI binding row held via the independent post-A2 source-derived audit; the full `fsm verify → generate → check → baseline` loop is CI/factory-integratable, machine-readable, deterministic). The owner's governing **pipeline-before-UI** principle is satisfied; the convenience layer landed. **The owner has now un-parked the *proof half* of the parked Certifiability/factory-integration track** (`memory/project_user_messages_embeded_fsm_2026_05_17_testing_gap_factory_epic.md`): *"the product's value is reliability; I want every corner case verified, no error class unguarded, and a pipeline I can push-and-it-runs."* This is a **new epic, design-first**, with the existing disciplines (behavioural-not-symbol-presence, the differential oracle, verify-the-record).

**Phase-6.0 = the "Factory-Reliability & Push-and-it-Runs CI" epic — 7 waves, the design-first opener (W0) first:**

| Wave | One-line scope | Risk shape | In the MUST-set? |
|---|---|---|---|
| **W0 — this plan-of-record** | Formalize the accepted architecture into the in-repo plan (the Doc 31 design-first precedent) | None (one doc) | Yes (this doc) |
| **W1 — the R7 generic host-trace differential** | The **keystone prerequisite**: a codegen-emitted `#ifdef FSM_TRACE` `StepRecord`-isomorphic hook, wired into the existing host gcc gate, asserting generated-C trace == the `fsm_simulator` `StepRecord` oracle — **gcc already on the box, zero new install, locally provable** | Medium (the codegen hook is the only Rust-touching surface; bounded by the existing `StepRecord` shape) | **YES — irreducible** |
| **W2 — the on-target QEMU lane** | The **keystone proper**: cross-compile the *identical* `FSM_TRACE` C with `arm-none-eabi-gcc`, run on `qemu-system-arm -M mps2-an385` (Cortex-M3, semihosting), assert byte-equal to the **same** simulator oracle on the MVP corpus | Medium (new CI-runner toolchain; logic already proven host-side by W1) | **YES — irreducible** |
| **W3 — G7 enforcement + bulk fixtures** | A build-failing lock test derived from the **live `DiagnosticCode` enum** (the proven `EXPECTED:73` pattern): every live code → a conformance fixture OR an enumerated justified `non_fixture_codes` allowlist; + bulk fixture authoring | Medium (fixture-authoring is the deferrable exhaustive tail; the lock mechanism is small) | Strongly-recommended (mechanism MVP; exhaustive fixtures the deferrable tail) |
| **W4 — coverage ratchet gates** | `cargo-llvm-cov` (Rust branch coverage, additive-`stable` job) + `c8` (V8 coverage for `editors/vscode/src/**` under the `xvfb-run` host) — **ratchet-from-measured-floor**, monotonic non-decreasing | Medium (instrumentation is CI-runner-only; the floor is measured, not guessed) | Strongly-recommended |
| **W5 — ci.yml integration + the `make ci-local` gate** | Add `conformance` / `extension-host` / `coverage-rust` / `on-target` lanes; **+ the one-command local quality gate (an owner explicit ask, a FIRST-CLASS deliverable)** | Medium (CI YAML is owner-remote-only until G9; the local gate de-risks the leap) | **YES — irreducible** |
| **W6 — phase-audit + pre-tag four-lens + GATE + cold-quad + tag** | The §11.3 phase-audit (the W1/W2 keystone re-derive), the independent pre-tag four-lens, `GATE_VERIFICATION_<tag>.md`, the §11.30 cold-quad WITH the NOTE-1 STEP 0 + the binding JS lane, the tag | Process (the proven §11.30 closeout) | **YES — irreducible** |

**The keystone invariant carries forward UNCHANGED, restated for the target surface (§2, a BINDING tag-gate row):** the differential oracle is **always** the shipped `fsm_simulator` `StepRecord` (the *same* seam `crates/fsm-cli/src/cmd/{test,baseline}.rs` use — verified: `cmd/baseline.rs:141` consumes `fsm_simulator::{execute_trace, StepRecord}`, "the interpreter remains the sole semantic oracle"). The generated-C trace (host *and* target) is asserted **byte-equal to that oracle**. There is **NO** forked comparator, **NO** second trace-recorder, **NO** re-implemented step semantics in the codegen runtime, the QEMU harness, or the CI glue. **A forked oracle is the cardinal regression** — the exact P0-1 / MV5-1 / v1.4-keystone / v1.5-keystone-in-UI class the project keeps re-learning. W6 re-derives this **from source**, not from this doc, not from a wave's self-report (the §11.29 / v1.4-L1 verify-the-record rigor).

**The most important sequencing fact (the keystone prerequisite — encode this).** *"Extend the host differential to the target"* (the owner's literal ask, item (1)) **presumes a host C↔simulator *trace* differential that DOES NOT EXIST.** Verified: `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` is an explicit **v1.0 placeholder** (its own module doc: *"v1.0 placeholder. The full equivalence gate requires the generated C runtime to emit `StepRecord`-shaped trace events back to the host so we can diff them against the simulator. `fsm-codegen-c` does not yet expose the instrumented hooks needed for this … 1. Smoke-checks the simulator … so the conformance harness can flip a flag to enable real diffing once codegen-c lands the hooks. 2. Skips silently if `gcc` is not on PATH"*). The host gate today is **hand-written per-example assertions** + the gcc-compile gate (`crates/fsm-codegen-c/tests/gcc_compile.rs` — gate G5, *compiles & runs* generated C but does **not** diff a `StepRecord` trace vs the simulator). This is **R7 — the named v1.4→v1.5 deferral** (`GATE_VERIFICATION_v1_5.md` §6 carries it: *"the R7 mechanised sim≡codegen deferral … still v1.5-stretch/later — not folded into the convenience layer"*). **Therefore W1 builds the R7 generic host-trace differential FIRST** (gcc already on the box ⇒ zero new install, locally provable), and **W2 then extends the *identical* `FSM_TRACE` C to the emulated target.** Building W2 (the on-target lane) before W1 (the host differential it presumes) would be building the roof before the load-bearing wall — the single most important sequencing fact of this epic.

**Critical path:** `W0 (this) → W1 (R7 host-trace differential — keystone prerequisite, gcc-only, locally provable) → W2 (on-target QEMU lane — keystone proper, MVP corpus) → W3 (G7 enforcement + bulk fixtures) → W4 (coverage ratchet) → W5 (ci.yml + the make ci-local gate) → W6 (phase-audit + pre-tag four-lens + GATE + §11.30 cold-quad + tag)`. **MUST-set (irreducible): W1 / W2 / W5 / W6.** Strongly-recommended in-epic: W3 / W4. **Explicit fallback split:** this-epic = on-target+CI (W1/W2/W5/W6); a follow-on = the enforcement gates (W3/W4). **Default = attempt the full matrix** with the enforcement *mechanisms* (the W3 lock test, the W4 ratchet gates) as MVP and the *exhaustive* fixture-authoring (W3's bulk tail) as the deferrable tail (§6 R-fallback).

**A note on this document's discipline (the Doc 31 verify-as-you-write precedent, applied here).** Per `[[feedback_ck_vpd_crm_source_of_truth_extraction]]` + `[[feedback_verify_status_claims_vs_code]]`: every load-bearing ground-truth claim was **independently verified against code at the worktree base, not transcribed from the accepted-architecture brief**. The §"Ground-truth verification ledger" records each verification and **flags the three divergences found** (GT-7: the example FSMs with frozen traces are **5, not "~8"** — the MVP corpus is sized to 5; GT-8: the conformance corpus asserts **~11 distinct codes via the MANIFEST**, while `COVERAGE_MAP.md`'s "Tested 39 / UNTESTED 34" is a *different axis* — emitter-unit-test coverage, not conformance-fixture coverage — the "~10 codes with a conformance fixture" claim is directionally true *for the conformance-fixture axis* but must be stated precisely; GT-9: `COVERAGE_MAP.md`'s drift is **75→73 internal + a 39/34/75-row Summary**, not a clean "78/75/73", and is itself the regenerate-as-artifact finding). The architecture's *decisions* survive intact; the *numbers* are corrected to ground truth.

---

## Ground-truth verification ledger (the Doc 31 verify-as-you-write discipline — divergences FLAGGED)

Every claim the accepted architecture rests on was re-derived from source at the worktree base `ef303a0`. `file:line`/command cited for each. Toolchain-probe-trap honoured: every rustc fact asserted via `rustup show active-toolchain` *from inside the repo*, never bare `rustc`.

| # | Accepted-architecture claim | Verification (command/file) | Verdict |
|---|---|---|---|
| GT-1 | `.github/workflows/ci.yml` has exactly 2 jobs `build`(matrix)+`sca`, on `push:[main]`+`pull_request` | `cat .github/workflows/ci.yml`: `on: push: branches:[main]` + `pull_request`; jobs `build` (matrix `os:[ubuntu-latest,macos-latest,windows-latest]`, fmt/clippy/build/test on the 1.75 pin via `rustup show active-toolchain \|\| rustup toolchain install`) + `sca` (a **distinct** job: `rustup toolchain install stable` → `cargo +stable install cargo-audit --locked` → `cargo audit`; the comment states the additive-`stable` rationale "Kept a DISTINCT job so it never gates the 1.75 build matrix") | ✅ **CONFIRMED** — the 2-job baseline + the proven additive-`stable`-isolated-job pattern (the `sca` precedent W4's `coverage-rust` mirrors) is exactly as the architecture states |
| GT-2 | The 1.75 pin is honoured only from inside the repo (the probe-trap) | `rustup show active-toolchain` (from `/root/dev/embeded-fsm-sdk-wt-fw0`) ⇒ `1.75.0-x86_64-unknown-linux-gnu (overridden by '…/rust-toolchain.toml')`; `rust-toolchain.toml` = `channel="1.75.0"`, `components=["rustfmt","clippy"]`, `profile="minimal"` | ✅ **CONFIRMED** — `[[feedback_embeded_fsm_toolchain_probe_trap]]` is live; every Phase-6.0 brief MUST assert rustc facts via `rustup show active-toolchain` from the repo, never bare `rustc` (a bare reading → box-default stable, a benign artifact, NOT pin drift) |
| GT-3 | `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` is an explicit v1.0 placeholder; the host C↔sim trace differential does NOT exist (the R7 keystone prerequisite) | `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` module doc, verbatim: *"v1.0 placeholder. The full equivalence gate requires the generated C runtime to emit `StepRecord`-shaped trace events back to the host so we can diff them against the simulator. `fsm-codegen-c` does not yet expose the instrumented hooks needed for this … Skips silently if `gcc` is not on PATH"* | ✅ **CONFIRMED — the load-bearing keystone-prerequisite fact.** The host trace differential is genuinely absent; W1 builds it; W2 presumes W1. The sequencing is mandatory, not stylistic |
| GT-4 | `StepRecord` is the shipped simulator oracle; `cmd/{test,baseline}.rs` consume it and NEVER fork | `crates/fsm-simulator/src/trace.rs:43` `pub struct StepRecord` (Doc 13 §11 normative shape, `#[serde(rename_all="camelCase")]`, byte-stable by construction). `crates/fsm-cli/src/cmd/baseline.rs:141` `use fsm_simulator::{execute_trace, StepRecord, TraceFile, TraceResult};` + its module doc: *"the interpreter remains the sole semantic oracle: capture is `execute_trace(ir, trace).actual`; compare is `execute_trace(ir, trace)`"*. `crates/fsm-cli/src/cmd/test.rs:37` `use fsm_simulator::{execute_trace, parse_trace_yaml};` | ✅ **CONFIRMED** — the keystone oracle is the *shipped* `fsm_simulator` `StepRecord` via the *same* `execute_trace` seam the CLI uses; W1/W2 drive THIS oracle, the no-fork invariant is exactly the v1.4/v1.5 class restated for the target |
| GT-5 | The live `DiagnosticCode` count = 73 via the `EXPECTED` lock (the proven build-failing pattern G7 mirrors) | `crates/fsm-diagnostics/src/lib.rs:820` `const EXPECTED: usize = 73;` inside `#[test] fn all_codes_matches_expected_count()` → `assert_eq!(DiagnosticCode::all_codes().len(), EXPECTED, "CODE_TABLE length drifted from EXPECTED")`. Comment: *"Live variants: 73 (51 E + 12 W + 4 I + 6 H)"* | ✅ **CONFIRMED** — 73 live codes; the `EXPECTED:73` build-failing lock is the *exact* proven pattern G7's enforcement test mirrors (derive the target set from the **compiled live enum**, not a doc) |
| GT-6 | The host gcc gate exists (the surface W1 wires the `FSM_TRACE` hook into) | `crates/fsm-codegen-c/tests/gcc_compile.rs` (module doc: *"End-to-end gcc compilation test — Doc 11 §1, gate G5 … invokes `gcc -std=c99 -Wall -Wextra -Wpedantic -Werror` … If gcc unavailable … prints a skip notice and exits successfully"*); + sibling `*_runs.rs`/`*_gcc.rs` (`opaque_extern_runs.rs`, `parallel_dispatch_runs.rs`, `defer_codegen_runs.rs`, …) + `crates/fsm-cli/tests/vending_machine_gcc.rs`. gcc IS on the box | ✅ **CONFIRMED** — the host gcc gate is real, skip-if-absent, gcc-on-box; W1's host-trace differential is **locally provable with the existing gcc, zero new install** (the keystone-prerequisite "locally provable" claim is true) |
| GT-7 | The MVP on-target corpus = the example FSMs with already-frozen traces | `examples/*/`: **5** FSMs have *both* a `.fsm` and a frozen `.trace`: `motor`, `submachine`, `traffic-light`, `vending-machine`, `deferred` (each also has a `.steps.json`). `examples/verify/` has `clean.fsm`/`deadlocks.fsm` but no `.trace`; `import-header`/`integration`/`per-machine-strategy` have no `.trace` | ⚠️ **CONFIRMED with a FLAGGED count divergence** — the accepted-architecture said *"~8 example FSMs with already-frozen traces"*; ground truth is **5** (motor, submachine, traffic-light, vending-machine, deferred). **The MVP corpus is sized to these 5** (the others are not frozen-trace examples; `verify/` is a verify-not-trace fixture). The *decision* (MVP corpus = the frozen-trace examples) is unchanged; the *number* is corrected 8→5 (do not over-promise the corpus) |
| GT-8 | A conformance fixture per diagnostic code is the G7 gap (~10 of 73 covered) | `tests/conformance/MANIFEST.json` (`{version, categories[]}` — per-category `fixtures[]` with `id`/`kind`/`path`/`code`). Distinct `FSM-[EW]\d{4}` codes asserted *in the MANIFEST*: **11** (`E0101 E0104 E0107 E0108 E0111 E0201 E0300 E0301 E0410 E0600 W0200`). `GATE_VERIFICATION_v1_5.md` §"Pinned gate numbers" records **conformance=26** (26 fixtures total, asserting ~11 distinct codes) | ⚠️ **CONFIRMED with a FLAGGED precision correction** — the gap is real and severe (only **~11 of 73** live codes have an exact-set *conformance* fixture; 26 conformance fixtures total). The architecture's "~10" is directionally right *for the conformance-fixture axis*. **But the precise statement matters:** this is **conformance-fixture** coverage, distinct from `COVERAGE_MAP.md`'s "Tested 39" which counts *emitter unit tests* (a different axis). W3's lock test must be derived from the **live enum** and judged over **conformance fixtures** (the exact-set discipline), not conflated with emitter-unit-test counts |
| GT-9 | `COVERAGE_MAP.md` is itself drifted (78/75/73) — regenerate it as a generated artifact | `tests/conformance/COVERAGE_MAP.md`: head text says *"75 in v1.0.0 … −1 (E0903 v1.1) … −1 (W0500 v1.2) … live enum = 73"*; the Summary table says **Tested 39 / UNTESTED 34 / RETIRED markers 2 / Total rows 75**. The "73 live" matches the GT-5 lock; the **39/34 split is hand-maintained and drift-prone** (a manual table claiming a per-code test-status it does not derive from source) | ⚠️ **CONFIRMED with a FLAGGED shape correction** — the drift is real but its *shape* is **75→73 internal reconciliation + a hand-maintained 39/34 Tested/UNTESTED split**, not a clean "78/75/73". The fold-in stands: **regenerate `COVERAGE_MAP.md` as a generated artifact** derived from the live enum × the conformance MANIFEST (W3's lock test is the generator's source of truth) — a hand-maintained coverage map IS a drift surface (the same class as the v1.x doc-honesty lesson) |
| GT-10 | The box: `act` absent, `docker` present; `arm-none-eabi-gcc`/`qemu-system-arm`/`renode` absent; disk tight | `which act` → ABSENT; `which docker` → `/usr/bin/docker` PRESENT; `arm-none-eabi-gcc`/`qemu-system-arm`/`renode` → all ABSENT; `cargo-llvm-cov` → ABSENT; `df -h /` → `78G total, 65G used, 8.8G avail, 89%` | ✅ **CONFIRMED** — the **CI-first box default is the correct safe default**: nothing heavy on this disk-tight box; `act` installable but pulls multi-GB images (Docker IS present, so `act` *can* run once installed); the on-target/coverage matrix is deliberately CI-runner-only; W1's keystone is the locally-provable part (existing gcc) |
| GT-11 | `c8` is net-new for the extension; `@vscode/test-electron` (the `xvfb-run` host harness) already exists | `editors/vscode/package.json`: `@vscode/test-electron: ^2.3.9` PRESENT (devDeps); `c8` **ABSENT** from devDependencies. Scripts: `test: node ./out/test/runTest.js`, `pretest`/`test:v6` present; `format`/`format:check` (prettier, the v1.5 C2 adoption) present | ✅ **CONFIRMED** — W4's `c8` is genuinely net-new (the extension's *own coverage*, not smoke); the `xvfb-run @vscode/test-electron` host harness already shipped (v1.5 A1/A2/C2 — the 41-test Extension-Host suite), so W4/W5 *add coverage to an existing host harness*, not a new harness |
| GT-12 | The §11.30 cold-quad + NOTE-1 STEP 0 + the re-activated JS lane mechanics are canonical (W6 re-uses them verbatim) | `docs/GATE_VERIFICATION_v1_5.md` §4 / §4.2 / §4.3: the release sequence (commit `X` → cold quad **at `X`** with the **NOTE-1 explicit `cargo clean -p` STEP 0** of ALL first-party crates → tag at `X`) + the **re-activated JS lane** (binding because the surface touched `editors/vscode/**`) is the canonical, just-exercised (5th consecutive prospective-clean) pattern; `crates/fsm-diagnostics/src/lib.rs:820` confirms the `EXPECTED:73` lock the gate doc pins | ✅ **CONFIRMED** — W6 re-uses the *identical* §11.30 closeout mechanics (no new release machinery); Phase-6.0 touches `editors/vscode/**` (W4 `c8`, W5 lanes) ⇒ the JS lane is **binding** at the gate-doc commit, exactly as v1.5 |

**Net result of the verify-as-you-write pass:** the accepted Phase-6.0 architecture **survives ground-truth re-verification**, with **three flagged number/precision corrections** (GT-7: the MVP corpus is **5** frozen-trace examples, not ~8; GT-8: the conformance gap is **~11 of 73** distinct codes via the MANIFEST — a *conformance-fixture* axis distinct from `COVERAGE_MAP.md`'s emitter-unit "39 Tested"; GT-9: `COVERAGE_MAP.md`'s drift is **75→73 + a hand-maintained 39/34 split**, the regenerate-as-artifact finding stands). **No architectural *decision* failed verification** — the keystone-prerequisite ordering (GT-3, the R7 placeholder is genuinely absent), the keystone oracle (GT-4, the shipped `StepRecord`/`execute_trace` seam), the proven `EXPECTED:73` lock pattern (GT-5), the host gcc gate's local-provability (GT-6), the additive-`stable`-job CI pattern (GT-1), and the §11.30 closeout mechanics (GT-12) are all exactly as the architecture states. The corrections **tighten** the plan (a 5-FSM MVP corpus is honest; a conformance-axis-precise G7 lock test is correct).

---

## 1. The 7 waves as the numbered wave table

Each implementer wave is a real **§5.4 behavioural-acceptance** gate. **For the differential lanes, §5.4 means *the generated-C trace (host or target) byte-equals the `fsm_simulator` `StepRecord` oracle on a real FSM* — never "the hook compiles", never "the lane is added to the YAML".** Depth-first = each wave delivers a thin-but-complete vertical slice with a real behavioural gate before the next builds on it (the v1.2 L1-spine / v1.3 V1-spine / v1.4 W1-spine / v1.5 A1-spine precedent).

### W0 — This plan-of-record (the design-first opener)

| Field | Content |
|---|---|
| **Scope** | Formalize the accepted, owner-endorsed architecture into the in-repo plan-of-record (the exact v1.5-A0→Doc 31 precedent: formalize the accepted architecture BEFORE any implementer wave), verifying every load-bearing ground-truth claim from source as written. |
| **Deliverable** | This single file `docs/32-Factory-Reliability-CI-Wave-Plan.md`. No code, no other doc, no build, no push. |
| **§5.4 behavioural acceptance** | (Planning wave — acceptance is *plan-integrity*, the Doc 31-W0 precedent.) The wave table, the QEMU decision + the R7-host-first keystone-prerequisite, the coverage-ratchet/G7/ci.yml specs, the box CI-first default, the §5.2-form gate-criteria (incl. the `make ci-local` line + the §11.30 NOTE-1 STEP 0 + the binding JS lane), the owner-action split, the publish-readiness framing, the risks table, and the GT-verification ledger with every divergence flagged are all present and internally consistent; every load-bearing claim is verified `file:line` from source. |
| **Parallel/seq** | **Sequential — first** (the epic opener; every implementer wave builds on this formalized plan). |
| **Worktree** | `phase6.0/factory-w0-doc32` (this branch; the orchestrator gates/merges). |

### W1 — The R7 generic host-trace differential (the keystone PREREQUISITE; gcc-only, locally provable)

| Field | Content |
|---|---|
| **Scope** | Build the **missing host C↔simulator *trace* differential** (the R7 v1.0 placeholder, GT-3). A codegen-emitted, compile-time-gated `#ifdef FSM_TRACE` instrumentation hook in the generated C runtime that emits a **`StepRecord`-isomorphic** record stream (the Doc 13 §11 / `crates/fsm-simulator/src/trace.rs:43` shape, GT-4) for each step. Wire it into the **existing host gcc gate** (`crates/fsm-codegen-c/tests/gcc_compile.rs` + the sibling `*_runs.rs` infra, GT-6) so the generated C's emitted trace is **diffed byte-equal against the `fsm_simulator` `StepRecord` oracle** (`execute_trace` — the *same* seam `cmd/{test,baseline}.rs` use, GT-4). Flip the `codegen_equivalence_smoke.rs` placeholder's "skip" path to the real diffing path it explicitly anticipates. |
| **Deliverable** | (1) The `#ifdef FSM_TRACE` codegen hook (compile-time-gated — **off** in normal/shipped codegen, so the production C is byte-unchanged; the v1.x append-only-instrumentation discipline); (2) the host harness that compiles with `-DFSM_TRACE`, runs, and asserts the emitted record stream **deep-equal + canonical-bytes-equal** to `fsm_simulator::execute_trace(ir, &trace)`'s `StepRecord` list on the MVP corpus (the 5 frozen-trace examples, GT-7); (3) the `codegen_equivalence_smoke.rs` placeholder retired into the real gate (the smoke→real flip the placeholder's own doc anticipates). The oracle is the **shipped** `fsm_simulator` — **NEVER a forked re-derivation** (the keystone). |
| **§5.4 behavioural acceptance** | A host test where, on **each of the 5 frozen-trace example FSMs** (GT-7), the generated-C `FSM_TRACE` record stream (compiled with the existing on-box gcc, run on host) **byte-equals** `fsm_simulator::execute_trace` on the same IR + trace (the differential-oracle proof, **locally provable, zero new install**). At least one fixture is a *behaviourally non-trivial* path (a guard/timer/submachine step, not just START/STOP) so the differential exercises real semantics, not a degenerate cycle. Symbol-presence ("the hook emits *something*") is explicitly **not** acceptance — the records must be byte-equal to the oracle. |
| **Parallel/seq** | **Sequential — the keystone prerequisite, BEFORE W2.** W2's on-target lane *extends the identical `FSM_TRACE` C*; building W2 before W1 is the roof-before-the-wall error (the §0 single-most-important sequencing fact). |
| **Worktree** | `phase6.1/factory-w1-r7-host-trace` (writer worktree, isolated per §6 H4). |
| **Keystone** | W1 **establishes** the differential whose oracle is the shipped `fsm_simulator` `StepRecord` via `execute_trace` (GT-4). The codegen hook is *instrumentation that re-emits the same semantics the simulator computes*, not a second semantics — the W6 phase-audit re-derives this from source (negative-grep: no step-transition/guard-eval logic *defined* in the trace-hook or harness; the diff target is `execute_trace`, not a fork). |

### W2 — The on-target QEMU lane (the keystone PROPER; MVP corpus)

| Field | Content |
|---|---|
| **Scope** | Extend the **identical** `FSM_TRACE` C from W1 to an **emulated MCU**: cross-compile with `arm-none-eabi-gcc`, link a minimal Cortex-M3 startup + semihosting retarget, run under **`qemu-system-arm -M mps2-an385`** (Cortex-M3, semihosting for the trace I/O + exit code), and assert the target's emitted `FSM_TRACE` record stream is **byte-equal to the SAME `fsm_simulator` `StepRecord` oracle** W1 diffs against (the host-side differential, extended to the target — the owner's literal ask, item (1)). |
| **Emulator decision (DECIDED — encode the rationale)** | **QEMU `mps2-an385` Cortex-M3 over Renode.** Rationale, binding: (a) **determinism for a differential oracle** — the oracle is byte-equality of a `StepRecord` stream; QEMU's instruction-accurate deterministic execution is exactly what a byte-differential needs; peripheral-timing fidelity is **irrelevant to this oracle** (the FSM runtime uses the `fsm_hal_*` clock seam, not real peripherals — the same host-HAL counter-clock pattern `gcc_compile.rs` already uses); (b) **one-`apt`-line CI-installability** — `qemu-system-arm` + `gcc-arm-none-eabi` are a single `apt-get install` in a CI runner (the canonical embedded-CI pattern; Renode needs a heavier mono/.NET install + a board script DSL); (c) **the canonical embedded-CI pattern** — QEMU+semihosting Cortex-M is the industry-standard CI smoke for transpiled embedded C; (d) **Renode's peripheral fidelity is irrelevant to this oracle** — Renode's value (UART/I2C/SPI peripheral models, multi-node) is not exercised by a `StepRecord` byte-differential. This is a **reversible, isolated-lane choice** (the lane is one additive CI job; switching emulators later is a contained edit, not an architecture migration — the Doc 30 R5 "alternatives are the menu, not mid-epic drift" discipline). |
| **Deliverable** | (1) A minimal `mps2-an385` Cortex-M3 target harness (startup + semihosting retarget for the `FSM_TRACE` stdout + `qemu` exit code; the `fsm_hal_*` clock seam stubbed deterministically — the established host-HAL counter-clock pattern); (2) the cross-compile + run wired as the **CI-runner-only `on-target` lane** (`apt-get install qemu-system-arm gcc-arm-none-eabi` in the runner — never on the box, GT-10 / OWNER-2); (3) the assertion that the **target** `FSM_TRACE` stream byte-equals `fsm_simulator::execute_trace` on the **MVP corpus = the 5 frozen-trace examples** (GT-7). The oracle is the **same shipped `fsm_simulator`** W1 uses — **NEVER a forked comparator** (the keystone). |
| **§5.4 behavioural acceptance** | On **each of the 5 MVP corpus FSMs** (GT-7), the QEMU-`mps2-an385` target's emitted `FSM_TRACE` record stream **byte-equals** `fsm_simulator::execute_trace` on the same IR+trace (the *host* differential extended to the *target* — the owner's item-(1) ask, end-to-end). At least one fixture exercises a non-trivial path (guard/timer/submachine). "The C cross-compiles" / "QEMU boots" is explicitly **not** acceptance — the *trace* must be byte-equal to the oracle. **Locally:** the *logic* is already smoke-proven by W1's host differential (zero cross-toolchain on the box by design — the on-target lane's correctness rests on W1's locally-provable host differential + the CI run). |
| **Parallel/seq** | **Sequential — after W1** (W2 extends W1's identical `FSM_TRACE` C; W1 is W2's prerequisite). |
| **Worktree** | `phase6.2/factory-w2-on-target-qemu` (writer worktree). |
| **Keystone — feeds the W6 source-derived phase-audit** | W2 adds the target harness + the `on-target` CI glue but **NOT a second oracle** — the comparison target stays `fsm_simulator::execute_trace`. The W6 phase-audit independently re-derives from source that no step/transition/guard semantics is *defined* in the QEMU harness or the CI glue (negative-grep ∅ on non-comment lines) and re-runs the differential (the v1.4/v1.5 keystone-audit method, applied to the target lane). |

### W3 — G7 enforcement (live-enum-derived lock) + bulk conformance fixtures

| Field | Content |
|---|---|
| **Scope** | Turn the G7 "≥1 conformance fixture per diagnostic code" gap (GT-8 — **~11 of 73** distinct codes covered via the MANIFEST) from aspiration into a **build-failing lock test**, derived from the **compiled live `DiagnosticCode` enum** (`DiagnosticCode::all_codes()` — NOT `COVERAGE_MAP.md`, which is itself drifted, GT-9). + author the bulk of the missing conformance fixtures. |
| **Deliverable** | (1) A **build-failing lock test** mirroring the proven `EXPECTED:73` pattern (`crates/fsm-diagnostics/src/lib.rs:820`, GT-5): for every live code in `DiagnosticCode::all_codes()`, it must be **either** a conformance fixture in the MANIFEST that triggers **exactly that code** (the exact-set discipline — the fixture triggers *that* code, not a superset) **or** in an enumerated, justified **`non_fixture_codes` allowlist** (codes only reachable via `fsm verify` / a non-conformance path, **each with a pointer to where it *is* tested** — never a silent omission); the test **fails the build** if a live code is in neither set (the `EXPECTED`-lock class). (2) `tests/conformance/COVERAGE_MAP.md` **regenerated as a generated artifact** from `the live enum × the MANIFEST` (GT-9 — a hand-maintained coverage map IS a drift surface; the lock test is the generator's source of truth). (3) The bulk of the missing conformance fixtures authored (the deferrable exhaustive tail — §6 R-fallback). |
| **§5.4 behavioural acceptance** | The lock test **fails the build** when a live `DiagnosticCode` variant has neither a conformance fixture nor a `non_fixture_codes` entry (asserted by a deliberate temporary removal in the test's own scaffolding, the `EXPECTED:73`-lock proof method); each newly-authored conformance fixture, run through the conformance harness, triggers **exactly** its target code (exact-set, behaviourally — not "the fixture exists"); the regenerated `COVERAGE_MAP.md` is byte-derivable from the live enum × the MANIFEST (no hand-maintained 39/34 split). Symbol-presence (a `.fsm` file exists) is **not** acceptance — the fixture must trigger exactly the target code. |
| **Parallel/seq** | **Sequential — after W2** (W3 is independent of the on-target lane but the depth-first spine keeps one build-heavy wave at a time; the *mechanism* (the lock test + the regenerated map) is the MVP, the *exhaustive* fixture-authoring is the deferrable tail). |
| **Worktree** | `phase6.3/factory-w3-g7-enforcement` (writer worktree). |

### W4 — Coverage ratchet gates (Rust `cargo-llvm-cov` + extension `c8`)

| Field | Content |
|---|---|
| **Scope** | Turn "100% covered" from aspiration into an **enforced, monotonic ratchet**. Rust: `cargo-llvm-cov` **branch** coverage as its **own additive-`stable` CI job** (the proven `sca`-job pattern, GT-1 — the 1.75 pin **untouched**). Extension: `c8` (V8 coverage) for `editors/vscode/src/**` under the **existing `xvfb-run @vscode/test-electron` host** (GT-11 — the extension's *own coverage, not smoke*; `c8` is net-new, the host harness already shipped). |
| **Deliverable** | (1) A `coverage-rust` CI job: `rustup toolchain install stable` → `cargo +stable install cargo-llvm-cov` → branch-coverage with a **per-package floor gate** (the exact additive-isolated-job pattern as `sca`, GT-1 — never gates the 1.75 matrix). (2) `c8` wired into the extension's `xvfb-run` host run with a per-area floor. (3) **Ratchet-from-measured-floor:** Day-1 enforced per-package floor = `floor(measured − 2%)`, **monotonic non-decreasing** (a floor never drops; it only ratchets up). The **recorded climbed-to targets** are 85/75 line/branch; **`fsm-verify` stricter (90/80)**; an **enumerated, reviewed exclusion allowlist** for genuinely-uncoverable arms (each entry justified — e.g. an `unreachable!()` on a type-system-guaranteed-impossible variant). **NEVER a blanket lower number; NEVER coverage-gaming tests** (the gate accepts only **behavioural §5.4** tests — a test that executes a line without asserting behaviour does not count). |
| **§5.4 behavioural acceptance** | The `coverage-rust` job **fails CI** when a package drops below its ratcheted floor (asserted by a deliberate floor-bump-without-tests in the job's own scaffolding proof); the `c8` extension gate **fails** when `editors/vscode/src/**` drops below its floor; the floor is **measured first, then `floor(measured−2%)` enforced** (no guessed number); the exclusion allowlist is enumerated with a per-entry rationale (no blanket exclusion). A coverage delta achieved by an assertion-free test is rejected in review (the §5.4 behavioural-not-symbol-presence discipline applied to coverage). |
| **Parallel/seq** | **Sequential — after W3.** Could in principle parallelize with W3 (file-disjoint: a CI job + coverage config vs the conformance lock test), but the depth-first one-build-heavy-wave-at-a-time posture (§6 H1, the box is disk-tight, GT-10) sequences it. |
| **Worktree** | `phase6.4/factory-w4-coverage-ratchet` (writer worktree). |

### W5 — ci.yml integration + the one-command `make ci-local` gate (an owner explicit ask — FIRST-CLASS)

| Field | Content |
|---|---|
| **Scope** | Wire **all** of it into `.github/workflows/ci.yml` so it runs automatically on push (the owner's item (4)): **keep** `build`(matrix)+`sca` (GT-1, untouched); **add** `conformance` (the W3 lock test + the corpus), `extension-host` (`xvfb-run @vscode/test-electron` + the W4 `c8` gate), `coverage-rust` (the W4 additive-`stable` job), `on-target` (`apt-get install qemu-system-arm gcc-arm-none-eabi` in the runner + the W2 lane). **+ the one-command local quality gate** (the owner's explicit ask — a **FIRST-CLASS deliverable**). |
| **The `make ci-local` gate (owner's explicit ask — encode it precisely)** | A **`make ci-local`** (or equivalent single command) that runs *everything runnable WITHOUT a push*: (a) the per-wave **quad** (`cargo fmt --check`/`clippy -D warnings`/`build`/`test` on the 1.75 pin); (b) **conformance** (the W3 lock test + the corpus); (c) the **`xvfb-run` Extension-Host JS lane** (the 41-test host suite + the `c8` gate, GT-11); (d) an **`act` dry-run of the *Linux* `build`/`sca` ci.yml lanes** (the YAML-on-the-Linux-runner shape, locally); (e) a **workflow-lint of the new lanes' YAML** (the YAML is syntactically + structurally valid). **Honest scope (encode it verbatim — the `[[feedback_infra_constraint_escalation]]` honest-bottleneck discipline):** `act` is **absent but installable** (GT-10) and **Docker IS present** (so `act` *can* run once installed); the box is **disk-tight (~8.8G, 89%)** and `act` pulls **multi-GB runner images** — so the `act` lane is the *Linux build/sca* lanes only (NOT the heavy matrix); the **`on-target`/`coverage` matrix is deliberately CI-runner-only** (the CI-first default, OWNER-2). Therefore the local gate covers **quad + conformance + JS-lane + Linux-CI-`act`-dry-run + workflow-lint** (high confidence), while the **on-target *logic* is locally smoke-proven by W1's host differential** (zero cross-toolchain by design). This **materially de-risks G9** (the push becomes a *confirmation/switch*, not a leap — the owner can run one command and know it is green *before* any push). |
| **Deliverable** | (1) The 4 new ci.yml lanes added (`build`+`sca` kept untouched, GT-1); per-push = **warm-cache fast net**; the §11.30 **cold-from-source quad = the tag-gate event only** (W6); every new heavy install is **CI-runner-only** (the additive-job isolation — the 1.75 matrix never contaminated, the `sca`-job precedent). (2) The `make ci-local` one-command gate, with its honest-scope documented inline (the `act`-Linux-only + on-target-CI-only + W1-host-smoke-proven framing). |
| **§5.4 behavioural acceptance** | `make ci-local` runs end-to-end and **a deliberately-broken commit (a failing test / a clippy warning / a malformed lane YAML) makes it exit non-zero** (it actually gates, it is not a no-op); the new ci.yml lanes are workflow-lint-clean and structurally reference the W2/W3/W4 artifacts; the `act` dry-run executes the *Linux* `build`/`sca` lanes (or, if `act` is not installed locally, the gate **clearly reports that lane as skipped-pending-`act`-install** rather than silently passing — honest skip, the `gcc_compile.rs` skip-notice precedent). Symbol-presence ("a Makefile target named `ci-local` exists") is **not** acceptance — it must actually fail on a broken tree. |
| **Parallel/seq** | **Sequential — after W4** (it integrates W2/W3/W4's lanes; it cannot wire lanes that do not yet exist). |
| **Worktree** | `phase6.5/factory-w5-ci-integration` (writer worktree). |

### W6 — Phase-audit + pre-tag four-lens + GATE + §11.30 cold-quad + tag (the closeout)

| Field | Content |
|---|---|
| **Scope** | The §11.3 phase-audit (the W1/W2 **keystone re-derive from source**), the standing independent pre-tag four-lens audit, the `GATE_VERIFICATION_<tag>.md` + the batched Doc 00 §11 / CHANGELOG / ROADMAP consolidation, the §11.30 cold-from-source quad **WITH the NOTE-1 STEP 0** + **the binding JS lane**, the tag. |
| **Deliverable** | (1) A **mandatory source-derived keystone phase-audit** (the v1.4 W1/W2 / v1.5 post-A2 precedent): independently re-derive from source (NOT this doc, NOT a wave's self-report) that the differential oracle is the shipped `fsm_simulator::execute_trace`/`StepRecord` (GT-4) and **NO** step/transition/guard/deadlock semantics is *defined* in the W1 trace-hook, the W2 QEMU harness, or the CI glue (negative-grep ∅ on non-comment lines), **and** re-run the host + target differential battery on the MVP corpus (byte-equal). Frozen evidence doc, never overwritten (the `AUDIT_PHASE_*` convention). (2) The independent pre-tag four-lens (0 open P0, ≤5 well-scoped P1 — the v1.1/v1.2/v1.3/v1.4/v1.5 §3 standard). (3) `GATE_VERIFICATION_<tag>.md` + the batched closeout (the §5 sub-steps). (4) The §11.30 cold-quad **at the gate-doc commit `X`** WITH the **NOTE-1 explicit `cargo clean -p` STEP 0** + the **binding JS lane** (GT-12). (5) The tag at `X`. |
| **§5.4 behavioural acceptance** | (Closeout wave — acceptance is the §5 gate-criteria, the v1.4/v1.5 W-closeout precedent.) The keystone phase-audit returns **keystone-intact, re-derived from source** (not echoed); the pre-tag four-lens returns 0 open P0; the cold-quad + the binding JS lane are green at `X`; the §5 closeout sub-steps (memory-SoT freshness + release-record symmetry) are executed *in the same closeout pass*. A `completed`-envelope wrapping an error is **not** a verdict (§6 H7 — independently probe ground truth before any gate advance). |
| **Parallel/seq** | **Sequential — LAST.** Depends on every implementer wave. The §11.3 phase-boundary cadence is **at minimum the W1/W2 keystone phase-audit** (the only fork-possible boundary — the v1.4/v1.5 keystone-first precedent) plus the standing pre-tag four-lens; the periodic-audit cadence (`[[feedback_periodic_audits]]`, every 2–3 implementer batches) is pre-budgeted per §7. |
| **Worktree** | `phase6.6/factory-w6-closeout` (writer worktree; the orchestrator places the tag, never an implementer — `[[feedback_agent_no_reset]]` / the no-push rule). |

---

## 2. The "drive-the-oracle, never-fork" keystone — the target surface (a BINDING tag-gate row — Doc-31-§2 form)

> This is the load-bearing structural invariant of the epic, stated in the binding gate-row form (the `GATE_VERIFICATION_v1_5.md` §2/§5 / Doc 31 §2 pattern). It is **NOT a fresh decision** — it is the *target/CI-surface* application of the epic-level architecture invariant the project codified at v1.4 (Doc 00 §11.63) and re-applied at the editor surface in v1.5 (Doc 31 §2). The verifier/differential **drives the shipped semantics, never forks them**. v1.5 extended it from the CLI to the editor; **Phase-6.0 extends it from the host to the target and into CI.**

**KEYSTONE — DRIVE-THE-ORACLE-NEVER-FORK (binding Phase-6.0 tag-gate criterion):**

> The differential oracle is **always** the shipped `fsm_simulator` `StepRecord` via `fsm_simulator::execute_trace` (`crates/fsm-simulator/src/trace.rs:43` + the *same* seam `crates/fsm-cli/src/cmd/{test,baseline}.rs` use — GT-4). The generated-C trace — **host (W1)** *and* **emulated-target (W2)** — is asserted **byte-equal to that oracle** on the MVP corpus (the 5 frozen-trace examples — GT-7). There is **NO** forked comparator, **NO** second `StepRecord` re-derivation, **NO** re-implemented step / transition-selection / guard-eval / deadlock semantics in the W1 codegen trace-hook, the W2 QEMU/`mps2-an385` harness, or the W5 CI glue (Rust *or* C *or* YAML). The **W6 §11.3 phase-audit re-derives this FROM SOURCE** (negative-grep ∅ on non-comment lines across the W1 trace-hook + the W2 target harness + the CI glue; the host+target byte-equality differential **re-run by the auditor on the MVP corpus**) — **not** from this doc's claim and **not** from any wave's self-report (the §11.29 / v1.4-L1 / v1.5-post-A2 verify-the-record rigor — the v1.5 closeout's own discipline: *"a `KEYSTONE-INTACT` verdict … is worse than a found problem if it merely echoes the wave's own claim"*). **A forked oracle is the cardinal regression** — the exact P0-1 / MV5-1 / v1.4-keystone / v1.5-keystone-in-UI class the project keeps re-learning. This row PASSES iff the W6 source-derived audit returns the keystone intact AND the host+target differential battery is byte-equal on the MVP corpus.

This is one auditable checkpoint by construction: the only places a fork could enter are **W1** (the codegen trace-hook) and **W2** (the target harness). Concentrating the fork-possible surface into two sequential gated waves with a single mandatory W6 source-derived audit is the structural realization of "keystone enforcement = a single named, budgeted checkpoint" (the v1.4 L1/L7 + v1.5 post-A2 lesson, applied to the target/CI surface). **W1's instrumentation re-emits the semantics `fsm_simulator` already computes** (it is a trace-tap, not a second interpreter); **W2 extends that identical instrumented C to a different execution substrate** (QEMU) — the substrate changes, the *oracle* does not.

---

## 3. Certifiability / MISRA–CERT-C codegen-mode track — STILL the PARKED OWNER-C (this epic = the proof-half only)

> Surfaced exactly as Doc 30 §3 / Doc 31 §3 surfaced it: a crisp one-paragraph value/cost framing with the recommended slot and an explicit owner GO/defer, **NOT pre-decided**, **NOT auto-scoped into this epic**.

**The item.** `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART A ranks **#1** a documented **MISRA-C / CERT-C compliance posture for the generated C** (a `--profile misra` codegen-mode + a rule-conformance matrix). The owner-endorsed 2026-05-17 Directive (`memory/project_user_messages_embeded_fsm_2026_05_17_testing_gap_factory_epic.md` item 6) **explicitly scoped this epic to the factory-reliability *proof* layer (test/coverage/CI/on-target) ONLY** and stated the MISRA/CERT-C *codegen-mode* half is **NOT in this directive's words — keep it a separate owner decision unless the owner folds it in.**

**Value/cost framing (one paragraph, for the owner).** *Value:* without a compliance posture the named factory/safety audience structurally cannot adopt the tool into a regulated line — the single highest *strategic* leverage gap; it composes with this epic's on-target proof into a coherent Certifiability arc. *Cost:* a MISRA/CERT-C rule-conformance matrix + a `--profile misra` codegen-mode + a conformance corpus is a substantial dedicated minor — explicitly **NOT** a testing/proof-layer item; folding it into Phase-6.0 would reproduce the mega-minor anti-pattern Doc 30 §2.2 / Doc 31 §3 rejected. *Recommended slot:* its **own gated epic, sequenced after this factory-reliability proof epic** (the same "separate minor, not one mega-minor" discipline as Doc 30 §2.2.3 / Doc 31 §3).

**Owner decision (PARKED — non-blocking default).** This is **OWNER-3** in §4. **Default if the owner is silent:** Phase-6.0 proceeds as the factory-reliability *proof* layer **without** any MISRA/CERT-C codegen work; the Certifiability codegen-mode arc stays a **named, parked candidate** (recorded in `GATE_VERIFICATION_<tag>.md` §"carried owner-escalations" so it is not silently dropped — the §11.62 / v1.5 §6 discipline). The owner's explicit GO is required to *schedule* it; absent that GO it does **not** enter this epic and is **not** auto-started.

---

## 4. Owner-action vs proceed-autonomously split (safe parked defaults — per `[[feedback_infra_constraint_escalation]]`)

> The architecture is **explicitly owner-endorsed** (the owner endorsed the drafted Directive verbatim + reaffirmed full TL/PM autonomy + this session said "keep going / no push needed now / on-target-vision confirmed" ⇒ the safe defaults are **owner-affirmed**). So **everything architectural = proceed-autonomously**; the only residuals are the three posture choices below, each with a **non-blocking safe parked default** so no wave ever stalls on the owner (the Doc 30 §3 / Doc 31 §4 escalation pattern; the `[[feedback_dont_ask_technical_permission]]` line — engineering hygiene ships, only product-strategy/destructive/credential choices escalate).

| ID | Owner decision | Safe parked default (owner-affirmed this session — proceed on this) |
|---|---|---|
| **OWNER-1** | **G9 — the owner's publication / CI switch.** *Reframed by this epic:* G9 is now needed for the **automatic remote pipeline + publication to ever exist** — it is **NOT** needed for quality OR progress (both are **local**: the per-wave quad every wave + the new `make ci-local` gate). The local-only-repo rule (owner controls the remote) is a hard project rule. | **Build CI correct + ready + the `make ci-local` gate; record the realized push-and-it-runs payoff as contingent on the owner's push** (`GATE_VERIFICATION_<tag>.md` §"carried owner-escalations", the §11.62 / v1.5 §6 re-state-don't-drop discipline — G9 carried unrun from v1.3/v1.4/v1.5, now with the new lanes ready). The tag is placed **LOCAL, §11.30-clean** regardless; G9 is the owner's switch for **when they choose to publish**. |
| **OWNER-2** | **The CI-first box default** — nothing heavy installed on this disk-tight box (`qemu-system-arm`/`gcc-arm-none-eabi`/coverage-instruments are CI-runner-only). A small one-time local cross-toolchain install is the explicit owner-fork **ONLY** if a harness wave proves it needed (likely **not**, by design — W1's keystone is locally provable with the existing on-box gcc, GT-6/GT-10). | **Nothing on the box** (the CI-first default — GT-10: box ~8.8G/89%, `qemu`/`arm-gcc`/`renode`/`llvm-cov` all absent). W1's keystone is locally provable with the existing gcc; the on-target/coverage matrix is CI-runner-only (the additive-job isolation, the `sca`-job precedent). **Never grind the box** (`[[feedback_infra_constraint_escalation]]`); surface a one-time local install as the explicit owner-fork only if a wave proves it strictly needed (the safe default is it is not). |
| **OWNER-3** | **The MISRA/CERT-C codegen-mode track GO** (§3 — a strategic ROADMAP-scheduling decision; the parked Certifiability *codegen-mode* half, explicitly NOT in this directive's words). | **PARKED.** Phase-6.0 proceeds as the factory-reliability *proof* layer with **no** MISRA/CERT-C codegen work; the arc stays a named parked candidate recorded in the gate doc's carried-escalations. Requires explicit owner GO to schedule. |

**Everything else = proceed autonomously.** The wave sequencing, the R7-host-first keystone-prerequisite ordering, the QEMU-over-Renode decision, the `make ci-local` design, the G7 live-enum lock, the coverage-ratchet-from-measured-floor, the ci.yml lane integration, the worktree isolation, the §11.3/§11.30 release mechanics — all are delegated and require no owner sign-off (engineering hygiene ships; only the three product-strategy/credential residuals above escalate, each with a non-blocking owner-affirmed default).

---

## 5. §11.30-clean Phase-6.0 tag-gate criteria (Doc-31-§5 / `GATE_VERIFICATION_v1_5.md`-§4 form)

> The `GATE_VERIFICATION_v1_5.md` §1/§4/§5.2 / Doc 31 §5 gate-row form, applied to Phase-6.0. The **6th consecutive prospective-clean release** (v1.1 retroactively reconciled §11.30; v1.2/v1.3/v1.4/v1.5 prospectively; Phase-6.0 prospectively).

- **KEYSTONE — DRIVE-THE-ORACLE gate row (§2) — BINDING.** The W6 source-derived §11.3 phase-audit returns the keystone intact (negative-grep ∅ across the W1 trace-hook + the W2 QEMU/`mps2-an385` harness + the W5 CI glue on non-comment lines) **AND** the **host (W1) + target (W2)** generated-C trace byte-equals `fsm_simulator::execute_trace`/`StepRecord` on the MVP corpus (the 5 frozen-trace examples — GT-7), **re-derived from source + re-run by the auditor**, not echoed. A forked oracle is the cardinal regression and a hard tag-blocker.
- **§5.4 behavioural-acceptance on every wave** — for the differential lanes: a *real FSM's generated-C trace (host AND target) byte-equals the `fsm_simulator` `StepRecord` oracle*, with at least one non-trivial path; for W3: the lock test *fails the build on a missing fixture*, each fixture *triggers exactly its code*; for W4: the coverage job *fails on a sub-floor drop*, the floor *measured-then-`floor(measured−2%)`*, **no coverage-gaming tests**; for W5: `make ci-local` *exits non-zero on a deliberately-broken tree*. **Symbol-presence is never acceptance** (a hook that emits *something*, a lane in the YAML, a Makefile target named `ci-local` ≠ the differential / the gate).
- **The one-command `make ci-local` gate — a NAMED gate line item (owner explicit ask).** The W5 `make ci-local` runs the per-wave **quad + conformance + the `xvfb-run` Extension-Host JS lane + the `act` Linux-`build`/`sca` dry-run + the new-lanes' workflow-lint** and **exits non-zero on a broken tree** (it materially de-risks G9 — the push is a confirmation, not a leap). Its honest scope is documented inline: the `on-target`/`coverage` matrix is CI-runner-only (OWNER-2) and the on-target *logic* is locally smoke-proven by W1's host differential; `act` covers the *Linux* `build`/`sca` lanes only (multi-GB images, box disk-tight, GT-10 — Docker present so `act` runs once installed; honest skip-pending-`act`-install if absent).
- **§11.30-clean cold-from-source quad green at the gate-doc commit** (§11.22 / §11.30 strictly; the `GATE_VERIFICATION_v1_5.md` §4 precedent, GT-12): the release sequence is (1) author+commit `GATE_VERIFICATION_<tag>.md` + the batched Doc 00 §11 / CHANGELOG / ROADMAP consolidation → commit `X`; (2) run the canonical cold quad **at `X`**; (3) `git tag -a <tag>` **at `X`** + `git tag checkpoint/<date>` **at `X`** — quad-commit ≡ tag-commit ≡ `X`, zero off-by-one by construction.
- **The MANDATORY NOTE-1 genuine-cold STEP 0** (`[[feedback_embeded_fsm_tag_cold_quad_method]]`; `GATE_VERIFICATION_v1_5.md` §4.2 / Doc 00 §11.70): **before** any full-workspace gate, run **`cargo clean -p` of ALL first-party workspace crates** (`fsm-diagnostics fsm-lexer fsm-parser fsm-ir fsm-analyzer fsm-codegen-c fsm-simulator fsm-formatter fsm-cli fsm-lsp fsm-verify`; cargo 1.75 needs the repeated `-p` form) **then** the canonical quad. The box-infeasible "fresh non-shared `CARGO_TARGET_DIR`" ideal is **explicitly rejected** (a virgin target rebuilds the ~14 GiB dep graph; the box runs ~8.8 GiB free, GT-10). The all-first-party `clean -p` evicts every first-party build+test artifact (incl. any stale `-wt-`-baked test binary — the v1.4 L3 *false-red* hazard) **while preserving the content-addressed registry dep cache**. The recorded disk-protective conventional-CI-cache equivalent, **not a workaround to grind past**.
- **The BINDING JS lane at the gate-doc commit** (Phase-6.0's surface **does touch `editors/vscode/**`** — W4 `c8`, W5 `extension-host` lane — *exactly like v1.5, unlike v1.4*, so the JS lane is **binding**, the `GATE_VERIFICATION_v1_5.md` §4.3 re-activated-JS-lane precedent, GT-11/GT-12): at commit `X`, `npm audit` (or the repo equivalent) clean/triaged + the **`xvfb-run @vscode/test-electron` Extension-Host suite green** + the **`c8` extension-coverage gate green**. The Rust cold quad and the binding JS lane are both gated at `X`.
- **`#![forbid(unsafe_code)]` invariant preserved.** W1 adds a codegen trace-hook + a host harness (no new crate root); W2 adds a target harness + CI glue (C/YAML, no new Rust crate); W3/W4/W5 add tests/CI config. The forbid-unsafe root count is **expected unchanged at 12 roots / 11 crates** (the v1.4/v1.5 figure). This is **re-derived from source at the gate-doc commit, not echoed** (`git grep -l '#![forbid(unsafe_code)]' -- 'crates/*/src/*.rs'` — the v1.4/v1.5 §"Pinned gate numbers" method); if any wave *does* add a crate this is flagged, not assumed.
- **Release-record symmetry check (the v1.4 L4 / v1.5 standing closeout sub-step).** The gate doc's Rust-delta framing **must be re-derived from `git diff`, not echoed** — Phase-6.0's Rust delta is *the W1 codegen trace-hook + the W3 lock test + the W4 coverage config + harness Rust* (W2's target harness is C; the CI glue is YAML). State it as *exactly that delta* — neither under-stated (the v1.3 "zero Rust delta" cardinal error) nor over-stated. Grep the record for the v1.3-cardinal-error form (`zero|no|empty .* (rust )?delta` → 0 hits) AND independently re-derive `git diff <v1.5-tag-commit> X -- crates/ Cargo.toml Cargo.lock` (`[[feedback_embeded_fsm_verify_record_release_layer]]`; `GATE_VERIFICATION_v1_5.md` §"Pinned gate numbers" precedent).
- **The G7/COVERAGE_MAP pinned-numbers re-derivation (the verify-the-record discipline applied to this epic's own numbers).** The gate doc MUST re-derive at `X`, from source not echoed: live `DiagnosticCode` count (the `EXPECTED:73` lock — GT-5; flag if it moved), the conformance-fixture count + the distinct-codes-covered count (the W3 lock test's own output — GT-8), and confirm `COVERAGE_MAP.md` is the W3-regenerated artifact (GT-9 — not hand-maintained). The `~8`/`~10`/`78-75-73` accepted-architecture numbers were corrected to `5`/`~11`/`75→73` in the GT ledger; the gate doc re-confirms the *post-W3* numbers from source.
- **Memory-SoT status-line-freshness gate at the epic closeout (the v1.4 D.7(c)#3 / v1.5 standing gate).** The closeout MUST, *in the same closeout pass*: advance the backlog SoT head status line to the Phase-6.0 SHIPPED+TAGGED state + add a Phase-6.0 RELEASE RECORD section with the wave→commit map (the D.3 deliverable, not deferred).
- **0 open P0, ≤5 well-scoped P1** in the independent pre-tag four-lens audit (the v1.1/v1.2/v1.3/v1.4/v1.5 §3 standard).
- **Carry-overs honoured/recorded (re-stated, not necessarily resolved — the §11.62 / v1.5 §6 discipline):** **G9 CI-never-run + the new lanes' first-remote-run** (owner push, OWNER-1 — now *with the lanes ready + `make ci-local` proving local-green*); the 5-platform binary tail (carried, OWNER-1 lineage); VSIX packaged-not-published (v1.5 OWNER-A lineage, owner credential); No top-level `LICENSE`/`CONTRIBUTING`/`CoC` (the carried owner/legal escalation); the **Certifiability/MISRA codegen-mode arc (OWNER-3, parked)**; the **v1.6 D1 landing-page + the doc-honesty pass that must precede/accompany it** (the publish-readiness framing, §"publish-readiness note" below — recorded, NOT scoped into this epic) — all re-stated in `GATE_VERIFICATION_<tag>.md` §"carried owner-escalations" so none is silently dropped.
- **CHANGELOG entry** + the Doc 00 §11 rows (assigned at closeout — see the header numbering note, NOT pre-allocated) + a ROADMAP retrospective.

---

## 5a. Publish-readiness note (record the relationship; D1 is NOT scoped into this epic)

> The owner explicitly wants the small landing page **and is not ready to publish**; record the framing so this epic's "factory-grade" deliverable is positioned correctly relative to the v1.6 landing, **without folding D1 in** (the §3 / Doc 31 §3 "surface, don't auto-scope" discipline; the owner-endorsed Directive scoped this epic to the *proof* half only).

- **"Publish-ready" = this factory-reliability proof + the v1.6 D1 landing both land.** The small landing page is **D1, slipped to v1.6 — NOT dropped** (the v1.5 Doc 31 §1/§4 OWNER-D lineage: D1 was the explicitly-droppable-from-v1.5-tag item, slipped, not cut). A **small doc-honesty pass** (README / Doc-07 / Doc-14 staleness — `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B) **must precede or accompany** D1 so the public site is accurate (the Doc 31 §6 H6 "D1 must not broadcast a stale README" lineage). **This epic does NOT scope D1 or the doc-honesty pass** — it records the relationship.
- **`v1.5.0` is the stable baseline NOW but NOT to be branded the public 1.0.** The §11.30 release tags (`v1.0.0`…`v1.5.0` + the `checkpoint/<date>` anchors) **already ARE** the immutable "stable version" markers — there is **no redundant/moving "public 1.0" label** to add (a separate moving brand-label would be exactly the kind of drift the project's verify-the-record discipline forbids). The product is at a *stable, tagged baseline*; "the public 1.0" is a *publication event*, not a new version number.
- **G9 = the owner's publication switch, decoupled from BOTH progress AND quality.** Progress is the wave sequence; **quality is local every wave** (the per-wave quad + the new `make ci-local`). G9 is the switch for *when the owner chooses to publish* (the automatic-remote-pipeline + publication to ever exist) — it gates neither progress nor quality. Recorded in §4 OWNER-1 + here so the epic's "factory-grade" deliverable is correctly positioned: **this epic delivers the factory-reliability proof; the v1.6 D1 landing + the owner's G9 publication switch are the publish-readiness completion, recorded-not-scoped.**

---

## 6. Risks + sequencing

| # | Risk | Likelihood | Mitigation |
|---|---|---|---|
| **H1** | **Disk pressure** on the shared box (~8.8 GiB free / 89%, GT-10; `cargo` + a JS toolchain + the `xvfb-run` Electron download + `c8` are heavy; `act` would pull multi-GB runner images; cross-toolchains/QEMU are heavier still) | High | **One build-heavy wave at a time** (the standing posture; the depth-first spine sequences W1→W6 serially). **The CI-first box default (OWNER-2):** `qemu`/`arm-gcc`/`llvm-cov` are **CI-runner-only**, never on the box; W1's keystone is locally provable with the existing on-box gcc (GT-6). `act` is the *Linux build/sca* lanes only (NOT the matrix) and honest-skips if not installed. `df -h /` checked before/after every build-heavy wave; the §5 `clean -p`-first-party reclaim; out-of-tree JS/`node_modules`/Electron caches (do NOT let them land in `target/` or multiply per worktree). **Never grind the box** (`[[feedback_infra_constraint_escalation]]`) — surface a one-time local install as an explicit owner-fork only if a wave proves it strictly needed (the safe default: it is not). |
| **H2** | **Keystone violated** — a forked oracle / second trace-recorder / re-implemented step semantics enters via W1 (the codegen hook) or W2 (the QEMU harness) — the §2 cardinal regression | Medium (a "just compute the expected trace in the harness" reimplementation is tempting) | **The §2 mechanism**: W1/W2 are the only fork-possible waves; the mandatory **W6 source-derived §11.3 phase-audit** (negative-grep ∅ across the trace-hook + the target harness + the CI glue, *and* the host+target byte-equality differential **re-run by the auditor on the MVP corpus**, *not* echoed from any wave's self-report — the §11.29 / v1.4-L1 / v1.5-post-A2 rigor; the v1.5 closeout's own *"a `KEYSTONE-INTACT` verdict … is worse than a found problem if it merely echoes the wave's own claim"*). The oracle is **always** the shipped `fsm_simulator::execute_trace`/`StepRecord` (GT-4). This is a **binding tag-gate row** (§5), not a nice-to-have. |
| **H3** | **Keystone-prerequisite inversion** — a wave attempts the W2 on-target lane before the W1 host differential it presumes (the §0 single-most-important sequencing fact) | Medium (the owner's literal phrasing "extend the host differential" can read as if the host differential already exists; GT-3 proves it does NOT) | **Hard sequencing: W1 BEFORE W2** (encoded in §0, §1, the critical path). The W1 brief states explicitly it is *building* the R7 host differential (the placeholder is genuinely absent, GT-3), and the W2 brief states explicitly it *extends W1's identical `FSM_TRACE` C* (it does not invent a target oracle). The W6 audit re-derives that W2's comparison target IS W1's `execute_trace` oracle, not a fork (H2). |
| **H4** | **Toolchain-probe-trap** — a verification brief asserts a rustc fact from a bare `rustc --version` (box default stable) and misreads it as pin drift | Medium (a recurring live trap; `[[feedback_embeded_fsm_toolchain_probe_trap]]`; GT-2) | Any rustc assertion in any Phase-6.0 brief MUST be `rustup show active-toolchain` **from inside the repo** (expects `1.75.0 … (overridden by '…/rust-toolchain.toml')`), **never** bare `rustc`. A bare reading → box-default stable, a benign probe-context artifact, **NOT** a pin violation (the v1.4/v1.5 §"Pinned gate numbers" toolchain-row precedent, Doc 00 §11.51). The W4 `coverage-rust` + W2 `on-target` lanes are **additive CI jobs** — the 1.75 matrix is never contaminated (the `sca`-job isolation precedent, GT-1). |
| **H5** | **Concurrent-writer commit races** — any parallel fan-out orphaning each other's commits | Medium (the recurring concurrent-agent class) | **Every writer wave gets its own isolated worktree** (`phase6.N/...` branches, the per-wave worktree column in §1); shared-tree only for pure readers; the orchestrator merges. The depth-first spine sequences W1→W6 (no implementer parallelism is *required* — the build-heavy waves are serial by §6 H1 anyway). No agent `git reset`/`git push` (`[[feedback_agent_no_reset]]` / the no-push rule). |
| **H6** | **CI-lane scope-creep** — the WS simulator server / Web IDE / wasm / a 5-platform binary matrix pulled into the ci.yml lanes | Medium (the ROADMAP/backlog bundle adjacent minors) | **Phase-6.0 ci.yml = `build`+`sca` (kept) + `conformance`+`extension-host`+`coverage-rust`+`on-target` (added) ONLY.** Explicitly **NOT** a 5-platform *binary-bundling* lane (the carried OWNER-1-lineage tail — an infra-escalation, not a ci.yml lane), **NOT** the Doc 13 WS simulator server, **NOT** the Web IDE / wasm32 / Monaco. The §2.1-style "alternatives are the menu, not mid-epic drift" discipline (Doc 30 R5 / Doc 31 H5). |
| **H7** | **Coverage-gaming** — assertion-free tests written to lift the W4 ratchet number without behavioural value | Medium (a number-gate invites number-gaming) | **The W4 §5.4 acceptance rejects coverage achieved by assertion-free tests** (a test that executes a line without asserting behaviour does not count — the behavioural-not-symbol-presence discipline applied to the coverage gate). The exclusion allowlist is **enumerated with a per-entry rationale** (genuinely-uncoverable arms only). The ratchet is **from the measured floor** (`floor(measured−2%)`), not a guessed target — so the gate measures reality before enforcing. |
| **H8** | **Liveness / false-completion** — a `completed`-shaped notification wrapping an error advances a gate (esp. the W6 keystone audit or the pre-tag four-lens) | Medium (the recurring class; the v1.4 W4c transient-500 / v1.5 H7 instance) | **A `completed` envelope wrapping an error is neither a deliverable nor a verdict.** For any agent-completion that gates a release motion (the W6 §11.3 keystone audit; the pre-tag four-lens; the cold quad), independently probe ground truth (`ps` + the output-file mtime + `git`/worktree state — `[[feedback_verify_agent_liveness]]`) **before** any gate advance; read-only audit work is idempotently re-dispatchable; **never advance a gate on a dead/erroring agent**. |
| **H9** | **`make ci-local` confidence-gap mis-sold** — the local gate is presented as proving the *full* CI when the `on-target`/`coverage` matrix is CI-runner-only | Medium (the owner explicitly wants "run one command and know it is green") | **The honest scope is encoded inline (§1 W5 + §5):** `make ci-local` covers quad+conformance+JS-lane+Linux-`act`-dry-run+workflow-lint (**high** confidence); the on-target *logic* is locally smoke-proven by **W1's host differential** (zero cross-toolchain by design); the on-target/coverage matrix is **CI-runner-only** and exercised by the owner's G9 push. The gate's output **states this distinction** (it does not claim to have run the on-target lane locally — the `gcc_compile.rs` honest-skip-notice precedent). G9 becomes a *confirmation*, honestly framed as such — not over-sold as redundant. |

**Sequencing summary (the critical path, restated).** `W0 (this doc) → W1 (R7 host-trace differential — the keystone prerequisite; gcc-only, locally provable) → W2 (the on-target QEMU/mps2-an385 lane — the keystone proper; MVP corpus = the 5 frozen-trace examples) → W3 (G7 live-enum lock + bulk fixtures) → W4 (coverage ratchet — Rust llvm-cov + extension c8, from the measured floor) → W5 (ci.yml integration + the make ci-local gate) → W6 (the source-derived keystone phase-audit + the independent pre-tag four-lens + GATE_VERIFICATION_<tag>.md (the batched closeout incl. the §5 memory-SoT-freshness + release-record-symmetry + G7/COVERAGE_MAP pinned-numbers re-derivation sub-steps) → §11.30-clean cold-quad WITH the NOTE-1 STEP 0 + the binding JS lane at X → the tag at X, LOCAL)`. **MUST-set (irreducible): W1/W2/W5/W6.** Strongly-recommended in-epic: W3/W4. **Explicit fallback split:** if scope must be cut, this-epic = on-target+CI (W1/W2/W5/W6) and a follow-on epic = the enforcement gates (W3/W4); **default = attempt the full matrix** with the W3 lock test + W4 ratchet *mechanisms* as MVP and the *exhaustive* fixture-authoring (W3's bulk tail) as the deferrable tail. The §11.3 phase-boundary cadence is **at minimum the W6 keystone audit** (the only fork-possible boundary — the v1.4/v1.5 keystone-first precedent) plus the standing pre-tag four-lens; the periodic-audit cadence (`[[feedback_periodic_audits]]`, every 2–3 implementer batches) is pre-budgeted per §7.

---

## 7. Process tweaks carried from the v1.4 / v1.5 post-tag disciplines

> `docs/META_SESSION_2026_05_16.md` (the v1.4 post-tag closeout) + the `GATE_VERIFICATION_v1_5.md` §4/§"verify-the-record" closeout codified durable lessons. Phase-6.0 adopts them explicitly *up front in the plan* (the D.7(c)#1 directive: "name it in the wave plan so it is planned, not absorbed"; the Doc 31 §7 precedent).

1. **Pre-budget the keystone-audit surface (D.7(c)#1 + L1 + the v1.5-post-A2 precedent).** The W6 source-derived keystone phase-audit is the *expensive* part of this epic — it independently re-derives the drive-the-oracle invariant from source across W1's trace-hook + W2's target harness + the CI glue **and re-runs the host+target differential battery on the MVP corpus** (the v1.4/v1.5 triple-independent-derivation cost was real and "surprised the schedule"). It is **scheduled here as a first-class, named line item** (the W6 mandatory phase-audit row in §1 + the §2 binding gate row + the §5/§6-H2 gating), not absorbed silently. Phase-6.0's soundness surface is *the target/CI extension of an established invariant* (the keystone is already codified at v1.4/v1.5; this epic only needs to prove the *host+target+CI surface* honours it — a negative-grep + a re-run differential, not a from-scratch bisimulation) — but it is still budgeted, not assumed-free.
2. **The standing closeout release-record-symmetry check (D.7(c)#2 + L4 + the v1.5 §"Pinned gate numbers" precedent).** Every "zero / unchanged / no-delta" claim in a release record is load-bearing and must be re-derived from `git diff`/source, not echoed (the v1.3 "zero Rust delta" cardinal near-miss vs the v1.4/v1.5 clean re-derivations). Phase-6.0's Rust delta is *the W1 codegen trace-hook + the W3 lock test + the W4 coverage config + harness Rust* (W2's target harness is C; the CI glue is YAML) — a non-zero delta easy to mis-frame. The W6 gate-doc wave MUST re-derive it from `git diff <v1.5-tag-commit> X -- crates/ Cargo.toml Cargo.lock` and grep the record for the v1.3-cardinal-error form (0 hits) — codified in §5 so it cannot be skipped under time pressure (`[[feedback_embeded_fsm_verify_record_release_layer]]`).
3. **A memory-SoT status-line-freshness gate at the epic closeout (D.7(c)#3 — the single biggest drift v1.4 found, re-confirmed clean at v1.5).** The cardinal v1.4 backlog drift was the SoT head status line + missing RELEASE RECORD section lagging the actual tag. The Phase-6.0 closeout MUST, *in the same closeout pass* (not deferred): advance the backlog SoT head status line to the Phase-6.0 SHIPPED+TAGGED state + add a Phase-6.0 RELEASE RECORD section with the wave→commit map (codified in §5).
4. **The G7/COVERAGE_MAP "pinned numbers re-derived from source" sub-step (this epic's own application of the verify-the-record discipline).** This epic *generates* numbers that future docs will cite (the post-W3 conformance-fixture count + distinct-codes-covered; the W4 measured coverage floor; the re-confirmed `EXPECTED:73` live count). The GT ledger already corrected the accepted-architecture `~8`/`~10`/`78-75-73` to ground-truth `5`/`~11`/`75→73`; the W6 gate doc MUST re-derive the *post-W3* numbers from source (the W3 lock test's own output + the regenerated `COVERAGE_MAP.md`) — codified in §5 so the epic does not itself become a future doc-honesty drift surface (the GT-9 lesson applied reflexively).
5. **Carry (do NOT fix here) the v1.6 D1 + doc-honesty relationship + the in-repo `docs/ROADMAP.md` stale-pointer lineage.** The Doc 31 §6 H6 / §7#4 carried that `docs/ROADMAP.md` "See also" still calls `~/.claude/plans/toasty-prancing-goose.md` "the live multi-phase execution plan" (superseded). Phase-6.0 **records** (does NOT fix — the scope boundary forbids editing any other doc; this doc writes exactly one file) that the v1.6 D1 landing + its preceding doc-honesty pass (README/Doc-07/Doc-14 staleness + the ROADMAP pointer) is the **carried publish-readiness work, recorded-not-scoped** (§5a + §5 carried-escalations). The `[[feedback_verify_status_claims_vs_code]]` "a first-contact-surface pointer drifted stale is a lie" concern applies — tracked so it is not lost, to be fixed by the v1.6 doc-honesty batch that precedes/accompanies D1.

---

## 8. See also

- `docs/31-v1_5-UIDX-Wave-Plan.md` (the structural template mirrored 1:1 — the §5.2 gate-row form, the §4 owner-decision pattern, the ground-truth-verification-ledger discipline, the §7 process-tweak adoption) + `docs/30-v1_4-Simulation-Verification-Wave-Plan.md` (the §3 owner-escalation pattern + the `[[feedback_embeded_fsm_pipeline_before_ui]]` governing principle this epic completes the proof-half of)
- `docs/GATE_VERIFICATION_v1_5.md` (the §4/§4.2/§4.3 NOTE-1 cold-quad + re-activated-JS-lane mechanics W6 re-uses verbatim; the §"verify-the-record" closeout discipline §7 inherits; the carried owner-escalations — G9 the load-bearing carry) + `docs/GATE_VERIFICATION_v1_4.md` (§4.2 the NOTE-1 STEP 0; §6.3 the carried escalations)
- `docs/META_SESSION_2026_05_16.md` (Part D.8 L1–L7 + Part D.7(c) — the verify-the-record / release-record-symmetry / memory-SoT-freshness disciplines §7 carries) · `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` (PART A = the §3 parked MISRA/CERT-C codegen-mode track; PART B = the doc-honesty staleness the §5a v1.6-D1 must not propagate)
- `memory/project_user_messages_embeded_fsm_2026_05_17_testing_gap_factory_epic.md` (the owner-endorsed Directive — the actionable mandate this doc formalizes; item 6 scoped this epic to the proof-half only, MISRA stays OWNER-3/§3)
- `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` (the **R7 v1.0 placeholder** — the keystone prerequisite W1 builds; GT-3) · `crates/fsm-simulator/src/trace.rs` (`StepRecord:43` — the shipped oracle; GT-4) · `crates/fsm-cli/src/cmd/{baseline,test,verify}.rs` (`execute_trace`/`StepRecord` — the same seam the keystone drives, never forks; GT-4) · `crates/fsm-codegen-c/tests/gcc_compile.rs` (the host gcc gate W1 wires the `FSM_TRACE` hook into — gate G5; GT-6) · `crates/fsm-diagnostics/src/lib.rs:820` (the `EXPECTED:73` lock — the proven build-failing pattern W3 mirrors; GT-5)
- `tests/conformance/MANIFEST.json` (the G7 corpus — ~11 distinct codes; GT-8) · `tests/conformance/COVERAGE_MAP.md` (the drifted map W3 regenerates as a generated artifact; GT-9) · `.github/workflows/ci.yml` (the 2-job `build`+`sca` baseline W5 extends; the additive-`stable`-isolated-job pattern W4 mirrors; GT-1) · `editors/vscode/package.json` (`@vscode/test-electron` present, `c8` net-new — the existing `xvfb-run` host W4/W5 add coverage to; GT-11)
- `examples/{motor,submachine,traffic-light,vending-machine,deferred}/` (the **5** frozen-trace example FSMs — the W1/W2 MVP corpus; GT-7) · Doc 18 (CLI-Specification — the `fsm verify/baseline/generate/check` contract) · Doc 13 §11 (the `StepRecord` normative wire shape)
- `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 acceptance, §11.3 phase-audit, §11.22/§11.30 cold-quad + tag-ordering) · `docs/00-Decisions-And-Reconciliation.md` §11 (the ledger; Phase-6.0 rows assigned at the epic closeout — see the header numbering note, NOT pre-allocated here)

*End of FSM-PLAN-FACTORY-CI-V60 v1.0.0 — accepted plan-of-record; the orchestrator gates/merges; Phase-6.0 §11-ledger rows are assigned at the epic closeout, not pre-allocated here. The keystone is drive-the-oracle-never-fork; W1 builds the R7 host-trace differential the keystone presumes BEFORE W2 extends it to the emulated target; nothing heavy lands on the box (CI-first); the owner's `make ci-local` one-command gate de-risks G9; G9 stays the owner's publication switch, decoupled from progress AND quality.*
