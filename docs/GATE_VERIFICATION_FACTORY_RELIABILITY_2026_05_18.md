# GATE VERIFICATION — Factory-Reliability/CI Epic (Phase-6.0)

- **Document ID:** GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18
- **Date:** 2026-05-18
- **Milestone tag (locked TL decision — a quality milestone PRECEDING the owner-reserved v1.6):** `checkpoint/factory-reliability-ci-2026-05-18` (the proven `checkpoint/<name>` immutable-anchor pattern — LOCAL, no push; G9 is the owner's switch). **This milestone is deliberately NOT a `vX.Y.0` tag.** The owner's roadmap reserves **v1.6.0 for the landing-page + doc-honesty pass (post-factory)**; the factory-reliability/CI epic is the quality milestone that *precedes* it. Naming this `v1.6.0` would unilaterally consume the owner-reserved minor — so the `checkpoint/<name>` anchor (the same immutable-tag rigor, no version-number claim) is the correct vehicle. The §11.30/GT-12 cold-quad rigor applies to this checkpoint tag **identically** to a release tag.
- **Scope:** the FACTORY-EPIC delta over the v1.5.0 baseline `2ff8ac3` (`git rev-list -1 v1.5.0` = `2ff8ac3…`, re-derived — the epic-boundary anchor) through the Phase-6.0 arc: **W0** (Doc 32 the wave-plan-of-record, `83b912a`) → **W1** (the R7 generic host-trace differential — the keystone prerequisite, `71cc1bd`; the timer-over-fire FU `f92c1cb`) → the **FW109/FW110-FU codegen-correctness arc** (the differential surfaced + fixed real shipped codegen bugs: `a2aaba7` composite exit-set, `e10b65e` record-model, `ee623f8` corpus 5→11 + `_exit_<Final>`, `0ccf8c4` deep_history, `ac6d56b`/`de10291`/`35cfa48` FW110-FU-A choice/junction, `ff9d953` FW110-FU-C local-transition LCA, `47afd4a`/`c46ba50` FW110-FU-A2 analyzer choice/junction lowering, `ea1829c`/`1fb1d36` FW110-FU-D/E timer-budget, `265a8da` #118 STAGE-SOLID re-gate) → **W2** (the on-target QEMU/`mps2-an385` trace differential — the keystone proper, `0e0e61f`) → **W3** (the G7 build-failing live-enum-derived conformance lock + `COVERAGE_MAP.md`-as-generated-artifact + bulk exact-set fixtures, `0c69282`) → **W4** (the coverage-ratchet gates — `cargo-llvm-cov` Rust branch + `c8` extension, measured-floor, non-vacuous, `64a3233`) → **W5** (the ci.yml 4 additive-isolated lanes + the one-command `make ci-local` gate, `edd4483`) → the **W6a source-derived keystone phase-audit** (`654cf64`, verdict **`KEYSTONE-INTACT`**) → the **W6b independent pre-tag four-lens audit** (`b8368da`, verdict **`TAG-CLEAR`**, 0 P0 / 0 P1), and the release commit **`X`** (this document + the batched Doc 00 §11 / CHANGELOG / ROADMAP consolidation + the F-01/F-02 record-precision folds + the F-05 doc-comment tidy (the only code-tree touch; F-02's optional code-comment half deferred to a future codegen wave — §6.4); see §4 + §7 + §8).
- **Companion evidence (frozen, never edited — the `AUDIT_*` never-overwritten convention):** the W6a binding keystone phase-audit `docs/AUDIT_PHASE_FACTORY_W6_2026_05_18.md` (commit `654cf64`, audited `edd4483`, verdict `KEYSTONE-INTACT`); the W6b independent pre-tag four-lens audit `docs/AUDIT_PRE_TAG_FACTORY_2026_05_18.md` (commit `b8368da`, audited `654cf64`, verdict `TAG-CLEAR` — 0 P0 / 0 P1 / 3 P2 / 3 P3); the reliability stage-gate evidence `docs/AUDIT_RELIABILITY_STAGE_GATE_2026_05_18.md` (the in-epic FW110 stage-gate, frozen); Doc 32 `docs/32-Factory-Reliability-CI-Wave-Plan.md` (the accepted wave-plan-of-record, `83b912a` — frozen); the v1.0–v1.5 baselines `docs/GATE_VERIFICATION_v1_{0,1,2,3,4,5}.md`; the structural template `docs/GATE_VERIFICATION_v1_5.md` (mirrored 1:1).
- **Verdict:** **PASS — the Factory-Reliability/CI epic (Phase-6.0) is milestone-tag-ready.** 0 open P0; the **KEYSTONE — DRIVE-THE-ORACLE binding tag-gate row (Doc 32 §2/§5) PASSES** via the independent W6a source-derived phase-audit `654cf64` (verdict `KEYSTONE-INTACT` — no forked semantics anywhere in the W1 trace-hook / the W2 QEMU+target harness / the W5 CI glue; the shipped `fsm_simulator::execute_trace` oracle + the `cmd/{test,baseline}.rs` seam **byte-untouched across the entire epic arc** `71cc1bd..edd4483`, EMPTY diff + per-file blob-SHA-identical; the host **3×**-deterministic + the W2 toolchain-free differential battery re-run **by the auditor**, the corpus-size-agnostic catalogue-integrity contract `9+3+0=12` holding non-vacuously, re-derived from source — not echoed). The W6b independent four-lens pre-tag audit returned `TAG-CLEAR` (0 P0, 0 P1, 3 P2, 3 P3 — the v1.1–v1.5 §3 standard of 0 P0 AND ≤5 well-scoped P1 met with **zero P1**); the binding Rust SCA lens was **empirically run** = 0 vulnerabilities/0 warnings over 207 deps. Every NOTE is documentation-of-record / closeout-batch / accepted-tracked-debt, folded by this closeout or carried (§6). The independent pre-tag four-lens audit is satisfied (W6b, frozen). The canonical confirming **cold-from-source** quad **with the §11.30/NOTE-1 mandatory `cargo clean -p` STEP 0** (§4.2) **and the BINDING JS lane** (§4.3 — Phase-6.0's surface *does* touch `editors/vscode/**` via W4 `c8` + W5 `extension-host`, *exactly like v1.5*) at the release commit `X` is the gating step the milestone tag is contingent on (§4) — recorded here as the contingent gate, **not a completed result; no quad/tag result is fabricated**.

This document is the canonical release record for the Factory-Reliability/CI epic (Phase-6.0). It captures the MVP-gate status (§1), the Phase-6.0 surface (§2 — the host+target differential keystone + the G7 lock + the coverage ratchet + the ci.yml lanes + the `make ci-local` gate), the verification evidence (§3 — the W6a keystone phase-audit + the W6b four-lens pre-tag audit, both frozen + the contingent cold-quad), the §11.22/§11.30-mandated **cold-from-source** quad posture **with the NOTE-1 explicit `cargo clean -p` STEP 0** and **the binding JS lane** (§4), the post-tag owner actions (§5), the accepted-tracked-debt + carried owner-escalations a consumer must be aware of (§6), the sign-off (§7), and the §11.30-clean tag topology + the **Pinned gate numbers re-derived at `X` not echoed** (§8).

---

## 1. MVP gate (Doc 23 §9) — Phase-6.0 status

| Gate | Phase-6.0 status | Evidence |
|---|---|---|
| **G1** `fsm check` ok / clean reject, no panic, no UB | ✅ | The Phase-6.0 Rust delta vs the v1.5.0 tag (`2ff8ac3`) is **substantial-but-bounded-by-design**: the X-projected `git diff --shortstat 2ff8ac3 X -- crates/ Cargo.toml Cargo.lock` = **22 files / +8192 / −190** (independently re-derived at the gate-doc commit — §8 "Pinned gate numbers"). It is the W1 codegen trace-hook (`emit/trace_hook.rs` +717, a `#ifdef FSM_TRACE`-gated tap) + the FW110-FU codegen-correctness fixes (`emit/{timer,history,transition,entry_exit,dispatch_*,pseudostate,source}.rs`, `lower/state.rs`, `ast/state.rs` — genuine shipped-bug fixes the differential surfaced, W6a/W6b frozen) + the W3 lock test (`conformance_code_coverage_lock.rs` +915) + the W1/W2 differential harnesses (`codegen_equivalence_smoke.rs`, `on_target_qemu_differential.rs` + the target C) + 2 internal dev-dep edges in `Cargo.lock` (`fsm-codegen-c`, `tempfile` into `fsm-simulator` — **0 new external `[[package]]`**, W6b D-04 frozen + §8). `fsm check`'s parse→analyze→diagnostics path emits **no new diagnostic code** (the live `DiagnosticCode` count is re-derived = **73**, unchanged — §8); the differential **oracle** `crates/fsm-simulator/src` + the `cmd/{test,baseline}.rs` seam are **byte-untouched** across the whole epic arc (W6a §1 EMPTY-diff + blob-SHA-identical; the keystone holds — §3). |
| **G2** `fsm check broken.fsm` → exit 1 + Rust-style error | ✅ | Unchanged — Phase-6.0 adds no diagnostic code and touches no `fsm-diagnostics` emission path (the Rust delta is the codegen trace-hook + the FW110 codegen-correctness fixes + test/harness/CI glue, **not** `crates/fsm-diagnostics/src`). The live `DiagnosticCode` count re-derived = **73**; the CI-lock `const EXPECTED: usize = 73` (§8). |
| **G3** `fsm generate` emits the C unit set + HAL | ✅ | Preserved. The W1 trace-hook is **compile-time-gated**: every emission is `#ifdef FSM_TRACE … #endif`, so a default `fsm generate` (no `-DFSM_TRACE`) carries the hook only as preprocessor-stripped text (W6b Lens-1 D-09: 21 `FSM_TRACE` tokens in a default `Motor.c`, *all* inside `#ifdef…#endif`). The FW110-FU fixes **did deliberately change production-C semantics for the better** (a real timer-over-fire bug the differential caught: single-shot `elapsed_ms` decrement → a correct multi-period budget-loop — W6b §"why this audit is not a rubber-stamp" / D-08, frozen) — a *correctness* fix, not a regression; the keystone is *no forked oracle*, not frozen output, and that holds (W6a/W6b). |
| **G4** `gcc -std=c99 -Wall -Wextra -Wpedantic -Werror` zero-warning | ✅ | Preserved + **strengthened**: the W1/W2 differential battery compiles **and RUNS** the generated C under `-std=c99 -Wall -Wextra -Wpedantic -Werror -DFSM_TRACE` on every corpus member (host gcc; the W2 on-target lane adds `arm-none-eabi-gcc -mcpu=cortex-m3` + QEMU, CI-runner-only). Subsumed inside `cargo test --workspace` in the cold quad (§4). |
| **G5** `fsm fmt` idempotent | ✅ | Unchanged — `crates/fsm-formatter/` is **not** in the Phase-6.0 delta (`git diff --stat 2ff8ac3 X -- crates/fsm-formatter/` ⇒ ∅; the §8 file list contains no formatter crate). |
| **G6** all examples full-chain + sim-trace-match | ✅ | **Strengthened by this epic.** Phase-6.0's whole point: the host (W1) **and** target (W2) generated-C trace **byte-equals** the shipped `fsm_simulator::execute_trace`/`StepRecord` oracle for the 9 `BYTE_EQUAL` corpus members, with the 3 `BEHAVIOURALLY_EQUIVALENT_JUSTIFIED` members proven non-vacuously quiescent-equivalent per-prefix and `KNOWN_DIVERGENT` literally empty (W6a §3, frozen; W6b D-10 re-ran `7 passed; 0 failed`). |
| **G7** ≥1 test per diagnostic code | ⚠️ tracked (improved this epic) | The W3 lock makes the live-enum→fixture coverage a **build-failing** invariant. Re-derived from the W3 lock test's **own runtime output** at the gate-doc commit (not echoed): `G7 lock OK: 73 live codes = 37 exact-set conformance fixtures + 36 justified non-fixture allowlist entries.` (the partition `37 + 36 = 73` is dynamically asserted). The conformance MANIFEST grew **26 → 53** fixtures (parser 7 / validator 11 / semantic 28 / codegen-c 4 / formatter 3 — re-derived §8; W3 "bulk fixtures authored"). The `.contains` codegen-c conformance oracle remains a tracked deferred-tail (the #123-class item, owned by the G7 wave for behavioural conversion — W6b F-04, accepted-tracked §6); the W1/W2 differential + the W3 lock are the binding behavioural gate in the interim. **Improved, not regressed.** |
| **G8** `cargo build/test/clippy/fmt` clean | ✅ | **cold-from-source** quad posture below (§4), with the **NOTE-1 explicit `cargo clean -p` STEP 0** written into §4.2 as a mandatory step **and** the **binding JS lane** (§4.3 — Phase-6.0 touches `editors/vscode/**` via W4 `c8` + W5 `extension-host`). De-risking predicate (NOT a substitute for the canonical run): the W6a auditor re-ran the host differential **3×** deterministic `7 passed / 0 failed` + the W2 toolchain-free guards `6 passed` at `edd4483`; W6b independently re-ran the host differential (`7 passed; 0 failed`), the W2 guards (`6 passed`), and the W3 lock (`4 passed`) at `654cf64`. The §11.30-clean canonical confirming run at `X` re-confirms from cold regardless — the contingent gating step. |
| **G9** CI matrix linux/macos/windows | ⚠️ tracked (lanes now READY, never run remote) | `.github/workflows/ci.yml` is now the original 2-job `build`(1.75 matrix)+`sca`(`cargo audit`) **byte-untouched** + **4 additive own-job `needs:`-free lanes appended** (`conformance` / `extension-host` / `coverage-rust` / `on-target` — W6b Lens-1 D-05: one hunk `68a69,321`, build+sca untouched). **None of these has ever executed on a remote CI runner** (local-only repo — owner controls the remote, a hard project rule). The `make ci-local` gate makes the local payoff real (quad + conformance + the `xvfb-run` Extension-Host JS lane + the `act` Linux-`build`/`sca` dry-run + the new-lanes' workflow-lint), so G9 becomes a *confirmation*, honestly framed (the on-target/coverage matrix is CI-runner-only per OWNER-2; the on-target *logic* is host-proven by W1's identical-C/same-oracle differential). Post-tag owner action §5. |

G7 + G9 are the **same explicitly-tracked partials accepted at the v1.0–v1.5 gates** — neither is a Phase-6.0 regression (G7 *improved*: a build-failing lock + corpus 26→53; G9's lanes are now *ready* + locally `make ci-local`-provable, still remote-unrun); both are documented in CHANGELOG `[factory-reliability-ci]` *Known limitations* and the ROADMAP.

---

## 2. Phase-6.0 surface delivered (since v1.5.0 `2ff8ac3`)

**Theme: the Factory-Reliability/CI proof layer — close ALL testing gaps to factory-grade + a push-and-it-runs CI, AFTER the FACTORY-COMPLETE pipeline.** The owner's governing **pipeline-before-UI** sequencing principle (`[[feedback_embeded_fsm_pipeline_before_ui]]`) was satisfied at v1.4 (W4a `FACTORY-COMPLETE`); the v1.5 UI/DX convenience layer shipped on top. This epic is the owner-endorsed factory-reliability/CI directive (Doc 32, owner-affirmed) — it un-parks the *testing half* of Certifiability/OWNER-C (the MISRA-codegen half stays separately parked, OWNER-3 / §6). The headline is **a host+target generated-C ↔ simulator-oracle byte-differential that drives the shipped, never-forked oracle**, plus the enforcement gates (the G7 live-enum lock, the coverage ratchet) and the additive-isolated ci.yml lanes + the one-command `make ci-local`.

### 2.1 W0 — the wave-plan-of-record (`83b912a`)

Doc 32 `docs/32-Factory-Reliability-CI-Wave-Plan.md` — the accepted plan-of-record (the Doc 28/29/30/31 epic-kickoff-doc precedent; the next free integer after Doc 31). It formalised the keystone DRIVE-THE-ORACLE binding gate row (§2), the 3 owner-decision residuals with non-blocking safe parked defaults (§4), the §11.30 Phase-6.0 tag-gate criteria (§5), and the §11-ledger numbering refinement (Phase-6.0 rows assigned at the *closeout*, NOT pre-allocated — the v1.4-L6 / v1.5-Doc-31 precedent). Frozen — not annotated by this closeout (Doc 32 is the wave-plan of record).

### 2.2 W1 — the R7 generic host-trace differential (the keystone prerequisite) (`71cc1bd` + the FW110-FU arc)

The R7 host-trace differential the keystone presumes (genuinely absent before — the v1.0 placeholder, GT-3). `crates/fsm-codegen-c/src/emit/trace_hook.rs` (+717) is a **compile-time-gated trace tap, NOT a second interpreter**: every emission is `#ifdef FSM_TRACE`-gated; it records *what the generated C itself did* (the event it processed, the transition its OWN dispatch selected via compile-time `stable_id`/`source`/`target` literals, its OWN `_active[]` snapshot) and never consults/re-derives the `fsm_simulator` step/transition/guard semantics. `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` drives the **shipped, byte-untouched** `fsm_simulator::execute_trace` oracle and byte-diffs its `StepRecord` projection against the FSM_TRACE-compiled-and-RUN generated C. The differential surfaced + fixed **real shipped codegen bugs** (the FW109/FW110-FU arc — a timer over-fire `f92c1cb`/`ea1829c`/`1fb1d36`, composite exit-set `a2aaba7`, deep_history `0ccf8c4`, choice/junction lowering `ac6d56b`...`c46ba50`, local-transition LCA `ff9d953`, closed by the #118 STAGE-SOLID re-gate `265a8da`) — **the audit working as designed**, not a fork (W6a §1/§2, frozen). **§5.4 acceptance (W6a-re-derived, frozen):** the host generated-C trace byte-equals the oracle on the 9 `BYTE_EQUAL` members; the differential REDs on a deliberately-corrupted oracle (the non-vacuous never-game guard).

### 2.3 W2 — the on-target QEMU/`mps2-an385` trace differential (the keystone proper) (`0e0e61f`)

`crates/fsm-simulator/tests/on_target_qemu_differential.rs` (+1207) + `tests/on-target/{startup_mps2_an385.c,mps2_an385.ld,semihost.h}` extend W1's **identical** `FSM_TRACE` C to a cross-compiled `arm-none-eabi-gcc -mcpu=cortex-m3` binary run under `qemu-system-arm -M mps2-an385` semihosting — comparing against the **SAME** `fsm_simulator::execute_trace` oracle (it does **not** invent a target oracle; W6a §2(b) negative-grep ∅, frozen). The on-target lane is **CI-runner-only by design** (OWNER-2 / GT-10 — `arm-none-eabi-gcc`/`qemu-system-arm` are never on the disk-tight box); the on-target *logic* is locally smoke-proven by W1's identical-C/same-oracle host differential; the toolchain-free no-fork keystone guards + the host-gcc-strengthened W2 driver pass locally (W6a §3 `6 passed`, frozen). The honest-skip is **NOT a finding** (W6a explicitly; W6b Lens-3).

### 2.4 W3 — the G7 build-failing live-enum-derived conformance lock + COVERAGE_MAP-as-generated-artifact (`0c69282`)

`crates/fsm-cli/tests/conformance_code_coverage_lock.rs` (+915): a `cargo test` that **fails the build** unless every live `DiagnosticCode` variant has EITHER an exact-set conformance fixture OR a justified `NON_FIXTURE_CODES` allowlist entry (the live target derived from the compiled `DiagnosticCode::all_codes()`, not a doc — W6b D-12). `tests/conformance/COVERAGE_MAP.md` became a **generated artifact** whose byte-derivability is asserted read-only; regeneration is feature-gated (`--features regen-coverage-map`) + `#[ignore]`d so neither the normal suite nor a stray `--ignored` run can rewrite it (GT-9 closed — W6b Lens-3). Bulk exact-set fixtures authored (conformance 26 → 53). **§5.4 acceptance (W6b-re-derived, frozen):** the RED-PROOF (`red_proof_lock_fails_when_a_live_code_is_neither_fixture_nor_allowlisted`) factors the gap logic into a shared `uncovered_codes()` core so the proof exercises the **identical** logic + a deliberate-removal RED half — genuinely non-vacuous (W6b D-12, `4 passed`).

### 2.5 W4 — the coverage-ratchet gates (measured-floor, non-vacuous) (`64a3233`)

`scripts/coverage-gate.py` (+508) + `coverage-floors.toml`: a `cargo-llvm-cov` Rust-branch gate + a `c8` extension-coverage gate, the floor = `floor(measured−2%)` (**measured-then-ratcheted**, not a guessed Day-1 target), `--check-monotonic` rejects a lowered floor (the script **enforces**, it does not **decide** — the single source of truth is `coverage-floors.toml`). `coverage-floors.toml` carries an explicit anti-gaming statement ("NO assertion-free line-execution test was added to inflate any baseline"). **§5.4 acceptance (W6b-re-derived, frozen):** the ratchet is from the measured floor + `--check-monotonic` + the explicit no-assertion-free anti-gaming statement (W6b Lens-3, a durable never-game guard). W4 added the `c8` coverage devDep + its 24-package transitive tree — **devDependency-only, 0 new vulnerabilities** (independently audited — W6b D-19; this corrects the loosely-phrased "W5 added zero npm deps" — F-01 §6.4).

### 2.6 W5 — the ci.yml 4 additive-isolated lanes + the one-command `make ci-local` gate (`edd4483`)

`.github/workflows/ci.yml`: the original 2-job `build`(1.75 matrix)+`sca`(`cargo audit`) **byte-untouched**, **4 own-job `needs:`-free lanes appended** (`conformance` / `extension-host` / `coverage-rust` / `on-target` — the proven `sca`-job isolation; a new lane RED never gates the 1.75 matrix; W6b D-05/D-06). The `Makefile` `ci-local` target runs the per-wave **quad + conformance + the `xvfb-run` Extension-Host JS lane + the `act` Linux-`build`/`sca` dry-run + the new-lanes' workflow-lint** and **exits non-zero on a broken tree** (the honest scope documented inline: the on-target/coverage matrix is CI-runner-only OWNER-2; the on-target *logic* is host-proven by W1's host differential; `act` covers the *Linux* `build`/`sca` lanes only, honest-skip-pending-`act`-install if absent). `scripts/workflow-lint.py` (+260) lint-checks the new lanes' structure. **§5.4 acceptance:** `make ci-local` exits non-zero on a deliberately-broken tree (the Doc 32 §5.4 named gate line item; symbol-presence is never acceptance — W6a §2(c) frozen: the CI glue defines NO step/transition/guard/oracle semantics, only invokes the W1–W4 mechanisms).

**Planning doc of record:** Doc 32 `docs/32-Factory-Reliability-CI-Wave-Plan.md` (frozen — not annotated by this closeout; it is the wave-plan of record).

---

## 3. Independent phase-boundary audits (read-only; frozen evidence)

The §11.3 cadence for Phase-6.0 was, per Doc 32 §5/§6-H2/§7.1, **at minimum the W6a source-derived keystone phase-audit** (the W1 trace-hook + the W2 target harness + the W5 CI glue being the only fork-possible surfaces — the v1.4/v1.5 keystone-first precedent) plus the standing independent pre-tag four-lens (W6b). The W6a keystone audit is the **expensive, binding** one (it independently re-derives the DRIVE-THE-ORACLE invariant from source across all three fork-possible surfaces **and re-runs the host+target differential battery on the MVP corpus** — pre-budgeted as a first-class line item per Doc 32 §7.1, *not* absorbed silently).

| Audit (frozen doc / commit) | HEAD audited | Verdict | Key result |
|---|---|---|---|
| **W6a KEYSTONE** `AUDIT_PHASE_FACTORY_W6_2026_05_18.md` / `654cf64` | `edd4483` | **`KEYSTONE-INTACT`** (0 P0; the cardinal regression — a forked oracle / re-implemented step-transition-guard-deadlock / a catalogue-integrity violation / a non-deterministic differential — explicitly **absent**) | _Verdict independently re-read in full at this closeout (the §11.29 verify-the-record discipline — not echoed; the doc's VERDICT/§1/§2/§3/Explicit-confirmations each state `KEYSTONE-INTACT`)._ The auditor **re-derived the no-fork from source** (not from a wave's self-report): `git diff 71cc1bd..edd4483 -- crates/fsm-simulator/src crates/fsm-cli/src/cmd/{test,baseline}.rs` = **EMPTY** (exit 0) **+ per-file blob-SHA-identical at both endpoints** (`trace.rs`/`lib.rs`/`test.rs`/`baseline.rs`), and the *entire* `fsm-simulator/src` tree blob-identical — the oracle + seam byte-untouched across the **whole** epic arc (`71cc1bd` an independently-verified ancestor of HEAD); negative-grep ∅ for step/transition-selection/guard-eval/deadlock semantics *defined* in **all three** fork-possible surfaces (the W1 trace-hook, the W2 QEMU+target harness, the W5 CI glue — **every grep match read + adjudicated** an emitter/formatter/invocation/disclaiming-comment, not a definition); the projection/comparator fns read in full = formatting-only, applied identically to both sides, driving the shipped GT-4 seam. The auditor **re-ran the host differential 3× deterministic `7 passed; 0 failed; 2 ignored`**, re-derived the catalogue-integrity contract from source AND from the differential's own `--nocapture` output (`CORPUS`=12, `BYTE_EQUAL`=9, `BEHAVIOURALLY_EQUIVALENT_JUSTIFIED`=3, `KNOWN_DIVERGENT`=`&[]`; partition `9+3+0=12` dynamically enforced; the never-game guard **proof bodies read** + run non-vacuous; the `FW109 CATALOGUE STALE` ratchet forces a converged fixture to move), and re-ran the W2 toolchain-free keystone guards `6 passed` with the on-target lane honest-skipping **by design** (NOT a finding — the auditor did not install ARM/QEMU). |
| **W6b pre-tag four-lens** `AUDIT_PRE_TAG_FACTORY_2026_05_18.md` / `b8368da` | `654cf64` | **`TAG-CLEAR`** — **0 P0 / 0 P1 / 3 P2 / 3 P3** | _Verdict independently re-read in full at this closeout (not echoed; the doc's Verdict/§Findings/end-note each state `TAG-CLEAR`, "No P0. No P1.")._ The standing independent pre-tag four-lens (the v1.1–v1.5 §3 pattern), scoped to the **factory-epic delta** `2ff8ac3..654cf64` (NOT a keystone re-litigation — W6a did that, frozen; NOT a whole-codebase re-audit). Each lens independently re-derived (not echoed from W6a / the stage-gate docs / any wave): **Lens 1 Arch** PASS (the W5 ci.yml change purely additive — one hunk, build+sca byte-untouched; the trace-hook a clean compile-gated sibling; 0 new external dep / no cycle; `forbid(unsafe_code)` re-derived 12/11 unchanged); **Lens 2 Correctness** PASS (the FW110 fixes are genuine corrections — the auditor built `motor` at *both* baselines, stripped `#ifdef FSM_TRACE`, and FALSIFIED-then-correctly-scoped the loosely-phrased "byte-identical production C": the non-gated C *did* change because a real timer-over-fire bug was fixed — the audit working; F-02); **Lens 3 Reliability** PASS (the differential drives the unforked oracle; the W3 RED-PROOF + W4 ratchet non-vacuous, re-derived from the test bodies); **Lens 4 Security/SCA** PASS (binding) — `cargo +stable audit` **empirically run** over 207 deps = **0 vulnerabilities / 0 warnings** (NOT honest-skipped — the tool is present), the 3 npm-audit findings re-derived at TWO bases (byte-identical at `2ff8ac3` & `654cf64`), pre-existing devDep-only/not-in-runtime/not-exploitable-in-our-usage/no-non-breaking-fix → each `accept-with-written-rationale`, the epic introduced **0** of them (F-03 §6.3). 6 findings total, **none gating** (F-01/F-02/F-03/F-04 P2, F-05 P3, F-06 P3-out-of-scope). |
| **W6a + W6b applicability at `X`** *(re-derived at this closeout, not echoed)* | `X` | **applicable by construction** | W6a froze `KEYSTONE-INTACT` at `edd4483`; W6b independently verified `654cf64` is W6a's doc-only commit over `edd4483` (`git diff --shortstat edd4483..654cf64 -- crates/ Cargo.toml Cargo.lock` = **EMPTY**, W6b D-02). `X` adds this gate doc + the batched Doc 00 §11 / CHANGELOG / ROADMAP consolidation + the **F-05 cosmetic doc-comment tidy ONLY** in `codegen_equivalence_smoke.rs` (a comment change — the `fn run_differential(example: &str)` signature + body byte-unchanged; the differential re-run **2×** post-edit at this closeout = `7 passed; 0 failed; 2 ignored` deterministic, §7). No keystone/oracle/seam/comparator-fn logic is touched at `X` — so W6a's `KEYSTONE-INTACT` and W6b's `TAG-CLEAR` hold byte-for-byte at `X` by construction. The contingent cold quad + JS lane at `X` (§4) is the empirical re-confirmation. |

### 3.1 Why these audits are not rubber-stamps (recorded so the record is honest)

A `KEYSTONE-INTACT` verdict on the only fork-possible boundaries of an epic that *deliberately changed production codegen semantics* (the FW110 fixes), doubled the conformance corpus, and added a new npm devDep tree is worse than a found problem if it merely echoes a wave's claim. It did not: W6a did **not** trust any wave's negative-grep or doc-comments — it ran the EMPTY `git diff 71cc1bd..edd4483` itself **+ per-file blob SHAs**, read **every** negative-grep hit across all three fork-possible surfaces and adjudicated each, read the projection/comparator fns in full, **re-ran the host differential 3×** + re-derived the catalogue-integrity contract from both source and the differential's own `--nocapture` output + **read the never-game-guard proof bodies**, and re-ran the W2 toolchain-free guards with the honest-skip explicitly diagnosed. W6b independently **falsified-then-correctly-scoped** the loosely-phrased "byte-identical production C" claim (it built `motor` at both bases and proved the non-gated C *did* change — a real timer bug fix, the audit working), re-derived the npm SCA at **two** bases, **actually ran** `cargo +stable audit` (not honest-skipped), and re-derived the gates' non-vacuity from the test bodies. The epic is clean *because* every wave reported-then-verified with per-commit `git show` — corroborated, not assumed.

---

## 4. Cold-from-source release quad (§11.22 / §11.30 / NOTE-1) + the binding JS lane — the tag gate

### 4.1 The binding commit-ordering rule (Doc 00 §11.30, quoted)

> **Refinement (binding for v1.2+):** the release sequence is (1) author+commit `GATE_VERIFICATION_<tag>.md`, (2) run the cold-from-source quad at THAT commit, (3) `git tag -a` THAT commit + the `checkpoint/<date>` anchor — quad-commit ≡ tag-commit exactly.

This document makes code-summarising assertions (the §1 gate evidence; the §8 "Pinned gate numbers" — live `DiagnosticCode`=73, `forbid(unsafe_code)`=12 roots/11 crates, conformance=53, the X-projected Phase-6.0 Rust delta = 22 files / +8192 / −190) that a code delta could in principle falsify. The §11.30 sequence is followed strictly: this doc + the batched Doc 00 §11 / CHANGELOG / ROADMAP consolidation + the F-01/F-02 record-precision folds + the F-05 doc-comment tidy (the only code-tree touch) commit first (commit `X`), the canonical cold quad + the binding JS lane run **at that exact commit `X`**, and the milestone tag placed on **that same commit** — zero off-by-one by construction (the v1.2/v1.3/v1.4/v1.5 prospective-§11.30 pattern, the **6th consecutive** prospective-clean release: v1.1 retroactively reconciled §11.30; v1.2/v1.3/v1.4/v1.5 prospectively; Phase-6.0 prospectively).

### 4.2 Cold-quad method (§11.22) — **with the MANDATORY NOTE-1 `cargo clean -p` STEP 0 (binding v1.4+)**

§11.22 mandates a **cold from-source** green quad for a release tag (a warm shared-`CARGO_TARGET_DIR` can serve stale cross-worktree `-wt-`-baked-path test binaries — the v1.4 L3 *false-red* hazard; a warm pass is necessary-but-not-sufficient). **NOTE-1 hardening (Doc 00 §11.70 — binding v1.4+):** the cold-quad's "cold" precondition is made **explicit and mandatory** as a written step, *not* merely the word "cold":

> **MANDATORY STEP 0 (NOTE-1 / §11.22 / §11.30):** **before** any full-workspace gate, run **`cargo clean -p` of ALL first-party workspace crates** — `cargo clean -p fsm-diagnostics -p fsm-lexer -p fsm-parser -p fsm-ir -p fsm-analyzer -p fsm-codegen-c -p fsm-simulator -p fsm-formatter -p fsm-cli -p fsm-lsp -p fsm-verify` (cargo 1.75 needs the repeated `-p` form) — **then** the canonical quad. This is non-negotiable. The box-infeasible "fresh non-shared `CARGO_TARGET_DIR`" ideal is **explicitly rejected** (a virgin target rebuilds the ~14 GiB dep graph; the box runs disk-tight). The all-first-party `clean -p` evicts every first-party build+test artifact (incl. any stale `-wt-`-baked test binary) **while preserving the content-addressed registry dep cache** (Cargo fingerprint is path-independent), and Phase-6.0 added **0 new external deps** (re-derived §8 — the 2 Cargo.lock edges are internal `fsm-codegen-c`/`tempfile` dev-deps already in the lock) so the registry cache is unchanged from v1.5. The recorded disk-protective conventional-CI-cache equivalent, **not a workaround to grind past**.

After Step 0: `cargo build/test/clippy/fmt --workspace` + the conformance runner + the §5.4 `gcc` compile-and-**RUN** acceptance (subsumed inside `cargo test --workspace`) + the W3 lock + the W4 ratchet check. `df -h /` checked before/after. **De-risking predicate (NOT a substitute for the canonical run):** the W6a auditor re-ran the host differential 3× `7 passed / 0 failed` + the W2 toolchain-free guards `6 passed` at `edd4483` (19 G free, no clean-p needed); W6b re-ran the differential (`7 passed`), the W2 guards (`6 passed`), and the W3 lock (`4 passed`) at `654cf64` (17 G free); this closeout re-ran the differential **2×** post-F-05 (`7 passed; 0 failed; 2 ignored`) + the W3 lock (`4 passed`) at the working tree (17 G free) — a red canonical run is improbable, **not** a substitute for the contingent run at `X`.

### 4.3 The BINDING JS lane at the gate-doc commit (binding for Phase-6.0 — the surface *does* touch `editors/vscode/**`)

Phase-6.0's surface **does touch `editors/vscode/**`** (W4 `c8` extension-coverage devDep + its config, W5 the `extension-host` ci.yml lane + the `xvfb-run` Extension-Host run in `make ci-local`) — *exactly like v1.5, unlike v1.4*. The JS lane is therefore a **binding gate at the gate-doc commit `X`**, the `GATE_VERIFICATION_v1_5.md` §4.3 re-activated-JS-lane precedent (GT-11/GT-12):

> **The JS lane at `X`:** (a) `npm audit` (the repo's equivalent) clean / **triaged** — at `X`, expected to re-derive the W6b finding: **3 PRE-EXISTING devDependency-only transitive vulns** (`{esbuild, mocha, serialize-javascript}` — 1 moderate / 2 high), each `accept-with-written-rationale` (build-time/test-only, NOT in the shipped extension runtime, no non-breaking fix, the epic introduced **ZERO** of them — W6b §4.2 / D-16, frozen); they do **not** gate the milestone tag under the Doc 00 §11.48/§11.42 standard applied **honestly** to pre-existing non-shipped devDep transitives (§6.3); (b) the **`xvfb-run @vscode/test-electron` Extension-Host suite** green; (c) the **`c8` extension-coverage gate** green. The Rust cold quad (§4.2) and the binding JS lane are **both** gated at `X`.

This run **has not yet happened** at the time this document is written; it is described here as the **gating step the milestone tag is contingent on**, not a completed result — **no run-at-`X` transcript is fabricated**. The de-risking predicate is W6b's two-base `npm audit --json` set-diff (byte-identical pre-existing `{esbuild,mocha,serialize-javascript}` at both `2ff8ac3` & `654cf64`; `c8`'s 24-pkg tree adds 0 vulns) — re-confirmed at `X` by the contingent JS lane.

### 4.4 The canonical confirming quad + JS lane (the step the milestone tag is contingent on)

The **canonical confirming cold-from-source quad — with MANDATORY STEP 0 (§4.2) — and the binding JS lane (§4.3) are executed at the release commit `X` itself** (orchestrator-run after this document + the batched consolidation commit), and the transcript is recorded in the **annotated `checkpoint/factory-reliability-ci-2026-05-18` tag message**. So quad-commit ≡ JS-lane-commit ≡ tag-commit = `X`, satisfying §11.22 (cold from-source) **and** §11.30 (zero off-by-one) **and** NOTE-1 (the explicit `cargo clean -p` STEP 0) simultaneously. The milestone tag is placed **only if** the canonical cold quad (incl. Step 0 + conformance + the §5.4 gcc-RUN battery + the W3 lock + the W4 ratchet) **and** the binding JS lane (npm audit triaged + the Extension-Host suite green + the `c8` gate green) are green **and** the independent pre-tag four-lens audit returned `TAG-CLEAR` (W6b — frozen, satisfied) **and** the W6a keystone phase-audit is `KEYSTONE-INTACT` (frozen, satisfied; applicable at `X` per §3). **Recorded here as the contingent gate, not a completed result — no quad/tag result is fabricated** (the `GATE_VERIFICATION_v1_5.md` §4 "recorded here as the contingent gate, not a completed result — no verdict fabricated" precedent verbatim).

---

## 5. Post-tag actions required of the repository owner

The milestone tag and the unpushed commits are **local only** (the owner controls the remote — a hard project rule). The carried owner/infra dependencies (the v1.4/v1.5 §5 documented-accepted posture; the Phase-6.0-specific changes noted inline):

1. **G9 CI matrix + the new lanes' first remote run (the v1.5 §5 carry-forward; Phase-6.0-specific update — now *with the lanes ready + `make ci-local` proving local-green*).** The GitHub Actions matrix (linux/macos/windows × the 1.75 `build` matrix + `sca`), and the **4 NEW additive lanes** (`conformance` / `extension-host` / `coverage-rust` / `on-target`) have **never executed against any v1.2–v1.5 OR any Phase-6.0 commit** (local-only repo). For Phase-6.0 the JS lane is a **binding local gate** (§4.3 — run at `X`) and `make ci-local` makes the local payoff real, but the new lanes have **never run on a remote CI runner** — G9 is now **load-bearing** for the *automatic remote pipeline + the on-target/coverage matrix to ever exist* (OWNER-1). **The owner should push the milestone tag + the LOCAL `v1.0.0`–`v1.5.0` tags + checkpoints + commits to exercise the full CI matrix + the SCA job + the 4 new lanes, and keep a patch lane ready.** G9 gates neither progress nor quality (both are local — the per-wave quad + `make ci-local`); it is the switch for *when the owner chooses to publish*.
2. **The G9-gated 5-platform binary-bundling tail.** Carried unchanged (the OWNER-1 lineage) — the host-only artifact ships; the full 5-platform tail is **blocked-on-G9 existing** (no cross toolchains on the box; an infra-escalation, not a grind). Accepted-tracked-debt until the owner provisions the cross-OS matrix. Explicitly **NOT** a ci.yml lane in this epic (Doc 32 §6 H6).
3. **VS Code Marketplace publish / signing (the v1.5 OWNER-A lineage).** The extension is **Marketplace-VALID and publish-READY** (v1.5 B1 — a lint-clean VSIX, `repository.url` corrected, `icon`/`keywords`/`version 1.0.0`) — but it is **NOT published** (no PAT/credential used by any wave; Phase-6.0 added no extension runtime code). An owner credential/product decision.
4. **The v1.6 D1 landing page + the doc-honesty pass that must precede/accompany it (recorded — NOT scoped into this epic; §5a-style publish-readiness framing).** This epic delivers the **factory-reliability proof half only** (the owner-endorsed Directive scoped it so). "Publish-ready" = this factory-reliability proof **+** the v1.6 D1 landing both land. D1 (the static EdgeForge-modeled docs/landing site — the v1.5 OWNER-D droppable-from-tag item, slipped to v1.6, NOT cut) + a **small doc-honesty pass** (README / Doc-07 / Doc-14 staleness — `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B; the in-repo `docs/ROADMAP.md` "See also" stale-pointer lineage, Doc 31 §6 H6 / Doc 32 §7-tweak-5) **must precede or accompany D1** so the public site is accurate. **This epic does NOT scope D1 or the doc-honesty pass** — it records the relationship (Doc 32 §5a). The §11.30 release tags (`v1.0.0`…`v1.5.0` + the `checkpoint/<date>`/`checkpoint/<name>` anchors) **already ARE** the immutable "stable version" markers — there is **no redundant moving "public 1.0" label** to add; "the public 1.0" is a *publication event* (G9 + D1), not a new version number. No hosting/DNS action is taken autonomously.
5. **The memory-SoT status-line-freshness gate (Doc 32 §7-tweak-3 / §5 — recorded; the SoT advance is the orchestrator's, decoupled from this commit by the no-push/no-tag boundary).** Per the D.7(c)#3 / v1.4-cardinal-drift discipline, *in the same closeout pass* the project backlog SoT head status line must advance to the Phase-6.0 SHIPPED+milestone-TAGGED state + a Phase-6.0 RELEASE RECORD section with the wave→commit map must be added. The wave→commit map is recorded in this gate doc's §"Scope" (W0 `83b912a` → W1 `71cc1bd`+`f92c1cb` → the FW109/FW110-FU arc `a2aaba7`…`265a8da` → W2 `0e0e61f` → W3 `0c69282` → W4 `64a3233` → W5 `edd4483` → W6a `654cf64` → W6b `b8368da` → W6c=`X`) so the orchestrator can advance the SoT head + RELEASE RECORD in the same closeout motion. The backlog SoT is the orchestrator's memory file (outside the repo + outside this wave's hard-guardrail scope, exactly as the v1.5 closeout recorded it in `GATE_VERIFICATION_v1_5.md` §8 / §11.79 and the orchestrator advanced it) — recorded here, honest-prospective ("gate-doc landed; the milestone tag + the SoT-freshness advance pending the contingent steps", no fabricated tag).

---

## 6. Accepted-tracked-debt + owner-escalations (consumer-relevant)

> Re-stated so none is silently dropped (the §11.62 discipline) and surfaced in CHANGELOG `[factory-reliability-ci]` *Known limitations* + Doc 00 §11 (the Phase-6.0 rows). The W6b carry-list + the Doc 32 §5 carry-overs are folded in ONE batch here (the §11.62 batched-doc discipline — never piecemeal).

### 6.1 The W6b pre-tag four-lens P2/P3 carry-list (recorded; none gating — W6b is `TAG-CLEAR`)

From `docs/AUDIT_PRE_TAG_FACTORY_2026_05_18.md` §Findings (FROZEN — all non-gating; W6b verdict `TAG-CLEAR` 0 P0 / 0 P1):

- **[F-03 P2 — the 3 extension transitive devDep npm vulns]** `{esbuild, mocha, serialize-javascript}` (1 moderate / 2 high). Pre-existing (byte-identical at `2ff8ac3` & `654cf64` — W6b D-16), **devDependency/build-time/test-only**, **NOT in the shipped extension runtime** (runtime deps = `{elkjs, vscode-languageclient}`; the VSIX = the esbuild bundle, `.vscodeignore` excludes `node_modules/**` — W6b D-17/D-18), **not exploitable in our build/CI usage** (no esbuild dev-server — one-shot bundler only; trusted-input-only Mocha over the project's own test fixtures), **no non-breaking fix** (the offered fixes are a major esbuild bump / a major mocha downgrade — W6b D-20), and the **epic introduced ZERO of them** (W4's `c8` 24-pkg tree adds 0 vulns — W6b D-19). Each `accept-with-written-rationale`; **does NOT gate the milestone tag** (the §11.48/§11.42 standard applied honestly to pre-existing non-shipped devDep transitives — the v1.5 precedent). Tracked for the **v1.6 FE-hygiene batch** (await an upstream mocha pulling serialize-javascript ≥7.0.5; the esbuild major-bump its own scoped chore).
- **[F-04 P2 — the `.contains` codegen-c conformance oracle (the #123-class deferred tail)]** The normative codegen-c conformance oracle is substring-presence (`.contains`), flagged **P1 hollow-AND-correctness-critical** in `docs/AUDIT_RELIABILITY_STAGE_GATE_2026_05_18.md:306` (frozen, in the epic delta), **owned by the G7 wave** for behavioural conversion (the 4 codegen-c MANIFEST fixtures are the tail). `accept-tracked-with-rationale` — a known/owned/deferred test-debt item, **NOT regressed** by the epic, **NOT a tag-blocker**: the W1/W2 differential + the W3 lock are the binding behavioural gate for the corpus in the interim.
- **[F-05 P3 — the `run_differential` stale doc-comment]** `crates/fsm-simulator/tests/codegen_equivalence_smoke.rs` ~L807: the doc-comment mentioned a `Some(corrupt)` param absent from `fn run_differential(example: &str)`; the green path applies no corruption (body read); corruption lives in the dedicated never-game guard. **FIXED in this closeout-batch** (§7 — a pure comment tidy; the signature/body byte-unchanged; the differential re-run 2× post-edit deterministic-green). No longer a carry.
- **[F-06 P3 — `crates/fsm-analyzer/src/symbol_table.rs:194` over-claiming E0025]** **OUT OF SCOPE** — `git diff --stat 2ff8ac3..654cf64 -- …/symbol_table.rs` = **EMPTY** (W6b D-23): not in the factory-epic delta, a pre-existing micro untouched by the epic. **Deferred to a general doc-honesty pass, NOT this epic's closeout-batch** (the W6b explicit disposition). NOT touched by this closeout.

### 6.2 The Doc 32 §7-tweak-5 v1.6 doc-honesty relationship (recorded, NOT fixed here — the scope boundary)

The broader **doc-honesty staleness pass** (the repo-root `README.md` / Doc 14 / Doc 07 staleness — `docs/ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B) is a **SEPARATE already-identified backlog item** (Doc 31 §6 H6 / Doc 32 §7-tweak-5), to be fixed by the doc-honesty batch that precedes/accompanies the deferred v1.6 D1. **It is FLAGGED here, NOT fixed in this epic.** The in-repo `docs/ROADMAP.md` "See also" stale pointer was **already reconciled at the v1.5 closeout** (it now points at the backlog SoT + the per-epic wave-plan docs — `GATE_VERIFICATION_v1_5.md` §6.4 / the v1.5 retrospective); Phase-6.0's ROADMAP edit is the factory-epic retrospective + the milestone-tag record only (it does not re-touch the already-fixed pointer). D1 (when it lands in v1.6) must not be the thing that broadcasts a stale README; this gate doc records the dependency so it is not lost (the `[[feedback_verify_status_claims_vs_code]]` "a first-contact-surface pointer drifted stale is a lie" concern).

### 6.3 Carried owner-escalations (re-stated, NOT resolved — the §11.62 / v1.5 §6.3 / Doc 32 §5 discipline)

- **G9 CI-never-run + the new lanes' first-remote-run — owner action (OWNER-1).** The push of the LOCAL `v1.0.0`–`v1.5.0` tags + checkpoints + commits + this milestone tag; the CI matrix / SCA / the 4 NEW lanes (`conformance`/`extension-host`/`coverage-rust`/`on-target`) have **never run on a remote runner** (the JS lane is now a binding *local* gate, §4.3, + `make ci-local` proves local-green, but remote-unrun). G9 is now **load-bearing** (the automatic remote pipeline + the on-target/coverage matrix to ever exist depend on it — OWNER-1). Owner push action; re-stated, **not resolved**.
- **The 5-platform binary tail — OWNER-1 lineage.** The host-only artifact ships; the full 5-platform cross-OS tail is **blocked-on-G9** (no cross toolchains on the box; an infra-escalation, not a grind). Accepted-tracked-debt. Explicitly **NOT** a ci.yml lane in this epic (Doc 32 §6 H6).
- **VSIX packaged-not-published (the v1.5 OWNER-A lineage).** The extension is **Marketplace-VALID + publish-READY** (v1.5 B1), **NOT published**. Still an owner credential/product action (no PAT used; Phase-6.0 added no extension runtime). Re-stated, not resolved.
- **No top-level `LICENSE`/`CONTRIBUTING.md`/`CODE_OF_CONDUCT.md`** at the repo root (carried from v1.4/v1.5 §6.3 — the license choice is owner/legal, coupled to publishing; the v1.5 OWNER-D lineage now carried to v1.6 with the D1 slip). Re-stated, not resolved.
- **The Certifiability / MISRA–CERT-C codegen-mode arc — PARKED (OWNER-3).** Doc 32 §3/§4 OWNER-3: a strategic ROADMAP-scheduling decision (a MISRA-C/CERT-C compliance posture for the *generated C* — the **codegen-mode** half). This epic un-parks **only the testing half** of Certifiability/OWNER-C (the factory-reliability proof); the MISRA-codegen half stays a **named, parked ROADMAP-v1.6+ candidate** requiring an explicit owner GO to schedule (it composes with verification into a "Certifiability & factory-integration" arc; *not* auto-started). Re-stated, not resolved.
- **The v1.6 D1 landing + the doc-honesty pass — recorded, NOT scoped (§5a / §5#4).** D1 (the slipped v1.5 OWNER-D item, slipped not cut) + its preceding/accompanying doc-honesty pass (README/Doc-07/Doc-14 + the ROADMAP-pointer lineage) is the **carried publish-readiness work, recorded-not-scoped into this epic**. "Publish-ready" = this factory-reliability proof + the v1.6 D1 landing both land. Re-stated, not resolved.
- **R7 — the mechanised sim≡codegen-equivalence proof.** Phase-6.0 delivers the *empirical host+target differential* (a real-FSM generated-C-trace byte-equals the simulator oracle on the corpus); the *fully mechanised* sim≡codegen-equivalence **formal proof** remains the carried v1.4-stretch/later named DEFERRAL (unchanged from v1.4/v1.5 §6 — *not* folded into this epic's proof; named-not-omitted). Re-stated, not resolved.
- **task #17 — CI-matrix still owner-blocked.** The CI matrix is still owner-blocked (local-only repo, owner controls the remote). Re-stated, not resolved.

### 6.4 FLAGGED record-precision tidies folded by this closeout-batch (W6b F-01/F-02 — non-gating)

- **[F-01 P2]** The brief's "W5 added zero npm deps" was imprecise: **W4** added the `c8` coverage devDep + a 24-pkg transitive tree (consistent with Doc 32 GT-11 "c8 is genuinely net-new"). That tree is **vuln-free** (W6b D-19, independently diffed) and **devDependency-only**; the substantive claim (the epic added no *vulnerable* / no *runtime* npm dep) is **TRUE**. This gate doc states it precisely (§2.5 / §4.3 / §6.3): "W4 added the `c8` coverage devDep + its 24-pkg transitive tree (devDependency-only, 0 new vulnerabilities — independently audited); W5 added 0 npm deps." Provenance precision only — **non-gating**, folded here.
- **[F-02 P2]** The loosely-phrased "byte-identical production C" (the `trace_hook.rs` `//!` doc + the brief) is precise about the *trace-hook block* (preprocessor-stripped when `FSM_TRACE` undefined — W6b D-09) but the **epic DELIBERATELY changed production-C semantics** via the FW110-FU fixes (the `motor` non-`FSM_TRACE` C's timer-advance was rewritten single-shot → a correct multi-period budget-loop — W6b D-08, a real timer-over-fire fix the differential caught). This gate doc scopes the production-C delta **correctly** (§1 G3 / §2.2 / §3.1 / §8): "the FW110 codegen-correctness fixes (intended) + the compile-gated trace-hook (zero default-codegen effect)" — the change is a *correctness fix*, not a regression; the keystone is *no forked oracle*, not frozen output, and that holds (W6a/W6b). **Disposition: the F-02 PRIMARY remedy — the release-record precision — is FOLDED HERE** (this gate doc states the production-C delta precisely throughout, the W6b F-02 *recommended action*). The *optional* `trace_hook.rs` `//!` code-comment qualifier tightening is **deferred to a future codegen-touching wave** (the W6b F-02 explicit disposition: "optionally tighten the `trace_hook.rs` `//!` qualifier — *a future codegen-touching wave*") — it is **NOT** done in this closeout, because the closeout's code-tree touch is scoped to the single F-05 cosmetic doc-comment tidy only (the hard-guardrail "F-05 is the ONLY code-tree touch"; touching `crates/fsm-codegen-c/src` here would exceed the closeout's mandate and was deliberately not done — the trace_hook.rs `//!` is byte-identical to HEAD at `X`). **Non-gating**, the record-precision half folded here, the code-comment half a tracked future-codegen-wave item.

---

## 7. Sign-off

The Factory-Reliability/CI epic (Phase-6.0) meets the release gate: 0 open P0; the **KEYSTONE — DRIVE-THE-ORACLE binding tag-gate row (Doc 32 §2/§5) PASSES** via the independent W6a source-derived phase-audit `654cf64` (verdict `KEYSTONE-INTACT` — the no-fork re-derived from source by the auditor: the shipped oracle + the `cmd/{test,baseline}.rs` seam byte-untouched across the whole epic arc `71cc1bd..edd4483`, EMPTY-diff + per-file blob-SHA-identical; negative-grep ∅ across all three fork-possible surfaces with every hit read; the host 3×-deterministic + the W2 toolchain-free differential battery re-run by the auditor; the catalogue-integrity contract `9+3+0=12` holding non-vacuously); the W6b independent four-lens pre-tag audit returned `TAG-CLEAR` (0 P0, 0 P1, 3 P2, 3 P3 — every lens independently re-derived, the loosely-phrased "byte-identical production C" specifically falsified-then-correctly-scoped, the Rust SCA empirically run = 0 vulns/0 warnings). Every NOTE is documentation-of-record / closeout-batch / accepted-tracked-debt, folded by this closeout or carried (§6). Scope statements are code-verified and conservative — **every gate numeric independently re-derived at the gate-doc commit, not echoed** (live `DiagnosticCode`=73 unchanged because Phase-6.0 touched no `fsm-diagnostics`; `forbid(unsafe_code)`=12 roots/11 crates unchanged because the epic added a dev-dep edge + harnesses + CI config, no crate root; conformance=53 re-derived from the MANIFEST [grew 26→53, W3]; the W3 lock's own runtime output `73 live = 37 fixtures + 36 allowlist`; the X-projected Phase-6.0 Rust delta = the **substantial additive** W1 trace-hook + the FW110 codegen-correctness fixes + the W3 lock + the W1/W2 differential harnesses **22 files / +8192 / −190** vs `2ff8ac3` with **0 new external deps** — stated as exactly that delta, **neither under-stated** [the v1.3 cardinal "zero Rust delta" error] **nor over-stated**, and **NOT** echoed from W6b's pre-F-05 `+8188/−190` but independently re-derived at `X` incl. the +4-net F-05 comment tidy; §8). The recurring prose-vs-code reconciliation rigor was applied **at the closeout layer** (the W6a/W6b verdicts re-read in full not echoed; every pinned numeric independently re-derived at the gate-doc commit; the frozen W6a/W6b audits + Doc 32 NOT overwritten — the §11.29 symmetric verify-the-record discipline; the v1.3 "zero Rust delta" cardinal-error form **grep'd in this record = 0 hits**, §8). The F-05 doc-comment tidy is a *cosmetic* change only (the `fn run_differential(example: &str)` signature + body byte-unchanged) — the differential was re-run **2×** post-edit at this closeout = `7 passed; 0 failed; 2 ignored` deterministic-green (the §5.4 never-game guard `differential_goes_red_on_a_deliberately_corrupted_oracle` still RED-proves the differential). The F-01/F-02 record-precision tidies are folded (§6.4); F-06 is OUT OF SCOPE (untouched — not in the epic delta). All residuals + owner-escalations are re-stated in CHANGELOG `[factory-reliability-ci]`, the ROADMAP, and the Doc 00 §11 Phase-6.0 rows. The release is contingent on the canonical cold-from-source quad — **with MANDATORY STEP 0 (the NOTE-1 `cargo clean -p` precondition, §4.2)** — **and the binding JS lane (§4.3)** — at the release commit `X` being green (§4.4); the independent pre-tag four-lens (W6b) returned `TAG-CLEAR` (frozen, satisfied) and the W6a keystone audit is `KEYSTONE-INTACT` (frozen, satisfied; applicable at `X` per §3). **Recorded as the contingent gate, not a completed result — no quad/tag result is fabricated.**

Tagging the Factory-Reliability/CI quality milestone as `checkpoint/factory-reliability-ci-2026-05-18` (annotated, LOCAL) — the proven `checkpoint/<name>` immutable-anchor pattern, **deliberately NOT a `vX.Y.0` tag** (v1.6.0 is owner-reserved for the landing+doc-honesty post-factory; this milestone *precedes* it). (The `v1.0.0`–`v1.5.0` release tags + the `checkpoint/<date>` anchors are immutable and **not moved**.) The orchestrator places the tag + runs the cold-quad at `X` (the no-push/no-tag boundary; this closeout is the sole writer of its scoped files, the commit is NOT merged/pushed/tagged).

— TL/PM, FSM Studio

---

## 8. Tag topology (pre-emptively correct per §11.30) + the Pinned gate numbers

Constructed §11.30-clean from the start (no off-by-one to reconcile — the 6th consecutive prospective-clean release):

- **Sequence:** (1) author + commit this document + the batched Doc 00 §11 (the Phase-6.0 rows appended at the next free integer) / CHANGELOG `[factory-reliability-ci]` / ROADMAP (the factory-epic retrospective + the milestone-tag record) + the F-01/F-02 record-precision folds + the F-05 doc-comment tidy (the only code-tree touch; F-02's optional code-comment half deferred to a future codegen wave — §6.4) → commit `X`; (2) run the canonical cold-from-source quad **at `X`** — **with MANDATORY STEP 0** (`cargo clean -p` of all 11 first-party crates, the NOTE-1 §4.2 precondition) — **and the binding JS lane at `X`** (§4.3) — transcript recorded in the annotated tag message; (3) `git tag -a checkpoint/factory-reliability-ci-2026-05-18` **at `X`**. Result: **cold-quad-commit ≡ JS-lane-commit ≡ tag-commit ≡ `X`**, exactly — zero off-by-one by construction (§11.30 satisfied at its source, the v1.2–v1.5 prospective pattern). **The orchestrator places the tag + runs the cold-quad at `X`; this closeout does NOT tag/push/run-the-quad.**
- **Pin lineage (immutable, not moved):** v1.0.0 → `b4800036…`; v1.1.0 → `abc70044…` (== `checkpoint/2026-05-15`); v1.2.0 → `71043935…` (== `checkpoint/2026-05-16`); v1.3.0 → `a036c381…` (== `checkpoint/2026-05-16-v1_3`); v1.4.0 → `3bbcd0f5…` (== `checkpoint/2026-05-16-v1_4`); v1.5.0 → `2ff8ac31…` (== `checkpoint/2026-05-17-v1_5`); the Factory-Reliability/CI milestone → `X` (== `checkpoint/factory-reliability-ci-2026-05-18`). Release tags are **immutable and never moved** (re-cutting a release tag is a destructive op never performed). v1.6.0 is **owner-reserved** (landing+doc-honesty, post-factory) — this milestone is a `checkpoint/<name>` anchor, **not** a version-number claim.
- **The `git rev-parse` vs `git rev-list -1` annotated-tag-object distinction.** The v1.x release tags + the annotated `checkpoint/<name>` anchor are **annotated**: `git rev-parse <tag>` yields the tag-**OBJECT** sha; `git rev-list -1 <tag>` yields the **commit**. They differ and must not be conflated. Re-confirmed at this commit: `git rev-list -1 v1.5.0` = `2ff8ac31…` (the commit — the epic-boundary anchor used throughout this doc). Recorded pre-emptively so no future metrics/audit wave misreads the `checkpoint/factory-reliability-ci-2026-05-18` tag-object sha as a mismatched commit (the `GATE_VERIFICATION_v1_{1,2,3,4,5}` §7/§8 lineage).
- **Frozen-attestation discipline:** `GATE_VERIFICATION_v1_{0,1,2,3,4,5}.md` correctly cite their own pinned values *at their pinned commits*; a frozen attestation is never rewritten. This document cites the Phase-6.0 live values *at `X`* — each independently re-derived at `X` (not echoed). The frozen W6a/W6b audits + Doc 32 are NOT overwritten.

### Pinned gate numbers (independently re-derived at the gate-doc commit `X` — DERIVED from `git`/source at the working tree == `X` after the F-05 tidy, NOT echoed)

| Metric | Value | Method (independently re-derived, not echoed — command + result) |
|---|---|---|
| v1.5.0 baseline anchor (the epic boundary) | `2ff8ac3…` | `git rev-list -1 v1.5.0` ⇒ `2ff8ac31f91eff922e48fb29ab1026b91a9166df`. The `git rev-parse` (tag-OBJECT) vs `git rev-list -1` (commit) distinction observed — the **commit** is used for every delta below. |
| Phase-6.0 Rust delta vs v1.5.0 `2ff8ac3` (X-projected, **incl. the F-05 tidy**) | **22 files / +8192 / −190; 0 new external deps** | `git diff --shortstat 2ff8ac3 -- crates/ Cargo.toml Cargo.lock` **at the working tree == `X`** (the F-05 doc-comment tidy applied) ⇒ **22 files changed, 8192 insertions(+), 190 deletions(-)**. This is the W1 codegen trace-hook (`emit/trace_hook.rs` +717) + the FW110-FU codegen-correctness fixes (`emit/{timer,history,transition,entry_exit,dispatch_switch,dispatch_table,pseudostate,source,header}.rs`, `lower/state.rs`, `ast/state.rs` — genuine shipped-bug fixes the differential surfaced, W6a/W6b frozen) + the W3 lock test (`conformance_code_coverage_lock.rs` +915) + the W1/W2 differential harnesses (`codegen_equivalence_smoke.rs` +2277-incl-the-F-05-tidy, `on_target_qemu_differential.rs` +1207, the target C `startup_mps2_an385.c`/`mps2_an385.ld`/`semihost.h`) + 2 internal dev-dep edges in `Cargo.lock` (`fsm-codegen-c`, `tempfile` into `fsm-simulator`). **W2's target harness is C; the W5 CI glue is YAML/Python/Make — not counted in the Rust delta** (Doc 32 §7-tweak-2). **NOT echoed from W6b's `2ff8ac3..654cf64` = +8188/−190** (pre-F-05): independently re-derived at `X`, the +4-net delta is exactly the F-05 doc-comment tidy (`git diff --shortstat HEAD -- …/codegen_equivalence_smoke.rs` ⇒ 1 file / +7 / −3, a 3-line stale comment → a 7-line precise comment). **A SUBSTANTIAL ADDITIVE delta, stated correctly here as exactly that — NEITHER under-stated (the v1.3 cardinal "zero Rust delta" error) NOR over-stated.** `Cargo.lock`: `git diff 2ff8ac3 -- Cargo.lock` ⇒ exactly `+ "fsm-codegen-c",` `+ "tempfile",` (the `fsm-simulator` dep list; both already in the lock at `2ff8ac3` — W6b D-04) ⇒ **0 new external `[[package]]`**. |
| v1.3-cardinal-error-form grep of this record | **0 hits (no "no/zero/empty Rust delta" *assertion*)** | The broad form `grep -nioE '(zero\|no\|empty) (rust \|external \|new )*(rust )?delta' docs/GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` returns 5 lexical hits; filtering out the explicit anti-pattern citations + the substantial-additive framing (`grep -viE 'NOT.*delta\|neither\|nor over\|0 new external\|cardinal\|anti-pattern'`) ⇒ **0 hits** — every "zero/empty Rust delta" occurrence in this doc is an explicit citation of the v1.3 cardinal-error *as the anti-pattern being avoided* (`[the v1.3 cardinal "zero Rust delta" error]`, `the v1.3 "zero Rust delta" cardinal release-record sin is **absent**`), and the only "0 new" claim is the **0 new external `[[package]]`** Cargo.lock fact (re-derived true). **There is NO claim asserting Phase-6.0 has no/zero/empty Rust delta** — the record states the substantial additive `22 files / +8192 / −190` delta explicitly throughout (the v1.3 "zero Rust delta" cardinal release-record sin is **absent** — `[[feedback_embeded_fsm_verify_record_release_layer]]`). |
| `#![forbid(unsafe_code)]` | **12 roots / 11 crates** | `git grep -l '#![forbid(unsafe_code)]' -- 'crates/*/src/*.rs'` at HEAD==`X` ⇒ **12** files (fsm-analyzer/lib, fsm-cli/main, fsm-codegen-c/lib, fsm-diagnostics/lib, fsm-formatter/lib, fsm-ir/lib, fsm-lexer/lib, **fsm-lsp/lib + fsm-lsp/main = 2 roots / 1 crate**, fsm-parser/lib, fsm-simulator/lib, fsm-verify/lib); the distinct crate set ⇒ **11** crates. Same at `2ff8ac3` ⇒ **12** files (re-derived, not echoed). **UNCHANGED 12/11** — Phase-6.0 added a dev-dep edge + harnesses + CI config, **no new crate root**; flagged-if-changed, not assumed (the precondition was verified — W6b D-07 corroborates independently). |
| Live `DiagnosticCode` count (the `EXPECTED:73` lock) | **73** | `git grep -n 'const EXPECTED' -- crates/fsm-diagnostics/src/lib.rs` at HEAD==`X` ⇒ `crates/fsm-diagnostics/src/lib.rs:820:        const EXPECTED: usize = 73;` (the CI-lock `all_codes_matches_expected_count`). **UNCHANGED from the frozen v1.5 figure** — Phase-6.0 added/retired **no** code; the Rust delta is the codegen trace-hook + the FW110 codegen-correctness fixes + test/harness/CI glue (no `crates/fsm-diagnostics/src` change — confirmed: not in the §"Rust delta" file list). **Flagged-if-moved (it did not).** |
| Conformance fixtures (post-W3) | **53** (parser 7 / validator 11 / semantic 28 / codegen-c 4 / formatter 3) | `git show HEAD:tests/conformance/MANIFEST.json` parsed (`sum(len(c['fixtures']) for c in categories)`) ⇒ **53**; by-category `{parser:7, validator:11, semantic:28, codegen-c:4, formatter:3}`. **Grew 26 → 53** (W3 "bulk fixtures authored" — the conformance corpus roughly doubled; a positive quality delta, correctly attributed; W6b D-22 corroborates independently). The accepted-architecture `~8`/`~10`/`78-75-73` numbers were corrected to the GT-ledger ground truth; this gate doc re-confirms the **post-W3** numbers from source (Doc 32 §7-tweak-4). |
| G7 lock partition (the W3 lock test's OWN runtime output) | **73 live = 37 exact-set conformance fixtures + 36 justified non-fixture allowlist entries** | `cargo test -p fsm-cli --test conformance_code_coverage_lock -- --nocapture` at HEAD==`X` ⇒ `G7 lock OK: 73 live codes = 37 exact-set conformance fixtures + 36 justified non-fixture allowlist entries.` (`4 passed; 0 failed`). The partition `37 + 36 = 73` is **dynamically asserted in-test** (`assert_eq!(n_cov + n_allow, n_live)`) — re-derived from the W3 lock test's **own output**, not a doc (Doc 32 §7-tweak-4 / GT-8). The live target is derived from the compiled `DiagnosticCode::all_codes()` (W6b D-12), not echoed. |
| `COVERAGE_MAP.md` is the W3-regenerated artifact (not hand-maintained) | **CONFIRMED generated (GT-9 closed)** | `crates/fsm-cli/tests/conformance_code_coverage_lock.rs` — the lock asserts `coverage_map_md_is_byte_derivable_from_the_live_enum_and_manifest` (read-only byte-derivability vs the live enum × the MANIFEST as the single source of truth); regeneration is **feature-gated** (`cargo test --features regen-coverage-map -- --ignored`) + `#[ignore]`d so neither the normal suite nor a stray `--ignored` run can rewrite it (the in-file header comment "COVERAGE_MAP.md — GENERATED ARTIFACT (GT-9)" + the test `4 passed`; W6b Lens-3 corroborates GT-9 closed). `tests/conformance/COVERAGE_MAP.md` is therefore a **regenerated artifact**, NOT hand-maintained. |
| Post-F-05 differential determinism (the only code-tree touch is a comment tidy) | **`7 passed; 0 failed; 2 ignored` ×2** | `cargo test -p fsm-simulator --test codegen_equivalence_smoke` re-run **2×** at the working tree == `X` (the F-05 doc-comment tidy applied) ⇒ run #1 `7 passed; 0 failed; 2 ignored` (2.49s), run #2 `7 passed; 0 failed; 2 ignored` (2.44s). Deterministic-green — the comment-only tidy did **not** change `run_differential`'s behaviour (the §5.4 never-game guard `differential_goes_red_on_a_deliberately_corrupted_oracle` is among the 7 and still RED-proves the differential). |
| Toolchain (asserted from inside the worktree) | `1.75.0` (pinned) | `rustup show active-toolchain` from `/root/dev/embeded-fsm-sdk-wt-w6-closeout` ⇒ `1.75.0-x86_64-unknown-linux-gnu (overridden by '/root/dev/embeded-fsm-sdk-wt-w6-closeout/rust-toolchain.toml')`. The `rust-toolchain.toml` pin is honored for all in-repo `cargo` work (a bare `rustc --version` reporting the box default would be a probe-context artifact, NOT a pin violation — the toolchain-probe-trap, Doc 32 §6 H4 / Doc 00 §11.51; W6a/W6b assert the same from the worktree). |
| Doc 00 §11 rows total | **86** (Phase-6.0 rows §11.80–§11.86, +7 this batch) | `grep -cE '^\| §11\.[0-9]+ \|' docs/00-Decisions-And-Reconciliation.md` at `X` ⇒ **86**. Last existing row before this batch = **§11.79** (independently confirmed from the file — the v1.5 last row, **NOT pre-allocated** per Doc 32 §13's header numbering refinement + the v1.4 L6 lesson); Phase-6.0 *implementation* rows = **§11.80–§11.86** (7 rows, this batched closeout, appended starting at the next free integer §11.80). Append-only verified: all 79 pre-existing §11 rows byte-identical in the working tree; the single permitted deletion is the §11-footer-line swap (the v1.3 D15 / v1.4/v1.5 invariant — a sorted set-difference old\new = exactly that one footer line). |
| df-gate | **17 G free / 79%** | `df -h /` at the worktree ⇒ `/dev/sda1 78G 58G 17G 79% /` — ≥ the ~6 G `clean -p` threshold (deps warm; no `cargo clean -p` needed for this read+comment-tidy closeout; the canonical cold-quad-with-Step-0 is the orchestrator's at `X`). The ≥5%-margin disk discipline holds throughout. |
