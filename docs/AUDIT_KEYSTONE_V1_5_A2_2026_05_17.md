# Post-A2 §11.3 Source-Derived Keystone Phase-Audit — v1.5 W-A2 (the LSP-embedded `fsm/verify`)

**Document ID:** FSM-AUDIT-KEYSTONE-V15-A2
**Version:** 1.0.0 (frozen evidence doc — never overwrite; a new audit is a new versioned file)
**Audit type:** Doc 31 §1 W-A2 / §2 BINDING tag-gate keystone phase-audit (the §11.3 phase-boundary form; the v1.4 `AUDIT_PHASE_V1_4_W{1,2}` frozen-evidence precedent). READ + RE-DERIVE judgment — the no-fork is re-derived from source and the differential battery is re-run by this auditor, **not** restated from the wave's self-report.
**Audited commit:** `7eb7e59` (`v1.5 W-A2 (VS Code): the LSP-live verify command, debounced + ceiling-guarded`). A2 is two commits: `4737259` (Rust: embed `fsm-verify` in `fsm-lsp` via the `fsm/verify` custom request) + `7eb7e59` (VS Code: the LSP-live client wiring). A1 = `5558d68`; the v1.4 tag-commit = `3bbcd0f`.
**Worktree / branch:** `/root/dev/embeded-fsm-sdk-wt-v15a2audit` on `phase5.2/v15-a2-keystone-audit` (cut from `7eb7e59`, clean). Quad re-run by this auditor at `7eb7e59` (NOT echoed from the wave): `cargo test --workspace` = **867 passed / 0 failed**, clippy `-D warnings` clean, fmt clean; the differential battery byte-cross-checked by the auditor's own LSP harness vs the auditor's own `fsm`-binary oracle.
**Toolchain (asserted from inside the repo, NOT bare `rustc` — the [[feedback_embeded_fsm_toolchain_probe_trap]] guard):** `rustup show active-toolchain` ⇒ `1.75.0-x86_64-unknown-linux-gnu (overridden by '/root/dev/embeded-fsm-sdk-wt-v15a2audit/rust-toolchain.toml')`. Disk: `/` 7.5G→7.2G free (90→91%) across the build-heavy quad — well above the ~5% floor; no `cargo clean -p` needed, no breach.
**Governing record:** Doc 31 §1 W-A2 + §2 (the KEYSTONE-IN-UI binding gate row) + §5 (the v1.5 tag-gate criteria) + §6 H2/H7; the v1.4 `AUDIT_PHASE_V1_4_W2_2026_05_16.md` structure/quality bar; Doc 26 §6 (the `fsm-lsp` MUST NOT depend on the `fsm-cli` *binary* crate boundary); [[feedback_verify_status_claims_vs_code]]; [[feedback_embeded_fsm_verify_record_release_layer]].

---

## 0. Executive summary (read this first)

**Verdict: `KEYSTONE-INTACT`.**

A2 introduces the `fsm/verify` LSP capability — the **only Rust delta and the only keystone-violable point of the entire v1.5 epic** — and it is a **pure frontend of `fsm-verify`**, re-derived from source at `7eb7e59` by this auditor (not restated from the wave's self-report; the v1.4-W2→§11.3 precedent that found load-bearing issues by independent re-derivation). The three highest-stakes properties are **each confirmed by my own derivation**:

1. **No fork — `fsm-verify` + the CLI oracle are byte-untouched, and `fsm-lsp`/`editors` define ZERO verification semantics.** `git diff --stat 5558d68..7eb7e59 -- crates/fsm-verify crates/fsm-cli` is **empty** (re-run; the differential oracle is intact). The entire A2 Rust enabling change is a **single `Cargo.toml` path edge** (`fsm-verify = { path = "../fsm-verify" }`) + a 1-line `Cargo.lock` dependency-list entry — **no new crate**, so `#![forbid(unsafe_code)]` stays **12 roots / 11 crates** (re-derived from source via `git grep -l '#![forbid(unsafe_code)]' 7eb7e59 -- 'crates/*/src/*.rs'`, **not** echoed from the v1.4 figure). The negative-grep over `crates/fsm-lsp/src/**` + `editors/vscode/src/**` for any `fn` *defining* reachability / deadlock / transition-selection / guard-eval / completion / exploration / bfs / frontier / fixpoint / lca logic is **∅ (non-comment, read not echo-trusted)**: the two pre-existing completion hits (`server.rs:620` standard `textDocument/completion`, `complete.rs:314` `completions`) are untouched v1.2/L4 LSP capability (A2's diff-stat against `complete.rs` is **empty**), explicitly NOT verification logic per the brief.

2. **`crates/fsm-lsp/src/capabilities/verify.rs` is a pure frontend (read IN FULL).** It (i) parses via the EXISTING `crate::analysis::analyze` reuse seam (the SAME `fsm_parser::parse` + `fsm_analyzer::analyze_with_source` the CLI's `fsm check`/`fsm verify` use — no second front-end); (ii) calls **`fsm_verify::verify`** + **`fsm_verify::reachability_diagnostics`** — the *identical* public functions `crates/fsm-cli/src/cmd/verify.rs:155,176` calls, verified against `fsm-verify/src/lib.rs:59-65` `pub use` and both call-sites; (iii) marshals the returned `VerifyOutcome` + the `fsm-verify`-produced diagnostics **verbatim** into the documented `fsm-verify/v1` JSON, making **ZERO** verification decisions — `verdict`/`exitCode`/per-property `result` are a *total projection* of `outcome.verdict` + `has_e0400` (itself derived only from the `Diagnostic`s `reachability_diagnostics` produced), **transcribed line-for-line** from CLI `verify.rs:180-190` (verdict/exit) + `verify.rs:236-319` (`build_json`). `compute_line_col(.., Scalar)` is the **single canonical `fsm_diagnostics::compute_line_col`** (`fsm-diagnostics/src/lib.rs:172`) the CLI uses — defined **nowhere** in `fsm-lsp`/`editors` (NOT a forked position converter). The disclosed "re-stated marshalling" is genuinely pure data-serialisation of an `fsm-verify` value-object — the **same category** as `verify.rs`'s own `build_json` and A1's `verifyResult.ts` — justified by the Doc 26 §6 boundary (`fsm-lsp` must not depend on the `fsm-cli` binary crate). **No verification fact is computed locally.** The VS Code `runVerifyLive` (A2's `7eb7e59` client commit) reads every datum verbatim from `result.verifyJson`, renders through **A1's** `verifyResult.ts` — zero TS verification logic.

3. **Differential-oracle byte-equality re-run BY THE AUDITOR (not the wave's assertion).** I built the `fsm` binary, independently captured `fsm verify --json` on `clean.fsm` / `deadlocks.fsm` / `clean.fsm --max-states 1` / `unreachable.fsm`, then drove the **real `fsm/verify` LSP request via my own throwaway harness** (built EXACTLY as `fsm_lsp::run_stdio` wires it) and **deep-equal + canonical-bytes-equal + (newline-normalised) exact-byte-equal** the LSP `verifyJson` against my oracle on all four. The **inconclusive case returns `verdict:"inconclusive"`, `bound.hit:true`, `exitCode:2` — asserted NOT a false `verified`** (the cardinal-sin guard). The large-FSM ceiling short-circuits within bounded wall-time (the wave's `verify_lsp_acceptance.rs` battery-5 explosive-model timing test passes in a 0.13s total binary run; full `verify_lsp_acceptance` = 7/7; the pre-existing `lsp_client_acceptance` L1–L7 = **35/35 unregressed**).

No P0. No keystone-violable defect. The findings are P3 documentation/observability nits only (the project's batched-doc discipline), consolidated as the §6 carry-list. **`KEYSTONE-INTACT` — B1∥C1 may be dispatched; this gate-row PASSES.**

### Findings table

| ID | Sev | One-line | Evidence (file:line) | Disposition |
|---|---|---|---|---|
| **K1** | — | No fork: `fsm-verify` + `fsm-cli` byte-untouched by A2; the only Rust delta is the one `Cargo.toml` `fsm-verify` path edge (+1-line `Cargo.lock`); no new crate ⇒ forbid-unsafe unchanged 12/11 (re-derived) | `git diff --stat 5558d68..7eb7e59 -- crates/fsm-verify crates/fsm-cli` = ∅; `crates/fsm-lsp/Cargo.toml` +`fsm-verify`; `git grep -l '#![forbid(unsafe_code)]' 7eb7e59` = 12 | accepted (PASS) |
| **K2** | — | Negative-grep ∅: zero `fn` defining reachability/deadlock/transition-sel/guard-eval/completion/exploration in `fsm-lsp/src` or `editors/vscode/src` (non-comment, every hit read); the 2 completion hits are untouched v1.2/L4, not verification | `server.rs:620`/`complete.rs:314` = pre-A2 (A2 diff-stat vs `complete.rs` ∅); negative-grep non-comment = ∅ | accepted (PASS) |
| **K3** | — | `capabilities/verify.rs` is a pure frontend: same `analyze` seam, identical `fsm_verify::{verify,reachability_diagnostics}`, verbatim `VerifyOutcome` marshalling; verdict/exit/`build_verify_json` line-for-line from CLI `verify.rs`; `compute_line_col` is the canonical `fsm_diagnostics` one | `verify.rs:206,234,262,274-284,318-408`; CLI `verify.rs:155,176,180-190,236-319`; `fsm-verify/src/lib.rs:59-65`; `fsm-diagnostics/src/lib.rs:172` | accepted (PASS) |
| **K4** | — | Differential battery byte-equal **by the auditor's own re-run**: LSP `verifyJson` deep-/canonical-/byte-equals `fsm verify --json` on clean/deadlock/inconclusive/unreachable; inconclusive = NOT false-verified (cardinal-sin guard); no editor-hang | auditor oracle vs auditor LSP harness, 4/4; `verify_lsp_acceptance` 7/7 + battery-5 timing | accepted (PASS) |
| **K5** | — | Quad green at `7eb7e59` re-run by auditor: 867 tests / 0 failed; clippy `-D warnings` clean; fmt clean; `lsp_client_acceptance` L1–L7 35/35 unregressed. No NOTE-1 false-red (all binaries from the canonical shared target) | `cargo test --workspace`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --all -- --check` | accepted (PASS) |
| **K6** | P3 | The disclosed "re-stated marshalling" duplicates ~90 lines of `verify.rs`'s `build_json`/verdict-map in `verify.rs` (justified by the Doc 26 §6 boundary; A1's `verifyResult.ts` is the same pattern). Sound today (the differential oracle catches any drift) but is a *drift-by-future-edit* hazard: a v2 schema bump must touch THREE mirrors (CLI, LSP, TS) | `crates/fsm-lsp/src/capabilities/verify.rs:274-408` vs `crates/fsm-cli/src/cmd/verify.rs:180-319` | closeout-batch (note in `GATE_VERIFICATION_v1_5`: the `fsm-verify/v1` mirror-set + the standing differential-oracle test ARE the durability mechanism; consider a future shared `fsm-verify`-side `build_json` helper if a v2 ever lands — NOT a v1.5 change) |
| **K7** | P3 | The `fsm-verify/vN` schema-versioning policy that A2 now also depends on (three frontends keyed on it) still lives only in `cmd/verify.rs:70-101` doc-comment; no Doc 18 reflection (the v1.4-W2 F9 carry, now with an additional consumer) | `crates/fsm-cli/src/cmd/verify.rs:70-101`; Doc 18 (absent) | closeout-batch (re-state the v1.4 F9 carry — the LSP frontend is a new factory-contract consumer of `fsm-verify/v1`) |
| **K8** | P3 | The large-FSM ceiling (`LARGE_FSM_LINE_CEILING=4000`) + debounce (`VERIFY_LIVE_DEBOUNCE_MS=200`) are client-side TS constants with no Doc 14/Doc 26 capability-spec reflection (the keystone is unaffected — `fsm-verify` is bounded-by-construction and the test proves no-hang — but the trigger-policy contract is undocumented at the spec layer) | `editors/vscode/src/commands/verify.ts:88,98` | closeout-batch (fold the trigger-policy into the C1/D1 doc pass or Doc 27; non-blocking) |

**Verdict line: `KEYSTONE-INTACT` — no forked semantics anywhere in `fsm-lsp`/`editors`; `fsm-verify`/CLI byte-untouched; the differential battery byte-equal (incl. the inconclusive-not-verified guard); the large-FSM no-hang; 0 P0. B1∥C1 unblocked.**

---

## 1. Independent Derivations table

Every load-bearing claim, with the exact command/`file:line`, the output, and **echoed-or-derived: DERIVED** (each was re-run / read by this auditor at `7eb7e59`, not transcribed from Doc 31 or the wave completion notes).

| # | Claim | Command / `file:line` | Output (verbatim / summarised) | echoed-or-derived |
|---|---|---|---|---|
| D-1 | `fsm-verify` + the CLI oracle are byte-untouched by A2 | `git diff --stat 5558d68..7eb7e59 -- crates/fsm-verify crates/fsm-cli` | *(empty — no files)* | **DERIVED** |
| D-2 | The full A2 file delta is 9 files; the only Rust-semantics-bearing new file is `crates/fsm-lsp/src/capabilities/verify.rs` (+408); the rest are wiring/test/manifest/TS | `git diff --stat 5558d68..7eb7e59` | `Cargo.lock 1+`, `fsm-lsp/Cargo.toml 13+`, `capabilities/mod.rs 18+`, `capabilities/verify.rs 408+`, `lib.rs 12±`, `server.rs 37+`, `tests/verify_lsp_acceptance.rs 445+`, `vscode/package.json 20+`, `vscode/.../verify.ts 247±` | **DERIVED** |
| D-3 | The Cargo.toml delta is exactly the one `fsm-verify` path edge (no new crate) | `git diff 5558d68..7eb7e59 -- crates/fsm-lsp/Cargo.toml` | one added line `fsm-verify = { path = "../fsm-verify" }` (+ a WHY comment) | **DERIVED** |
| D-4 | The Cargo.lock delta is a 1-line dependency-list entry (no new external crate pulled — `fsm-verify`'s deps already in-workspace) | `git diff 5558d68..7eb7e59 -- Cargo.lock` | a single `+ "fsm-verify",` inside `fsm-lsp`'s `dependencies` block; no new `[[package]]` | **DERIVED** |
| D-5 | `#![forbid(unsafe_code)]` = 12 roots / 11 crates at `7eb7e59` (re-derived; NOT the echoed v1.4 figure) | `git grep -l '#![forbid(unsafe_code)]' 7eb7e59 -- 'crates/*/src/*.rs'` | 12 files: fsm-analyzer, fsm-cli/main, fsm-codegen-c, fsm-diagnostics, fsm-formatter, fsm-ir, fsm-lexer, **fsm-lsp/lib + fsm-lsp/main**, fsm-parser, fsm-simulator, **fsm-verify** (11 distinct crates; fsm-lsp has lib.rs+main.rs) | **DERIVED** |
| D-6 | Negative-grep ∅: no `fn` defining verification semantics in `fsm-lsp/src` (non-comment, read) | `grep -rnE '\b(pub )?fn ' crates/fsm-lsp/src/ \| grep -iE 'reachab\|deadlock\|select.?transition\|enabled_set\|eval.?guard\|compute.?lca\|completion_drain\|fire.?timer\|region.?join\|frontier\|fixpoint\|state.?explor\|bfs\|explore' \| grep -vE '://\|//!\|/\*'` | *(no output)* — only `verify` fns are the frontend (`from_value`, `response`, `run_verify`, `is_error`, `build_verify_json`, `verify_request`) | **DERIVED** (read each, not an echo branch; no `head\|\|echo` in the pipeline) |
| D-7 | Negative-grep ∅ in `editors/vscode/src` | `grep -rnE '(function\|=>\|const) .*(comput\|derive\|calculate).*(verdict\|witness\|reachab\|deadlock)' editors/vscode/src/ \| grep -vE '//\|/\*'` | *(no output)* | **DERIVED** |
| D-8 | The 2 completion hits are pre-A2 (untouched v1.2/L4), not verification logic | `git log -1 --date=short -- crates/fsm-lsp/src/capabilities/complete.rs` ⇒ `43acb3b 2026-05-15 Implement fsm-lsp L4`; `git diff --stat 5558d68..7eb7e59 -- .../complete.rs` ⇒ ∅ | `complete.rs` untouched by A2; `server.rs:620` = standard `textDocument/completion` | **DERIVED** |
| D-9 | `fsm-verify` public API the LSP frontend calls == what the CLI calls | `fsm-verify/src/lib.rs:59-65` `pub use deadlock::DeadlockReport; pub use diagnostics::reachability_diagnostics; pub use engine::{verify, …}; pub use reachability::ReachabilityReport;` | CLI `cmd/verify.rs:155` `verify(&ir, opts)` + `:176` `reachability_diagnostics(machine, &outcome.reachability, outcome.stats.stop_reason)`; LSP `capabilities/verify.rs:234` `fsm_verify::verify(&ir, opts)` + `:262` `fsm_verify::reachability_diagnostics(machine, &outcome.reachability, outcome.stats.stop_reason)` — **identical fns, identical args** | **DERIVED** |
| D-10 | The verdict/exit/`build_json` projection is a line-for-line mirror (no second decision) | read `crates/fsm-lsp/src/capabilities/verify.rs:274-408` vs `crates/fsm-cli/src/cmd/verify.rs:180-319` | same 4-arm `Verdict` match → same strings; same exit map; same sorted-`Map` key order/value projections; same `compute_line_col(src, d.span.start, Scalar)` | **DERIVED** |
| D-11 | `compute_line_col` is single-canonical in `fsm-diagnostics`; NOT forked in `fsm-lsp`/`editors` | `grep -rn 'pub fn compute_line_col' crates/fsm-diagnostics/src/` ⇒ `lib.rs:172`; `grep -rn 'fn compute_line_col' crates/fsm-lsp/src/ editors/vscode/src/` ⇒ none | one definition; both CLI + LSP frontend call `fsm_diagnostics::compute_line_col` | **DERIVED** |
| D-12 | `crate::analysis::analyze` is the SAME parse+analyze front-end the CLI uses (no second front-end) | `crates/fsm-lsp/src/analysis.rs:38,43,94` `use fsm_analyzer::{analyze_with_source,…}; use fsm_parser::{parse,…}; pub fn analyze(...)`; module-doc maps each step to `check.rs` | `parse` + `analyze_with_source` (same as CLI `cmd/verify.rs:130-131`) | **DERIVED** |
| D-13 | Auditor's own oracle: real `fsm verify --json` | built `cargo build -p fsm-cli --bin fsm`; ran `fsm verify --json` on the 4 fixtures | clean=`verified`/0; deadlocks=`property-violated`/1/witness `["ARM"]`; clean+`--max-states 1`=`inconclusive`/2/hit; unreachable=`property-violated`, FSM-E0400 @ line 37 col 5, 2 diags | **DERIVED** |
| D-14 | Auditor's own LSP harness (built EXACTLY as `run_stdio`) `verifyJson` == the oracle | a throwaway `tests/zz_auditor_independent_dump.rs` driving the real `fsm/verify`, then `python3` deep-equal + `json.dumps(sort_keys)` canonical-equal + `cmp` after newline-normalise | all 4: `deep-equal=True canonical-bytes-equal=True`; `EXACT BYTE-EQUAL after newline-normalize ✓` (the only raw diff was a trailing-`\n` *harness* artifact I introduced via `fs::write` vs the CLI's `println!`, not a verifier divergence) | **DERIVED** |
| D-15 | The inconclusive LSP case is NOT a false `verified` (the cardinal sin) | auditor LSP dump of `clean.fsm` w/ `maxStates:1` | `verdict='inconclusive' bound.hit=True` (`exitCode=2`) | **DERIVED** |
| D-16 | Full quad green at `7eb7e59` (auditor re-run) | `cargo test --workspace`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --all -- --check` | **867 passed / 0 failed**; clippy `Finished` 0 warnings/0 `error:`; fmt no diff. `verify_lsp_acceptance` 7/7, `lsp_client_acceptance` 35/35, `fsm_lsp` lib 77/77, `fsm_verify` lib 13/13 + accept 8+5 | **DERIVED** |
| D-17 | No NOTE-1 stale `-wt-` false-red | every `     Running tests/...` line in the workspace run pointed at `/root/dev/embeded-fsm-sdk-target/debug/deps/...` (the canonical shared target), recompiled for this worktree; 0 FAILED, 0 `error:` | disposed: no false-red occurred (no scoped-clean needed) | **DERIVED** |
| D-18 | Toolchain pin honoured (probe-trap guard) | `rustup show active-toolchain` from the repo | `1.75.0-… (overridden by '…/rust-toolchain.toml')` | **DERIVED** |
| D-19 | Worktree clean at `7eb7e59` before the doc write (auditor dump removed) | `git status --porcelain` after `rm tests/zz_auditor_independent_dump.rs` | *(empty)*; `git log --oneline -1` = `7eb7e59` | **DERIVED** |

---

## 2. Why this audit is not a rubber-stamp

A `KEYSTONE-INTACT` verdict is worse than a found problem if it merely echoes the wave's own claim. The v1.4 W2→§11.3 precedent caught load-bearing issues precisely because the auditor *re-derived from source and re-ran the differential battery itself*. This audit applies the same rigour to A2 — the **single Rust-adding, single keystone-violable wave** of v1.5. Concretely, the two cardinal axes were established by **my own commands, not the wave's**:

**(a) The no-fork is source-derived, not asserted.** I did not trust the wave's "negative grep ∅" or the module doc-comments in `verify.rs`/`mod.rs` claiming purity. I independently:

- Re-ran `git diff --stat 5558d68..7eb7e59 -- crates/fsm-verify crates/fsm-cli` and confirmed it is **empty** — the differential oracle (`fsm-verify` + `crates/fsm-cli/src/cmd/verify.rs`) is byte-for-byte the same code the CI runs; A2 did **not** fork it, add a parallel entrypoint, or touch it (D-1).
- Re-derived the forbid-unsafe count **from source at `7eb7e59`** (`git grep -l … 7eb7e59`) rather than echoing the v1.4 "12/11" — it is independently 12 files / 11 crates, and the Cargo.lock delta (D-4) shows **no new `[[package]]`**, so the dep edge added a crate-internal edge, not a new unsafe-capable root.
- Read **every** negative-grep hit's actual code (D-6/D-8): the only two matches (`server.rs:620`, `complete.rs:314`) are the **pre-existing v1.2/L4 standard-LSP completion** capability — `complete.rs` was last touched by `43acb3b` (2026-05-15, the L4 commit) and A2's diff-stat against it is **empty**. These are textDocument/completion, not verification, exactly as the brief anticipates. I used a comment-stripping `grep -vE '://|//!|/\*'` with **no `head`/`echo` branch**, so the empty result is a true empty, not the recurring `cmd | head && echo X` false-positive.
- Read `crates/fsm-lsp/src/capabilities/verify.rs` **in full** (all 408 lines) and the canonical `crates/fsm-cli/src/cmd/verify.rs` **in full**, then *line-matched* the verdict-map (LSP `:274-284` ≡ CLI `:180-190`), the `build_verify_json` (LSP `:318-408` ≡ CLI `:236-319`), and confirmed the call-sites invoke the **identical** `fsm_verify::verify` / `fsm_verify::reachability_diagnostics` (D-9/D-10) and the **single canonical** `fsm_diagnostics::compute_line_col` (D-11 — defined nowhere in `fsm-lsp`/`editors`). The disclosed re-stated marshalling is genuinely the *same category* of pure data-serialisation as `verify.rs`'s own `build_json` and A1's `verifyResult.ts` (the Doc 26 §6 boundary forbids `fsm-lsp`→`fsm-cli`-binary): every value is read **verbatim** off the `VerifyOutcome`/`Diagnostic`s `fsm-verify` returned; **no verification fact (`verdict`, `result`, `has_e0400`, reachable/unreachable sets, witness, config, line/col) is computed locally** — they are a total projection of `fsm-verify` outputs (K6 records the future-drift hazard, not a present defect).

**(b) The differential byte-equality is the auditor's own re-run, not the wave's assertion.** The wave ships `crates/fsm-lsp/tests/verify_lsp_acceptance.rs` (a genuine in-process tower-lsp differential-oracle suite). I ran it (7/7) **but did not stop there** — I independently:

- Built the `fsm` binary myself and captured `fsm verify --json` on `examples/verify/clean.fsm`, `examples/verify/deadlocks.fsm`, `clean.fsm --max-states 1`, and `editors/vscode/src/test/fixtures/unreachable.fsm` (D-13).
- Wrote my **own throwaway harness** (`tests/zz_auditor_independent_dump.rs`, since removed — D-19) that builds the `LspService` **exactly as `fsm_lsp::run_stdio` does** (`LspService::build(Backend::new).custom_method("fsm/verify", Backend::verify_request).finish()`), issues the real `fsm/verify` request, and writes the raw `verifyJson` to disk.
- Cross-checked LSP-vs-oracle with `python3` **deep-equal** + **canonical-bytes-equal** (`json.dumps(sort_keys=True)`) + `cmp` **exact-byte-equal after trailing-newline normalisation** on **all four** fixtures — all pass (D-14). The *only* raw-`diff` discrepancy was a trailing `\n` that **I introduced** in my harness (`fs::write` of `to_string_pretty` vs the CLI's `println!`); the JSON content is identical line-for-line — this is a self-inflicted I/O artifact I explicitly diagnosed, **not** a verifier divergence, and I disposed of it by re-comparing newline-normalised.
- Specifically asserted the inconclusive case (`clean.fsm` + `maxStates:1`) returns `verdict:"inconclusive"`, `bound.hit:true`, `exitCode:2` — **NOT a false `verified`** (D-15, the cardinal verification-UI sin), and that the explosive-model path returns within bounded wall-time (the wave's battery-5 + my harness's sub-second completion: no editor-hang).

**(c) The quad is my own re-run with the NOTE-1 hazard explicitly disposed.** I re-ran `cargo test --workspace` (867/0), `cargo clippy --workspace --all-targets -- -D warnings` (clean), `cargo fmt --all -- --check` (clean) at `7eb7e59` myself (D-16). I checked every `     Running tests/...` path: all resolve to the canonical shared target `/root/dev/embeded-fsm-sdk-target`, recompiled for this worktree — **no stale `-wt-`-baked binary masqueraded as a red** (D-17), so the NOTE-1 false-red hazard did not occur and no scoped `cargo clean -p` disposition was needed. The pre-existing `lsp_client_acceptance` L1–L7 suite is **35/35 unregressed** — A2 added the capability without disturbing the v1.2 LSP surface. Toolchain asserted from inside the repo (`1.75.0 … overridden`), never bare `rustc` (D-18).

If either the no-fork or the differential byte-equality had been anything less than fully confirmed by my own derivation, this verdict would be `BLOCK`. Both are fully confirmed. The residual findings (K6–K8) are P3 doc/observability nits handled by the v1.5 batched closeout — none touches the keystone.

---

## 3. Lens 1 — Source-derived no-fork (the cardinal lens)

**Claim under audit:** A2 did not fork the verifier; `fsm-verify` + the CLI oracle are byte-untouched; `crates/fsm-lsp/**` + `editors/vscode/**` define ZERO reachability/deadlock/transition-selection/guard-eval/completion/exploration logic; `capabilities/verify.rs` is a pure frontend.

### 3.1 `fsm-verify` + the CLI oracle are byte-untouched; the dep edge is the only Rust enabling change (PASS — K1)

`git -C /root/dev/embeded-fsm-sdk diff --stat 5558d68..7eb7e59 -- crates/fsm-verify crates/fsm-cli` ⇒ **empty output** (D-1). A2 did **not** fork `fsm-verify`, did **not** add a parallel entrypoint to it, and did **not** touch `crates/fsm-cli/src/cmd/verify.rs` (the differential oracle). The full A2 delta (D-2) is 9 files; the Cargo.toml delta (D-3) is exactly:

```
+fsm-verify = { path = "../fsm-verify" }
```

(plus a WHY comment). The Cargo.lock delta (D-4) is a single `+ "fsm-verify",` line inside `fsm-lsp`'s `dependencies` array — **no new `[[package]]`** (the crate's transitive deps were already in the workspace lock). Therefore **no new crate root**, and the forbid-unsafe inventory re-derived from source at `7eb7e59` (D-5) is **12 files / 11 crates** (`fsm-lsp` contributing `lib.rs`+`main.rs`; `fsm-verify` already `#![forbid(unsafe_code)]` at `lib.rs:51`) — re-derived, **not** echoed from the v1.4 figure, and consistent with Doc 31 §5's "expected unchanged at 12/11" *because* I verified the precondition (no new crate), not because I assumed it.

### 3.2 Negative-grep ∅ (non-comment, every hit read — K2)

`fsm-lsp/src` (D-6) and `editors/vscode/src` (D-7) yield **no** `fn` *defining* reachability / deadlock / transition-selection / guard-evaluation / completion-drain / timer-fire / region-join / frontier / fixpoint / state-exploration / bfs / lca logic. The two hits a broader completion-token grep produced — `crates/fsm-lsp/src/server.rs:620` (`async fn completion(...)`, the standard `textDocument/completion` LSP handler) and `crates/fsm-lsp/src/capabilities/complete.rs:314` (`pub fn completions(...)`) — are **pre-existing v1.2/L4 LSP completion**: `git log -1` on `complete.rs` = `43acb3b 2026-05-15 Implement fsm-lsp L4: context-aware completion`, and A2's `git diff --stat 5558d68..7eb7e59 -- crates/fsm-lsp/src/capabilities/complete.rs` is **empty** (D-8). These are textDocument/completion, **not** verification semantics — exactly the "pre-existing v1.2-LSP capability, NOT verification logic" case the brief calls out. Every `verify`-bearing `fn` in `fsm-lsp/src` is in the audited frontend (`server.rs:225` `verify_request` wiring; `capabilities/verify.rs:174` `response`, `:194` `run_verify`, `:301` `is_error`, `:318` `build_verify_json`, `:126` `from_value`) — all proven §3.3 to read `fsm-verify` outputs. The grep pipeline strips comment lines (`grep -vE '://|//!|/\*'`) and has **no `head`/`echo` branch**, so ∅ is a true empty (not the recurring false-positive).

### 3.3 `capabilities/verify.rs` read IN FULL — a pure frontend (PASS — K3)

Read all 408 lines + the canonical `crates/fsm-cli/src/cmd/verify.rs` (365 lines) in full. The three purity conditions hold:

- **(i) Same front-end, no second parser.** `verify.rs:206` calls `analyze(&params.text, &path)` where `analyze` = `crate::analysis::analyze` (`analysis.rs:94`), which `analysis.rs:38,43` imports `fsm_analyzer::analyze_with_source` + `fsm_parser::parse` and the module-doc explicitly maps step-for-step to `check.rs`/`fsm verify`'s `parse` + `analyze_with_source` (D-12). The CLI does the identical `parse(&src)` + `analyze_with_source(&pr,&label,&src)` (`cmd/verify.rs:130-131`). The analyze-gate (errors ⇒ exit 4, "cannot verify a model that does not compile") is the SAME posture (LSP `:207-224` ≡ CLI `:132-147`).
- **(ii) Identical `fsm-verify` public API.** `fsm-verify/src/lib.rs:59-65` re-exports `verify`, `reachability_diagnostics`, `ReachabilityReport`, `DeadlockReport`, the `engine::{…}` set. The CLI calls `verify(&ir, opts)` (`cmd/verify.rs:155`) + `reachability_diagnostics(machine, &outcome.reachability, outcome.stats.stop_reason)` (`:176`). The LSP frontend calls `fsm_verify::verify(&ir, opts)` (`verify.rs:234`) + `fsm_verify::reachability_diagnostics(machine, &outcome.reachability, outcome.stats.stop_reason)` (`:262`) — **the same functions, the same arguments, the same `VerifyOptions` construction** (LSP `:229-233` ≡ CLI `:149-153`, absent knobs ⇒ the SAME `fsm_verify::DEFAULT_*`), the **same machine-selection** (named else first — LSP `:246-257` ≡ CLI `:160-170`) (D-9).
- **(iii) Verbatim marshalling, zero local verification decision.** `verdict_label`/`exit_code` (`verify.rs:274-284`) is a 4-arm match on `&outcome.verdict` + `has_e0400` producing the SAME strings/codes as CLI `:180-190`. `build_verify_json` (`:318-408`) builds the SAME sorted `Map` with the SAME keys (`schema`/`machine`/`verdict`/`exitCode`/`properties.{deadlockFree,reachability}`/`bound`), the SAME value projections, and the SAME `compute_line_col(src, d.span.start, LineColUnit::Scalar)` as CLI `build_json` `:236-319` — and `compute_line_col` is the **single canonical** `fsm_diagnostics::compute_line_col` (`fsm-diagnostics/src/lib.rs:172`), defined **nowhere** in `fsm-lsp`/`editors` (D-11, **not a forked position converter**). `has_e0400`/`any_e0400` are derived **only** from the `Diagnostic`s `fsm_verify::reachability_diagnostics` produced (`:267`, `:344`); the `reach_result`/`deadlock`/`bound` blocks read **only** `outcome.verdict`/`outcome.reachability`/`outcome.stats` — all `fsm-verify` outputs. **The handler decides none of "which transition fires / is this a deadlock / is this state reachable / was the bound hit".** The disclosed "re-stated marshalling" is genuinely pure data-serialisation of an `fsm-verify` value-object, the **same category** as `verify.rs`'s own `build_json` + A1's `verifyResult.ts`, justified by the Doc 26 §6 boundary (`fsm-lsp` must not depend on the `fsm-cli` binary crate). This is **NOT** a P0 — no verification fact is computed locally; the only residual is the future-drift hazard K6 (P3, closeout-batch).

The `server.rs` wiring (`verify_request`, `:225`) parses params then `tokio::task::spawn_blocking(run_verify)` — it computes nothing; the offload keeps the async reactor responsive (the bound is `fsm-verify`'s own `max_states`/`max_steps`, so an explosive model returns `inconclusive` within bound, never a hang). `lib.rs` registers it via the canonical `LspService::build(..).custom_method("fsm/verify", Backend::verify_request).finish()` — pure wiring. The VS Code `runVerifyLive` (the `7eb7e59` client commit, read in full from the diff): resolve doc → large-FSM line-count ceiling guard → require `State.Running` → client-side generation-counter debounce → `client.sendRequest("fsm/verify", {uri,text})` → render via **A1's** `renderVerifyReport`/`publishReachabilityDiagnostics`/`verdictLabel`. Every datum is read verbatim from `result.verifyJson`; `verifyJson===null` ⇒ honest exit-4 error; the toast branches on `v.verdict==="verified"|"inconclusive"|else` (it **reads** the verdict, never computes it). **Zero TS verification logic** (D-7).

---

## 4. Lens 2 — Differential-oracle RE-RUN (the binding behavioural proof)

**Claim under audit:** the LSP `fsm/verify` verdict ≡ the CLI `fsm verify --json` by construction, on the fixture battery, including the inconclusive-not-verified guard and the large-FSM no-hang.

### 4.1 The auditor's own oracle (PASS — D-13)

Built the `fsm` binary: `cargo build -p fsm-cli --bin fsm` ⇒ `Finished` in 10.1s; binary at `/root/dev/embeded-fsm-sdk-target/debug/fsm`. Toolchain asserted from inside the repo (`rustup show active-toolchain` ⇒ `1.75.0 … overridden by rust-toolchain.toml`). Independently ran `fsm verify --json`:

| Fixture | exit | verdict | key fact |
|---|---|---|---|
| `examples/verify/clean.fsm` | 0 | `verified` | `bound.hit=false` |
| `examples/verify/deadlocks.fsm` | 1 | `property-violated` | `counterexample.witness = ["ARM"]` |
| `examples/verify/clean.fsm --max-states 1` | 2 | `inconclusive` | `bound.hit=true` (NOT verified) |
| `editors/vscode/src/test/fixtures/unreachable.fsm` | 1 | `property-violated` | `FSM-E0400` @ line 37 col 5, 2 diagnostics |

### 4.2 The auditor's own LSP harness, byte-cross-checked (PASS — K4, D-14/D-15)

A throwaway `crates/fsm-lsp/tests/zz_auditor_independent_dump.rs` (since removed — D-19) built the `LspService` **exactly as `fsm_lsp::run_stdio`** (`LspService::build(Backend::new).custom_method("fsm/verify", Backend::verify_request).finish()`), issued the real `fsm/verify` request for each fixture, and wrote `verifyJson` to `/tmp`. The run completed in **0.06s / 4 tests** (no hang). Cross-check (auditor LSP vs auditor oracle):

| Fixture | deep-equal | canonical-bytes-equal (`json.dumps(sort_keys)`) | exact-byte-equal (newline-normalised) |
|---|---|---|---|
| clean | True | True | ✓ |
| deadlock (witness `["ARM"]`) | True | True | ✓ |
| inconclusive (`maxStates:1`) | True | True | ✓ |
| unreachable (FSM-E0400 @ 37:5) | True | True | ✓ |

The only raw-`diff` discrepancy was a single trailing `\n` (the CLI's `println!` adds one; my harness's `fs::write(to_string_pretty)` does not) — a **self-inflicted harness I/O artifact**, explicitly diagnosed, disposed of by the newline-normalised `cmp` (✓ on all 4) and the content-level `deep-equal`/`canonical-bytes-equal` (True on all 4). **No verifier divergence exists.** The inconclusive case (D-15): LSP `verdict='inconclusive'`, `bound.hit=true`, `exitCode=2` — **asserted NOT a false `verified`** (the cardinal verification-UI sin guard holds end-to-end).

### 4.3 The large-FSM no-hang + the full battery (PASS — D-16)

The wave's `crates/fsm-lsp/tests/verify_lsp_acceptance.rs` (7 tests incl. battery-5, a 12-context-var explosive model verified with `maxStates:8` asserting `inconclusive` within a 20s ceiling) ran **7/7 in 0.13s** — orders of magnitude inside the editor-hang bar; the explosive model short-circuits to `inconclusive` (bound hit), never a false `verified`, never a wedge. The pre-existing `lsp_client_acceptance` L1–L7 = **35/35 unregressed**. Auditor's full quad re-run at `7eb7e59`: **867 tests passed / 0 failed**; `cargo clippy --workspace --all-targets -- -D warnings` clean (no `error:`/warnings; all crates incl. `fsm-lsp`/`fsm-verify`/`fsm-cli` checked); `cargo fmt --all -- --check` clean. **NOTE-1 disposed (D-17):** every test-binary path resolved to the canonical shared target `/root/dev/embeded-fsm-sdk-target/debug/deps/...` recompiled for this worktree — no stale `-wt-`-baked binary, no false-red, no scoped `cargo clean -p` needed. Disk held 7.5G→7.2G free (90→91%) across the quad — no ~5% breach.

---

## 5. Process notes

- **Disk.** `df -h /` 7.5G→7.2G free (90%→91%) start-to-end (the `fsm` build, the auditor LSP harness, the full workspace test, clippy, fmt). The shared `CARGO_TARGET_DIR` (`/root/dev/embeded-fsm-sdk-target` per `.cargo/config.toml`) was warm, so builds were 10–15s and disk barely moved. **No ~5% breach; no `cargo clean -p` invoked** (it was not needed — there was no NOTE-1 false-red and disk never tightened). Stayed inside the repo's `target/`; deleted nothing outside it.
- **The recurring `cmd | head && echo X` false-positive** was avoided: every negative-grep used a comment-stripping `grep -vE` with no `head`/`echo` branch, and every grep hit's actual code was read (D-6/D-8) — the ∅ results are true empties.
- **Auditor-only test removed.** `crates/fsm-lsp/tests/zz_auditor_independent_dump.rs` was created solely for the §4.2 independent cross-check and **removed** before the doc write; `git status --porcelain` is empty at `7eb7e59` except this doc (D-19). The brief's "commit only that one file" is honoured.

---

## 6. The frozen P3 carry-list (v1.5 batched closeout — record, do not fix)

**FROZEN. All items are doc/observability only — no code change is owed (the keystone is intact).** The v1.5-closeout agent folds these in ONE batch (the project's batched-doc discipline; the v1.4-W2 §5 carry-list precedent). None blocks B1∥C1 or the v1.5 tag on the keystone axis.

1. **[K6] Re-stated-marshalling drift-by-future-edit hazard.** `crates/fsm-lsp/src/capabilities/verify.rs:274-408` duplicates ~90 lines of `crates/fsm-cli/src/cmd/verify.rs:180-319`'s verdict-map + `build_json`. Sound **today** (sound by construction + the standing `verify_lsp_acceptance` differential-oracle test is the durability mechanism), but a future `fsm-verify/v2` schema bump would have to touch **three** mirrors (CLI `build_json`, LSP `build_verify_json`, A1's `verifyResult.ts`). *Closeout note in `GATE_VERIFICATION_v1_5`:* record that the `fsm-verify/v1` three-frontend mirror-set is intentional (Doc 26 §6 boundary) and the differential-oracle test is its guard; *consider* — NOT a v1.5 change — a future shared `fsm-verify`-side `build_json` helper iff a v2 ever lands.
2. **[K7] `fsm-verify/vN` versioning policy still source-only (the v1.4-W2 F9 carry, now with a new consumer).** The binding additive-vs-breaking policy lives only in `crates/fsm-cli/src/cmd/verify.rs:70-101`; no Doc 18 reflection. A2 makes the LSP frontend a **second factory-contract consumer** of `fsm-verify/v1`. *Closeout:* re-state the v1.4-W2 F9 item, noting the additional LSP consumer, when folding the policy into Doc 18.
3. **[K8] Client-side trigger-policy undocumented at the spec layer.** `LARGE_FSM_LINE_CEILING=4000` + `VERIFY_LIVE_DEBOUNCE_MS=200` (`editors/vscode/src/commands/verify.ts:88,98`) have no Doc 14/Doc 26/Doc 27 reflection. The keystone is unaffected (`fsm-verify` is bounded-by-construction; the no-hang is test-proven), but the in-editor trigger contract is spec-undocumented. *Closeout:* fold into the C1/D1 doc pass or Doc 27 (non-blocking).

---

## 7. Summary for the orchestrator

- **Verdict: `KEYSTONE-INTACT`.** B1∥C1 may be dispatched; the Doc 31 §2 KEYSTONE-IN-UI binding gate-row **PASSES**.
- **No fork — source-derived (highest priority).** `git diff --stat 5558d68..7eb7e59 -- crates/fsm-verify crates/fsm-cli` = **empty** (the differential oracle byte-untouched). The only Rust enabling change is one `Cargo.toml` `fsm-verify` path edge + a 1-line `Cargo.lock` entry — **no new crate**, forbid-unsafe re-derived from source = **12/11** (not echoed). Negative-grep ∅ (non-comment, every hit read; the 2 completion hits are untouched v1.2/L4, not verification). `capabilities/verify.rs` read in full = a **pure frontend**: same `analyze` seam, **identical** `fsm_verify::{verify,reachability_diagnostics}` the CLI calls, verbatim `VerifyOutcome` marshalling line-for-line mirrored from CLI `verify.rs`, the **canonical** `fsm_diagnostics::compute_line_col` (not forked). **Zero verification facts computed locally.** VS Code `runVerifyLive` reads `verifyJson` verbatim, renders via A1's code — zero TS verification logic.
- **Differential battery — auditor's own re-run.** I built the `fsm` binary, captured `fsm verify --json` on clean/deadlock/inconclusive/unreachable myself, drove the real `fsm/verify` LSP request via my own `run_stdio`-faithful harness, and proved **deep-equal + canonical-bytes-equal + (newline-normalised) exact-byte-equal** on all four. The inconclusive case is **`inconclusive`, NOT a false `verified`** (cardinal-sin guard holds). The explosive model short-circuits sub-second (no editor-hang). Wave `verify_lsp_acceptance` 7/7; `lsp_client_acceptance` L1–L7 **35/35 unregressed**.
- **Quad green at `7eb7e59` (auditor re-run).** 867 tests / 0 failed; clippy `-D warnings` clean; fmt clean. NOTE-1 disposed (no stale `-wt-` false-red; all binaries from the canonical shared target). Toolchain asserted from inside the repo (`1.75.0 … overridden`).
- **0 P0. 0 keystone-violable defect.** 3 P3 doc/observability nits (K6 re-stated-marshalling future-drift, K7 vN-policy spec-surfacing with a new consumer, K8 client trigger-policy spec gap) → the FROZEN §6 v1.5-closeout carry-list. None blocks B1∥C1 or the v1.5 tag on the keystone axis.
- **Branch:** `phase5.2/v15-a2-keystone-audit` (this doc only; auditor harness removed; not merged/pushed; no implementation, no other doc touched). For orchestrator gate before B1∥C1 dispatch.

*End of FSM-AUDIT-KEYSTONE-V15-A2 v1.0.0 — frozen evidence; do not overwrite. A rubber-stamp here would be worse than a found problem; the no-fork was re-derived from source at `7eb7e59` and the differential byte-equality was re-run by this auditor's own harness vs its own `fsm`-binary oracle, not restated from the wave's self-report.*
