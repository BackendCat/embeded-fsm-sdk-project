# FSM Studio — Roadmap

**Status:** Living document. Updated each release; major edits are commit-tracked.
**Owner:** TL/PM (in-conversation Claude under user-granted autonomy).
**Last updated:** 2026-05-18.

This roadmap captures the strategic direction of FSM Studio beyond v1.0. It is informed by user product-strategy conversations, audit findings, and the spec corpus. Items are prioritized by user impact AND by what reduces the cost of subsequent features.

---

## v1.0 — SHIPPED (tagged `v1.0.0`, 2026-05-15, local)

**Theme: Correct, tested, embedded-ready core.**

What ships:
- FSM-Lang DSL (full UML statechart syntax, hierarchical, parallel regions, history, choice, junction, fork/join)
- Compiler pipeline: lexer → parser (Rowan CST + AST) → analyzer (semantic + AST→IR lowering) → IR
- C99 code generator with two dispatch strategies (`--strategy {switch,table,auto}`)
- In-process simulator (RTC interpreter, virtual clock, deterministic JSON traces)
- Canonical formatter (`fsm fmt`, idempotent)
- CLI: `fsm check / generate / fmt / parse / test / doc / decompile / init`
- HAL contract (`fsm_hal_clock_now_ms`, `fsm_hal_assert`), mandatory
- 3 worked examples (motor, traffic-light, vending-machine), all pass full chain check → generate → gcc -Werror → fmt --check → simulator-trace-match

Known v1.0 limitations (each becomes a v1.1+ item):
- `defer EVENT` rejected at analysis (FSM-E0903); will land in v1.1
- Submachine — _absent end-to-end in v1.0_ (the earlier "IR + analyzer landed; codegen no-op" wording was inaccurate; full epic landed in v1.1 — see the v1.1 section + CHANGELOG [1.1.0] "Corrected" note)
- C++17 backend not started (Doc 12 spec exists; impl v1.1+)
- LSP / VS Code / Web IDE / simulator WebSocket all deferred (Docs 13/14/22/05 specs exist; impl v1.2+)
- Conformance coverage: 36/75 codes have formal fixtures (rest have crate-level negative tests); v1.1 closes the gap

See `docs/GATE_VERIFICATION_v1_0.md` for proof of MVP gate compliance (G1–G9).
See `docs/00-Decisions-And-Reconciliation.md` §11 for the full implementation-time decisions log.

---

## v1.1 — Integration ergonomics & UML completion — ✅ SHIPPED (tagged `v1.1.0`→`9634c7e`, 2026-05-15, local; `checkpoint/2026-05-15` anchor)

**Theme: Make the DSL usable in real customer codebases. Finish UML feature parity.**

Adoption-blocking items first; UML completeness second.

> ✅ **W7-FU-1 (P0) RESOLVED** (`d957d12`) — guard-disambiguated same-event dispatch fixed Doc-08-§4.1/§4.2-correct on **both** strategies (simulator independently spec-verified as the oracle; codegen-only bug; tripwire converted to a positive test; conformance CGEN-004). Detail: Doc 00 §11.26, CHANGELOG *Fixed*.
>
> ✅ **W7-FU-2 (P1) RESOLVED** (`41b9e46`) — default transition priority reconciled to **100** (corpus-verified: Doc 04 §8.6, Doc 09 §6, the IR field's own doc-comment; the `unwrap_or(0)` impl was the bug). Class-of-issues: 8 materialization sites → one `DEFAULT_TRANSITION_PRIORITY` constant; byte-identity delta proven exactly `0→100` with no structural perturbation; no shipped-example runtime change. Detail: Doc 00 §11.27, CHANGELOG *Fixed*.
>
> ✅ **SHIPPED 2026-05-15.** 3-lens pre-tag audit (frozen `020f009`): Arch **0 P0** (AD-1/2/3 verified true in source); Correctness **0 P0**, 0 overstated Doc-00 rows; Reliability/Security test-integrity strong, found **SEC-P0-1** (the one P0 — `--import-header`/`fsm.toml` G-02 bypass) → fixed `2b3d221` by converging on `resolve_import` (no third variant; +17 security tests). REL-P1-1 was **stale vs current code** (nested-submachine reject had already shipped in P1-2 `02d4ded`); the record was corrected to the shipped `FSM-E0502` reality, not regressed — verify-vs-code applies to audit claims too (Doc 00 §11.29). **§11.22 cold-from-source quad GREEN:** `cargo clean -p` 9 crates (1436 files/6.4 GiB) → full from-source rebuild → 673 pass / 0 fail, **`-wt-` stale-path = 0**, clippy `-D warnings` clean, fmt clean, examples 5/5, conformance 25/25 (disk safe throughout — the conventional workspace-from-source approach fit without provisioning). Canonical record: `docs/GATE_VERIFICATION_v1_1.md`. **Post-tag owner action (only thing needing the user):** push the tag + commits to exercise the never-run CI matrix (G9; SEC-P0-1's path-canonicalization is the most Windows-divergent surface) — `GATE_VERIFICATION_v1_1.md` §5.

### Integration features
- **`fsm generate --import-header <path.h>`** — auto-extern from existing C header files. ✅ **COMPLETE 2026-05-15** (W5). Reduces friction for users with substantial existing C codebases who currently must hand-write each `extern` declaration. (OPAQUE-BUG-1, surfaced *by* this wave's honest scoping, then fixed in §11.23 — opaque pointer/struct HAL params now model end-to-end; relaxing W5's importer opaque-skip tracked as W5-FU-1.)
- **`examples/integration/{make,cmake,cargo-rust,platformio}/`** — ✅ **COMPLETE 2026-05-15** (W6 `e74888a`). Four worked end-to-end integrations; each *genuinely* builds the generated C under `gcc -std=c99 -Wall -Wextra -Wpedantic -Werror` (make/cmake/cargo-rust run+assert the full lifecycle; platformio ships the real `pio` recipe + a verified host-gcc fallback since `pio` is absent on the box). No codegen defect surfaced — independent corroboration the pipeline holds under real ecosystems.
- **Per-machine strategy override** via `fsm.toml`:
  ```toml
  [machine.MotorControl]
  strategy = "switch"
  ```
  More fine-grained than CLI flag; enables mixed dispatch in one project.
- **`docs/25-Integration-Guide.md`** — ✅ **COMPLETE 2026-05-15** (W6). First-class doc: generated-C ABI contract, C/C++ `extern "C"` recipe, Rust `cc`+FFI recipe (incl. workspace-isolation nuance), Make/CMake/PlatformIO fragments, mandatory-HAL + `_POSIX_C_SOURCE`-ordering gotcha, integration knobs, a 10-row troubleshooting matrix. Cross-linked from README + `examples/integration/README.md`.

### UML completion
- **`defer EVENT` real implementation.** Bounded defer queue per state; replay on state exit. Removes the v1.0 hard-fail with FSM-E0903.
- **Submachine support — EPIC COMPLETE 2026-05-15.** Originally mis-claimed "IR+analyzer already lower submachines; codegen no-op" — that was false (absent end-to-end; the P0-1 aspirational-prose pattern). Reconstructed as a real 4-wave epic, all merged: **W2a** parser/CST/AST `7a69612` → **W2b** analyzer/IR + FSM-E0103/E0610/E0500/E0501/E0502 `2384608` → **W2c** simulator RTC Doc 08 §12 `8b66dc2` → **W2d** C99 codegen both strategies `af8c300`. Implemented **end-to-end for top-level `state X is Sub`** — gcc -Werror (auto+table) + sim≡codegen verified, exercised by `examples/submachine/`. **Precise scope (shipped reality, verified in code 2026-05-15):** a submachine ref *nested inside a composite/parallel state* is **rejected at analysis with `FSM-E0502`** + an actionable message (move it to top level); the lowerer additionally refuses it (defence-in-depth) so codegen never receives it — **no broken C is emitted** (clean-reject > broken-output, the retired-E0903/defer precedent, G1). Shipped by **P1-2 `02d4ded`**; test-pinned by `crates/fsm-analyzer/tests/nested_submachine_rejected.rs`. **SUB-FU-2** tracks proper nested *support* (→ v1.1.x). Accurate statement: **full UML for top-level `state X is Sub`** (gcc+sim≡codegen verified); a nested submachine ref is a documented, diagnosed limitation (FSM-E0502) — NOT a blanket full-UML claim. Lesson ([[verify-status-claims-vs-code]]): this residual was over- then *mis*-corrected several times ("inert leaf" → "emits broken C / fix in progress" — both stale; the pre-tag REL-P1-1 audit even repeated the stale "emits broken C" from an older audit doc). The discipline — verify the *current* code, conservative completed-tense wording — applies to audit claims too; the record was corrected to the shipped E0502 reject, not regressed. Doc 00 §11.29.
- **`likely` / `rare` transition annotations** → `__builtin_expect` codegen. ✅ **COMPLETE 2026-05-15** (W4 `300d1e4`): full vertical slice — contextual keyword (back-compat, lexer unchanged) → CST `BRANCH_HINT` → IR `TransitionObject.hint` (serde-default, schema-valid) → analyzer lowering → portable `<PFX>_LIKELY/_UNLIKELY` macro (`__builtin_expect` on GNU/clang, `(x)` fallback + `<PFX>_NO_BUILTIN_EXPECT` opt-out), both dispatch strategies, gcc -Werror -pedantic clean. Sim accepts+ignores (proven layout-only: sole `Motor.c` delta is the macro wrap; sim≡codegen + 5/5 traces unchanged). Spec'd in Doc 04 §8.8 + Doc 11 §28. +10 tests (589 total).

### Test + conformance hardening
- Populate the untested diagnostic codes with conformance fixtures (G7 evidence becomes full formal-suite coverage, not just crate-level). _(Target is **all live codes**; the live count is now **73** — `FSM-E0903` retired v1.1, `FSM-W0500` retired v1.2; the original "75/75" wording predates those retirements. See `tests/conformance/COVERAGE_MAP.md` + Doc 00 §11.47.)_
- Add cross-architecture gcc cross-compile test (ARM Cortex-M0 + AVR + RISC-V) for at least the Motor example.
- Add a fuzzing harness for the parser (cargo-fuzz integration).

### Quality cleanup carried over from Audit B/C
- LCA dedup (currently 3 implementations across analyzer/simulator/codegen). Generic `ParentResolver` trait in fsm-ir.
- analyzer → AST coupling (16 sites reach into fsm-parser's CST instead of using typed AST). Extend AST accessors then strip cst::* imports.
- `pub → pub(crate)` sweep across 9 crates (~340 over-exposed items per Audit B P2-A1).

### Release criteria for v1.1
- 0 P0, ≤5 well-scoped P1s in pre-tag audit — **0 open P0, 0 open spec-conformance items (W7-FU-1 + W7-FU-2 both resolved); pre-tag audit in progress; final gate = cold-from-source full-workspace quad**
- 600+ tests passing
- One real-world customer-style integration example proven end-to-end on a representative embedded target — ✅ **SATISFIED 2026-05-15** (W6: four ecosystems, each gcc -Werror RUN-verified; platformio `native`+`uno` AVR target)

---

## v1.1 retrospective (post-tag, 2026-05-15)

Codified post-release step (this section's own "How the roadmap evolves" rule). Metrics snapshot: `docs/metrics/2026-05-15-v1.1.0.json` (v1.0→v1.1 deltas, D.8 contract).

- **The §5.4 behavioural-acceptance mandate was the single highest-ROI process change of the sprint.** Requiring every behaviour-touching wave to gcc-compile *and RUN* the generated C and assert observable behaviour surfaced **four pre-existing P0/P0-class defects** that ~600+ symbol-presence-era tests had passed straight over: **OB1** opaque-extern silent-drop (Doc 00 §11.23), **W7-FU-1** guard-disambiguated dispatch broken on *both* strategies (§11.26), **W7-FU-2** default-priority spec divergence `0` vs `100` (§11.27), **SEC-P0-1** import-header G-02 containment bypass (§11.28). These were latent in shipped code; nothing structural would have caught them. `.contains("Motor_init")` proves a name exists, not that the machine *does* anything — the mandate is now the project's load-bearing correctness guard (26 gcc-RUN acceptance files concentrated on exactly the risk surface).

- **`verify-status-claims-vs-code` is symmetric (§11.29).** The pre-tag REL-P1-1 finding ("nested submachine emits non-compilable C") was a *stale pessimistic audit claim* — the auditor traced an older pre-P1-2 doc; current code rejects that case cleanly at analysis with `FSM-E0502`. Blindly applying the recommendation would have regressed the release record into a falsehood. **An audit finding is a claim; code at HEAD is ground truth.** The same rigor that catches optimistic overstatement must catch a pessimistic stale audit, or the record drifts the other way.

- **The 3-lens pre-tag gate is well-calibrated.** Across a 15-wave surface it caught **exactly one** real P0 (SEC-P0-1) — **0 P0 arch, 0 P0 correctness, 0 overstated Doc-00 rows**. It neither over-fires (didn't manufacture blockers) nor under-fires (caught the one real security regression on a new file-read surface). Keep the 3-lens gate as-is for v1.2.

- **Infra/disk pattern validated — no provisioning needed.** The §7 warm-cache sawtooth sustained 15 build-heavy waves on a shared 78G box (steady ~7–9G target, never runaway). The §11.22 release gate's mandatory **cold** quad fit via the conventional CI-cache approach: `cargo clean -p` the 9 first-party crates (drops every first-party + test binary, including stale `-wt-`-tainted ones) while **keeping the content-addressed registry dep cache** (Cargo-fingerprint path-independent). Cold quad ran 673/0 with `-wt-` stale-path = 0; disk stayed ≥3.4G throughout. This is the recorded validated pattern — do not provision disk for a cold release quad; clean first-party-from-source, keep registry deps.

- **Feed-forward to v1.2 (recommendation — the orchestrator decides):**
  - **(a) Run a dedicated cleanup wave BEFORE v1.2 features.** Both pre-tag audits flagged the same two accumulating debts as the standing v1.2 gate: `pub` over-exposure (only a small fraction used cross-crate; `fsm-parser` is the dominant offender at ~230–280 public items — add `unreachable_pub` + `missing_docs` lints and a `pub→pub(crate)` sweep) and analyzer→parser-CST coupling (~15 sites reaching into `cst::*` instead of typed AST — extend AST accessors, strip the imports). Both are non-behavioural and ship-acceptable *now*, but they compound: every v1.2 feature built on the over-wide surface makes the eventual narrowing harder. Sequence the cleanup wave first; it is bounded and has no feature risk.
  - **(b) Sequencing opinion for the v1.2 feature set — LSP-first over C++17 codegen.** Both are in v1.2 scope. **Recommendation: do the LSP server (Doc 14) first.** Rationale: LSP is the *required substrate* for both the VS Code extension and the future Web IDE — it unlocks two downstream deliverables, so an early LSP de-risks the largest chunk of v1.2+v1.3 dependency. C++17 codegen (Doc 12) is self-contained (it wraps the already-correct C emitter with `extern "C"` + a CRTP shell — no new pipeline semantics, low coupling), so it can land *in parallel* or *after* without blocking anything and carries little integration risk. Front-loading the high-fan-out item (LSP) and trailing the isolated one (C++17) maximises critical-path progress. This is a reasoned recommendation, not a decree — the orchestrator owns the call.

---

## v1.2 — Developer tooling — ✅ SHIPPED (pending tag `v1.2.0`, 2026-05-16, local)

**Theme: The `fsm-lsp` Language Server.**

> **Re-scoped 2026-05-16 (user-endorsed; Doc 00 §11.40).** `v1.2.0` ships **the LSP server only** — large, cohesive, independently valuable, and the substrate everything else (VS Code, Web IDE) needs. The **VS Code extension moved to v1.3** (its natural LSP-client home; Doc 27), the old v1.3 "Simulation & verification" theme to **v1.4**, and **C++17 codegen (Doc 12) to its own minor** (orthogonal backend, low coupling, lands in parallel/after without blocking anything). This keeps the validated small-tight-tagged cadence (v1.0/v1.1) and the post-v1.1 retrospective's LSP-first feed-forward; a mega-v1.2 (LSP + VS Code + C++17) would contradict the shippable-increment pattern. The re-version used this doc's own "propose-with-rationale" mechanism (§"How the roadmap evolves") under delegated product-owner autonomy; reversible.

What shipped:
- **LSP server** (`fsm-lsp`, Doc 14, Doc 26): `tower-lsp`-based; `initialize` (UTF-8/UTF-16 `positionEncoding` negotiation) + `publishDiagnostics` · `documentSymbol` · `foldingRange` · `hover` · `definition` · `completion` · `references` · `prepareRename`/`rename` · `semanticTokens` (full+range) · `codeAction` · `inlayHint` — the full L1–L7 epic. **All fed by ONE `analyze()`** byte-consistent with `fsm check` (editor squiggles can never disagree with the CLI), `tower-lsp`-client behaviourally tested, phase-audited (L1 + L5, both PROCEED with structural verification), `#![forbid(unsafe_code)]` workspace 10/10. Single-file in v1.2 (cross-file workspace intelligence is v1.3 with the Web IDE story). Detail: CHANGELOG `[1.2.0]`, Doc 00 §11.32–§11.38.
- v1.2-gate debt cleared: FU#67 (`fsm.toml [compiler] allow/deny` now wired), FU#68 (`unreachable_pub` self-enforcing), DRIFT-2 (one byte→line/col core), DRIFT-1/-3/-4/-5 (Doc 20 reconciled to shipped reality), FU-DEAD-CODES (W0200 implemented, W0500 retired — live code count **73**), SEC-FU (RUSTSEC-2026-0009 eliminated; SCA wired into CI). Doc 00 §11.41–§11.48.

Known v1.2 limitations / accepted-tracked debt:
- `analyzer → parser-CST coupling` (~15 files) deferred out of the release critical path to a tracked post-v1.2 wave — arch-audit-rated ship-acceptable & non-behavioural; a big refactor of the most behaviourally-critical crate at a release boundary is worse-EV (v1.1 shipped the same debt class tracked the same way). Doc 00 §11.49.
- Cross-file/workspace LSP intelligence, the `wasm32` build of `fsm-lsp`, and `workspaceSymbol` remain v1.3 (no in-tree project-index substrate; single-file in v1.2, shipping Doc 14 §8's own cross-file degradation message). Doc 26 §9.

See CHANGELOG `[1.2.0]` for the full surface and `docs/00-Decisions-And-Reconciliation.md` §11.32–§11.49 for the implementation-time decisions log. `docs/GATE_VERIFICATION_v1_2.md` (canonical gate evidence, citing live code count **73**) is authored at tag-time.

### v1.2 — what was deliberately NOT in this minor (moved out by the 2026-05-16 re-scope)
- **VS Code extension** → **v1.3** (Doc 27): an LSP client — its language intelligence comes free over `vscode-languageclient` now the LSP shipped; only the diagram Webview is substantive-new.
- **C++17 code generator** (Doc 12) → **its own minor**: wraps the already-correct C emitter with `extern "C"` + a CRTP class API (no-STL profile per Doc 12 §3). Self-contained, low coupling — lands in parallel/after without blocking anything.

### v1.2 refinement — LSP-first sequencing (extraction pass, 2026-05-15 — historical context for the re-scope above)

The v1.1 retrospective feed-forward (b) recommended LSP-first over C++17; the extraction-first-for-epics discipline (FSM-PROC-SUBAGENT) then produced a deep-extraction architecture pass scoped against the *current code at HEAD `0a657d4`*. Result: **`docs/26-LSP-Architecture.md`** (FSM-ARCH-LSP v1.0.0) — the authoritative LSP crate shape + wave plan; it refines (does not contradict) Doc 20 §9.

**Why LSP-first holds (confirmed against code, not prose):** the LSP is the required substrate for both the VS Code extension (Doc 22, an LSP client) and the Web IDE (Doc 05 §2.5, LSP client in a Web Worker) — largest downstream fan-out. C++17 codegen is self-contained (wraps the already-correct C emitter) and may land in parallel or after without blocking anything.

**Dependency edge:** the LSP epic is gated on the standing v1.2 pub-hygiene wave (retrospective feed-forward (a) — `pub→pub(crate)` sweep + `unreachable_pub`/`missing_docs`). That wave MUST run **first** and be briefed with the Doc 26 §6 "keep-public, document-as-intended-`fsm-lsp`-API" allowlist, so the surface narrowing is informed by the LSP's real needs rather than blind. This is listed in Doc 26 as L0 (the gate prerequisite, not new scope).

**Wave plan (one line per wave; full briefs + §5.4-LSP behavioural-acceptance definitions in Doc 26 §8):**
- **L0** (gate) — pub-hygiene sweep + Doc 26 §6 intended-API allowlist (the existing standing gate wave; pinned here as the dependency edge).
- **L1** — MVP spine: `fsm-lsp` crate + `fsm-lang-server` bin + `initialize` (UTF-8/UTF-16 `positionEncoding` negotiation) + `publishDiagnostics` reusing the *exact* `fsm check` pipeline (`parse` + `analyze_with_source`). Proves the reuse seam + position encoding end-to-end before any breadth.
- **L2** — `documentSymbol` + `foldingRange` (pure forward-`SymbolTable`/CST; no new analysis).
- **L3** — `hover` + `definition` (single-file; `resolve_*` decl spans + IR enrichment).
- **L4** — `completion` (context-sensitive; keywords from Doc 04 §1.5; snippets from Doc 22 §9).
- **L5** — `references` + `rename` + `prepareRename` (introduces the one new analysis: `ReferenceIndex`, a derived CST walk resolved through `SymbolTable`).
- **L6** — `semanticTokens` (consumes the already-drafted Doc 21; decl-vs-ref disambiguation).
- **L7** — `codeAction` + `inlayHint` (diagnostic-driven fixes + IR-derived hints).

Phase-boundary audits (FSM-PROC-SUBAGENT §11.3) after L1 and after L5.

**Explicitly scoped OUT of v1.2 (Doc 26 §9 — no implied freebies):** incremental CST parsing (`parse_incremental` does **not** exist — Doc 20 §4.5/L787 is aspirational prose; full re-parse-on-debounce is the v1.2 contract); cross-file/workspace-wide definition/references/rename + the startup workspace scan (no in-tree project-index substrate — single-file in v1.2, shipping Doc 14 §8's own cross-file degradation message; deferred to v1.3 with the Web IDE multi-file story); the `wasm32` build of `fsm-lsp` (architecture stays WASM-compatible — analysis core is tokio-free — but compiling it is v1.3 scope); `workspaceSymbol` (same project-index dependency).

---

## v1.3 — VS Code extension — ✅ SHIPPED (pending the `v1.3.0` annotated tag, 2026-05-16, local; `checkpoint/2026-05-16-v1_3` anchor)

**Theme: First-class editor experience on top of the shipped LSP.**

Re-scoped here 2026-05-16 (Doc 00 §11.40): the VS Code extension is the natural consumer of the v1.2 LSP server. Architecture + wave plan: **`docs/27-VSCode-Extension-Architecture.md`** (FSM-ARCH-VSCE) + **`docs/28-v1_3-VSCode-Wave-Plan.md`** (FSM-PLAN-VSCE-V13, the V1–V6 epic plan + tag gate) — all language intelligence comes **free** over `vscode-languageclient` now the LSP shipped; only the ELK diagram Webview is substantive-new.

> ✅ **SHIPPED 2026-05-16 (pending the contingent tag step).** The deferred §11.49 analyzer→CST-coupling debt was paid first as **W0** (`ceb8efd` == `checkpoint/2026-05-16-w0-clean`; DRIFT-2-grade, additive-only, 0 new deps, non-behavioural — 13 documented R-1..R-4 leave-and-explain residuals; Doc 00 §11.50). Then the **VS Code extension epic V1–V6 landed** (`editors/vscode/`, **zero Rust delta**): V1 client spine, V2 grammar/snippets, V3 commands, V4 read-only diagram WebviewPanel, V5 activity-bar tree views, V6 host-only bundled installable VSIX. Each wave has a real `@vscode/test-electron` behavioural-acceptance gate (34 Extension-Host tests, oracles independently recomputed — never symbol-presence). The two Doc-28-mandated §11.3 phase-boundary audits (post-V1, post-V4) plus the orchestrator-cadenced post-W0 + post-V2/V3 audits each returned **PROCEED-WITH-NOTES**. Canonical record: **`docs/GATE_VERIFICATION_v1_3.md`**. The release is contingent on the cold-from-source cargo quad **and** the JS Extension-Host lane (`npm ci` + `@vscode/test-electron` under `xvfb` + `npm audit`) at the gate-doc commit being green + an independent pre-tag four-lens audit returning TAG-CLEAR (the §11.30/§11.22 sequence). **Owner/v1.3.x carry-overs (not blockers; `GATE_VERIFICATION_v1_3.md` §5):** the never-run G9 CI matrix + the new JS lane (owner push); the **G9-gated 5-platform binary-bundling tail** (V6 ships host-only — owner/infra action); **VSIX publishing/signing OUT** (owner credential decision); **no top-level `LICENSE`** (owner/legal, coupled to publishing).

- **VS Code extension** (Doc 22 / Doc 27 / Doc 28): ✅ **SHIPPED** — packaged extension wiring `vscode-languageclient` to `fsm-lang-server`, the Doc 21 TextMate grammar, syntax highlighting, the 7 client/CLI commands, the activity-bar tree views, and an ELK-laid-out diagram `WebviewPanel`. Per Doc 27/28 V1–V6. Detail: CHANGELOG `[1.3.0]`, Doc 00 §11.50–§11.62.

**Deferred out of the shipped v1.3 scope (carried to a later minor — explicitly NOT delivered in the v1.3 extension epic, Doc 28's V1–V6 was the v1.3 contract):**
- **`wasm32` build of `fsm-lsp`** (Doc 26 §9): the analysis core is tokio-free and WASM-compatible by design; this is the compile/packaging work that also unlocks the Web IDE. **Deferred to v1.4** (paired with the Web IDE multi-file story; not in the Doc 28 V1–V6 extension scope).
- **Cross-file / workspace LSP intelligence** (Doc 26 §4.6/§9): the project-index substrate enabling cross-file `definition`/`references`/`rename`, `workspaceSymbol`, and the startup workspace scan — single-file was the explicit v1.2 contract. **Deferred to v1.4** (paired with the Web IDE multi-file story; not in the Doc 28 V1–V6 extension scope).

---

## v1.4 — Verification core — ✅ SHIPPED (pending the `v1.4.0` annotated tag, 2026-05-16, local; `checkpoint/2026-05-16-v1_4` anchor)

**Theme: Trustable behavioral validation.**

> Was "v1.3 — Simulation & verification"; pushed to v1.4 by the 2026-05-16 re-scope (Doc 00 §11.40) when the VS Code extension took the v1.3 slot. **Scope CONFIRMED by the owner (Doc 00 §11.63):** v1.4 = the **complete Verification core** = cut **A** (bounded explicit-state reachability + deadlock detection driving the shipped `fsm_simulator::Interpreter` as the semantic oracle, surfaced as `fsm verify`, honestly closing FSM-E0400/FSM-W0602) **+ B** (trace differential replay, `fsm baseline`). The WS server / Web IDE / wasm32 / cross-file LSP items below are **NOT v1.4** — they are the explicitly-deferred menu (WS server → v1.5; Web IDE/wasm/Monaco/cross-file project-index → v1.6; mechanised sim≡codegen → v1.4-stretch/v1.5; the owner did NOT re-cut).

> ✅ **SHIPPED 2026-05-16 (pending the contingent tag step).** The verification core landed across **W1** (the keystone `fsm-verify` bounded-reachability+deadlock spine + the `fsm verify` CLI + FSM-E0400/W0602 emission, `2866821`/`dd2d41e`), **W2** (the load-bearing snapshot-completeness P0 `efae559` + composite/parallel/history/timer/submachine coverage `3447881`), **W3** (`fsm baseline` trace differential replay, `fc88ec1`), and **W4a** (the factory-integration completeness audit, `4eb04dc`, verdict **`FACTORY-COMPLETE`**), closed out by **W4b** (this batched doc/ledger closeout = the §11.30 gate-doc commit). The verification core is a **NEW single `fsm-verify` crate** that **drives the shipped Interpreter as the sole semantic oracle** (never re-implements FSM semantics — the §4.1 keystone, an epic-level architecture invariant); the canonical interface is the **CLI** (`fsm verify`/`fsm baseline`, distinct verified/property-violated/inconclusive exit codes, machine-readable `--json`, deterministic, zero-daemon — no server, no plugin host). The two §11.3 phase-boundary audits (post-W1 keystone `a75cd69`, post-W2 breadth) each returned **PROCEED-WITH-NOTES**; the W4a factory-audit returned **FACTORY-COMPLETE**. Canonical record: **`docs/GATE_VERIFICATION_v1_4.md`**. The release is contingent on the independent pre-tag four-lens audit returning TAG-CLEAR + the §11.22/§11.30 cold-from-source quad at the gate-doc commit being green (the §11.30 sequence, the 4th-consecutive prospective-clean release). **Owner/v1.4 carry-overs (not blockers; `GATE_VERIFICATION_v1_4.md` §6):** the **R7 mechanised sim≡codegen-equivalence proof is an explicit named DEFERRAL** (v1.4-stretch/v1.5 — not omitted); the carried v1.3 owner-escalations unchanged (G9 CI matrix + JS lane never-run; the G9-gated 5-platform binary-bundling tail; VSIX publishing OUT; no top-level `LICENSE`; the v1.3.x `makeNonce` CSPRNG one-liner; JC-3 config-discoverability).

- **Model checking integration**: ✅ **SHIPPED** — `fsm verify` dispatches over the reachable configuration space (driving the Interpreter), proves deadlock-free within a bound (honest INCONCLUSIVE on bound-hit, never a false proof), and reports unreachable states/transitions via **FSM-E0400 / FSM-W0602 — now IMPLEMENTED in v1.4** (the §1.3 / R8 *catalog-reserved-but-unimplemented* drift is **closed in the record**: both codes were catalogued from v1.0 but had no emission site until v1.4; the live `DiagnosticCode` count is unchanged at 73 — v1.4 gave them their first emission site, Doc 10 annotated). Per Doc 30 §4.2-W1/W2; Doc 00 §11.64/§11.65. _(Note: the original "extend the existing dead-transition analyzer" framing was superseded — verification is a NEW `fsm-verify` crate driving the Interpreter, NOT an `fsm-analyzer` extension; Doc 30 §1.3/§4.2-W0 / §11.63 reconciled.)_
- **Trace replay & differential testing**: ✅ **SHIPPED** — `fsm baseline --record`/`--check` captures a frozen `fsm-trace/v1` corpus from a known-good build and flags any semantic drift on a later build with a precise first-mismatch report. Per Doc 30 §4.2-W3; Doc 00 §11.67.
- **Simulator WebSocket protocol** (Doc 13): **DEFERRED to v1.5** (Doc 00 §11.63 — batch verification for a factory is a CLI invocation, not a service; the WS *simulator* server is its own v1.5 concern with its own SEC-P0-1 gate then). JSON-RPC 2.0 server lets VS Code / Web IDE drive the simulator interactively. Step-debugging, breakpoints, watchpoints.
- **Web IDE** (Doc 05) + **`wasm32` `fsm-lsp` build** + **cross-file/workspace LSP intelligence**: **DEFERRED to v1.6** (Doc 00 §11.63; folded here from the deferred-v1.3 carry-overs, now re-deferred — not in the v1.4 verification-core cut). Monaco editor + WASM-compiled toolchain + ELK-laid-out diagram; the project-index substrate enabling cross-file `definition`/`references`/`rename` + `workspaceSymbol`.

## v1.4 retrospective (post-shipped, pending-tag, 2026-05-16)

Codified post-release step (this section's own "How the roadmap evolves" rule). Metrics snapshot: `docs/metrics/2026-05-16-v1_4.json` (v1.3→v1.4 deltas, D.8 contract).

- **The keystone-as-architecture-invariant held end-to-end.** "Drive the shipped Interpreter as the sole semantic oracle; never fork the semantics" (the recurring P0-1 / MV5-1 lesson, elevated to an epic-level invariant by Doc 00 §11.63) was independently re-derived from source at **both** the post-W1 and post-W2 §11.3 audits and proven behaviourally against an independent interpreter BFS — zero re-implemented transition-selection / guard-eval / timer-fire / completion anywhere in `fsm-verify`. The two-audit cadence (keystone-first, then breadth) caught the **load-bearing D-2** (`InterpreterSnapshot` lossy for submachines/timers — a latent false-`ProvenNoDeadlock` foot-gun) at the W1 boundary, *before* W2 built on it; W2's first unit fixed it (lossless snapshot, round-trip-gated). Auditing the spine before the breadth wave is the stronger application of §11.3.

- **`verify-the-record` was load-bearing at the closeout, not just mid-epic (the §11.29 lineage).** The W2-audit restated the post-W1 verdict as PROCEED-WITH-NOTES; the v1.4 closeout **re-read the frozen post-W1 audit `a75cd69` in full** to cite its actual verdict in the gate doc rather than echo the restatement, and **independently re-derived every pinned numeric at the gate-doc commit** (live `DiagnosticCode`=73 — unchanged because E0400/W0602 were *catalogued* pre-v1.4 and v1.4 only gave them an *emission site*; `forbid(unsafe_code)`=12 roots/11 crates; conformance=26 unchanged; the v1.4 Rust delta = the *large additive* `fsm-verify` crate + W2-P0 + the verify/baseline CLI, +7398/−6 vs v1.3, **0 new external deps** — the symmetric analogue of v1.3's cardinal "zero Rust delta" error, stated correctly here as a *substantial* additive delta).

- **The pipeline-before-UI bar (`[[feedback_embeded_fsm_pipeline_before_ui]]`) shaped the whole epic.** The verification core shipped as a CLI-canonical, machine-readable, deterministic, zero-daemon pipeline (no server, no plugin host — Doc 00 §11.63) and the W4a factory-audit proved the full `fsm verify → generate → check → baseline` loop is CI/factory-integratable headless with **zero workflow gaps** by driving the real binary end-to-end twice. The single W4a finding (§3.G — the exit-2-INCONCLUSIVE Doc 18 discoverability gap) was a doc-surfacing item (the in-source verdict family is owner-mandated-correct), folded into Doc 18 §3 by this closeout — not a code change, not a tag blocker.

- **R7 named, not omitted.** v1.4's "trustable validation" theme is the *runtime* verification core + the differential-replay drift oracle; the **mechanised sim≡codegen-equivalence proof is an explicit, named deferral** (v1.4-stretch/v1.5 — the §11.49 leave-and-explain precedent applied to scope). The project keeps re-learning the overstatement sin: an unaddressed sub-claim of the theme must be scoped-out loudly, not silently dropped (recorded in `GATE_VERIFICATION_v1_4.md` §6 + CHANGELOG `[1.4.0]` *Known limitations* + Doc 00 §11.71).

- **NOTE-1 → an explicit cold-quad precondition (the §11.22/§11.30 hardening).** A validated-this-epic operational hazard — a stale parallel-worktree test binary masquerading as a *false red* (the symmetric danger to a false green) — is codified: the §11.22 cold quad now **explicitly mandates** a `cargo clean` (or non-shared `CARGO_TARGET_DIR`) *before* the full-workspace gate, written into `GATE_VERIFICATION_v1_4.md` §4.2 as a mandatory step (Doc 00 §11.70). Removes the ambiguity at its source for every future release.

---

## v1.5 — Convenience Layer (UI/DX) — ✅ SHIPPED (pending the `v1.5.0` annotated tag, 2026-05-17, local; `checkpoint/2026-05-17-v1_5` anchor)

**Theme: surface the v1.4 verification core in VS Code (the UI/DX convenience layer).**

> **Scope (Doc 31, owner-pre-framed + owner-delegated).** With the headless pipeline FACTORY-COMPLETE at v1.4 (the `[[feedback_embeded_fsm_pipeline_before_ui]]` "pipeline before UI" bar satisfied), v1.5 is the unblocked UI/DX convenience layer. **5 sub-epics:** **A1** (verification in VS Code via CLI-spawn — zero Rust delta), **A2** (the `fsm/verify` LSP-embed — the single Rust-adding wave), **B1** (Marketplace-readiness manifest), **C1→C2** (the TS frontend-practices audit → scoped remediation), **D1** (static docs/landing site — the only droppable-from-tag item). The WS *simulator* server, Web IDE / wasm32 / Monaco / cross-file project-index remain the explicitly-deferred menu (→ v1.6; **not** re-cut into the convenience layer).

> ✅ **SHIPPED 2026-05-17 (pending the contingent tag step).** The convenience layer landed across **A0** (Doc 31 the wave-plan-of-record, `df16055`), **A1** (`fsm.verify`/`fsm.baseline` via the proven CLI-spawn seam, zero Rust delta, `5558d68`), **A2** (the `fsm/verify` LSP-embed — the single Rust-layer change: one `fsm-verify` path edge in `crates/fsm-lsp/Cargo.toml`, **no new crate** — `4737259` Rust + `7eb7e59` client), the **post-A2 source-derived keystone phase-audit** (`d22ef3b`, verdict **`KEYSTONE-INTACT`**), **B1** (the Marketplace-readiness manifest pass, `7b04f91`), **C1** (the read-only TS frontend-practices audit, `38f80c1`), and **C2** (the `fsm.verifyLive` Extension-Host E2E `dc8d926` + prettier `d5fba3c`), closed out by this batched doc/ledger closeout = the §11.30 gate-doc commit. **KEYSTONE-IN-UI held** (the binding tag-gate row): VS Code verification is *either* a spawn of the same `fsm` binary the CI calls (A1) *or* `fsm-lsp` calling the *same* `fsm_verify::verify`/`reachability_diagnostics` the CLI calls (A2) — **no** verifier re-implementation in `crates/fsm-lsp/**` or `editors/vscode/**`, independently re-derived from source by the post-A2 audit (the differential oracle byte-untouched; the LSP verdict+witness byte-equals `fsm verify --json` incl. the inconclusive-NOT-false-"verified" guard). The C1 audit returned a disciplined disposition table (0 P0; 2 fix-in-v1.5 executed by C2; the rest leave-and-explain). Canonical record: **`docs/GATE_VERIFICATION_v1_5.md`**. The release is contingent on the independent pre-tag four-lens audit returning TAG-CLEAR + the §11.22/§11.30 cold-from-source quad (with the mandatory NOTE-1 `cargo clean -p` Step 0) **and the re-activated JS lane** at the gate-doc commit being green (the §11.30 sequence, the **5th-consecutive** prospective-clean release).

- **Verification surfaced in VS Code**: ✅ **SHIPPED** — `fsm.verify`/`fsm.baseline` (A1, CLI-spawn, zero Rust delta) + `fsm.verifyLive` (A2, the LSP-embed `fsm/verify` custom request — a pure `fsm-verify` frontend, debounced + large-FSM-ceiling-guarded). INCONCLUSIVE rendered honestly (never a false "verified"); the deadlock witness as a navigable list; reachability `FSM-E0400`/`FSM-W0602` as a VS Code `DiagnosticCollection`. Differential-tested vs the real `fsm` binary at both the Rust (`verify_lsp_acceptance.rs`) and the Extension-Host (`verify.test.ts`/`verifyLive.test.ts`) layers. Doc 31 §1 W-A1/W-A2; Doc 00 §11.73/§11.74/§11.75.
- **Marketplace-ready VS Code extension**: ✅ **SHIPPED publish-READY (NOT published)** — `version 1.0.0`, `icon`/`keywords`, `repository.url` corrected to the owner repo, a Marketplace README + a user CHANGELOG, `fsmLang.codegen.*` settings; a lint-clean installable VSIX (publishing is an owner credential action). The carried v1.3.x `makeNonce` `Math.random`→CSPRNG nit + JC-3 config-discoverability resolved by B1. Doc 31 §1 W-B1; Doc 00 §11.76.
- **TS frontend-practices**: ✅ **AUDITED + scoped-remediated** — C1 (read-only) found the `editors/vscode/**` surface fundamentally sound (Zero-Legacy, the R6-philosophy Extension-Host suite in the correct primitive); C2 executed the 2 fix-in-v1.5 items (the `fsm.verifyLive` Extension-Host E2E hardening A1/A2 + prettier). The flat-ESLint-9 / `madge` / `noUncheckedIndexedAccess` items are a **deferred-tracked v1.6 "VS Code extension FE-hygiene" batch** (real-but-modest, must not ride the Marketplace pass — the leave-and-explain discipline). Doc 31 §1 W-C1/W-C2; Doc 00 §11.77.
- **Static docs/landing site (D1)**: **DEFERRED to v1.6** — the only droppable-from-tag item (owner pre-decision); its v1.5 agent run was output-content-filtered and left uncommitted unreviewed work, **deliberately discarded (not shipped, not built upon)**. Re-scoped to v1.6, carrying with it the LICENSE/CONTRIBUTING/CoC reconcile + the client-trigger-policy doc-surfacing. Doc 00 §11.78.

## v1.5 retrospective (post-shipped, pending-tag, 2026-05-17)

Codified post-release step (this section's own "How the roadmap evolves" rule). Metrics snapshot: `docs/metrics/2026-05-17-v1_5.json` (v1.4→v1.5 deltas, D.8 contract).

- **KEYSTONE-IN-UI was a binding tag-gate row, source-derived not asserted.** The keystone ("drive the shipped verifier, never fork the semantics") was extended from the CLI to the editor and made a *binding* tag-gate criterion. Only A2 added Rust (one `fsm-verify` path edge into `fsm-lsp`, **no new crate**); the mandatory **post-A2 source-derived §11.3 phase-audit** independently re-derived the no-fork from source (the differential oracle `fsm-verify`+`fsm-cli` byte-untouched; negative-grep ∅ non-comment with every hit read; `capabilities/verify.rs` read in full = a pure frontend) **and re-ran the differential battery itself** (the auditor's own `run_stdio`-faithful harness vs its own `fsm`-binary oracle, byte-equal incl. the inconclusive-NOT-"verified" cardinal-sin guard) — **not** echoed from the wave. Verdict `KEYSTONE-INTACT`. Isolating the only fork-possible surface into one gated, pre-budgeted audit (Doc 31 §7.1) is the structural realisation of "keystone enforcement = a single named checkpoint".

- **The release-record-symmetry check, v1.5-shaped.** v1.5's Rust delta is *small by design* (A2's dep edge + the LSP capability; A1/B1/C2 are TS, C1 is docs) — exactly the framing-trap shape (a small-but-nonzero delta easy to mis-state as "no Rust change"). The closeout **re-derived it from `git diff --shortstat 3bbcd0f d5fba3c`** = 7 files / +933 / −1, **0 new external deps**, the differential oracle byte-untouched — stated as exactly that *small additive* delta, **neither under-stated** (the v1.3 cardinal "zero Rust delta" error) **nor over-stated** (the v1.4 +7398 verification-core epic). Every pinned numeric was independently re-derived at the gate-doc commit (live `DiagnosticCode`=73, conformance=26, `forbid(unsafe_code)`=12/11 — all unchanged because v1.5 added no code and A2 added a dep edge not a crate).

- **The JS lane was re-activated as a binding tag gate.** Unlike v1.4 (which touched no `editors/` and carried the JS lane unrun), the v1.5 surface *is* `editors/vscode/**` (TS delta 41 files / +2693 / −1026). The JS lane (`npm audit` triaged + `vsce package` lint-clean + the 41-test Extension-Host suite) is a **binding gate at the gate-doc commit**, described as the contingent gating step (no fabricated transcript — the honest-prospective posture; the remote CI matrix has still never run, carried to the owner).

- **The memory-SoT freshness gate + the §7.4 stale-pointer reconcile were done in the same closeout pass.** The single biggest drift the v1.4 meta-session found (the backlog-SoT head status line + the release-record section lagging the tag) was pre-empted: the backlog SoT head line was advanced + a `v1.5.0 RELEASE RECORD` section added in this same closeout (honest-prospective — "gate-doc landed; tag pending the contingent steps", no fabricated tag). The Doc 31 §7.4-mandated in-repo `docs/ROADMAP.md` "See also" stale pointer (it called the v1.1-era `~/.claude/plans/toasty-prancing-goose.md` "the live multi-phase execution plan") was reconciled to point at the backlog SoT + Doc 31 — fixed *here*, in the closeout (Doc 31 explicitly carried it to the v1.5 closeout, not a code wave). The broader README/Doc-07/Doc-14 doc-honesty staleness pass remains a **separate already-identified backlog item** (flagged in `GATE_VERIFICATION_v1_5.md` §6.4 — *not* fixed in this minor).

### v1.6 candidates (carried/named, not yet scheduled)

> _Sequencing note (updated 2026-05-19): the factory-reliability/CI epic SHIPPED (`checkpoint/factory-reliability-ci-2026-05-18`) and the interactive debug-interface epic SHIPPED (`checkpoint/2026-05-19-debug-interface`), both deliberately **`checkpoint/<name>`-not-`vX.Y.0`** so neither consumes the owner-reserved v1.6. The owner sequence is: factory-reliability/CI → the debug-interface minor → **v1.6 = landing+doc-honesty** → the full `signal`-primitive epic (OWNER-3, D-3) → the visual-DSL-constructor capstone. v1.6 is still owner-reserved + owner-paced._

- **D1 — the static EdgeForge-modeled docs/landing site** (the slipped v1.5 item) + the OSS-files reconcile (LICENSE/CONTRIBUTING/CoC presence) + the doc-honesty staleness pass (README/Doc-07/Doc-14 — Doc 31 §6 H6) that must precede/accompany D1 so it does not broadcast a stale README. **+ now also: the Doc 33 §5 CF-2 Doc 16 §2–§6 HAL-symbol staleness (`fsm_hal_clock_now_ms`/`fsm_hal_assert(bool,msg)` vs the older `_ms`/`_fail(file,line,msg)` documented shapes — cross-ref task #103) + the DF-02 DBGUX-prose `.trace.yaml`→`.trace.json` precision + the Doc 33 GT-7 DBGUX `trace.rs:64`/`:172`→`:65`/`:174` line-drift** — the same v1.6 doc-honesty batch (the implementations are correct + the emitted symbols are the correct shipped contract; only the docs drifted; the frozen design docs are design-evidence — fixed by the v1.6 pass, not in-place).
- **The VS Code extension FE-hygiene batch** — flat ESLint-9 + `typescript-eslint@8` (C1-F3), `madge` import-cycle guard (C1-F7), the `noUncheckedIndexedAccess` strictness delta (one coherent tooling pass, deliberately *not* churned onto the v1.5 Marketplace pass). **+ the 3 pre-existing devDependency-only transitive npm vulns `{esbuild, serialize-javascript, mocha}` (DF-03 — byte-identical at v1.5/factory/debug-interface; build-time/test-only, not in the shipped runtime, no non-breaking fix; await an upstream mocha pulling serialize-javascript ≥7.0.5 + the esbuild major-bump its own scoped chore).**
- **The WS-server-or-not decision** — the Doc 13 WS *simulator* server is its own later concern (with its own SEC-P0-1 gate); whether it is the v1.6 epic or deferred again is an owner/orchestrator decision, **not** pre-resolved here. _(The interactive debug-interface epic deliberately rode `fsm-lsp`'s existing stdio `custom_method` seam — owner D-2 — so the standalone WS daemon stays the documented not-built alternative; this does **not** pre-resolve the WS-server question.)_
- **The full `signal`-primitive epic (OWNER-3 — D-1 decided the shape, D-3 sequenced it after the debug-interface minor AND after the owner-reserved v1.6)** — the full first-class `signal` primitive (poll/interrupt/edge/debounce, `feature signals`-gated, append-only keywords, cohesively retiring the #127 AUDIT-DETPRIM `edge`/`level`/`debounce` GAPs); its own gated Doc-34-class epic; its Part-2(d) per-family interrupt-model dependency already delivered (FSM-DESIGN-CHIPMATRIX §1.2); scope captured Doc 33 §8, tracked-not-lost; opened only on explicit orchestrator/owner schedule.
- **CF-1 — the AVR Harvard `.rodata` + TABLE-dispatch UNTESTED CORRECTNESS HAZARD (Doc 33 §5 / OWNER-4)** — a CI-runner-only `avr-gcc -mmcu=atmega328p` link+run on-target-matrix candidate for the **factory-reliability epic's on-target-matrix extension** (a *codegen-correctness* item, **not** a debug-interface item — the factory W1→W2 on-target-differential method extended to a second family; the box is disk-tight ⇒ CI-runner-only, an infra-escalation not a grind). Named, not auto-scoped.
- **The parked Certifiability / MISRA–CERT-C owner-review** (Doc 31 §3/§4 OWNER-C / Doc 32 OWNER-3) — a documented MISRA-C/CERT-C compliance posture for the generated C; a named parked ROADMAP-v1.6+ candidate requiring an explicit owner GO to schedule (composes with verification into a "Certifiability & factory-integration" arc; *not* auto-started — the factory epic un-parked only the *testing/proof* half of Certifiability).

---

## Factory-Reliability/CI epic (Phase-6.0) — ✅ SHIPPED (pending the `checkpoint/factory-reliability-ci-2026-05-18` annotated milestone tag, 2026-05-18, local)

**Theme: close ALL testing gaps to factory-grade + a push-and-it-runs CI — the factory-reliability *proof* layer.**

> **A quality milestone, NOT a `vX.Y.0` (the locked TL tag-naming decision).** The owner's roadmap reserves **v1.6.0 for the landing-page + doc-honesty pass (post-factory)**; the factory-reliability/CI epic is the quality milestone that *precedes* it. The milestone tag is therefore `checkpoint/factory-reliability-ci-2026-05-18` (the proven `checkpoint/<name>` immutable-anchor pattern — LOCAL, no push; G9 the owner's switch), deliberately **not** `v1.6.0` or any `vX.Y.0` (that would unilaterally consume the owner-reserved v1.6). The §11.30/GT-12 cold-quad rigor applies to this checkpoint tag identically to a release tag.

> **Scope (Doc 32, owner-endorsed; the 2026-05-17 factory-reliability/CI directive).** With the pipeline FACTORY-COMPLETE (v1.4) and the UI/DX convenience layer shipped (v1.5), this epic un-parks the **testing half** of Certifiability/OWNER-C. The **MISRA-codegen half stays separately PARKED** (OWNER-3 — explicit owner GO required; it composes with verification into a "Certifiability & factory-integration" arc, *not* auto-started). G9 stays the owner's publication switch, decoupled from BOTH progress AND quality (quality is local every wave — the per-wave quad + the new `make ci-local`).

> ✅ **SHIPPED 2026-05-18 (pending the contingent tag step).** The factory-reliability/CI proof layer landed across **W0** (Doc 32 the wave-plan-of-record, `83b912a`), **W1** (the R7 generic host-trace differential — the keystone prerequisite, `71cc1bd`; + the FW109/FW110-FU codegen-correctness arc the differential *surfaced + fixed* — a real timer over-fire, composite exit-set, deep_history, choice/junction, local-transition LCA: `f92c1cb`…`265a8da`), **W2** (the on-target QEMU/`mps2-an385` trace differential — the keystone proper, `0e0e61f`; CI-runner-only by design), **W3** (the G7 build-failing live-enum-derived conformance lock + `COVERAGE_MAP.md`-as-generated-artifact + conformance 26→53, `0c69282`), **W4** (the measured-floor coverage-ratchet gates — `cargo-llvm-cov` + `c8`, `64a3233`), **W5** (the ci.yml 4 additive-isolated lanes + the one-command `make ci-local` gate, `edd4483`), the **W6a source-derived keystone phase-audit** (`654cf64`, verdict **`KEYSTONE-INTACT`**, FROZEN), the **W6b independent pre-tag four-lens audit** (`b8368da`, verdict **`TAG-CLEAR`** — 0 P0 / 0 P1 / 3 P2 / 3 P3, FROZEN), closed out by **W6c** = this batched doc/ledger closeout = the §11.30 gate-doc commit. **KEYSTONE — DRIVE-THE-ORACLE held** (the binding tag-gate row): the host **and** target generated-C trace byte-equals the shipped, never-forked `fsm_simulator::execute_trace` oracle — **no** forked semantics in the W1 trace-hook / the W2 QEMU+target harness / the W5 CI glue, the differential oracle byte-untouched across the whole epic arc, independently re-derived from source by the W6a audit (the host differential 3×-deterministic + the W2 toolchain-free guards re-run by the auditor; the catalogue-integrity contract `9+3+0=12` non-vacuous). The FW110-FU fixes **deliberately changed production-C semantics for the better** (a real timer-over-fire bug the differential caught — a *correctness* fix, not a regression and not a fork; the keystone is *no forked oracle*, not frozen output). Canonical record: **`docs/GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md`**. The release is contingent on the §11.22/§11.30 cold-from-source quad (with the mandatory NOTE-1 `cargo clean -p` Step 0) **and the binding JS lane** (the surface touches `editors/vscode/**` via W4 `c8` + W5 `extension-host` — exactly like v1.5) at the gate-doc commit being green (the §11.30 sequence, the **6th-consecutive** prospective-clean release); the independent pre-tag four-lens (W6b) returned `TAG-CLEAR` (frozen, satisfied) and the W6a keystone audit is `KEYSTONE-INTACT` (frozen, satisfied). **Owner/post-tag carry-overs (not blockers; `GATE_VERIFICATION_FACTORY_RELIABILITY_2026_05_18.md` §5/§6):** the **G9** push (now LOAD-BEARING — the automatic remote pipeline + the on-target/coverage matrix depend on it; the 4 new lanes never run remote); the **5-platform binary tail** (blocked-on-G9, not a ci.yml lane); **VSIX packaged-not-published** (v1.5 OWNER-A lineage); the top-level **`LICENSE`/`CONTRIBUTING`/`CoC`** gap (owner/legal, carried to v1.6 with D1); the **parked Certifiability/MISRA codegen-mode** arc (OWNER-3, owner GO required); the **v1.6 D1 landing + the doc-honesty pass** (recorded-NOT-scoped — "publish-ready" = this factory-reliability proof + the v1.6 D1 landing both land); **R7** the mechanised sim≡codegen-equivalence *formal proof* (Phase-6.0 delivers the *empirical* host+target differential; the fully-mechanised proof remains the named v1.4-stretch/later deferral, unchanged).

## Phase-6.0 retrospective (post-shipped, pending-tag, 2026-05-18)

Codified post-release step (this section's own "How the roadmap evolves" rule).

- **The keystone-as-architecture-invariant held end-to-end, host AND target.** "Drive the shipped `fsm_simulator::execute_trace` oracle; never fork the semantics" (the recurring P0-1/MV5-1 lesson, an epic-level invariant since Doc 00 §11.63) was extended from the CLI to a host *and* an on-target QEMU generated-C differential and made a **binding tag-gate row**. The mandatory W6a source-derived phase-audit independently re-derived the no-fork from source across all three fork-possible surfaces (the W1 trace-hook + the W2 QEMU+target harness + the W5 CI glue — every negative-grep hit read + adjudicated; the oracle + the `cmd/{test,baseline}.rs` seam byte-untouched across the whole epic arc, EMPTY-diff + per-file blob-SHA-identical) **and re-ran the host+target differential battery itself** — verdict `KEYSTONE-INTACT`. Isolating the only fork-possible surface into one gated, pre-budgeted audit (Doc 32 §7.1) is the structural realisation of "keystone enforcement = a single named checkpoint" (the v1.4/v1.5 keystone-first precedent, applied to the factory layer).

- **The differential found a real shipped bug 800+ tests had passed over — the methodology working, with evidence.** The W1 host differential immediately surfaced a genuine **timer over-fire** in the shipped codegen (single-shot `elapsed_ms` decrement where a multi-period budget-loop was needed) plus a composite exit-set, a deep_history, choice/junction-lowering, and a local-transition-LCA defect — the FW109/FW110-FU arc. ~800 symbol-presence/structural tests had never caught them; a *behavioural* host-and-target byte-differential against the shipped oracle did, the first time it ran. This is the §5.4-behavioural-acceptance lesson (the v1.1 retrospective's "single highest-ROI process change") generalised to the factory-reliability layer: the audit is load-bearing *because* it caught a real bug, not in spite of finding one.

- **The release-record-symmetry check, Phase-6.0-shaped.** Phase-6.0's Rust delta is *substantial* (the W1 trace-hook + the FW110 codegen-correctness fixes + the W3 lock + the W1/W2 differential harnesses) — the symmetric framing-trap to v1.3's "zero Rust delta" cardinal near-miss (a large delta is as easy to *mis-attribute* as a small one is to under-state). The closeout **re-derived it from `git diff --shortstat 2ff8ac3 X`** = 22 files / +8192 / −190, **0 new external deps**, and explicitly **did NOT echo W6b's pre-F-05 `+8188/−190`** — it independently re-derived `+8192` *at `X`* (the +4-net being exactly the F-05 cosmetic doc-comment tidy). Every pinned numeric was independently re-derived at the gate-doc commit (live `DiagnosticCode`=73, `forbid(unsafe_code)`=12/11 — both unchanged because the epic added a dev-dep edge + harnesses + CI config, no crate root; conformance 26→53; the W3 lock's *own* runtime output `73 live = 37 fixtures + 36 allowlist`; `COVERAGE_MAP.md` confirmed the W3-regenerated artifact). The v1.3-cardinal-error-form grep of the record = 0 hits asserting "no/zero Rust delta" (`[[feedback_embeded_fsm_verify_record_release_layer]]` applied symmetrically at the closeout layer).

- **The checkpoint-not-`vX.Y.0` tag-naming was a deliberate owner-reserved-minor decision.** v1.6.0 is owner-reserved for the landing+doc-honesty pass; this quality milestone *precedes* it. Tagging it `v1.6.0` would have unilaterally consumed the owner's reserved minor — exactly the kind of unilateral product-numbering the project's verify-the-record/owner-autonomy discipline forbids. The proven `checkpoint/<name>` immutable-anchor pattern (the same §11.30/GT-12 cold-quad rigor, no version-number claim) is the correct vehicle; "the public 1.0" remains a *publication event* (G9 + the v1.6 D1 landing), not a moving version label.

### Forward-look (the owner roadmap — recorded, not auto-scheduled)

Per the owner's GLOBAL quality-first + stage-gate roadmap (2026-05-17): **factory-reliability/CI epic (this quality milestone — SHIPPED)** → **the interactive debug-interface minor (SHIPPED — see below)** → **v1.6 = the landing-page + the doc-honesty pass** (D1 the static EdgeForge-modeled docs/landing site, slipped from v1.5 not cut; **+** the README/Doc-07/Doc-14 doc-honesty staleness pass that must precede/accompany D1 so the public site is accurate — Doc 31 §6 H6 / `ASSESSMENT_PRODUCT_AND_DOCS_2026_05_16.md` PART B; the OSS-files reconcile LICENSE/CONTRIBUTING/CoC; **+ the Doc 33 §5 CF-2 Doc-16 + DF-02 DBGUX-prose + the Doc 33 GT-7 DBGUX line-drift**) → **the full `signal`-primitive epic** (OWNER-3, D-1 shape / D-3 sequenced after the debug-interface minor AND after v1.6) → **the visual DSL constructor = the FINAL capstone feature** (the owner-named end-state — a visual statechart constructor on top of the now-factory-trustable, fully-tested pipeline). The parked Certifiability/MISRA–CERT-C *codegen-mode* arc (OWNER-3) remains a named candidate requiring an explicit owner GO (the factory epic delivered only the *testing/proof* half of Certifiability). These are **recorded, not auto-scheduled** — the orchestrator/owner owns the sequencing call (the §3 / Doc 31 §3 "surface, don't auto-scope" discipline).

---

## Interactive debug-interface epic (Phase-8.0) — ✅ SHIPPED (pending the `checkpoint/2026-05-19-debug-interface` annotated milestone tag, 2026-05-19, local)

**Theme: the interactive debug interface — an in-editor live simulation/debug surface that is JUST ANOTHER FRONTEND of the one shipped `fsm_simulator` oracle, never a second semantics.**

> **A quality milestone, NOT a `vX.Y.0` (the locked TL tag-naming decision; D-3/OWNER-1).** The owner's roadmap reserves **v1.6.0 for the landing-page + doc-honesty pass (post-factory)**; this minor lands as its **own post-v1.6 minor**. The milestone tag is therefore `checkpoint/2026-05-19-debug-interface` (the proven `checkpoint/<name>` immutable-anchor pattern — LOCAL, no push; G9 the owner's switch), deliberately **not** `v1.6.0` or any `vX.Y.0` (that would unilaterally consume the owner-reserved v1.6 — the factory-epic `checkpoint/<name>`-not-`v1.6.0` precedent, Doc 00 §11.80). The §11.30/factory-GT-12 cold-quad rigor applies to this checkpoint tag identically to a release tag.

> **Scope (Doc 33, owner-decided D-1/D-2/D-3).** With the pipeline FACTORY-COMPLETE (v1.4), the UI/DX convenience layer shipped (v1.5), and the factory-reliability/CI proof layer landed, this epic surfaces the shipped simulator in VS Code via the proven v1.5 W-A2 `LspService::build().custom_method` LSP seam. **The single keystone-violable surface is concentrated into ONE gated wave (W1)**; W2–W4 hold the no-fork invariant *trivially* (they render the oracle's output and compute nothing). **NO standalone WS daemon, NO new port/socket, NO new network attack surface** (the attack surface removed by construction — owner D-2; the Doc 13 WS daemon stays the documented not-built alternative, DBGUX §8 fork-1 option B). The full `signal`-primitive epic → OWNER-3 parked (D-1 shape / D-3 sequenced after this epic + the owner-reserved v1.6).

> ✅ **SHIPPED 2026-05-19 (pending the contingent tag step).** The interactive debug interface landed across **W0** (Doc 33 the wave-plan-of-record, `phase8.0/debug-iface-plan-of-record`), **W1** (the `fsm/simulate*` LSP request layer + the single sanctioned non-stepping `Interpreter::set_context_field` helper — the only keystone-violable Rust/LSP wave, `1de83b0`: a pure `Interpreter`-marshalling transport `crates/fsm-lsp/src/capabilities/simulate.rs` +693 over the proven v1.5 W-A2 stdio `custom_method` seam + the one additive non-stepping helper `crates/fsm-simulator/src/interpreter.rs` +25, a 3-line guarded `rt.context.insert`, **0 `StepRecord` by construction**; the single Rust-layer enabling change = one *internal* `fsm-lsp`→`fsm-simulator` path-dep edge, **no new crate, 0 new external `[[package]]`**), **W2** (the debug WebviewPanel — the v1.3 statechart reused VERBATIM + active-config overlay + Inject panel + the StepRecord timeline, all driven through W1's `fsm/simulate`, NO semantics, `032438c`), **W3** (breakpoints + time-travel — the client-side StepRecord-**predicate** evaluator + `Interpreter::snapshot`/`restore`, NO semantics, `b79e052`/`ec903e5`), **W4a** (the capture→`.trace.json` round-trip — recorded-from-the-oracle via the shipped `write_trace_yaml`, byte-identical by construction; the `.trace.json`-not-`.trace.yaml` self-correction is **real and load-bearing**, `ae774dd`→`8092d3a`), the **W4b source-derived keystone phase-audit** (`phase8.0/w4b-keystone-audit`, audited `8092d3a`, verdict **`KEYSTONE-INTACT`**, FROZEN), the **W4c independent pre-tag four-lens audit** (`phase8.0/w4c-four-lens-audit`, audited `8092d3a`, verdict **`TAG-CLEAR`** — 0 P0 / 0 P1 / 3 P2 / 2 P3, FROZEN), closed out by **W4d** = this batched doc/ledger closeout = the §11.30 gate-doc commit. The gating **#128 F-1/F-2 codegen-correctness fixes** + the `defer` doc-drift fold landed on the same arc (genuine #110-class divergent-second-resolver corrections — F-1 unifies the const-resolver, F-2 the queue-config, each with a hard rejecting diagnostic `FSM-E0411`/`FSM-E0412` for the deferred form, never silent; the silent-misconfig class closed end-to-end). **KEYSTONE-IN-DEBUG held** (the binding tag-gate row): every debug UI verb is exactly one shipped `fsm_simulator::Interpreter` call marshalled over the W1 `fsm/simulate*` LSP custom request; breakpoints a PREDICATE over the oracle-emitted `StepRecord` fields, NEVER a guard/transition re-eval; time-travel `Interpreter::snapshot`/`restore`; capture round-trips the SAME `write_trace_yaml`/`execute_trace` `fsm test` uses ⇒ byte-identical by construction; **no** forked semantics in `crates/fsm-lsp/**` or `editors/vscode/**`, the differential oracle byte-untouched across the whole epic arc save the one sanctioned non-stepping helper, independently re-derived from source by the W4b audit (the host differential 3×-deterministic + the W1 LSP acceptance + the 47-test ExtHost suite re-run by the auditor; the catalogue-integrity contract `11+3+0=14` non-vacuous; the §W4 `.trace.json` replay independently hand-confirmed non-vacuous). Canonical record: **`docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md`**. The release is contingent on the §11.22/§11.30 cold-from-source quad (with the mandatory NOTE-1 `cargo clean -p` Step 0) **and the binding JS lane** (the surface touches `editors/vscode/**` via W2/W3/W4 — exactly like v1.5/factory) at the gate-doc commit being green (the §11.30 sequence, the **7th-consecutive** prospective-clean release); the independent pre-tag four-lens (W4c) returned `TAG-CLEAR` (frozen, satisfied) and the W4b keystone audit is `KEYSTONE-INTACT` (frozen, satisfied). **Owner/post-tag carry-overs (not blockers; `docs/GATE_VERIFICATION_DEBUG_INTERFACE_2026_05_19.md` §5/§6):** **OWNER-1** the release-numbering call (the `checkpoint/`-not-`vX.Y.0` parked default); **OWNER-2 / G9** the push of the LOCAL `v1.0.0`–`v1.5.0` tags + **all** `checkpoint/*` anchors (incl. `checkpoint/factory-reliability-ci-2026-05-18` + this epic's `checkpoint/2026-05-19-debug-interface`) + commits + the never-run remote CI matrix (G9 stays load-bearing; the JS lane a binding *local* gate but remote-unrun); **OWNER-3** the deferred full `signal`-primitive epic (D-1 shape / D-3 sequenced after this epic + v1.6); **VSIX packaged-not-published** (v1.5 OWNER-A lineage — Phase-8.0 added only the `fsm.openDebug` manifest contributions, no extension runtime dep); the top-level **`LICENSE`/`CONTRIBUTING`/`CoC`** gap (owner/legal, carried to v1.6 with D1); the **parked Certifiability/MISRA codegen-mode** arc (OWNER-3, owner GO required); the **v1.6 D1 landing + the doc-honesty pass** (recorded-NOT-scoped — "publish-ready" = the factory-reliability proof + this debug interface + the v1.6 D1 landing all land; **incl. the Doc 33 §5 CF-2 Doc-16 + DF-02 DBGUX-prose** doc-honesty items); the **5-platform binary tail** (blocked-on-G9, not a ci.yml lane); **R7** the mechanised sim≡codegen-equivalence *formal proof* (the named v1.4-stretch/later deferral, unchanged); **CF-1** the AVR Harvard `.rodata`+TABLE-dispatch hazard (a CI-runner-only factory on-target-matrix candidate, **not** a debug-interface item); **task #17** CI-matrix owner-blocked.

## Phase-8.0 retrospective (post-shipped, pending-tag, 2026-05-19)

Codified post-release step (this section's own "How the roadmap evolves" rule).

- **KEYSTONE-IN-DEBUG — the single-oracle architecture invariant held a FOURTH time, concentrated into one gated wave.** "The debug interface is just another frontend of the one shipped `fsm_simulator` oracle; never a second semantics" (the recurring P0-1/MV5-1/v1.4-§11.63/v1.5-KEYSTONE-IN-UI/factory-KEYSTONE-DRIVE-THE-ORACLE lesson) was applied a fourth time. The *structural* realisation: only **W1** adds Rust / a `crates/` dep edge / a `crates/` method; W2–W4 are TS (the webview) and hold the invariant *trivially* (they render the oracle's output and compute nothing). The mandatory W4b source-derived phase-audit independently re-derived the no-fork from source across the whole debug surface (the oracle + the `cmd/test.rs` `execute_trace` seam + `write_trace_yaml` byte-untouched across the whole epic arc save the one sanctioned non-stepping `set_context_field` helper, per-file blob-SHA-identical; negative-grep ∅ across `simulate.rs` + `editors/vscode/src/debug/**`, every hit read; every `fsm/simulate` op handler = exactly one shipped `Interpreter` call) **and re-ran the host differential 3× + the W1 LSP acceptance + the 47-test ExtHost suite + the §W4 `.trace.json` replay non-vacuity hand-run itself** — verdict `KEYSTONE-INTACT`. Concentrating the only fork-possible surface into one gated, pre-budgeted audit is the structural "keystone enforcement = a single named checkpoint" (the v1.4/v1.5/factory keystone-first precedent, applied to the single Rust/LSP-adding wave).

- **The `.trace.json`-not-`.trace.yaml` self-correction was load-bearing — a vacuous-pass trap caught in-flight.** The W4a wave self-corrected `ae774dd` (`capture→.trace.yaml`) → `8092d3a` (`capture→.trace.json`) because `fsm test`'s `collect_traces` discovers ONLY `*.trace`/`*.trace.json` — a `.trace.yaml` would be **silently skipped → a vacuous "pass" that verified nothing**. The W4b auditor independently re-derived the discovery rule from `cmd/test.rs:159` and **hand-ran `fsm test` on both forms**, confirming the `.trace.json` reports a real `pass` and the `.trace.yaml` is silently undiscovered. The recorded-from-the-oracle capture is byte-identical-by-construction *only because* it round-trips the same shipped `write_trace_yaml`/`execute_trace` `fsm test` uses — a property that is worthless if the artifact is the wrong extension. The self-correction is the §6-no-premature-success / no-vacuous-pass bar working in-flight.

- **The release-record-symmetry check, Phase-8.0-shaped.** Phase-8.0's Rust delta is *moderate and easy to mis-attribute* (W2–W4 are TS — only W1 + the gating #128 F-1/F-2 fixes are Rust). The closeout **re-derived it from `git diff --shortstat fbae38b..8092d3a -- crates/`** = 28 files / +3260 / −214, **0 new external deps** (the single `Cargo.lock` edge the *internal* `fsm-lsp`→`fsm-simulator` path dep), and explicitly **did NOT echo W4c's whole-delta `57 files / +10045 / −243`** — it independently re-derived the `-- crates/`-scoped delta at `X` (both correct at their respective scopes — the git-diff-scope distinction, not a discrepancy). Every pinned numeric was independently re-derived at the gate-doc commit: live `DiagnosticCode`=**75** (the F-1/F-2 +2 vs the frozen factory 73 — *correctly attributed* to the two new rejecting diagnostics, NOT a silent drift); conformance=**55** (53→55, +VAL-NEG-009/010); `forbid(unsafe_code)`=**12 roots/11 crates** unchanged (W1 added an internal path-dep edge + a helper into the already-forbid-unsafe `fsm-simulator`, no crate root); the differential CORPUS partition re-derived from source AND the test's `--nocapture` output (`11+3+0=14==CORPUS.len()`, dynamically asserted — the never-game discipline). The v1.3-cardinal-error-form grep of the record = 0 hits asserting "no/zero Rust delta" (a Python context-check of all 12 lexical hits — `[[feedback_embeded_fsm_verify_record_release_layer]]` applied symmetrically at the closeout layer; NEITHER under-stated [the v1.3 cardinal error] NOR over-stated [the v1.4 +7398 / factory +8192]).

- **The checkpoint-not-`vX.Y.0` tag-naming was, again, a deliberate owner-reserved-minor decision.** v1.6.0 is owner-reserved for the landing+doc-honesty pass; this minor lands as its own post-v1.6 minor. Tagging it `v1.6.0` would have unilaterally consumed the owner's reserved minor — exactly the kind of unilateral product-numbering the project's verify-the-record/owner-autonomy discipline forbids (the factory-epic `checkpoint/<name>` precedent applied a second time). The owner sequence is recorded, not auto-scheduled: factory-reliability/CI → this debug-interface minor → v1.6 (landing+doc-honesty) → the full `signal`-primitive epic → the visual-DSL-constructor capstone.

---

## C++17 code generator — its own minor (between v1.3 and v2.0; orthogonal, unscheduled)

Re-scoped out of v1.2 (Doc 00 §11.40) into a self-contained minor of its own — it does not block, and is not blocked by, the LSP/VS Code/simulation line.

- **C++17 code generator** (Doc 12): wraps the already-correct generated C with `extern "C"` + a CRTP class API for embedded C++ users. No-STL profile per Doc 12 §3. Low coupling — no new pipeline semantics; may land in parallel with any of the above.

---

## v2.0 — Production scale

**Theme: Multi-machine systems + advanced optimization.**

- **Multi-machine projects with rich inter-machine messaging.** Today: `send EVT to Foo` works for a known instance. Tomorrow: discover, address, route across compile units.
- **Per-region `@strategy(switch|table)` annotations.** Mixed-strategy dispatch within a single machine for hot-vs-cold path optimization.
- **Profile-guided codegen** (opt-in). Optional dev-build instrumentation; profile data feeds back into codegen as branch hints.
- **`fsm bench`** — micro-benchmark generated C against reference embedded targets (Cortex-M0, M3, M4, AVR). Reports ROM, RAM, dispatch latency.
- **Code coverage for generated firmware** via `gcov` hooks.
- **Plugin API** for custom code generators (per Doc 03 §plugin notes). Third parties can ship `fsm-codegen-rust`, `fsm-codegen-vhdl`, etc.

---

## Cross-version commitments (the "always" promises)

Regardless of version:
- **Deterministic output.** Same `.fsm` source + same compiler version + same `fsm.toml` → byte-identical generated C. Reproducible builds.
- **Heap-free generated runtime.** No `malloc`, no `new`, no growable containers. The embedded G2 promise.
- **Backwards compatibility within a major version.** v1.x consumes v1.y `.fsm` source without surprise. Breaking changes wait for v2.0.
- **Audit + CHANGELOG + tag rigor.** No tag ships without 0 P0s + ≤5 well-scoped P1s + comprehensive CHANGELOG entry + signed-off MVP gate verification.

---

## Out-of-scope (never)

These have been considered and rejected for embedded reasons:
- **Hash-map / bloom-filter dispatch.** ROM cost > switch/table for typical embedded FSMs.
- **Runtime profile collection** built into firmware. Overhead exceeds benefit; users with that need can implement themselves via `pure extern` calls.
- **Native distributed FSM mesh.** This is a code generator, not an actor framework. Users wanting distributed coordination wrap the generated machines with their own transport.

See `docs/00-Decisions-And-Reconciliation.md` §10 + §11 for the full set of rejected-for-good-reasons items.

---

## How the roadmap evolves

- After each tag (v1.0, v1.1, v1.2, ...): pre-tag audit + post-tag retrospective update this doc with empirical lessons. Later minors may get reshuffled based on what current customers want.
- Strategic conversations with the user (recorded in `memory/project_user_messages_*.md`) feed into roadmap revisions.
- Audit findings drive priority shifts: a P1 from a fresh audit that affects user value typically gets fast-tracked into the next minor.
- **Worked example (2026-05-16, Doc 00 §11.40):** v1.2 was re-scoped from a mega-minor (LSP + VS Code + C++17) to LSP-only via the propose-with-rationale process below — VS Code → v1.3, the old v1.3 "Simulation & verification" → v1.4, C++17 → its own minor; user-endorsed, reversible. This is the mechanism working as designed, not an exception to it.

If you (a future maintainer / agent / contributor) think a feature should jump versions, propose the move with: (a) what user value it unlocks, (b) what dependency chain it sits on, (c) what risk it adds. The orchestrator weighs and updates.

---

## See also

- `docs/00-Decisions-And-Reconciliation.md` — normative TL decisions + §11 implementation log
- `docs/AUDIT_*_*.md` — historical audit evidence (informs priorities)
- `docs/processes/SUBAGENT_CONVENTIONS.md` — how wave-level work gets done
- `CHANGELOG.md` — what shipped when
- **The live execution plan-of-record is the project backlog SoT** (`memory/project_embeded_fsm_sdk_backlog.md` — phases/waves/gate criteria/status) **+ the per-epic wave-plan docs** (the latest is `docs/31-v1_5-UIDX-Wave-Plan.md` for the v1.5 convenience-layer epic; Doc 30 for v1.4, Doc 28/29 for v1.3). _(Reconciled 2026-05-17, the Doc 31 §7.4 closeout item: this "See also" previously pointed at `~/.claude/plans/toasty-prancing-goose.md` calling it "the live multi-phase execution plan" — that was a **v1.1-era plan, superseded two minors ago** by the backlog SoT and the per-epic wave-plan docs; the stale pointer is removed so a first-contact reader is not misled.)_
