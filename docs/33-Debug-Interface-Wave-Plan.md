# FSM Studio — Epic: Interactive `.fsm` Debug Interface — Wave-Plan-of-Record

**Document ID:** FSM-PLAN-DBGIFACE-33
**Version:** 1.0.0 (epic-kickoff plan-of-record; produces this doc only — no implementation, no other-doc edit, no build, no merge, no tag, no push).
**Status:** **Accepted plan-of-record.** The three foundational decisions (full `signal` primitive as a *separate later epic*; ride `fsm-lsp` for the debug surface; debug-interface FIRST) are **owner-decided 2026-05-19** (not recommendations awaiting confirmation — they resolve the three forks the design docs surfaced). This document **formalizes the wave decomposition** built on exactly those decisions. The Doc 31 / Doc 32 §"owner-action vs proceed-autonomously" residual-posture pattern applies: everything below the owner decisions is delegated engineering hygiene with safe parked defaults (§7).
**Author:** senior architect / TL-delegate (planning extraction under delegated autonomy; the implementer of this doc only).
**Date:** 2026-05-19.
**Base commit:** `caf49b2` (main; the worktree branch `phase8.0/debug-iface-plan-of-record` base — the commit that consolidated the three debug-interface design docs onto main).
**Depends on / reconciles:**
- The three decided design docs on main: `docs/design/DEBUG_INTERFACE_UX_DESIGN_2026_05_19.md` (FSM-DESIGN-DBGUX — the UX + the keystone-honoring architecture), `docs/design/EXTERNAL_SIGNAL_MECHANISM_AND_ISR_DESIGN_2026_05_19.md` (FSM-DESIGN-EXTSIG — the deferred `signal`-epic scope), `docs/design/TARGET_CHIP_MATRIX_2026_05_19.md` (FSM-DESIGN-CHIPMATRIX — the per-family interrupt model + the carried Zero-Legacy findings).
- The wave-plan-of-record FORMAT precedents: `docs/30-v1_4-Simulation-Verification-Wave-Plan.md`, `docs/31-v1_5-UIDX-Wave-Plan.md`, `docs/32-Factory-Reliability-CI-Wave-Plan.md` (the structure this doc matches: epic framing, the ground-truth verification ledger, the numbered wave table with per-wave scope/input-docs/deliverables/§5.4-behavioural-acceptance/verification-quad/worktree-isolation/scope-boundary, the binding keystone tag-gate stated up front, the deferred menu, the owner/proceed split, the §11.30-clean tag-gate criteria, the risks table, the decision-ledger DRAFT rows).
- `docs/13-Simulator-Protocol.md` (the JSON-RPC wire SHAPE reused as the message schema *inside* the LSP requests — normative for the schema, NOT a separate server).
- `docs/26-LSP-Architecture.md` §8 (the LSP-wave + phase-boundary-audit precedent — "a phase-boundary audit … after L1 and after L5") + the v1.5 W-A2 `fsm/verify` `custom_method` embed seam this epic rides.
- `docs/00-Decisions-And-Reconciliation.md` §11 (the append-only decision ledger; this doc's §9 DRAFT rows are **NOT** appended here — the orchestrator independently re-derives & appends at closeout, per the release-layer verify-the-record discipline).
- `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 behavioural-acceptance / PD-2; §11.3 phase-boundary audits; §11.22 / §11.30 cold-quad + tag-ordering).

> **Why a new sibling doc (numbering justification, the Doc 28/29/30/31/32 precedent).** Doc 28 (v1.3 VSCode), Doc 29 (v1.3 W0 CST), Doc 30 (v1.4 Sim-Verification), Doc 31 (v1.5 UI/DX), Doc 32 (Factory-Reliability/CI) each established that an *epic-kickoff extraction + wave plan* is a first-class numbered sibling, distinct from the static spec corpus (Docs 01–27) and the per-release `GATE_VERIFICATION_*` / `AUDIT_PHASE_*` evidence docs. The interactive debug interface is the next epic; its plan is **Doc 33** — the next free integer after Doc 32 (verified: `ls docs/` ⇒ 30, 31, 32 exist, no 33). It does **not** replace Doc 13 (the simulator *protocol spec*, which stays normative as the wire-shape source) or Doc 26/27 (LSP / extension architecture, which stay normative); it is the plan that *sequences* the debug-interface work on top of those specs + the three decided design docs, exactly as Doc 31 sequenced the convenience layer on top of Doc 22/27.

> **§11-ledger numbering refinement (the "waves report rows, no mid-flight churn" discipline — stated up front, the Doc 31/32 precedent).** Per the v1.4 L6 lesson and the standing "waves report rows, orchestrator batch-appends" refinement: **this epic's Doc 00 §11-ledger rows are assigned at the epic closeout (W4 batched consolidation), NOT pre-allocated here.** The current ledger head is **§11.86** (verified: highest actual `§11.NN` row in `docs/00-Decisions-And-Reconciliation.md` is §11.86, the factory-reliability closeout batch). Pre-allocating "this epic's rows start §11.87" would be exactly the mid-flight-churn anti-pattern. This document deliberately does **not** name §11 row numbers for the waves; §9 carries DRAFT rows explicitly marked for the orchestrator to independently re-derive & append in one controlled commit (the Doc 00 append-only `comm -23` discipline).

---

## 0. Executive summary (read this first)

> **⚠️ ORCHESTRATOR POST-AUTHORING RECONCILIATION (2026-05-19, added at merge — NOT by the kickoff wave; the Doc-30/31/32 "divergences FLAGGED" discipline).** This plan was authored *before* the owner's 2026-05-19 directive folding the factory data-plane primitives (state-gated request handling · request/response signal framing · a bounded data-plane queue/stack/FIFO) into the deterministic-primitive completeness audit as a first-class **Round 2** (`docs/AUDIT_DETERMINISTIC_PRIMITIVE_COMPLETENESS_R2_FACTORY_DATA_PLANE_2026_05_19.md`, task **#135**, core-adjacent/kernel-touching). The §0 "**the interactive debug-interface endpoint is therefore unblocked**" statement below is therefore **SUPERSEDED**: the debug-interface **DESIGN** is complete and correct (this Doc 33 + the three decided design docs), but its **IMPLEMENTER waves W1–W4 are RE-GATED behind the R2 audit + the TL primitive-selection gate decision**. Rationale (the owner's, explicit): a bounded-data-plane primitive would add oracle/`StepRecord` surface the debug UI must visualize — building before the primitive set is settled forces exactly the rework the directive warns of. **W0 (this plan-of-record) stands and is correct as the design record; W1 dispatch is `blocked-by` #135.** The TL's primitive-selection recommendation is surfaced to the owner at the R2 gate; the defer-call (owner-delegated) was decided **NOT to defer**. See `[[project_user_messages_embeded_fsm_2026_05_19_factory_data_plane_primitives_directive]]` + `[[project_embeded_fsm_debug_interface_design_teeup]]`.

The headless pipeline is FACTORY-COMPLETE (v1.4), the UI/DX convenience layer is shipped (v1.5 SHIPPED+TAGGED, `checkpoint/2026-05-17-v1_5`), the factory-reliability/CI proof layer is shipped (`checkpoint/factory-reliability-ci-2026-05-18`), and the deterministic-primitive completeness audit (the gate that the backlog's 2026-05-18 authoritative sequence placed *before* the debug layer) has landed (`docs/AUDIT_DETERMINISTIC_PRIMITIVE_COMPLETENESS_2026_05_18.md`). **The interactive debug-interface endpoint is therefore unblocked** and is the next epic per the owner-affirmed sequence (backlog "Next … (2) the interactive debug-interface endpoint").

**The goal (the explicit owner ask):** make `.fsm` *design-by-simulation* the convenient loop — author → _simulate / step / inspect / rewind / capture_ → iterate — **inside VS Code**, driving the already-shipped `fsm_simulator::Interpreter` as the **single semantic oracle**. This closes the F1–F6 friction (no interactive event injection; config/context as a JSON scrollback blob; invisible virtual-clock behaviour; no breakpoints; no rewind; test-authoring is backwards) the DBGUX design measured.

**The binding single-oracle keystone (carried forward, restated for the debug surface — a BINDING tag-gate row, §2):** every UI verb is *exactly one* shipped `Interpreter` call; breakpoints are a *predicate over the `StepRecord` fields the oracle emits* (never a guard/transition re-evaluation); time-travel is `Interpreter::snapshot`/`restore`; capture is `write_trace_yaml` round-tripped by the SAME `execute_trace` `fsm test` uses ⇒ byte-identical by construction. **No second FSM semantics anywhere** — not in the LSP transport, not in the webview. This is the *editor-surface application of the v1.4 epic-level architecture invariant* (Doc 00 §11.63; the v1.5 KEYSTONE-IN-UI row; the factory KEYSTONE-DRIVE-THE-ORACLE row), not a fresh decision.

**The locked owner decisions this plan is built on (2026-05-19 — not re-litigated):**

| # | Decision | Consequence in this plan |
|---|---|---|
| **D-1** | **Full `signal` primitive** (the FSM-DESIGN-EXTSIG Part 2 first-class `signal`: poll/interrupt/edge/debounce, `feature signals`-gated, append-only keywords; cohesively retires the #127 AUDIT-DETPRIM edge/level/debounce GAPs) — a **SEPARATE epic, sequenced AFTER the debug interface**. | NOT a wave here. Scoped + tracked in §8 so it is not lost; cross-references the already-delivered FSM-DESIGN-CHIPMATRIX per-family interrupt model (its Part-2(d) dependency). |
| **D-2** | **Ride `fsm-lsp`** — the debug surface is new `fsm/simulate*` LSP **custom requests** reusing the proven v1.5 W-A2 embed seam; Doc 13's JSON-RPC wire SHAPE is reused as the message schema *inside* the LSP requests; **NO standalone WS daemon, NO new port, NO new network surface** (fork 1A in DBGUX §8). | W1 is the single LSP/Rust wave. The Doc 13 WS-daemon (1B) is the documented, not-built alternative; security posture collapses to "the attack surface is removed by construction" (DBGUX §8 fork-2 1A branch). |
| **D-3** | **Debug interface FIRST**, then the `signal`-feature epic. Neither consumes the owner-reserved **v1.6 (landing + doc-honesty)**; the debug-interface minor lands as its **OWN post-v1.6 minor**, `checkpoint/<name>`-tagged until gated, **LOCAL only** (G9 = the owner's push). | The epic sequence is: this epic now → (v1.6 owner-reserved landing+doc-honesty, owner-paced, untouched) → the `signal` epic → the visual-DSL-constructor capstone. The release-numbering call (which `vX.Y.0`, before/after v1.6) is the orchestrator/owner's at closeout; until tagged it is a `checkpoint/<name>` anchor, never a `vX.Y.0` claim (the factory-epic `checkpoint/`-not-`v1.6.0` precedent, Doc 00 §11.80). |

**Scope = MVP-core (the DBGUX §7 "MVP" row) + exactly ONE tiny additive `crates/` delta.** `init` · `dispatch`(+payload) · `advanceClock` · context-inspect · breakpoints (enter/exit/transition) via snapshot · time-travel rewind · capture→`.trace.yaml` · the v1.3 statechart-reuse overlay · **plus the single sanctioned `crates/` delta of the entire epic: one additive, non-stepping `Interpreter::set_context_field` micro-helper** (the DBGUX §3.3 helper — the owner's MVP recommendation included it; SPEC'd here as a W1 deliverable, **NOT implemented in this doc**). Tier 2/3 (true queue-suspend single-step, watchpoints, `multiMachine`, `replay`) = the explicitly **named deferred menu** (§6), not mid-epic drift.

**Critical path:** `W0 (this doc) → W1 (the `fsm/simulate*` LSP request layer + the `set_context_field` helper — the single keystone-violable Rust/LSP wave) → W2 (the VS Code debug WebviewPanel — overlay/inject/inspector/timeline, NO semantics) → W3 (breakpoints + time-travel — the StepRecord-predicate + snapshot/restore client logic) → W4 (capture→`.trace.yaml` round-trip + the binding post-W1 keystone phase-audit + the independent pre-tag four-lens + §11.30 cold-quad + `checkpoint/<name>` closeout, LOCAL)`. W1 is the **single auditable keystone checkpoint** (it is the only wave that adds Rust / a `crates/` dep edge); W2–W4 are TS (the webview), each gated by a real Extension-Host E2E (NOT symbol-presence).

---

## Ground-truth verification ledger (the Doc 30/31/32 verify-as-you-write discipline — divergences FLAGGED)

Every claim this plan rests on — especially the keystone surface the design doc cites — was **independently re-derived from source at the worktree base `caf49b2`, not transcribed from the design doc**. `file:line`/command cited for each. **The design doc's cited line numbers were checked against reality; the drift found is FLAGGED (it is the exact verify-the-record discipline — a design doc's `file:line` is intent, not ground truth).**

| # | Claim the plan rests on | Verification (command/file @ `caf49b2`) | Verdict |
|---|---|---|---|
| GT-1 | `Interpreter` exposes `new` | `crates/fsm-simulator/src/interpreter.rs:102` `pub fn new(ir: &Ir) -> Result<Self, StepError>` | ✅ **CONFIRMED — symbol present, line exact as DBGUX §3 cites (`:102`)** |
| GT-2 | `Interpreter::init(InitOptions)` → `Vec<StepRecord>` | `interpreter.rs:129` `pub fn init(&mut self, opts: InitOptions) -> Result<Vec<StepRecord>, StepError>`; `InitOptions` `interpreter.rs:48` with `machine_name`, `initial_context: Option<BTreeMap<String,Value>>`, `virtual_clock_start_ms` | ✅ **CONFIRMED — symbol + line exact as DBGUX cites (`:129`, `:48`)** |
| GT-3 | `dispatch` / `dispatch_with_payload` | `interpreter.rs:228` `pub fn dispatch(&mut self, event_name: &str) -> Result<Vec<StepRecord>, StepError>` (body = `self.dispatch_with_payload(event_name, None)`); `:232` `pub fn dispatch_with_payload(&mut self, event_name: &str, payload: Option<BTreeMap<String,Value>>) -> Result<Vec<StepRecord>, StepError>` | ✅ **CONFIRMED — both symbols + lines exact as DBGUX cites (`:228`, `:232`)** |
| GT-4 | `advance_clock(delta_ms)` → `Vec<StepRecord>` | `interpreter.rs:285` `pub fn advance_clock(&mut self, delta_ms: u64) -> Result<Vec<StepRecord>, StepError>` | ✅ **CONFIRMED — symbol + line exact as DBGUX cites (`:285`)** |
| GT-5 | `context()` (read-only) + `current_states_named()` + `virtual_clock_ms()` | `interpreter.rs:397` `pub fn context(&self) -> Result<&BTreeMap<String,Value>, StepError>`; `:357` `pub fn current_states_named(&self) -> Vec<String>`; `:404` `pub fn virtual_clock_ms(&self) -> u64` | ✅ **CONFIRMED — all three symbols + lines exact as DBGUX cites (`:397`, `:357`, `:404`)** |
| GT-6 | `snapshot()` / `restore()` (the time-travel + breakpoint mechanism) | `interpreter.rs:415` `pub fn snapshot(&self) -> Result<InterpreterSnapshot, StepError>`; `:444` `pub fn restore(&mut self, snap: InterpreterSnapshot) -> Result<(), StepError>`; `InterpreterSnapshot` is `pub use`d from `runtime` (`lib.rs:37-40`) | ✅ **CONFIRMED — both symbols + lines exact as DBGUX cites (`:415`, `:444`)** |
| GT-7 | `StepRecord` carries `entered_states` / `exited_states` / `transition_taken.stable_id` + `StepKind` (the breakpoint-predicate fields) | `crates/fsm-simulator/src/trace.rs`: `pub struct StepRecord` @ **`:43`**; `pub exited_states: Vec<String>` @ **`:62`**; `pub entered_states: Vec<String>` @ **`:65`**; `pub transition_taken: Option<TransitionTakenRecord>` @ `:59`; `pub struct TransitionTakenRecord` @ **`:172`** with `pub stable_id: String` @ **`:174`**; `pub enum StepKind` @ **`:113`** with `Init`/`Dispatched`/`Raised`/`TimerFired`/`Completion`/`Discarded` variants | ⚠️ **CONFIRMED with FLAGGED LINE-CITATION DRIFT** — every *symbol* exists exactly as the DBGUX breakpoint design (§3.2) requires, **but two of DBGUX's cited line numbers are off** (see the FLAG below). The *design is sound*; the *citations drift*. |
| GT-8 | `execute_trace` (replay) + `write_trace_yaml` (capture) — the byte-identity round-trip | `trace.rs:280` `pub fn execute_trace(ir: &Ir, trace: &TraceFile) -> Result<TraceResult, ExecError>`; `:274` `pub fn write_trace_yaml(t: &TraceFile) -> Result<String, TraceParseError>`; both `pub use`d (`lib.rs:41-45`). `crates/fsm-cli/src/cmd/test.rs:37` `use fsm_simulator::{execute_trace, parse_trace_yaml};` + `:223` `execute_trace(&ir, &trace)` — the SAME oracle the capture round-trips through | ✅ **CONFIRMED — both symbols + lines exact as DBGUX cites (`:280`, `:274`); the `cmd/test.rs` seam is genuinely the same `execute_trace`** |
| GT-9 | `StepError::CompletionLoop` (the "inconclusive ≠ done" honest-state guard, DBGUX §6) | `interpreter.rs:57` `pub enum StepError`; `CompletionLoop(u32)` @ **`:84`** (exact as DBGUX §6 cites `interpreter.rs:84`); also `QueueOverflow`, `GuardEval`, `NotInitialized`, `AlreadyInitialized` present | ✅ **CONFIRMED — symbol + line exact as DBGUX cites (`:84`)** |
| GT-10 | `TraceCommand` (the captured-event command shape) is `Dispatch{event,payload}` / `AdvanceClock{delta_ms}` / `Raise{event,payload}` | `trace.rs:226` `pub enum TraceCommand` with those three variants; `pub use`d (`lib.rs:41-45`) | ✅ **CONFIRMED — the capture command set the DBGUX §3 capture row needs exists** |
| GT-11 | The v1.5 W-A2 `fsm/verify` LSP `custom_method` embed seam exists (the proven pattern W1 mirrors) | `crates/fsm-lsp/src/lib.rs:177` `.custom_method("fsm/verify", Backend::verify_request)`; `crates/fsm-lsp/src/server.rs:204` `pub async fn verify_request(&self, params: serde_json::Value) -> RpcResult<serde_json::Value>` (offloads to `spawn_blocking`); `crates/fsm-lsp/src/capabilities/verify.rs:103` `pub struct VerifyRequestParams`, `:126` `pub fn from_value`, `:194` `pub fn run_verify`; client side `editors/vscode/src/commands/verify.ts:~405` `client.sendRequest<…>("fsm/verify", params)` | ✅ **CONFIRMED — the proven `custom_method` round-trip seam is real and shipped; W1 mirrors this exact pattern for `fsm/simulate*`** |
| GT-12 | `crates/fsm-lsp/Cargo.toml` does **NOT** yet depend on `fsm-simulator` (so W1's dep edge is genuinely net-new — the keystone-violable point) | `grep -c 'fsm-simulator' crates/fsm-lsp/Cargo.toml` ⇒ **0**. Present deps: `fsm-diagnostics` (workspace, `serde`), `fsm-parser`, `fsm-analyzer`, `fsm-ir`, `rowan` (workspace), `fsm-verify` (`../fsm-verify`, the v1.5 W-A2 edge), `tower-lsp`, `tokio`, `serde_json` | ✅ **CONFIRMED — `fsm-simulator` is ABSENT from `fsm-lsp`; adding `fsm-simulator = { path = "../fsm-simulator" }` is the SINGLE Rust-layer dep edge of the epic (plus the one `set_context_field` helper in `fsm-simulator` itself) — exactly the v1.5 W-A2 "single keystone-violable wave" shape** |
| GT-13 | No public context mutator exists on `Interpreter` (so the §3.3 `set_context_field` helper is genuinely net-new, not a duplication) | `grep -n 'set_context\|context_mut\|fn set_context_field\|pub fn.*context.*mut' crates/fsm-simulator/src/interpreter.rs` ⇒ **∅**. `context()` (`:397`) is the only context accessor and is `&self` read-only (returns `&BTreeMap`) | ✅ **CONFIRMED — the §3.3 helper is a genuine net-new additive method, not a re-expose; it is the SINGLE sanctioned `crates/` behaviour delta** |
| GT-14 | `fsm-cli` has no `simulate.rs` (the design specifies a NEW surface; no unbuilt CLI capability is assumed) | `ls crates/fsm-cli/src/cmd/` ⇒ `baseline.rs check.rs decompile.rs doc.rs fmt.rs generate.rs init.rs mod.rs parse.rs test.rs verify.rs` — **no `simulate.rs`**; only `test.rs` drives the simulator (via `execute_trace`) | ✅ **CONFIRMED as DBGUX §9.8 states — the design specifies the new LSP surface; it assumes no unbuilt oracle capability beyond the public methods read above** |
| GT-15 | v1.3 diagram-reuse targets exist (the W2 statechart reuse) | `editors/vscode/src/diagram/`: `diagramPanel.ts`, `emitIr.ts`, `irGraph.ts`, `index.ts`, `webview/diagramWebview.ts` all present | ✅ **CONFIRMED — the v1.3 `irGraph.ts` model + `diagramWebview.ts` renderer + `diagramPanel.ts` panel-identity contract + `emitIr.ts` IR seam the DBGUX §5 reuse-ledger names all exist; W2 rides them, not a new render stack** |

> **🚩 FLAGGED VERIFY-THE-RECORD FINDING — DBGUX line-citation drift (design sound, citations stale).**
> The FSM-DESIGN-DBGUX design doc cites specific `trace.rs` line numbers for the breakpoint-predicate fields that **do not all match the source at `caf49b2`**:
> - DBGUX §3.2 cites `entered_states` at **`trace.rs:64`** — actual: `:64` is the `#[serde(...)]` attribute line; the field `pub entered_states: Vec<String>` is **`:65`** (off by one).
> - DBGUX §3.2 cites `transition_taken.stable_id` at **`trace.rs:172`** — actual: `:172` is `pub struct TransitionTakenRecord {`; the field `pub stable_id: String` is **`:174`** (off by two).
> - DBGUX §3.2 cites `exited_states` at `trace.rs:62` — actual: `:62` **is exactly** `pub exited_states: Vec<String>` ✅ (correct).
> - DBGUX §3.1 cites `StepKind` at `trace.rs:113` — actual: `:113` **is exactly** `pub enum StepKind {` ✅ (correct).
> - All `interpreter.rs` citations (`:48`, `:84`, `:102`, `:129`, `:228`, `:232`, `:285`, `:357`, `:397`, `:404`, `:415`, `:444`) and `trace.rs:274`/`:280` are **exact** ✅.
>
> **Adjudication:** every *symbol* the DBGUX design depends on is **present in source exactly as the design requires** — the breakpoint-predicate design (predicate over `StepRecord.{entered_states, exited_states, transition_taken.stable_id}` + `StepKind`), the snapshot/restore time-travel, the `execute_trace`/`write_trace_yaml` round-trip, the `set_context_field` net-new-helper gap, and the `fsm/verify`-pattern reuse are all **verified true against source**. The drift is **two stale line numbers in the design doc's prose** (`:64`→`:65`, `:172`→`:174`), not a design defect and not a missing symbol. **This plan records the ACTUAL line numbers** (the GT-7 row above carries the corrected `:62`/`:65`/`:172`/`:174`/`:113`). The frozen design doc is **not edited** (it is design evidence; this doc writes exactly one file — Doc 33). W1's brief MUST cite the *verified* lines from this ledger, not re-transcribe DBGUX §3.2's prose. **Net: the keystone surface is verified present as designed; the only finding is a two-line citation drift, recorded here, not propagated.**

**Net result of the verify-as-you-write pass:** the debug-interface design **survives ground-truth re-verification** — all 11 oracle/StepRecord symbols, the snapshot/restore mechanism, the round-trip seam, the net-new-helper gap, and the proven `fsm/verify` `custom_method` seam are exactly as the design states. **One flagged finding:** two DBGUX line citations are stale (`:64`→`:65`, `:172`→`:174`); corrected in this ledger; not a design or symbol defect.

---

## 1. The wave decomposition (the heart of the plan)

> Each wave is a real **§5.4 behavioural-acceptance** gate (the PD-2 rule): **never** symbol-presence ("the method is registered", "the function exists"). For the LSP/Rust wave §5.4 means *a real round-trip test whose `StepRecord` stream byte-equals `fsm test`'s `execute_trace` output on the same events*. For the webview waves §5.4 means *an Extension-Host `xvfb-run` E2E driving the real extension* (the v1.5 W-C2 `@vscode/test-electron` discipline — the "Playwright is the only truth" / R6 standard), **not** a unit mock. Depth-first = each wave delivers a thin-but-complete vertical slice with a real behavioural gate before the next builds on it (the v1.2 L1-spine / v1.4 W1-spine / v1.5 A-spine precedent).

### W0 — This wave-plan-of-record (the epic-kickoff extraction)

| Field | Content |
|---|---|
| **Scope** | Author this Doc 33: epic framing, the ground-truth verification ledger (re-derive the keystone surface from source, flag any design-doc drift), the wave decomposition, the binding keystone tag-gate, the deferred `signal`-epic scope, the carried Zero-Legacy findings, the DRAFT §11 rows. |
| **Input docs** | The three decided design docs; Doc 30/31/32 (format precedent); Doc 13 (wire shape); Doc 26 §8; SUBAGENT_CONVENTIONS; Doc 00 §11. |
| **Deliverable** | `docs/33-Debug-Interface-Wave-Plan.md` (this file), committed on `phase8.0/debug-iface-plan-of-record`. NO implementation, NO other-doc edit, NO build, NO merge/tag/push. |
| **§5.4 behavioural acceptance** | (Planning wave — acceptance is *plan-integrity*, the Doc 31 W-A0 precedent.) Every keystone symbol independently re-derived from source with `file:line`; every design-doc citation drift flagged with the actual line; the wave table internally consistent (every wave has scope/input/deliverable/§5.4/quad/worktree/scope-boundary); the deferred menu named not silently dropped; the DRAFT §11 rows marked NOT-yet-in-the-ledger. |
| **Worktree** | `phase8.0/debug-iface-plan-of-record` (this worktree; doc-only; base `caf49b2`). |
| **Scope boundary** | **DO NOT** modify `crates/` / `Cargo*` / `.github/` / any other `docs/` file / Doc 00. **DO NOT** `git stash`, merge, tag, or push. Exactly one file written. |

### W1 — The `fsm/simulate*` LSP request layer + the `set_context_field` helper (the SINGLE keystone-violable Rust/LSP wave — GATED)

| Field | Content |
|---|---|
| **Scope** | (a) Add **one** dep edge `fsm-simulator = { path = "../fsm-simulator" }` to `crates/fsm-lsp/Cargo.toml` (currently ABSENT — GT-12; this is the **single Rust-layer enabling change of the entire epic**, exactly the v1.5 W-A2 single-dep-edge shape). (b) Add the **single sanctioned `crates/` behaviour delta**: one additive, **non-stepping**, guarded `Interpreter::set_context_field` helper on `crates/fsm-simulator` that writes one context field and **emits NO `StepRecord`** (GT-13 confirms no such mutator exists; this is keystone-clean — it sets state exactly as `InitOptions.initial_context` already does at init, runs *no* transition/guard/step). (c) Register the `fsm/simulate*` **custom LSP requests** via the proven `LspService::build().custom_method(...)` seam (GT-11 — the exact `fsm/verify` pattern), each backed by a long-lived per-document `Interpreter` session held in the LSP backend. The request/response message schema **reuses Doc 13's JSON-RPC wire SHAPE** (`sim/init`, `sim/dispatch`, `sim/advanceClock`, `sim/getContext`, `sim/setContext`, `sim/snapshot`, the `StepRecord` schema §11) as the payload contract *inside* the LSP requests — **NOT** a separate server, **NOT** a new port, **NOT** a new network surface (D-2). The CPU-bound interpreter calls run on `spawn_blocking` (the v1.5 W-A2 discipline — never block the LSP async executor). |
| **Input docs** | DBGUX §3 (the gesture→oracle-call table — use the **GT-verified** line numbers from this doc's ledger, NOT DBGUX §3.2's stale prose), §3.3 (the `set_context_field` helper spec), §4.1 (the one-semantics architecture); Doc 13 §3/§11 (the wire shape + `StepRecord` schema); GT-11 (the `fsm/verify` `custom_method` pattern to mirror); Doc 26 §3 (the single-analyze reuse seam). |
| **Deliverable** | (1) the one `Cargo.toml` `fsm-simulator` dep edge; (2) `Interpreter::set_context_field` (additive, non-stepping, guarded, **zero `StepRecord`** — the only `crates/` behaviour delta); (3) the `fsm/simulate*` custom-method handlers (init / dispatch(+payload) / advanceClock / getContext / setContext / snapshot — restore) over a per-document `Interpreter` session, Doc-13-shaped payloads, `spawn_blocking`-offloaded, honest JSON-RPC invalid-params on malformed input (never a verify/sim of empty/wrong input — the cardinal-sin bar at the request boundary, the v1.5 W-A2 precedent). |
| **§5.4 behavioural acceptance** | A **tower-lsp client round-trip test** (the v1.2-LSP / v1.5-W-A2 precedent): driving a fixture battery through `fsm/simulate*` (init → a dispatch sequence with payloads → advanceClock → snapshot/restore), the resulting `StepRecord` stream **byte-equals** `fsm test`'s `execute_trace` output on the *same* `TraceCommand` sequence (the **differential-oracle proof** — the debug transport is just another oracle frontend, exactly the v1.4/v1.5/factory differential discipline). A `StepError::CompletionLoop`/`QueueOverflow` fixture asserts the request returns that error **verbatim**, never a fabricated clean end-of-run (the "inconclusive ≠ done" honest-state, DBGUX §6). `set_context_field` asserts the context changed **AND zero `StepRecord` was produced**. Symbol-presence (handler registered / method exists) is explicitly **NOT** acceptance. |
| **Verification quad** | The canonical cold quad (fmt + clippy-deny + `cargo test --workspace` + the doc/example build) green at the wave commit; the tower-lsp differential-oracle test in the suite. (Build-heavy wave — one Rust-build-heavy wave at a time per H1.) |
| **Parallel/seq** | **Sequential — the epic spine.** W1 is the single keystone-violable wave and the differential oracle every later wave's behaviour is checked against; it must not run concurrently with anything that could mask a fork. |
| **Worktree** | `phase8.1/dbgiface-w1-lsp-sim-layer` (writer worktree, isolated). |
| **Scope boundary** | **DO NOT** touch `editors/vscode/**` (W2 owns the webview). **DO NOT** add ANY transition-selection / guard-eval / completion-synthesis / RTC-step / active-config-compute logic to `crates/fsm-lsp/**` (the keystone — the handlers only *call* `Interpreter` + marshal `StepRecord`/context). **DO NOT** add a second `crates/` behaviour delta beyond `set_context_field` (it is the *single* sanctioned one). **DO NOT** add a WS server / new port / new network surface (D-2). **DO NOT** `git stash`. |
| **Keystone** | W1 is the **only place a forked simulator semantics could enter** (it is the only wave adding Rust / a `crates/` dep edge / a `crates/` method). The wave is gated by the **mandatory post-W1 source-derived §11.3 keystone phase-audit** (§2 / W4) that independently re-derives the no-fork obligations from source — **not** from this doc, **not** from the wave's self-report (the §11.29 / v1.4-L1 / v1.5-W-A2 / factory-W6a "verify-the-record, re-derive don't echo" rigor). |

### W2 — The VS Code debug WebviewPanel (overlay / inject / inspector / timeline — NO semantics)

| Field | Content |
|---|---|
| **Scope** | A single `WebviewPanel` (`⬡ <Machine> — Debug`, `ViewColumn.Beside`, keyed by **resolved file path**, CSP-locked, `postMessage`-only, `retainContextWhenHidden`) that is **the v1.3 diagram panel + an additive overlay + a control rail** (DBGUX §2). **Reuse verbatim:** the v1.3 `irGraph.ts` IR→{nodes,edges} model + the bundled `diagramWebview.ts` ELK/SVG renderer + the `diagramPanel.ts` panel-identity/stale-banner/click→source contract + `emitIr.ts` (GT-15 — all confirmed present). **New (NO semantics):** the active-config highlight overlay, the transport rail (Init/Run/Pause/Step + clock readout + status line), the Inject panel (event picker + typed-payload form revealed inline under the picker), the advance-clock control + pending-timer list, the context inspector (the Δ column), the StepRecord timeline. Every verb marshals to a W1 `fsm/simulate*` request via the v1.5 W-A2 `client.sendRequest` boundary (GT-11). The v1.5 C1/C2 frontend baseline (CSP + nonce + `var(--vscode-*)` theme tokens + Extension-Host test discipline + honest-failure bar) is the bar. |
| **Input docs** | DBGUX §2 (the panel layout + the senior-UX proximity rationale + the empty/honest states), §4.1 (the architecture — overlay is additive, no second diagram), §5 (the reuse ledger), §6 (the UX guardrails — no premature success, honest verdicts); the v1.5 C1 frontend-practices audit (`docs/AUDIT_VSCODE_FRONTEND_PRACTICES_2026-05-17.md`) as the FE baseline; GT-11 (the `client.sendRequest` boundary). |
| **Deliverable** | The debug WebviewPanel: statechart (reused v1.3) + active-config overlay + transport rail + Inject panel (typed-payload form revealed adjacent to the picker — the proximity principle) + advance-clock + context inspector with the Δ column + the StepRecord timeline; all driven through the W1 `fsm/simulate*` requests; the v1.3 empty/honest states inherited (stale-banner on no-valid-IR, transport disabled-with-inline-reason, never blanks/fakes); a "captured ✓"-style affordance NOT yet wired (W4 owns capture). NO breakpoints/time-travel yet (W3). |
| **§5.4 behavioural acceptance** | An **Extension-Host `@vscode/test-electron` / `xvfb-run` E2E** (the v1.5 W-C2 discipline — drives the *real* extension in a real host, NOT a unit mock): open the panel on a fixture `.fsm` → assert the v1.3 statechart renders + transport disabled-until-Init; Init → assert the initial active-config highlight + StepRecord #0 on the timeline + context seeded (values **==** the W1 oracle's `getContext`); inject an event with a payload → assert the diagram animates the transition + the timeline appends + the context Δ column shows the change (the rendered config/context **byte-matches** the W1 `fsm/simulate*` response — the panel renders the oracle, computes nothing); a no-valid-IR fixture → assert the v1.3 stale-banner + transport disabled-with-reason (the honest-state bar). Symbol-presence (panel class exists) is **NOT** acceptance. |
| **Verification quad** | The JS lane at the wave commit (the v1.5-reactivated lane: `npm audit` triaged + the Extension-Host suite green incl. the new debug-panel E2E); the Rust cold quad unchanged (W2 is TS-only — no `crates/` touch). |
| **Parallel/seq** | **Sequential — after W1** (it consumes W1's `fsm/simulate*` contract; the panel renders what the oracle returns). |
| **Worktree** | `phase8.2/dbgiface-w2-debug-webview` (writer worktree). |
| **Scope boundary** | **DO NOT** touch `crates/` / `Cargo*` (W2 is pure TS over W1's contract). **DO NOT** compute ANY FSM semantics in the webview/TS (no transition selection, no guard eval, no active-config computation — the panel renders the W1 `StepRecord`/config/context, the keystone). **DO NOT** fork the v1.3 diagram render (additive overlay only — DBGUX §4.1 "no second diagram"). **DO NOT** let the user edit the machine *in the panel* (that is the future visual-constructor capstone, explicitly out of scope — DBGUX §6 "no scope bleed"). **DO NOT** `git stash`. |

### W3 — Breakpoints + time-travel (the StepRecord-predicate + snapshot/restore client logic — NO semantics)

| Field | Content |
|---|---|
| **Scope** | Breakpoints (state-enter / state-exit / transition) **as a client-side predicate over the `StepRecord` fields the W1 oracle emits** (DBGUX §3.2 — **NEVER** a guard/transition re-evaluation): state-enter `S` ⇒ `step.entered_states.contains(S)`; state-exit `S` ⇒ `step.exited_states.contains(S)`; transition `T` ⇒ `step.transition_taken.stable_id == T` (the GT-7-verified fields: `trace.rs:62`/`:65`/`:174`). The breakpoint glyph lives **on the diagram node/edge** (set by clicking it — the proximity principle), `◍` armed / `▣` hit; on hit, the status line says so + auto-pause (a durable state ⇒ a durable indicator, NOT a toast — DBGUX §2.2). Time-travel rewind: `snapshot()` taken after every revealed step; rewind = `restore(snapshots[N])` (W1's `fsm/simulate*` snapshot/restore). "Pause at the breakpoint" = restore the snapshot taken just *before* the breaking step + hold (machine state exactly pre-step — the debugger-correct "stopped at the breakpoint, not past it"). "Single-step" = the UI-paced reveal over W1's already-computed `Vec<StepRecord>` (DBGUX §3.1 — the client paces the *reveal*, the oracle owns the *semantics*; TRUE queue-suspend single-step is the §6 named deferral, NOT invented here). |
| **Input docs** | DBGUX §3.1 (the honest single-step mapping), §3.2 (the breakpoint-as-StepRecord-predicate mechanism — use the GT-7-verified `trace.rs` lines), §3.4 (the worked micro-scenario), §2.2 (the breakpoint-glyph-on-the-node proximity rationale); Doc 13 §10 (`sim/breakpointHit` notification — the wire shape the client emits internally). |
| **Deliverable** | Breakpoint glyphs on diagram nodes/edges (click-to-set, `◍`/`▣`); the client-side StepRecord-predicate evaluator (enter/exit/transition); auto-pause-at-breakpoint via restore-the-pre-step-snapshot; the timeline as the time-travel surface (`►` current, `◀ rewind to #N` as a row action — proximity); the UI-paced single-step reveal over W1's `Vec<StepRecord>`. NO capture yet (W4). |
| **§5.4 behavioural acceptance** | An **Extension-Host E2E** (v1.5 W-C2 discipline): the DBGUX §3.4 worked scenario end-to-end — set a transition breakpoint by clicking the edge glyph → inject the event → assert the machine **pauses pre-step** (active-config still the source state, the W1 oracle's pre-step `snapshot` restored, asserted against the W1 `getContext`/config) → step to reveal the breaking record → assert the diagram animates the exit/enter → `◀ rewind to #N` → assert config+context+clock+timeline-cursor jump to state #N (the restored state **byte-equals** the W1 oracle's snapshot at #N) and a new inject branches from there. Asserts the breakpoint predicate fires on the **oracle's `StepRecord` fields**, never a re-evaluated guard. Symbol-presence is **NOT** acceptance. |
| **Verification quad** | The JS lane at the wave commit (Extension-Host suite green incl. the breakpoint+rewind E2E); the Rust cold quad unchanged (W3 is TS-only). |
| **Parallel/seq** | **Sequential — after W2** (breakpoints/time-travel are interactions over the W2 panel + the W1 snapshot/restore). |
| **Worktree** | `phase8.3/dbgiface-w3-breakpoints-timetravel` (writer worktree). |
| **Scope boundary** | **DO NOT** touch `crates/` / `Cargo*`. **DO NOT** re-evaluate ANY guard/transition in the client — the breakpoint is a *filter on the oracle's own `StepRecord` output*, not a second semantics (the keystone — DBGUX §3.2). **DO NOT** implement TRUE queue-suspension single-step (the §6 named Tier-2 deferral; the MVP single-step is the UI-paced reveal — DBGUX §3.1). **DO NOT** `git stash`. |

### W4 — Capture→`.trace.yaml` round-trip + the binding keystone phase-audit + §11.30 closeout (LOCAL)

| Field | Content |
|---|---|
| **Scope** | (a) Capture: the `TraceCommand`s the author actually injected + the W1 oracle's actual `Vec<StepRecord>` → `write_trace_yaml` (GT-8) → a `.trace.yaml` fixture; `fsm test` later re-runs it via the SAME `execute_trace` (GT-8 — byte-identical by construction; this *inverts* F6: the test is recorded-from-the-oracle, not guessed). "Captured ✓" appears **only after `write_trace_yaml` returns and the file exists** (the no-premature-success / no-"Сохранено"-before-confirm bar, DBGUX §6). (b) The **binding post-W1 source-derived §11.3 keystone phase-audit** (the v1.4-W1 / v1.5-W-A2 / factory-W6a precedent — a FROZEN evidence doc, never overwritten): independently re-derive the §2 no-fork obligations from source. (c) The independent pre-tag four-lens audit (the v1.1–factory §3 standard, FROZEN). (d) The §11.30-clean closeout: author the GATE_VERIFICATION doc + metrics + CHANGELOG + ROADMAP retrospective + report the §11 rows; the §11.22/§11.30 cold-quad WITH the mandatory NOTE-1 `cargo clean -p`-all-first-party STEP 0 + the binding JS lane, at the gate-doc commit `X`; `git tag checkpoint/<date>-<name>` at `X` — quad-commit ≡ JS-lane-commit ≡ tag-commit ≡ `X`, **LOCAL only** (G9 = the owner's push, D-3). |
| **Input docs** | DBGUX §3 (the capture row + the byte-identity-by-construction argument), §6 (the no-premature-success bar); GT-8 (`write_trace_yaml`/`execute_trace` + the `cmd/test.rs` same-oracle seam); §2 of this doc (the no-fork proof obligations); SUBAGENT_CONVENTIONS §11.3/§11.22/§11.30; the v1.5/factory GATE_VERIFICATION docs (the gate-doc form). |
| **Deliverable** | The capture→`.trace.yaml` round-trip (recorded-from-oracle, "captured ✓" only after the file exists); the FROZEN post-W1 keystone phase-audit doc (verdict `KEYSTONE-INTACT` or the finding); the FROZEN pre-tag four-lens audit doc (verdict `TAG-CLEAR` or the findings); the GATE_VERIFICATION doc + metrics JSON + CHANGELOG + ROADMAP retrospective; the §9 §11 rows **reported** (orchestrator appends — §9); the `checkpoint/<date>-<name>` annotated tag at `X`, LOCAL. |
| **§5.4 behavioural acceptance** | (i) A round-trip test: capture a session → `write_trace_yaml` → `fsm test <the .trace.yaml>` → assert the replay's `execute_trace` `StepRecord` stream **byte-equals** the captured stream (byte-identical by construction — the same oracle); the capture E2E asserts "captured ✓" appears **only after** the file exists on disk (the no-premature-success bar, asserted in the Extension-Host host, not a mock). (ii) The post-W1 keystone audit returns `KEYSTONE-INTACT` from a **source-derived** re-derivation (the §2 obligations, re-run by the auditor, NOT echoed). (iii) The pre-tag four-lens returns 0 P0 / ≤5 well-scoped P1. (iv) The cold-quad (with NOTE-1 STEP 0) + the JS lane green at `X`. |
| **Verification quad** | The §11.30-clean cold-from-source quad WITH the mandatory NOTE-1 `cargo clean -p` of ALL first-party workspace crates STEP 0 (`fsm-diagnostics fsm-lexer fsm-parser fsm-ir fsm-analyzer fsm-codegen-c fsm-simulator fsm-formatter fsm-cli fsm-lsp fsm-verify`, cargo-1.75 repeated-`-p` form — preserves the registry cache, evicts every first-party artifact incl. any stale `-wt-`-baked test binary, the §11.30/factory-GT-12 disk-protective conventional-CI-cache equivalent, NOT a workaround to grind past) + the binding JS lane (the surface touches `editors/vscode/**` via W2/W3/W4 — exactly the v1.5/factory re-activated-lane shape), both at the gate-doc commit `X`. |
| **Parallel/seq** | **Sequential — LAST.** Depends on W1 (capture round-trips W1's oracle output; the keystone audit audits W1's Rust) + W2/W3 (the capture UI lives in the panel). |
| **Worktree** | `phase8.4/dbgiface-w4-capture-closeout` (writer worktree; the **sole `docs/` writer** for the closeout — exclusive-writer collision discipline, the v1.4-W4b/v1.5/factory precedent). The post-W1 keystone audit + the pre-tag four-lens are **separate read-only audit agents** (idempotently re-dispatchable; never advance a gate on a dead/erroring agent — H7). |
| **Scope boundary** | **DO NOT** add ANY `crates/` behaviour beyond W1's already-landed `set_context_field` (capture uses the *existing* `write_trace_yaml`/`execute_trace` — GT-8). **DO NOT** merge/push/tag to a `vX.Y.0` (it is a `checkpoint/<name>` anchor, LOCAL — D-3; the orchestrator owns the release-numbering call). **DO NOT** overwrite any FROZEN audit doc (audit evidence is never edited — the §11.29 discipline). **DO NOT** edit Doc 00 §11 (the orchestrator re-derives & appends — §9). **DO NOT** `git stash`. |

**Sequencing summary (the critical path, restated).** `W0 (this doc) → W1 (the fsm/simulate* LSP layer + the set_context_field helper — the single keystone-violable Rust/LSP wave) → W2 (the debug WebviewPanel, TS, NO semantics) → W3 (breakpoints + time-travel, TS, NO semantics) → W4 (capture round-trip + the binding source-derived post-W1 keystone phase-audit + the independent pre-tag four-lens + the §11.30-clean cold-quad WITH NOTE-1 STEP 0 + the binding JS lane + the checkpoint/<name> tag at X, LOCAL)`. The §11.3 phase-boundary cadence is **at minimum the post-W1 keystone audit** (the single fork-possible boundary — the v1.4/v1.5/factory keystone-first precedent) plus the standing pre-tag four-lens; the periodic-audit cadence (every 2–3 implementer batches) is pre-budgeted (the v1.4 D.7(c)#1 "name it in the wave plan so it is planned, not absorbed" — the post-W1 audit IS the expensive, named, first-class line item of this epic, §2).

---

## 2. The keystone-in-debug enforcement mechanism (a BINDING tag-gate row — the §4.2-DBGUX no-fork proof, stated up front)

> This is the load-bearing structural invariant of the epic, stated in the binding gate-row form (the `GATE_VERIFICATION_*` / Doc 30 §5.2 / Doc 31 §2 / Doc 32 §2 pattern). It is **NOT a fresh decision** — it is the *debug-surface application* of the v1.4 epic-level architecture invariant (Doc 00 §11.63; the verifier/simulator-frontend drives the shipped semantics, never forks them), the v1.5 KEYSTONE-IN-UI row, and the factory KEYSTONE-DRIVE-THE-ORACLE row. The debug interface is just another frontend of the one oracle.

**KEYSTONE-IN-DEBUG (binding tag-gate criterion):**

> Every debug UI verb is **exactly one** shipped `fsm_simulator::Interpreter` call (init / dispatch(+payload) / advance_clock / context / snapshot / restore — GT-1..GT-6, all verified present at the cited lines) marshalled over the W1 `fsm/simulate*` LSP custom request (the proven v1.5 W-A2 `custom_method` seam — GT-11). Breakpoints are a **predicate over the `StepRecord` fields the oracle emits** (`entered_states` `trace.rs:62`, `exited_states`-corrected `trace.rs:65`, `transition_taken.stable_id` `trace.rs:174` — GT-7), **NEVER** a guard/transition re-evaluation. Time-travel is `Interpreter::snapshot`/`restore`. Capture round-trips through the SAME `write_trace_yaml`/`execute_trace` `fsm test` uses (GT-8) ⇒ byte-identical by construction. There is **NO** transition-selection / guard-eval / completion-synthesis / RTC-step / active-config-computation logic in `crates/fsm-lsp/**` (the LSP transport) **or** `editors/vscode/**` (the webview), TS or Rust. The **single sanctioned `crates/` behaviour delta** of the entire epic is the one additive non-stepping `Interpreter::set_context_field` helper (GT-13 — it runs *no* transition/guard/step, sets state exactly as `InitOptions.initial_context` already does; it does **not** implement semantics). The **post-W1 §11.3 phase-audit re-derives this FROM SOURCE** — **not** from this doc, **not** from the wave's self-report (the §11.29 / v1.4-L1 / v1.5-W-A2 / factory-W6a verify-the-record rigor). **A second simulator semantics is the cardinal regression** — the exact P0-1 / MV5-1 / v1.4-keystone / v1.5-KEYSTONE-IN-UI / factory-KEYSTONE class the project keeps re-learning. This row PASSES iff the post-W1 source-derived audit returns the keystone intact AND the W1 differential battery is byte-equal to `fsm test`'s `execute_trace`.

**The binding post-W1 §11.3 source-derived keystone phase-audit MUST independently re-derive from source:**

1. **Negative-grep** the LSP-embed (`crates/fsm-lsp/**`, non-comment lines) **AND** the `editors/vscode/**` debug surface for ANY transition-selection / guard-evaluation / completion-event-synthesis / RTC-step / active-config-computation logic — **every hit read & adjudicated**; the expected result is **∅** (the W3 breakpoint predicate reads `StepRecord` fields only — that is the adjudicated-clean shape; the v1.4-W1/W2 / v1.5-W-A2 / factory-W6a negative-grep method).
2. **Byte-identity:** the debug transport's `StepRecord` stream for the fixture battery **==** `fsm test`'s `execute_trace` output for the same `TraceCommand`s (the same differential discipline as v1.4/v1.5/factory — the debug surface is just another oracle frontend; **re-run by the auditor**, not echoed from W1's self-report).
3. **The `fsm_simulator` crate's *semantics* + the `cmd/test.rs` `execute_trace` seam are byte-untouched** by the transport layer — the **only** sanctioned `crates/` delta is the one additive non-stepping `set_context_field` helper (a guarded raw field poke that runs nothing, GT-13); the structural diff on `fsm_simulator`'s transition/guard/step/clock paths is **EMPTY** (exactly the v1.5/factory keystone-audit "the oracle is byte-untouched" shape, modulo the one sanctioned additive helper which is explicitly NOT a semantics).

Stating these here makes the design *falsifiable against the keystone before a line ships* — the project's verify-the-record discipline applied forward. This is **one auditable checkpoint by construction**: only **W1** adds Rust / a `crates/` dep edge / a `crates/` method; W2–W4 are TS (the webview) and hold the invariant *trivially* (they render the oracle's output and compute nothing). Concentrating the only fork-possible surface into a single gated wave with a mandatory source-derived audit is the structural realization of "keystone enforcement = a single named, budgeted checkpoint" (the v1.4 L1/L7 lesson, applied a fourth time).

---

## 3. Scope spine — MVP-core vs the deferred menu (alternatives are the menu, not mid-epic drift)

| Tier | Capability | Oracle backing | Disposition |
|---|---|---|---|
| **MVP (core — THIS epic)** | `init` · `dispatch`(+payload) · `advanceClock` · context-inspect · breakpoints (enter/exit/transition) via snapshot · time-travel rewind · capture→`.trace.yaml` · the v1.3 statechart overlay · **the ONE additive non-stepping `set_context_field` helper** | all already-public `Interpreter` methods (GT-1..GT-10) **+** the single sanctioned `set_context_field` delta (GT-13) | **W1–W4 (this epic).** Closes F1–F6 entirely with **near-zero `crates/` change** (one helper). |
| **Tier 2** (deferred — named, NOT this epic) | TRUE queue-suspend single-step granularity (an additive `Interpreter` capability — DBGUX §3.1/§9.1 explicitly flags it deliberately NOT designed into `crates/` here) · watchpoints (context-predicate breakpoints) | a small additive `Interpreter` capability — **named, not invented here** | **Deferred menu.** Captured so it is *not* mid-epic drift (the §2.1-style scope-spine; the v1.4 "named deferral, not silent drop" precedent). |
| **Tier 3** (deferred — named, NOT this epic) | `multiMachine` (cross-machine `send`) · `replay` at 0/1×/2× speed · multi-instance list | Doc 13 §13 / §8 — the simulator already routes cross-machine internally; the UI is the tail | **Deferred menu.** Same discipline. |

**Discipline:** Tier 2/3 are the **menu**, captured so they are *not* mid-epic drift. The owner cuts the line if a future use case needs an item pulled in — a *new epic* decision, never silent mid-epic scope-bleed (the v1.4/v1.5/factory scope-spine discipline). The MVP closes F1–F6 *entirely* with maximal author value per unit of new attack/complexity surface (one `crates/` helper).

---

## 4. Owner-action vs proceed-autonomously split (safe parked defaults — the Doc 31 §4 / Doc 32 pattern)

> The three foundational decisions (D-1/D-2/D-3) are **owner-decided**. Everything below them is delegated engineering hygiene (the `[[feedback_dont_ask_technical_permission]]` line — engineering hygiene ships; only product-strategy/credential/destructive choices escalate), each with a non-blocking safe parked default so no wave ever stalls on the owner (the Doc 30 §3 / Doc 31 §4 / Doc 32 escalation pattern).

| ID | Owner decision | Safe parked default (proceed on this if the owner is silent) |
|---|---|---|
| **OWNER-1** | **The release-numbering call** for this minor (which `vX.Y.0`, and its position relative to the owner-reserved v1.6) — a product-numbering decision, the factory-epic `checkpoint/`-not-`v1.6.0` precedent (Doc 00 §11.80) | **Tag a `checkpoint/<date>-<name>` annotated anchor at the §11.30-clean gate-doc commit, LOCAL only; do NOT consume v1.6 (owner-reserved for landing+doc-honesty — D-3); do NOT unilaterally claim a `vX.Y.0`.** The orchestrator/owner assigns the final number at closeout (the same surface-don't-auto-number discipline the factory epic set). |
| **OWNER-2** | **The G9 push** (push of LOCAL `v1.0.0`–`v1.5.0` + all `checkpoint/*` anchors incl. this epic's; the never-run remote CI matrix) — carried unchanged from v1.4/v1.5/factory; an infra-escalation, NOT a grind | **Tag LOCAL, §11.30-clean; carry G9 in the GATE_VERIFICATION doc as accepted-tracked owner-escalation** (the §11.62 re-state-don't-drop discipline). G9 is the owner's publication switch, decoupled from BOTH progress AND quality (quality is local every wave — the per-wave quad + `make ci-local`). |
| **OWNER-3** | **The deferred full `signal`-primitive epic GO** (§8 — a language-roadmap-scheduling decision; D-1 decided its *shape*, not its *schedule*) | **PARKED — sequenced AFTER this debug-interface epic AND after the owner-reserved v1.6** (D-3). It is its own gated epic (append-only keyword/grammar = a minor-version event, Doc 00 §B-03); recorded in §8 so it is tracked, NOT auto-started. Requires the epic to be explicitly opened (the orchestrator/owner owns the *when*). |
| **OWNER-4** | **The carried Zero-Legacy findings disposition** (§5 — the AVR Harvard `.rodata` correctness hazard needs a CI-runner-only `avr-gcc` lane; the Doc 16 §2–§6 staleness folds into the v1.6 doc-honesty pass) | **Record both as tracked carried findings (§5); the AVR hazard's verification path is CI-runner-only (the box is disk-tight — infra-escalation, the factory-epic on-target-lane precedent); the Doc 16 staleness folds into the owner-reserved v1.6 doc-honesty pass (cross-ref task #103).** Neither is auto-scoped into this epic; neither is silently dropped. |

**Everything else = proceed autonomously.** Wave sequencing, the W1 LSP-layer + the one `set_context_field` helper, the W2–W4 webview waves, the keystone-in-debug enforcement, the §11.3/§11.30 release mechanics, the worktree isolation, the engineering hygiene — all delegated, no owner sign-off (only the four product-strategy/credential/destructive residuals above escalate).

---

## 5. Carried Zero-Legacy findings (recorded, NOT silently dropped — a "carried findings" subsection)

> The FSM-DESIGN-CHIPMATRIX threading surfaced two real Zero-Legacy / correctness items. They are **NOT in this epic's scope** (the debug interface touches neither AVR codegen nor the HAL doc) but are **recorded here so they are tracked, not lost** (the §11.49 leave-and-explain / §11.62 re-state-don't-drop discipline; the factory-epic carried-findings precedent).

### CF-1 — AVR Harvard `.rodata` + TABLE-dispatch: an UNTESTED CORRECTNESS HAZARD (TARGET_CHIP_MATRIX §1.3 / §2.3)

**The finding (verified in `docs/design/TARGET_CHIP_MATRIX_2026_05_19.md` §1.3 row "Harvard: `.rodata` is in a separate (flash) space"):** AVR is the **only Harvard-architecture target in scope**; code and data live in separate address spaces. The TABLE codegen strategy stores a `static const <M>_trans_table[]` in `.rodata` (Doc 11 §9). On AVR a *plain pointer deref* of `.rodata` reads **RAM, not flash** ⇒ **wrong data**. Correct AVR codegen needs `__flash`/`PROGMEM` + `pgm_read_*`, **which the current family-agnostic codegen does not emit**. The SWITCH strategy (no stored table) sidesteps this; **large machines that auto-select TABLE (>64 states, `config.rs:97-108`) are the exposed case**. This is an **UNTESTED CORRECTNESS HAZARD** — the chip matrix's honest verdict is that **only ARM Cortex-M3 is on-target-tested** (QEMU `mps2-an385`, the factory-epic W2 hard gate); every other family incl. AVR is family-agnostic-codegen *assumption*, host-gcc-C99-proven only.

**Proposed verification path (tracked, NOT scheduled here):** a real `avr-gcc -mmcu=atmega328p` **link + run** of a >64-state TABLE-strategy fixture on an AVR emulator (`simavr`/QEMU-AVR), differential-checked against the shipped `fsm_simulator::execute_trace` oracle — the **exact factory-epic W1→W2 on-target-differential method** extended to a second family. **This is CI-lane work — the box is disk-tight; it is CI-runner-only** (the factory-epic's on-target QEMU lane is CI-runner-only by the same disk constraint — an infra-escalation, NOT a grind to force locally). Disposition: a **named candidate for the factory-reliability/CI epic's on-target-matrix extension** (it is a *codegen-correctness* item, NOT a debug-interface item — it must not be auto-scoped here). Recorded so the AVR-TABLE hazard is auditable and not lost (OWNER-4 default).

### CF-2 — Doc 16 §2–§6 staleness vs the emitted HAL symbols (TARGET_CHIP_MATRIX threading finding (b))

**The finding (verified in `docs/design/TARGET_CHIP_MATRIX_2026_05_19.md` threading + the tee-up memory):** Doc 16 (HAL-Specification) §2–§6 is **STALE vs the actually-emitted HAL contract**: the codegen emits `fsm_hal_clock_now_ms` / `fsm_hal_assert(bool, msg)`, but Doc 16 §2–§6 documents the older `_ms` / `_fail(file, line, msg)` shapes. A docs-only first-contact-surface drift.

**Disposition (tracked, NOT fixed here):** fold into the **owner-reserved v1.6 doc-honesty pass** (the v1.5 Doc 31 §6 H6 / ASSESSMENT PART B doc-honesty lineage — the README/Doc-07/Doc-14 staleness pass that must precede/accompany D1; this is the **same class** and the same v1.6 batch). **Cross-reference task #103** (the backlog's Doc-16 doc-honesty / HAL-doc-staleness tracking item). It is **NOT a code change** (the emitted symbols are the correct shipped contract — the doc is what drifted, exactly the v1.4 §3.G Doc-18 finding shape) and **NOT a debug-interface item** (the debug interface does not touch the HAL). Recorded so the Doc 16 staleness is carried into the v1.6 doc-honesty scope, not dropped (OWNER-4 default; the §11.62 discipline).

---

## 6. Deferred menu (the named Tier-2/3 items — captured, not lost)

The §3 Tier-2/3 items are the deferred menu, restated here as the single discoverable list (the v1.4/v1.5/factory deferred-menu discipline — named so they are *not* mid-epic drift and *not* silently dropped):

- **Tier-2 — TRUE queue-suspend single-step granularity.** An additive `Interpreter` capability (suspend the RTC queue mid-drain). DBGUX §3.1/§9.1 explicitly flags it deliberately NOT designed into `crates/` here (the MVP single-step is the keystone-clean UI-paced reveal over the oracle's already-computed `Vec<StepRecord>`). A future additive-capability decision, owner-owned.
- **Tier-2 — Watchpoints** (context-predicate breakpoints — "break when `speed > 1000`"). A predicate over the W1 `getContext` between steps; additive to the W3 predicate engine. Named, not in MVP.
- **Tier-3 — `multiMachine`** (cross-machine `send` visualization). The simulator already routes cross-machine internally (Doc 13 §13); the UI is the tail. Named, not in MVP.
- **Tier-3 — `replay` at 0/1×/2× speed** + multi-instance list (Doc 13 §8/§13). Named, not in MVP.

Each is the **menu**, not a commitment; pulling any in is a *new epic* decision (never mid-epic scope-bleed). The Doc 13 standalone WS-daemon transport (DBGUX §8 fork-1 option B) is the **documented, not-built alternative** to D-2's `fsm-lsp`-ride — it stays the documented path if a non-VS-Code / web client is ever a near-term requirement, and would then be its **own SEC-gated minor** (a second long-lived process speaking a second protocol = pure surface + security cost for an editor-bound use case; D-2 removes that attack surface by construction).

---

## 7. §11.30-clean tag-gate criteria (the Doc-30-§5.2 / Doc 31 §5 / Doc 32 §5 form)

> The `GATE_VERIFICATION_*` §1/§4/§5.2 gate-row form, applied to this epic. This would be the **7th consecutive prospective-clean release** (v1.1 retroactively reconciled §11.30; v1.2/v1.3/v1.4/v1.5 prospectively; the factory-reliability checkpoint prospectively; this epic prospectively). The release is a **`checkpoint/<name>` anchor, LOCAL only** (OWNER-1/OWNER-2; D-3), with the §11.30/factory-GT-12 cold-quad rigor applied identically to a release tag.

- **KEYSTONE-IN-DEBUG gate row (§2) — BINDING.** The post-W1 source-derived §11.3 phase-audit returns the keystone intact (negative-grep ∅ in `crates/fsm-lsp/**` + `editors/vscode/src/**` on non-comment lines; the only sanctioned `crates/` delta is the one additive non-stepping `set_context_field`) **AND** the debug-transport `StepRecord` stream byte-equals `fsm test`'s `execute_trace` on the §5.4 fixture battery (the differential oracle, re-run by the auditor, NOT echoed). A second simulator semantics is the cardinal regression and a hard tag-blocker.
- **§5.4 behavioural-acceptance on every wave** — W1: a tower-lsp round-trip whose `StepRecord` stream byte-equals `fsm test`'s `execute_trace` (+ the `CompletionLoop`-verbatim honest-state + the `set_context_field`-emits-zero-StepRecord assertion). W2/W3/W4: a real Extension-Host `@vscode/test-electron`/`xvfb-run` E2E driving the *real* extension (panel-renders-the-oracle / breakpoint-pauses-pre-step+rewind / capture-round-trips-byte-identical + "captured ✓"-only-after-the-file-exists). **Symbol-presence is NEVER acceptance** (handler-registered ≠ verified; panel-class-exists ≠ verified).
- **§11.30-clean cold-from-source quad at the gate-doc commit** WITH the mandatory NOTE-1 genuine-cold STEP 0 (`[[feedback_embeded_fsm_tag_cold_quad_method]]`): the release sequence is (1) author+commit the GATE_VERIFICATION doc + the batched Doc 00 §11 / CHANGELOG / ROADMAP consolidation → commit `X`; (2) **before** the full-workspace gate, `cargo clean -p` of ALL first-party crates (`fsm-diagnostics fsm-lexer fsm-parser fsm-ir fsm-analyzer fsm-codegen-c fsm-simulator fsm-formatter fsm-cli fsm-lsp fsm-verify`, cargo-1.75 repeated-`-p`; preserves the content-addressed registry cache, evicts every first-party artifact incl. any stale `-wt-`-baked test binary — the recorded disk-protective conventional-CI-cache equivalent, NOT a workaround; the box-infeasible "virgin `CARGO_TARGET_DIR`" ideal is explicitly rejected) **then** the canonical quad **at `X`**; (3) `git tag -a checkpoint/<date>-<name>` **at `X`** — quad-commit ≡ JS-lane-commit ≡ tag-commit ≡ `X`, zero off-by-one by construction, LOCAL.
- **The binding JS lane at the gate-doc commit** (the surface touches `editors/vscode/**` via W2/W3/W4 — the v1.5/factory re-activated-lane shape): at `X`, `npm audit` (or the repo equivalent) clean/triaged + the Extension-Host suite green incl. the new debug-panel + breakpoint+rewind + capture E2Es. The Rust cold quad and the JS lane are both gated at `X`.
- **`#![forbid(unsafe_code)]` invariant preserved, re-derived from source at `X` (not echoed).** W1 adds `fsm-simulator = { path = "../fsm-simulator" }` to `fsm-lsp` (a dep edge, NOT a new crate) + the one `set_context_field` helper in the existing `fsm-simulator` (which already `#![forbid(unsafe_code)]`). The forbid-unsafe root count is **expected unchanged** (W1 adds no new crate root) — re-derived via `git grep -l '#![forbid(unsafe_code)]' -- 'crates/*/src/*.rs'` at `X` (the v1.4/v1.5 method); if W1 *does* somehow add a crate this is flagged, not assumed.
- **Release-record symmetry check (the v1.4 L4 / v1.5/factory standing closeout sub-step).** This epic's Rust delta is **small by design** (W1's one `fsm-simulator` dep edge + the one `set_context_field` helper + the `fsm/simulate*` handler wiring; W2/W3/W4 are TS) — **exactly the framing-trap shape** (a small-but-nonzero delta easy to mis-state as "no Rust change", the v1.3 cardinal near-miss). The closeout MUST re-derive the Rust delta from `git diff <base-commit> X -- crates/ Cargo.toml Cargo.lock` and grep the gate record for the v1.3-cardinal-error form (`zero|no|empty .* (rust )?delta` → 0 hits), stating it as **exactly that small additive delta** — neither under-stated nor over-stated (`[[feedback_embeded_fsm_verify_record_release_layer]]`).
- **Memory-SoT status-line-freshness gate at closeout (the v1.4 D.7(c)#3 / v1.5/factory standing gate).** The closeout MUST, *in the same closeout pass*: advance the backlog SoT head status line to reflect the shipped+tagged debug-interface epic + add a `<epic> RELEASE RECORD` section with the wave→commit map (the single biggest drift the v1.4 meta-session found — the SoT head + missing release-record section lagging the actual tag; codified here as a gate criterion so it cannot be skipped).
- **0 open P0, ≤5 well-scoped P1** in the independent pre-tag four-lens audit (the v1.1–factory §3 standard, FROZEN, never overwritten).
- **Carried owner-escalations re-stated, NOT necessarily resolved** (the §11.62 / v1.4 §6.3 discipline): the release-numbering call (OWNER-1); G9 push + the never-run remote CI matrix (OWNER-2); the deferred full `signal`-primitive epic (OWNER-3, §8); CF-1 the AVR-TABLE Harvard hazard (OWNER-4, §5 — a CI-runner-only on-target-matrix candidate); CF-2 the Doc 16 §2–§6 staleness (OWNER-4, §5 — folds into the owner-reserved v1.6 doc-honesty pass, cross-ref task #103); the carried v1.5/factory escalations unchanged (Marketplace publish OWNER-A; the 5-platform binary tail; no top-level LICENSE/CONTRIBUTING/CoC; the parked Certifiability/MISRA codegen arc) — all re-stated in the GATE_VERIFICATION doc's "carried owner-escalations" so none is silently dropped.
- **CHANGELOG** + the Doc 00 §11 rows (assigned at closeout — see the header numbering note, NOT pre-allocated; §9 carries the DRAFT) + a ROADMAP retrospective + a `docs/metrics/<date>-<epic>.json` snapshot (the v1.4/v1.5/factory D.8 metrics-contract precedent).

---

## 8. The deferred next epic — the full `signal` primitive (own section; scope captured so it is tracked, not lost)

> **D-1 decided the SHAPE; D-3 sequenced it AFTER this debug-interface epic AND after the owner-reserved v1.6.** This section captures its scope **lifted from the frozen `docs/design/EXTERNAL_SIGNAL_MECHANISM_AND_ISR_DESIGN_2026_05_19.md` Part 2** (the owner-chosen full primitive — FSM-DESIGN-EXTSIG §2.1, code-verified). It is **NOT a current-gate item** and **NOT a wave in this plan**; it is recorded so it is tracked, not lost (the §11.49/§11.62 discipline). It is its **own gated epic** with its own wave-plan-of-record (a future Doc 34-class doc), opened only when the orchestrator/owner explicitly schedules it (OWNER-3).

**Scope (frozen FSM-DESIGN-EXTSIG Part 2 — the owner-chosen full first-class `signal`):**

- **A new top-level `signal` declaration** (additive to `top_level_decl`, Doc 04 §2), gated by a new **`feature signals`** flag (Doc 04 §2.2). Two acquisition modes, declared explicitly:
  - `signal s : bool = poll read_s every <const> ms [debounce <const> ms] [{ on rising|falling -> raise EVT }]` — the poll mode reuses the **deterministic `every`-timer machinery** to sample the **existing `pure extern … : bool`** (no new HAL hook, no new I/O contract — acquisition stays the user's job; the language only schedules the sample and/or wires the edge to an event). A bare polled signal with no edge-block is usable in guards as `[s]` (sugar for "the most recently sampled value").
  - `signal s : bool = interrupt { on rising|falling -> raise EVT }` — emits a `<M>_signal_<s>_isr(<M>_t *m, bool new_level)` hook the user calls from **THEIR** vector; the hook's sole effect is **enqueue** (`<M>_post`), identical to the Part-1-verified `Motor.c:196–204` pattern; the deterministic RTC core is **never** re-entered from the ISR — it only runs from the host's single-threaded drain; **no vector is emitted** (consistent with the Part-1 code-verified ground truth that the codegen emits no ISR/vector today).
- **Append-only keyword/grammar additions** (`signal`, `poll`, `interrupt`, `edge`, `rising`, `falling`, `debounce` → Doc 04 §1.5) — a **minor-version event** (Doc 00 §B-03 append-only convention). This **cohesively retires the #127 AUDIT-DETPRIM `edge`/`level`/`debounce` GAPs** (those are the exact words FSM-AUDIT-DETPRIM §1.4/§1.5 classified as GAPs — this is the cohesive feature that closes them as owner-owned language-roadmap work).
- **Everything desugars to events + timers the simulator already executes** ⇒ the **single-oracle keystone is intact** (the verifier sees ISR-injected edge events as just "events that can occur" — the explicit-state engine already explores the full event alphabet; no soundness change, Doc 08 §13.5 untouched; debounce lowers to the existing deterministic `after`-timer — the bisimilarity-preserving "relative timer in disguise" shape FSM-AUDIT-DETPRIM §3 blesses). v1 supports `debounce` only on `poll` signals; `interrupt + debounce` is a documented later increment (an ISR-side software debounce fights determinism/atomicity — deferred, owner-owned).

**Part-2(d) dependency — the per-family interrupt model (ALREADY DELIVERED, cross-referenced).** The `interrupt`-mode codegen + the ISR↔FSM critical-section glue depend on the **per-family interrupt model** (ISR declaration syntax, vector-table model, the `M_post` critical-section primitive per family). This is **already delivered** in `docs/design/TARGET_CHIP_MATRIX_2026_05_19.md` §1.2 ("Per-family interrupt model — the column the ISR thread needs": Cortex-M NVIC / AVR fixed-vector+`SREG` / RISC-V CLINT-PLIC-or-CLIC / MSP430 ITC / ESP32-Xtensa interrupt-matrix / STM8 ITC, each with its ISR-decl-in-C + clock seam + the critical-section mechanism). The future `signal`-epic plan **cross-references FSM-DESIGN-CHIPMATRIX §1.2/§1.3** as its pre-delivered per-family input — it does **not** re-derive the interrupt model. (CF-1's AVR Harvard hazard is also relevant to AVR `interrupt`-mode codegen and is carried in §5.)

**Disposition:** OWNER-3 parked — sequenced after this debug-interface epic AND after the owner-reserved v1.6 (D-3); its own gated epic with its own Doc-34-class wave-plan-of-record; opened only on explicit orchestrator/owner schedule. Recorded here so it is tracked, not lost.

---

## 9. DRAFT decision-ledger rows for Doc 00 §11 — **DRAFT ONLY; the orchestrator independently re-derives & appends; NOT yet in the append-only ledger**

> **⚠️ THESE ARE DRAFT ROWS. They are NOT appended to `docs/00-Decisions-And-Reconciliation.md` by this doc.** Per the release-layer verify-the-record discipline (`[[feedback_embeded_fsm_verify_record_release_layer]]` — Doc 00 §11 is the orchestrator's append-only ledger; the orchestrator independently re-derives every load-bearing claim from source/git before appending, never echoes a wave's self-report) **and** the Doc 31/32 "§11-ledger rows are assigned at epic closeout, NOT pre-allocated" discipline: the current ledger head is **§11.86** (verified). These DRAFT rows are reported here for the orchestrator to **independently re-derive, renumber (§11.87+ at closeout, batched), and append in one controlled commit** (the `comm -23` append-only discipline). The row numbers below are **PLACEHOLDERS** (`§11.DRAFT-A`..`§11.DRAFT-D`), NOT assignments.

**`§11.DRAFT-A` — Debug-interface epic: the full `signal` primitive is the owner-decided shape (D-1), a SEPARATE epic sequenced AFTER the debug interface.**
> The owner decided 2026-05-19 that the external-input feature is the FSM-DESIGN-EXTSIG Part 2 **full first-class `signal` primitive** (poll/interrupt/edge/debounce, `feature signals`-gated, append-only keywords `signal`/`poll`/`interrupt`/`edge`/`rising`/`falling`/`debounce`, cohesively retiring the #127 AUDIT-DETPRIM edge/level/debounce GAPs) — **NOT** the minimal-ISR-hook alternative and **NOT** deferring the whole feature. It is a **SEPARATE epic, sequenced AFTER the debug-interface minor AND after the owner-reserved v1.6** (D-3). It desugars to events+timers the simulator already executes (single-oracle keystone intact; Doc 08 §13.5 untouched). Its Part-2(d) per-family interrupt-model dependency is **already delivered** (FSM-DESIGN-CHIPMATRIX §1.2). Recorded in Doc 33 §8 (scope captured, tracked-not-lost); its own future Doc-34-class wave-plan opens only on explicit orchestrator/owner schedule (OWNER-3). | <why: the owner's language-roadmap shape decision recorded commit-anchored so the chosen full-primitive (vs the rejected minimal-hook / defer) is auditable and the append-only-keyword minor-version implication (Doc 00 §B-03) is on record; honest — design-only, frozen design doc, NOT implemented> | Doc 33 §0 D-1 / §8 (the owner 2026-05-19 decision; scope lifted from the frozen `docs/design/EXTERNAL_SIGNAL_MECHANISM_AND_ISR_DESIGN_2026_05_19.md` Part 2)

**`§11.DRAFT-B` — Debug-interface transport: ride `fsm-lsp` via `fsm/simulate*` custom requests (D-2); NO standalone WS daemon, NO new network surface.**
> The owner decided 2026-05-19 (DBGUX §8 fork-1 option **A**) that the debug surface is new `fsm/simulate*` **LSP custom requests** reusing the proven v1.5 W-A2 `LspService::build().custom_method` embed seam (verified present: `crates/fsm-lsp/src/lib.rs:177` `.custom_method("fsm/verify", …)` + `server.rs:204` `verify_request` + `capabilities/verify.rs`), with **Doc 13's JSON-RPC wire SHAPE reused as the message schema *inside* the LSP requests** — **NOT** a standalone WS daemon, **NOT** a new port, **NOT** a new network attack surface (the security posture collapses to "the attack surface is removed by construction" — the v1.4/Doc-00-§G-02 removed-by-deletion-not-hardening pattern). The Doc 13 standalone-WS-daemon (option B) is the documented, not-built alternative if a non-VS-Code/web client is ever a near-term requirement (then its own SEC-gated minor). The single Rust-layer enabling change is **one** `fsm-simulator = { path = "../fsm-simulator" }` dep edge on `crates/fsm-lsp/Cargo.toml` (verified ABSENT at base — GT-12; the v1.5-W-A2 single-dep-edge shape) plus the **single sanctioned `crates/` behaviour delta**: one additive non-stepping `Interpreter::set_context_field` helper (verified no such mutator exists — GT-13). | <why: the owner's transport decision recorded commit-anchored so the no-new-network-surface posture + the single-keystone-violable-wave structure is auditable and the rejected WS-daemon path is on record as the documented alternative; honest — the proven `custom_method` seam independently re-verified present from source> | Doc 33 §0 D-2 / §1 W1 / §2 (the owner 2026-05-19 decision; the GT-11/GT-12/GT-13 source verification)

**`§11.DRAFT-C` — Debug-interface sequencing: debug-interface FIRST, then the signal epic; neither consumes the owner-reserved v1.6; LOCAL `checkpoint/<name>` until gated (D-3).**
> The owner decided 2026-05-19 that the **debug interface ships FIRST**, then the full-`signal`-primitive epic. **Neither consumes the owner-reserved v1.6** (landing-page + doc-honesty pass — repeatedly affirmed; the factory-epic `checkpoint/`-not-`v1.6.0` precedent, Doc 00 §11.80). The debug-interface minor lands as its **OWN post-v1.6 minor**, `checkpoint/<name>`-tagged at the §11.30-clean gate-doc commit until the release-numbering call is made, **LOCAL only** (G9 = the owner's push — OWNER-2). The epic sequence is: this debug-interface epic now → (owner-reserved v1.6 landing+doc-honesty, owner-paced) → the `signal` epic → the visual-DSL-constructor capstone. The final release number + its position relative to v1.6 is the orchestrator/owner's call at closeout (OWNER-1 — the surface-don't-auto-number discipline). | <why: the owner's sequencing + v1.6-reservation decision recorded commit-anchored so the epic order is auditable, the no-unilateral-v1.6-consumption rule is on record, and the LOCAL-checkpoint-until-gated release discipline is explicit; honest — numbering deliberately NOT pre-assigned> | Doc 33 §0 D-3 / §4 OWNER-1/OWNER-2 / §7 (the owner 2026-05-19 decision; the v1.6-reservation lineage Doc 00 §11.80 / ROADMAP)

**`§11.DRAFT-D` — Doc 33 (debug-interface wave-plan-of-record) authored; the binding keystone-in-debug tag-gate + the carried Zero-Legacy findings recorded; the DBGUX line-citation drift flagged-and-corrected.**
> Doc 33 (FSM-PLAN-DBGIFACE-33) is the epic-kickoff wave-plan-of-record (the Doc 28/29/30/31/32 numbered-sibling precedent; next free integer = 33, verified). It decomposes the MVP-core (DBGUX §7) into W1 (the single keystone-violable `fsm/simulate*` LSP layer + the one `set_context_field` helper) → W2 (the TS debug WebviewPanel, NO semantics) → W3 (breakpoints+time-travel, NO semantics) → W4 (capture round-trip + the binding post-W1 source-derived keystone phase-audit + the pre-tag four-lens + the §11.30-clean cold-quad-with-NOTE-1 + the binding JS lane + the LOCAL checkpoint tag). The **KEYSTONE-IN-DEBUG binding tag-gate row** (§2) is the debug-surface application of the v1.4/v1.5/factory keystone invariant — the post-W1 audit independently re-derives the no-fork obligations FROM SOURCE (negative-grep ∅ + the differential-oracle byte-equality re-run by the auditor, NOT echoed). The verify-the-record pass **independently re-derived the entire keystone surface from source** (GT-1..GT-15, all 11 `Interpreter`/`StepRecord` symbols + snapshot/restore + the round-trip seam + the net-new-helper gap + the proven `custom_method` seam verified present) and **FLAGGED a two-line DBGUX citation drift** (DBGUX §3.2 cites `trace.rs:64` for `entered_states` — actual `:65`; cites `trace.rs:172` for `transition_taken.stable_id` — actual `:174`; `exited_states:62`/`StepKind:113`/all `interpreter.rs` citations exact); the design is sound, only the citations drifted — corrected in Doc 33's GT-7; the frozen DBGUX doc NOT edited (design evidence). Two **carried Zero-Legacy findings** recorded (tracked-not-dropped): **CF-1** the AVR Harvard `.rodata`+TABLE-dispatch UNTESTED CORRECTNESS HAZARD (TARGET_CHIP_MATRIX §1.3/§2.3 — a CI-runner-only on-target-matrix `avr-gcc` link+run candidate, NOT a debug-interface item); **CF-2** the Doc 16 §2–§6 staleness vs the emitted `fsm_hal_clock_now_ms`/`fsm_hal_assert(bool,msg)` (folds into the owner-reserved v1.6 doc-honesty pass, cross-ref task #103). | <why: the plan-of-record + the binding keystone-in-debug gate + the verify-the-record line-drift finding + the two carried Zero-Legacy findings recorded commit-anchored so the epic's falsifiable-before-a-line-ships keystone obligation is on record, the design-doc drift is corrected-not-propagated, and the AVR/Doc-16 findings are tracked-not-lost; honest — doc-only, every keystone symbol re-derived from source, the one drift flagged not hidden> | Doc 33 (this doc, `phase8.0/debug-iface-plan-of-record`; the GT-1..GT-15 ledger; §2/§5/§8/§9)

---

## 10. See also

- `docs/design/DEBUG_INTERFACE_UX_DESIGN_2026_05_19.md` (FSM-DESIGN-DBGUX — the UX + the keystone-honoring architecture + the §7 MVP scope spine + the §8 forks D-1/D-2/D-3 resolve; **NB: §3.2's `trace.rs:64`/`:172` citations are stale — use Doc 33's GT-7 corrected `:62`/`:65`/`:113`/`:174`**)
- `docs/design/EXTERNAL_SIGNAL_MECHANISM_AND_ISR_DESIGN_2026_05_19.md` (FSM-DESIGN-EXTSIG — Part 1 frozen code-verified ground truth; Part 2 the owner-chosen full `signal`-primitive scope §8 lifts) · `docs/design/TARGET_CHIP_MATRIX_2026_05_19.md` (FSM-DESIGN-CHIPMATRIX — §1.2 the per-family interrupt model the §8 signal-epic cross-references; §1.3/§2.3 the CF-1 AVR Harvard hazard)
- `docs/30-v1_4-Simulation-Verification-Wave-Plan.md` / `docs/31-v1_5-UIDX-Wave-Plan.md` / `docs/32-Factory-Reliability-CI-Wave-Plan.md` (the structural template + the §5.2 gate-row form + the §3/§4 owner-decision pattern + the keystone-as-single-auditable-checkpoint precedent this epic applies a fourth time)
- `docs/13-Simulator-Protocol.md` (the JSON-RPC wire SHAPE reused as the message schema INSIDE the W1 LSP requests — normative for the schema, NOT a separate server) · `docs/26-LSP-Architecture.md` §8 (the LSP-wave + phase-boundary-audit precedent) · `docs/27-VSCode-Extension-Architecture.md` (the v1.3 diagram-panel contract W2 reuses)
- `crates/fsm-simulator/src/interpreter.rs` (the oracle — GT-1..GT-6,GT-9,GT-13; the `set_context_field` helper W1 adds) · `crates/fsm-simulator/src/trace.rs` (`StepRecord`/`StepKind`/`TransitionTakenRecord`/`TraceCommand`/`write_trace_yaml`/`execute_trace` — GT-7,GT-8,GT-10) · `crates/fsm-cli/src/cmd/test.rs` (the SAME `execute_trace` the capture round-trips through — GT-8) · `crates/fsm-lsp/{Cargo.toml,src/lib.rs,src/server.rs,src/capabilities/verify.rs}` (the proven `custom_method` seam W1 mirrors; the ABSENT `fsm-simulator` dep edge W1 adds — GT-11,GT-12) · `editors/vscode/src/diagram/**` (the v1.3 reuse targets W2 rides — GT-15)
- `docs/processes/SUBAGENT_CONVENTIONS.md` (§5.4 behavioural-acceptance / PD-2; §11.3 phase-boundary audits; §11.22/§11.30 cold-quad + tag-ordering) · `docs/00-Decisions-And-Reconciliation.md` §11 (the append-only ledger — head §11.86; this epic's rows assigned at closeout, §9 carries the DRAFT) · `memory/project_embeded_fsm_debug_interface_design_teeup` (the endpoint-design tee-up SoT)

*End of FSM-PLAN-DBGIFACE-33 v1.0.0 — accepted plan-of-record; the orchestrator gates/merges; the §11-ledger DRAFT rows (§9) are independently re-derived & appended by the orchestrator at epic closeout, NOT here.*
