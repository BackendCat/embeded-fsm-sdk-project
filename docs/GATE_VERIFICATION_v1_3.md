# GATE VERIFICATION — v1.3.0

- **Document ID:** GATE_VERIFICATION_v1_3
- **Date:** 2026-05-16
- **Scope:** v1.3.0 release gate — all commits since v1.2.0 `71043935` through the v1.3 corpus: W0 `ceb8efd` (the §11.49 analyzer→CST-coupling paydown, `== checkpoint/2026-05-16-w0-clean`) + the VS Code extension epic V1–V6 (`6707d1d` / `1542bfb`+`3c49d38`→`61cc3f0` / `4c4c6a2` / `1db80cd` / `fa3befc`) + the four §11.3 phase-audit docs (`255a3e7` / `4280406` / `4aaef5d` / `203b7d5`), and the release commit `X` (this document + the batched doc/ledger consolidation; see §4 + §7).
- **Companion evidence (frozen, never edited):** the four §11.3 phase-boundary audit docs `docs/AUDIT_PHASE_W0_2026_05_16.md` / `docs/AUDIT_PHASE_V1_2026_05_16.md` / `docs/AUDIT_PHASE_V2V3_2026_05_16.md` / `docs/AUDIT_PHASE_V4_2026_05_16.md`; the v1.0/v1.1/v1.2 baselines `docs/GATE_VERIFICATION_v1_0.md` / `docs/GATE_VERIFICATION_v1_1.md` / `docs/GATE_VERIFICATION_v1_2.md`; the v1.3 release record `docs/00-Decisions-And-Reconciliation.md` §11.50–§11.62; the v1.2 metrics base `docs/metrics/2026-05-16.json` and the v1.3 companion `docs/metrics/2026-05-16-v1_3.json`.
- **Verdict:** **PASS — v1.3.0 is tag-ready.** 0 open P0; the four Doc-28-mandated/orchestrator-cadenced §11.3 phase-boundary audits each returned **PROCEED-WITH-NOTES** (0 ship-blockers across W0/V1/V2V3/V4); every NOTE is documentation-of-record / CI-lane / cosmetic, folded by this closeout or carried as accepted-tracked-debt. The independent pre-tag four-lens audit is the contingent step the tag is gated on (the v1.2 §3 pattern). The canonical confirming cold-from-source quad **plus** the JS Extension-Host lane (`npm ci` + `@vscode/test-electron` under `xvfb`) at the release commit `X` is the gating step the tag is contingent on (§4).

This document is the canonical release record for v1.3.0. It captures the MVP-gate status, the v1.3 surface (the W0 §11.49 CST-coupling paydown + the VS Code extension V1–V6), the verification evidence (the two Doc-28-mandated phase-audits post-V1 / post-V4, plus the orchestrator-cadenced post-W0 and post-V2/V3-batch audits, and the per-wave + final §11.1 independent re-runs), the §11.22/§11.30-mandated **cold-from-source** quad + JS-lane posture, the accepted-tracked-debt a consumer must be aware of, and the post-tag actions that require the repository owner.

---

## 1. MVP gate (Doc 23 §9) — v1.3.0 status

| Gate | v1.3 status | Evidence |
|---|---|---|
| **G1** `fsm check` ok / clean reject, no panic, no UB | ✅ | **The v1.3 Rust delta vs the v1.2 tag (`71043935`) is exactly W0** — the §11.49 CST-coupling paydown (`7d6792a..ceb8efd`, 18 files +421/−93 in `fsm-analyzer` + additive-only `fsm-parser`; `git diff --stat 71043935 fa3befc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` = 18 files / +421 / −93 — **not** empty). W0 is the **only** Rust touch and is **non-behavioural by construction** (additive-only on `fsm-parser`: zero deletions, zero grammar/CST/Cargo change, **0 new deps**; the §11.1 W0 independent post-merge verification returned COLD-QUAD GREEN + IDENTITY CONFIRMED, 38-file `diff -r` byte-empty, pre-sha==post-sha, 806/0/108, 26/26 — `AUDIT_PHASE_W0` §1.1/§4.1). **There is zero further Rust delta after W0:** `git diff ceb8efd fa3befc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` is **empty** (independently re-derived; the four §11.3 audits each re-confirmed it from `git`) — the entire V1–V6 extension epic is `editors/vscode/`-only. So `fsm check` behaviour equals v1.2's NOT because nothing changed but because the sole Rust change (W0) is behaviour-identical + §11.1-cold-verified at `ceb8efd`. |
| **G2** `fsm check broken.fsm` → exit 1 + Rust-style error | ✅ | Unchanged; W0 preserves the byte-identical diagnostic stream (the R-2 unsorted-vector residual is *retained precisely because* reordering it would change the diagnostic vector — `AUDIT_PHASE_W0` §6 independently re-derived "no sort in the analyzer→CLI diagnostics path"). The VS Code extension *reuses* `fsm check --json` as its diagnostics + IR oracle; editor diagnostics cannot disagree with the CLI (the L1-epic single-`analyze_with_source` discipline, `AUDIT_PHASE_V1` §1.4 / `AUDIT_PHASE_V4` §1.1). |
| **G3** `fsm generate` emits the C unit set + HAL | ✅ | Unchanged from v1.2 (no codegen surface in the v1.3 W0+extension scope; V4's diagram consumes the codegen-gated `fsm generate --emit-ir` path unchanged — `AUDIT_PHASE_V2V3` §1.1). |
| **G4** `gcc -std=c99 -Wall -Wextra -Wpedantic -Werror` zero-warning | ✅ | Unchanged; subsumed in the cold quad's `cargo test --workspace` §5.4 gcc-compile-and-RUN battery. The sole v1.3 Rust change (W0) is non-behavioural + §11.1-cold-verified at `ceb8efd` and touches no codegen surface; nothing Rust changed after W0. |
| **G5** `fsm fmt` idempotent | ✅ | Unchanged from v1.0 (zero formatter delta). |
| **G6** all examples full-chain + sim-trace-match | ✅ | Unchanged; the W0 acceptance pinned `fsm test examples/` 5/5 + conformance 26/26 byte-unchanged pre/post (the SUBAGENT-§5.4 pre/post-identity proof for the refactor wave; `AUDIT_PHASE_W0` §0/§4.1). |
| **G7** ≥1 test per diagnostic code | ⚠️ tracked | **26** formal `tests/conformance/` fixtures (unchanged — v1.3 retired/added no diagnostic code; the live `DiagnosticCode` count is **independently re-derived at `fa3befc` = 73**, byte-identical to the v1.2 `71043935` value). Identical posture to v1.0/v1.1/v1.2; **not a behavioural gap** — every new v1.3 surface (the extension) is behaviourally tested by **34 Extension-Host tests** (the §5.4-analogue: real `@vscode/test-electron` host, oracle recomputed from `fsm check --json`, byte-compared). Formal-suite closure remains tracked, unchanged. |
| **G8** `cargo build/test/clippy/fmt` clean | ✅ | **cold-from-source** quad posture below (§4). The v1.3 Rust workspace delta vs the v1.2-tagged corpus **is exactly W0** (the §11.49 paydown, 18 files +421/−93, 0 new deps, non-behavioural); the §11.1 W0 post-merge run produced COLD-QUAD GREEN (806/0/108, 26/26, clippy -D clean, fmt clean) at `ceb8efd` as the pre/post-identity proof, and every subsequent §11.1 wave-run re-confirmed **zero further Rust delta after W0** (`ceb8efd..fa3befc` empty). The v1.2 cold-quad figures carry NOT because nothing changed but because W0 (the sole Rust change) was independently §11.1-cold-GREEN + behaviour-identical at `ceb8efd` and nothing Rust changed thereafter; the §11.30-clean canonical confirming run at `X` re-confirms from cold regardless — the gating step the tag is contingent on. |
| **G9** CI matrix linux/macos/windows | ⚠️ tracked | `.github/workflows/ci.yml` well-formed, carries the v1.2 SCA `cargo audit` job; **never exercised against any v1.2 OR v1.3 commit** (local-only repo — owner controls the remote, a hard project rule). v1.3 *adds a second never-run lane*: the Node/Extension-Host JS lane (Doc 28 §2.3 Option-A; `@vscode/test-electron` needs a virtual DISPLAY → `xvfb-run`, JC-1). The 5-platform binary bundling tail (V6) is *bound* to G9 existing. Post-tag owner action §5. |

G7 + G9 are the **same explicitly-tracked partials accepted at the v1.0, v1.1 and v1.2 gates** — neither is a v1.3 regression; both (plus the new JS-lane / G9-bound multi-platform tail) are documented in CHANGELOG `[1.3.0]` *Known limitations* and the ROADMAP.

---

## 2. v1.3 surface delivered (since v1.2.0 `71043935`)

**Theme: first-class editor experience on the shipped LSP — the VS Code extension (Doc 27), preceded by the deferred §11.49 analyzer-coupling paydown (W0).** v1.3 was scoped (Doc 00 §11.40, the v1.2-era user-endorsed re-version) to the VS Code extension: it is the natural `vscode-languageclient` consumer of the v1.2 LSP, so all language intelligence comes **free** over the client — only the ELK diagram Webview is substantive-new. The deferred §11.49 CST-coupling debt is paid as **W0 before V1** (the v1.1→v1.2 "L0-before-LSP" precedent, Doc 28 §4).

### 2.1 W0 — §11.49 analyzer→parser-CST coupling paydown (`ceb8efd`, == `checkpoint/2026-05-16-w0-clean`)

The accepted-tracked-debt carried forward from `GATE_VERIFICATION_v1_2.md` §6 (the arch-audit "ship-acceptable, non-behavioural P1", deferred per Doc 00 §11.49). Shipped **substantially-paid-with-documented-reasoned-residuals**, DRIFT-2-grade, additive-only, 0 new deps (`AUDIT_PHASE_W0` §3, the authoritative residual catalogue):

- **The clean Archetype-A removal:** `crates/fsm-analyzer/src/checks/parallel.rs` is the **only** file that fully shed `use fsm_parser::cst::*` (its sole `cst::` use — an `INITIAL_DECL` existence predicate — relocated to the typed `RegionDecl::initials()` accessor; spot-checked byte-identical, `AUDIT_PHASE_W0` §2 spot-check #1).
- **The B-clean single-construct accessor relocations:** `PriorityClause::value`, `{Transition,Internal,Local}Decl::payload_binding`, `{After,Every,EveryInternal}Decl::{action_block,duration}`, `{Machine,Region}Decl::initials` — ~7 new `pub fn` accessors on already-declared `ast_node!` types in `fsm-parser`, each body the *exact* CST walk the analyzer performed, relocated to its correct home (the W0 audit verified each verbatim against the deleted analyzer helper, incl. the correctly-disclosed decimal-only `priority` parse subtlety — §2 spot-check #2).
- **13 `fsm-analyzer` files deliberately retain `use fsm_parser::cst::*` as the documented R-1..R-4 leave-and-explain residual**, each carrying a textbook DRIFT-2-form code comment citing Doc 00 §11.44/§11.49 + the `LineIndex` precedent (all 13 verified present, `AUDIT_PHASE_W0` §3):
  - **R-1** OPAQUE-BUG-1 parent-node type resolution — removing it risks re-introducing the exact P0-1-class silent-data-loss bug W0 is forbidden from causing; the `TypeOrOpaque` widening is explicitly deferred to a dedicated parser-API wave (conservative — does not sneak a new public type onto the most behaviourally-critical seam in a non-behavioural wave).
  - **R-2** heterogeneous source-ordered / document-pre-order dispatch — the emitted **diagnostic vector is unsorted** (`AUDIT_PHASE_W0` §6 independently re-derived: no sort anywhere in the analyzer→CLI path), so traversal order across heterogeneous node kinds is **byte-load-bearing**; folding into per-kind typed iterators would reorder diagnostics and fail the §4.2 byte-identity gate.
  - **R-3** the deliberately-shallow expression/statement sublanguage — folding **relocates not eliminates** the CST walks (the dominant blast-radius driver); the `LineIndex`-precedent leave-and-explain verbatim.
  - **R-4** `util::span_of`/`loc_of`/`submachine_ref_is_nested` positional helpers — pure rowan-positional, **zero cross-crate callers**, de-facto crate-internal; threading a typed wrapper through 79 call sites is massive non-behavioural churn for zero clarity gain.
- **Discipline verdict (`AUDIT_PHASE_W0` §0/§4.1):** a **success of the C-1 / SUBAGENT §10 leave-and-explain discipline**, precedent-consistent with how DRIFT-2 and the v1.1 pub-hygiene debt closed (paid where clean, left-and-explained where forcing it was worse-EV). Nothing was contorted to hit a zero-`cst` count; the one genuinely-clean order-independent site **was** removed.

### 2.2 The VS Code extension — V1–V6 (`editors/vscode/`, pure additive — zero Rust delta)

Each wave is depth-first, additive on `editors/vscode/`, with a real Extension-Host behavioural-acceptance gate (the §5.4-analogue: real `@vscode/test-electron`, oracle recomputed from `fsm check --json`/`fsm generate --emit-ir`, byte-compared — never symbol-presence; Doc 28 §3, CHANGELOG `[1.3.0]` Added):

- **V1 — the MVP spine** (`6707d1d`): `editors/vscode/` greenfield TS; `vscode-languageclient@9.0.1` wired to `fsm-lang-server` over **stdio with `transport` deliberately OMITTED** (the only round-tripping form against the strict server arg-parser — `AUDIT_PHASE_V1` §1.1/§2.1, robust *by construction* against the locked client's `Executable`-vs-`NodeModule` dispatch); live diagnostics reusing the exact `fsm check` pipeline; the Doc 22 §2.2 binary-resolution Rule 1/2/3 (no silent PATH fallback) + Doc 22 §13.2 exponential-backoff crash recovery, both verified correct *by reading* (`AUDIT_PHASE_V1` §1.2/§1.3). 4 Extension-Host tests (the (a)/(d) oracle independently recomputed).
- **V2 — TextMate grammar + language-configuration + snippets** (`1542bfb`): the Doc 21 grammar (every Doc 21 §2 scope name preserved *verbatim* — `AUDIT_PHASE_V2V3` §3.D), `language-configuration.json`, snippets. Static assets; 7 grammar-test cases (`vscode-tmgrammar-test`-style assertions). The Doc 21 §3 *literal JSON* was structurally defective (single-line `match`, no body rule → `#action-block` swallowed the machine body); V2 corrected the **structure** to begin/end block rules while keeping the **scope names** Doc-21-verbatim (the N-6 corrected-in-shipped item, §6 of this doc).
- **V3 — thin CLI-wrapper + client-control commands** (`3c49d38`, merged `61cc3f0`): the 7 Doc 22 §4 commands + their menus/keybindings/`commandPalette` gating; `fsm.showOutputChannel` + `fsm.restartLanguageServer` contributed+registered (closing the V1-audit N-5 forward-dependency); the honest `cliRunner.ts`/`cliBinary.ts` seam + `copyIr.ts` codegen-gated `--emit-ir` path. 9 command-test cases.
- **V4 — read-only diagram WebviewPanel** (`4c4c6a2`): `fsm.openDiagram` → CSP-locked split-right Webview, data = the **one real** codegen-gated `fsm generate --emit-ir` path via the V3 seam (NO new LSP/server method — the R-9/R-10 DRIFT bar held), elkjs ELK-Layered in-webview, click→source via the IR `SourceLocation`. The recommended `copyIr.ts`→`emitIr.ts` shared-core extraction shipped (one honest `--emit-ir` site, `copyIR` pre/post-identical — a **POSITIVE** closeout credit, §6). The codegen-gated boundary is sealed: last-valid-render-retained + the Doc 05 §1.5.9 banner **verbatim**, asserted at the real Webview ack. 4 diagram-test cases.
- **V5 — activity-bar tree views + context-key chrome** (`1db80cd`): `fsm.machineExplorer`/`fsm.eventExplorer` `TreeDataProvider`s fed by the **free V1-client `documentSymbol`** seam (NOT V4's IR path — the V4-audit MV5-1 foot-gun forbidden; `AUDIT_PHASE_V4` §1.1/§1.3), Doc 22 §7 view containers, Doc 22 §11 context keys. 6 tree-test cases.
- **V6 — host-only binary bundling + installable VSIX** (`fa3befc`, HEAD): `scripts/populate-bin.mjs` builds the host-triple `fsm-lang-server` + `fsm` on the pinned 1.75 into `editors/vscode/bin/<host-triple>/` (the dir V1's never-exercised Rule-2 resolver actually reads — producer/consumer re-derived from the *same* `os.platform()`-`arch()` expression so they cannot drift); `@vscode/vsce package` → a lean **4.95 MB installable `.vsix`** (excludes `node_modules`/`src`/`out`/`test`, asserted via `vsce ls` + `unzip -l`). 4 V6-test cases prove Rule-2 fires against a real bundle, a real diagnostic round-trips through the bundled server byte-equal to the bundled `fsm check --json` oracle, and the SEC-P0-1 multibyte seam holds through the bundle. **The committed delta is ONLY packaging machinery** — `bin/` + `*.vsix` are gitignored build artifacts; V1 client-spawn + V2–V5 source + Rust + docs + the manifest `contributes` sections are byte-untouched. The 5-platform cross-compile tail is **G9-gated** (no cross toolchains — owner/infra action, not attempted); `vsce package` is **NOT publish** (publishing/signing = owner credential decision, Doc 27 §9 risk-4).

**Planning docs of record:** Doc 28 `docs/28-v1_3-VSCode-Wave-Plan.md` (the V1–V6 epic plan + the v1.3-tag gate definition §6.2; commit `87ffebd`), Doc 29 `docs/29-v1_3-W0-CST-Coupling-Plan.md` (the W0 extraction + seam design; commit `7d6792a`).

---

## 3. Independent phase-boundary audits (read-only; frozen evidence)

Doc 28 §3 mandates §11.3 phase-boundary audits **after V1** (the spine) and **after V4** (the substantive new subsystem). The orchestrator additionally ran them **after W0** (the analyzer-paydown gate that lets V1's oracle be computed against the post-W0 analyzer) and **after the V2/V3 batch** (before the most substantive remaining subsystem V4). All four are READ-ONLY, ZERO `cargo`/`npm`/build, every quantity independently re-derived from `git`/source/`grep` (never echoed from docs), and each layers a *judgment* gate on top of the already-produced §11.1 mechanical proof.

| Audit (frozen doc / commit) | HEAD audited | Verdict | Key result |
|---|---|---|---|
| **post-W0** `AUDIT_PHASE_W0_2026_05_16.md` / `255a3e7` | `ceb8efd` | **PROCEED-WITH-NOTES** (0 V1 ship-blockers) | W0 is a correctly-disciplined DRIFT-2 non-behavioural seam cleanup: additive-only on `fsm-parser`, behaviour-inert relocations proven by the §4.2 byte-identity gate, R-1..R-4 textbook leave-and-explain. The R-2 "unsorted-diagnostics → traversal-order byte-load-bearing" premise **independently re-derived TRUE** (§6). N-1: Doc 29 §3.1's "~7 files cleanly removable" plan-disposition is stale & internally inconsistent with §3.4-R-2 (shipped reality: `parallel.rs` sole-removal + R-2/R-3 generalized residuals — the **correct** DRIFT-2 call, not under-delivery). N-2: the §11.1 agent's "rustc 1.95.0 / pin not honored" line was a **probe-context error, not a pin violation** — the 1.75.0 pin IS honored for all in-repo `cargo` work (§5). |
| **post-V1** `AUDIT_PHASE_V1_2026_05_16.md` / `4280406` | `6707d1d` | **PROCEED-WITH-NOTES** (0 V2 ship-blockers) | Rust workspace byte-untouched vs `ceb8efd`. The two doc-overturning Findings 1+2 **independently re-confirmed from source** (the shipped code is correct on both seams; it is Doc 27/28 *prose* that is wrong-as-written): Finding 1 — `TransportKind.stdio` is a wrong literal, omit-`transport` is the only round-tripping form (robust *by construction*, §1.1/§2.1); Finding 2 — UTF-8 `positionEncoding` is **unreachable** through `vscode-languageclient` (v8/v9 hardcode `['utf-16']`, throw on non-UTF-16) so the correct negotiated value is `"utf-16"` (the shipped acceptance test already asserts this — §2.2). Binary Rule 2/3 + the backoff handler correct *by reading* (§1.2/§1.3). The exact correction-of-record text is `AUDIT_PHASE_V1` §3 (folded by this closeout — §6). |
| **post-V2/V3-batch** `AUDIT_PHASE_V2V3_2026_05_16.md` / `4aaef5d` | `61cc3f0` | **PROCEED-WITH-NOTES** (0 V4 ship-blockers) | Rust workspace byte-untouched across the whole W0→V3 batch. The V4 IR-data-source seam **RESOLVED, not assumed**: the one real codegen-gated `fsm generate --emit-ir` path exists and V3's `copyIr.ts`/`cliRunner.ts` already drives it and already handles the R-11 codegen-gated boundary honestly. V2/V3 strictly disjoint+additive on V1; M-1 (the transport foot-gun) honoured *by construction*. N-5 **CLOSED by V3**; N-6 **CORRECTED-IN-SHIPPED by V2** (Doc 21 §3 structurally defective; scope names verbatim). New items: **JC-3** (`fsmLang.codegen.*` config-discoverability gap), **JC-1** (xvfb required for the JS CI lane). |
| **post-V4** `AUDIT_PHASE_V4_2026_05_16.md` / `203b7d5` | `4c4c6a2` | **PROCEED-WITH-NOTES** (0 V5 ship-blockers) | Rust workspace byte-untouched across W0→V4. The V5 data-source seam **RESOLVED**: the **free V1-client `documentSymbol`** (`server.rs:251`, the same single-analysis seam) is shipped + client-registered; V4's IR path is the *wrong* seam for the tree (MV5-1 forbidden). The V4 codegen-gated DRIFT (K-3) **sealed**: the recommended `copyIr.ts`→`emitIr.ts` extraction shipped, `copyIR` pre/post-identical (the V3 copyIR tests re-passed), the ONLY `--emit-ir` site `grep`-verified. CSP/elkjs/click→source/read-only/IR-purity all faithful to Doc 27 §6.2/§6.3/§8-V4. New items: the `copyIr→emitIr` extraction (**POSITIVE** credit), V4 JC-2 (sound codegen-ICE test strategy), V4 JC-3 (panel-identity = file-path — also resolves a Doc 05 §1.5.1 prose tension), V4 JC-4 (strict Webview tsconfig — **POSITIVE** credit). Recorded a non-actionable `makeNonce()` `Math.random` nit (adequate under `default-src 'none'`). |

### 3.1 Per-wave + final §11.1 independent re-runs

Each wave's completion report was independently re-run by the orchestrator's §11.1 verification (decode-the-wire-and-assert-vs-oracle, never symbol-presence). The cumulative final combined gate (post-V6) is **34/34 Extension-Host tests green** with the V1/V3/V4 load-bearing oracles independently recomputed and byte-matched (V1 `documentSymbol`/diagnostic byte-range; V3 `copyIR` IR sha; V4 3-node/2-edge model + the verbatim Doc 05 §1.5.9 banner; V5 tree-node-set == `executeDocumentSymbolProvider`; V6 Rule-2-against-a-real-bundle + bundled-server diagnostic byte-equal to the bundled `fsm check --json` oracle), zero cross-wave regression, **zero Rust delta vs W0-clean `ceb8efd`** re-confirmed from `git` at every wave. The 34 tests break down per suite: V1 4, V2 7, V3 9, V4 4, V5 6, V6 4 (independently re-counted at `fa3befc`). **Rule-2 is proven against a real 4.95 MB bundle** (the V6 gate — V1's never-exercised bundled-binary resolver fired against an actual `vsce package` output for the first time).

### 3.2 Why these audits are not rubber-stamps (recorded so the record is honest)

Four phase-audits over a ~15-file refactor of the most behaviourally-critical crate + a 6-wave greenfield extension epic that find nothing are themselves suspect. They did not: the W0 audit **independently re-derived** the unsorted-diagnostics R-2 premise from `checks/mod.rs`+`cmd/check.rs` and found the genuine N-1 Doc-29-§3.1 internal inconsistency the plan author missed, *and* corrected the §11.1 agent's false "pin broken" line (N-2); the V1 audit **independently re-derived Findings 1+2 from source** (re-reading the locked `vscode-languageclient@9.0.1` `main.js`/`client.js` line numbers itself, proving the shipped code right and Doc 27/28 *prose* wrong); the V2/V3 audit traced the V4 IR seam to file:line and surfaced JC-3/JC-1; the V4 audit found the V4-JC-3 file-path-identity-vs-Doc-05 tension and confirmed the recommended `emitIr` extraction shipped with the refactor-safety bar met. The epic is unusually clean *because* every wave reported-then-§11.1-verified with per-commit `git show` — corroborated, not assumed.

---

## 4. Cold-from-source release quad + JS Extension-Host lane (§11.22 / §11.30) — the tag gate

### 4.1 The binding commit-ordering rule (Doc 00 §11.30, quoted)

> **Refinement (binding for v1.2+):** the release sequence is (1) author+commit `GATE_VERIFICATION_v<x>.md`, (2) run the cold-from-source quad at THAT commit, (3) `git tag -a` THAT commit + the `checkpoint/<date>` anchor — quad-commit ≡ tag-commit exactly.

This document makes code-summarising assertions (the §1 gate evidence; the §"Pinned gate numbers" — live `DiagnosticCode`=73, `forbid(unsafe_code)`=11 roots/10 crates, 34 Extension-Host tests, zero-Rust-delta-vs-`ceb8efd`) that a code delta could in principle falsify. The v1.1-§7 one-pure-docs-commit grace therefore does **not** apply; the §11.30 sequence is followed strictly: this doc + the batched doc/ledger consolidation commit first, the canonical cold quad + JS lane run **at that exact commit `X`**, and the tag is placed on **that same commit** — zero off-by-one by construction (the v1.2 §4.1 prospective-§11.30 pattern, repeated).

### 4.2 Cold-quad + JS-lane method (§11.22 / Doc 28 §6.2(4))

§11.22 mandates a **cold from-source** green quad for a release tag (a warm shared-`CARGO_TARGET_DIR` can serve stale cross-worktree `-wt-`-baked-path test binaries; a warm pass is necessary-but-not-sufficient). The method: invalidate the shared target (cold from-source — every first-party crate + every test/integration binary recompiled from source), then `cargo build/test/clippy/fmt --workspace` + the conformance runner (`fsm test tests/conformance`) + the §5.4 `gcc` compile-and-**RUN** acceptance (subsumed inside `cargo test --workspace`).

**v1.3 adds a second lane (Doc 28 §6.2(4)/(5), JC-1):** v1.3 adds a Node dependency tree plus exactly one Rust change — **W0** (the §11.49 paydown, 18 files +421/−93 vs the v1.2 tag, 0 new deps, non-behavioural + §11.1-cold-GREEN at `ceb8efd`); there is zero further Rust delta after W0 (`ceb8efd..fa3befc` empty). The cargo gate is therefore unchanged in *shape* NOT because the Rust corpus is untouched but because the sole Rust change (W0) was independently cold-verified behaviour-identical at `ceb8efd` and nothing Rust changed thereafter; the §11.30-clean cold-quad at `X` re-confirms from cold regardless. The new JS lane is `npm ci` → `tsc` → `eslint` → `esbuild` → `@vscode/test-electron` (the 34 Extension-Host tests) **under `xvfb-run`** (the suite launches a real Electron host needing a DISPLAY — JC-1, now strictly load-bearing post the V4 Webview test) **plus** an `npm audit` of `editors/vscode/` at 0 high/critical (the SCA discipline extended to the new Node surface). The JS caches (`node_modules` ~300–800 MB, the VS Code download ~150–250 MB) are pinned **out-of-tree** so the cold-cargo ~3 G-free trough is not worsened (Doc 28 §2 Option-A — the binding owner-resolved disk envelope).

### 4.3 Carry-forward reasoning (the v1.1 §7 logic, stated up front not retro)

The v1.3 Rust delta vs the v1.2 tag **is exactly W0**: `git diff --stat 71043935 fa3befc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` = 18 files / +421 / −93 (the §11.49 CST-coupling paydown — **not** empty; re-checked at `X`), but W0 (`7d6792a..ceb8efd`) is non-behavioural with 0 new deps (Cargo.lock unchanged — verified) **and was independently §11.1-cold-GREEN + behaviour-identical at `ceb8efd`** (the pre/post-identity proof), and `git diff ceb8efd fa3befc` over the same paths is **independently re-derived empty** (this doc §1 G1, the four §11.3 audits each from `git`) — zero further Rust delta after W0. So the v1.2 cold-quad figures carry NOT because the Rust corpus is unchanged but because the sole Rust change (W0) was cold-verified behaviour-identical at `ceb8efd` and nothing Rust changed thereafter; the §11.30-clean cold-quad at `X` re-confirms from cold regardless (§4.4, the contingent gating step). The gate-doc commit `X = fa3befc + a docs/ledger-only delta` (this file + the batched Doc-00/CHANGELOG/ROADMAP/metrics consolidation; `git diff --stat fa3befc X` is docs/metrics-only — zero `crates/`/`Cargo.*`/`editors/`/source/test surface). A Markdown/JSON-only delta **cannot** change `cargo build/test/clippy/fmt` / conformance / `gcc`-RUN / the Extension-Host outcomes.

### 4.4 The canonical confirming quad + JS lane (the step the tag is contingent on)

The **canonical confirming cold-from-source cargo quad AND the JS Extension-Host lane are executed at the release commit `X` itself** (orchestrator-run after this document + the batched consolidation commit), and both transcripts are recorded in the **annotated `v1.3.0` tag message**. So quad-commit ≡ JS-lane-commit ≡ tag-commit = `X`, satisfying §11.22 (cold from-source) **and** §11.30 (zero off-by-one) **and** Doc 28 §6.2(4) (the JS lane at the same commit) simultaneously. These runs **have not yet happened** at the time this document is written; they are described here as the **gating step the tag is contingent on**, not completed results — no run-at-`X` transcript is fabricated. The §11.1 W0 COLD-QUAD-GREEN run + the per-wave zero-Rust-delta re-confirmations + the 34/34 combined Extension-Host gate are the de-risking predicates that make a red canonical run highly improbable, not a substitute for it. The tag is placed only if the canonical cargo quad **and** the JS lane (incl. `npm audit` 0 high/critical) are green and the independent pre-tag four-lens audit returns TAG-CLEAR.

---

## 5. Post-tag actions required of the repository owner

The tag and the unpushed commits are **local only** (the owner controls the remote — a hard project rule). Three owner/infra actions are carried as explicit dependencies (the v1.2 §5 documented-accepted-with-patch-lane posture, extended):

1. **G9 CI matrix + the new JS lane (the v1.2 §5 carry-forward, now doubled).** The GitHub Actions matrix (linux/macos/windows × fmt/clippy/build/test) and the SCA `cargo audit` job have **never executed against any v1.2 OR v1.3 commit**. v1.3 adds a *second* never-run lane — the Node/Extension-Host JS lane (Doc 28 §2.3 Option-A; needs `xvfb-run`, JC-1). The local-equivalent cargo quad + JS lane are green, but platform-specific behaviour is unverified on the runners. **The owner should push the tag + commits to exercise the full CI matrix, the SCA job, AND the new JS lane, and keep a v1.3.1 patch lane ready** — exercised promptly post-push.
2. **The G9-gated 5-platform binary-bundling tail (V6).** V6 shipped **host-only** (the host-triple `fsm-lang-server`+`fsm` bundled into the VSIX; Rule-2 proven against the real bundle). The full 5-platform `bin/{triple}/` tail is **blocked-on-G9 existing** (no cross toolchains on the box; infra-escalation, not grind — the Doc 27 §7 risk-2 / v1.2 §5 posture). This is **accepted-tracked-debt** until the owner provisions the cross-OS matrix.
3. **VSIX publishing/signing (K-4).** v1.3 produces an **installable** `.vsix` (`vsce package`), **not** a *published* one. Publishing + marketplace signing is an **owner credential/product decision** (Doc 27 §9 risk-4 — the Resend/DNS-escalation pattern: surface, do not grind). No PAT was sought.

---

## 6. Accepted-tracked-debt + owner-escalations (consumer-relevant)

### 6.1 The §11.49 paydown residual — the analyzer→CST R-1..R-4 leave-and-explain (accepted, tracked)

§11.49 (the v1.2 accepted-tracked-debt) is now **substantially paid as W0** (`ceb8efd`), with **13 `fsm-analyzer` files deliberately retaining `use fsm_parser::cst::*` as the documented R-1..R-4 leave-and-explain residual** (§2.1). This is **not** a regression of the v1.2 deferral — it is the *resolution* of it, with the genuinely-clean removal made and the order-/shape-/positional-/correctness-load-bearing remainder left-and-explained rather than contorted to hit a zero count (the §10 / DRIFT-2 `LineIndex` precedent). The W0 phase-audit independently rated this a **success of the discipline, not under-delivery** (`AUDIT_PHASE_W0` §0/§4.1). A future dedicated parser-API wave (the R-1 `TypeOrOpaque` widening) may shrink R-1 further; the residual is recorded here, in Doc 00 §11.50, and in CHANGELOG `[1.3.0]` *Changed*. Precedent-consistent with how v1.1 pub-hygiene and v1.2 DRIFT-2 closed.

### 6.2 Owner-escalations (carried — §5)

- **G9 multi-platform tail** = owner/infra action (the 5-platform binary bundling, blocked-on-G9; §5.2).
- **VSIX packaged-not-published** = owner credential decision (§5.3).
- **CI-matrix + JS-lane never-run** = owner push action (§5.1) — the v1.2 §5 carry-forward, now doubled by the new Node lane.

### 6.3 No-LICENSE-file (owner/legal)

The repository has **no top-level `LICENSE` file** (`git ls-tree fa3befc` has no `LICENSE`/`LICENSE.md`/`COPYING`). Generated firmware code carries an SPDX header (Doc 00 §10.4: MIT-by-default, `--license` overridable) and the VSIX is host-only-unpublished, so this is not a v1.3 ship-blocker — but a published extension / distributed source should carry an explicit repository license. This is an **owner/legal decision** (the same surface-don't-grind discipline), recorded here and in Doc 00 §11.62 so it is not silently dropped.

### 6.4 Tracked v1.3.x doc/CI items (folded or carried by this closeout)

- **F-1+F-2 / N-1, N-2, N-3** (the V1-audit Doc 27/28 transport + UTF-8 + citation corrections) — **folded by this closeout** into Doc 00 §11.52/§11.53 + reconciliation pointers (§ Reconciliation, below); the shipped code was already correct, no code change.
- **N-4** (the status-bar "restarting" tooltip generic-string cosmetic) — V5 *is* the status-bar wave; carried as a V5-area v1.3.x cosmetic polish (not a blocker; `AUDIT_PHASE_V4` §2 N-4 MV5-3 fold-in).
- **N-6** (Doc 21 §3 structurally-defective grammar) — **corrected-in-shipped by V2**; the Doc 21 §3 prose reconciliation is folded (§ Reconciliation).
- **JC-3** (`fsmLang.codegen.outputDir`/`codegen.strategy` extension-consumed-but-not-`contributes.configuration`-discoverable) — a real IDE-discoverability gap, defaults match Doc 22 §8 so **not a correctness bug**; carried to an **owner/later config-owner wave** (Doc 00 §11.58), not a v1.3 ship-blocker (V4/V5 read no `codegen.*`).
- **JC-1** (xvfb required for the JS CI lane) — folded into §4.2 + the post-tag JS-quad transcript requirement (Doc 00 §11.59).
- **The `makeNonce()` `Math.random`→CSPRNG hardening** — low-risk under `default-src 'none'` with no remote/inline-injection vector (the V4-audit explicitly rated it non-contract-weakening); recorded as a **tracked v1.3.x senior-bar one-liner hardening** (Doc 00 §11.61), not a ship issue.

These are recorded in CHANGELOG `[1.3.0]` *Known limitations* and the ROADMAP where consumer-facing.

---

## 7. Sign-off

v1.3.0 meets the release gate: 0 open P0; the four §11.3 phase-boundary audits each returned **PROCEED-WITH-NOTES** (0 ship-blockers across W0/V1/V2V3/V4); every NOTE is a documentation-of-record / CI-lane / cosmetic item, folded by this closeout or carried as accepted-tracked-debt. Scope statements are code-verified and conservative — every gate numeric independently re-derived (live `DiagnosticCode`=73, `forbid(unsafe_code)`=11 roots/10 crates, 34 Extension-Host tests with the V1/V3/V4/V5/V6 oracles independently cross-checked + Rule-2 proven against a real 4.95 MB bundle, **zero Rust delta vs W0-clean `ceb8efd`**); the recurring prose-vs-code overstatement class is confirmed absent from the v1.3 surface (the V1-audit's two doc-overturning findings prove the *prose* wrong and the *shipped code* right — the inverse of the overstatement sin, handled by verify-the-record); the §11.49 paydown residual is tracked precedent-consistently; all residuals + owner-escalations are recorded in CHANGELOG `[1.3.0]`, the ROADMAP, and Doc 00 §11.50–§11.62. The release is contingent on (a) the canonical cold-from-source cargo quad **and** the JS Extension-Host lane (incl. `npm audit` 0 high/critical) at the release commit `X` being green (§4.4) and (b) the independent pre-tag four-lens audit returning TAG-CLEAR.

Tagging `v1.3.0` (annotated, local) with `checkpoint/2026-05-16-v1_3` as the rollback anchor. (The earlier `checkpoint/2026-05-16-w0-clean` anchor at `ceb8efd` — the post-W0-audit SUBAGENT §11.4 rollback point — and `checkpoint/2026-05-16` at the v1.2.0 commit `71043935` are immutable and not moved.)

— TL/PM, FSM Studio

---

## 8. Tag topology (pre-emptively correct per §11.30)

Constructed §11.30-clean from the start (no v1.1-style off-by-one to reconcile retroactively):

- **Sequence:** (1) author + commit this document + the batched Doc-00 §11.50–§11.62 / CHANGELOG `[1.3.0]` / ROADMAP / metrics consolidation → commit `X`; (2) run the canonical cold-from-source cargo quad **at `X`** + the JS Extension-Host lane (`npm ci` → `tsc`/`eslint`/`esbuild` → `@vscode/test-electron` under `xvfb` → `npm audit`) **at `X`**, both transcripts recorded in the annotated tag message; (3) `git tag -a v1.3.0` **at `X`** + `git tag checkpoint/2026-05-16-v1_3` **at `X`**. Result: **cargo-quad-commit ≡ JS-lane-commit ≡ tag-commit ≡ `checkpoint/2026-05-16-v1_3` commit ≡ `X`**, exactly — zero off-by-one by construction (§11.30 satisfied at its source, as v1.2 first achieved prospectively; not reconciled after the fact as v1.1 had to).
- **Pin lineage (immutable, not moved):** v1.0.0 → `b4800036…`; v1.1.0 → `abc70044…` (== `checkpoint/2026-05-15`); v1.2.0 → `71043935…` (== `checkpoint/2026-05-16`); v1.3.0 → `X` (== `checkpoint/2026-05-16-v1_3`). The intra-epic `checkpoint/2026-05-16-w0-clean` is at `ceb8efd` (the post-W0-audit SUBAGENT §11.4 rollback anchor) and is **not** a release tag. The annotated `v1.3.0` tag *object* SHA dereferences to commit `X` (the annotated-tag-object-vs-commit distinction §11.30 / `GATE_VERIFICATION_v1_1.md` §7 / `GATE_VERIFICATION_v1_2.md` §8 document — recorded here pre-emptively so no future metrics wave misreads it as a mismatch: `git rev-parse v1.3.0` yields the tag-OBJECT sha, `git rev-list -1 v1.3.0` yields commit `X`). Release tags are **immutable and never moved** (re-cutting a release tag is a destructive op never performed).
- **Frozen-attestation discipline:** `GATE_VERIFICATION_v1_0.md` / `_v1_1.md` / `_v1_2.md` correctly cite their own pinned values *at their pinned commits* (e.g. v1.2 "73 / 26 / 11 / 806-0" at `71043935`); a frozen attestation is never rewritten. This document cites the v1.3 live values *at `X`* — and because the sole v1.3 Rust change (W0, the §11.49 paydown) is non-behavioural (added/retired no diagnostic code, added no crate/forbid-root, conformance byte-unchanged pre/post — §11.1-cold-verified at `ceb8efd`) and nothing Rust changed thereafter, the live `DiagnosticCode`=73 / `forbid(unsafe_code)`=11/10 / conformance=26 figures are *unchanged from* and *consistent with* the frozen v1.2 attestation (independently re-derived at `fa3befc`, not echoed). Both are correct at their respective pinned commits — Doc 00 §11.47 / §11.40 codify exactly this.

### Pinned gate numbers (independently re-derived at `fa3befc` / commit `X`)

| Metric | Value | Method (independently re-derived, not echoed) |
|---|---|---|
| Live `DiagnosticCode` variants | **73** | `for_each_code!` macro body `=> (Severity,` definition-line count at `fa3befc:crates/fsm-diagnostics/src/lib.rs` (lines 262–403) = 73 — **equals the frozen v1.2 figure** (the sole v1.3 Rust change W0 added/retired no code; nothing Rust changed after W0) |
| `#![forbid(unsafe_code)]` | **11 roots / 10 crates** | `git grep -l '#![forbid(unsafe_code)]' fa3befc -- 'crates/*/src/*.rs'` = 11 files (fsm-lsp lib.rs+main.rs, fsm-cli main.rs, 8 other lib.rs) across 10 crates — **equals the frozen v1.2 figure** (W0 added no crate / touched no forbid root) |
| Conformance fixtures | **26** | equals the frozen v1.2 figure (the sole v1.3 Rust change W0 added/retired no diagnostic code; `tests/conformance/MANIFEST.json` byte-untouched; W0 preserved 26/26 byte-unchanged pre/post) |
| Extension-Host tests | **34** | `test(`/`it(` count at `fa3befc` per suite: V1 4 + V2 7 + V3 9 + V4 4 + V5 6 + V6 4 = 34 |
| Rust delta vs W0-clean `ceb8efd` | **0** (empty) | `git diff --stat ceb8efd fa3befc -- crates/ Cargo.toml Cargo.lock rust-toolchain.toml` = empty (re-derived; the four §11.3 audits each confirmed independently) |
| W0 paydown delta | **18 files / +421 / −93, Cargo.lock unchanged** | `git diff --stat 7d6792a ceb8efd` (the only Rust touch; 0 new deps confirmed) |
| `editors/vscode/` source LOC (excl tests) | **~3415** | `wc -l` over `fa3befc` `editors/vscode/src/*.ts` excluding `*/test/*` + `*.test.ts` |
